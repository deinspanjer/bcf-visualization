# This .gitignore is appropriate for repositories deployed to GitHub Pages and using
# a Gemfile as specified at https://github.com/github/pages-gem#conventional

# Basic Jekyll gitignores (synchronize to Jekyll.gitignore)
_site/
.data-release/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Additional Ruby/bundler ignore for when you run: bundle install
/vendor

# Specific ignore for GitHub Pages
# GitHub Pages will always use its own deployed version of pages-gem
# This means GitHub Pages will NOT use your Gemfile.lock and therefore it is
# counterproductive to check this file into the repository.
# Details at https://github.com/github/pages-gem/issues/768
Gemfile.lock

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.python-version
.ruff_cache/
.mypy_cache/
.pytest_cache/
.playwright-mcp/
tests/.tmp-data/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Subagent worktree scratch
.claude/

# Source EPUBs - copyrighted prose, kept locally, not redistributed via git.
# See README "Source material" section for where to obtain these.
data/raw/*.epub
data/raw/Brocktons_Celestial_Forge.source.json

# Private source files live in a separate private repository. The local
# checkout is nested here for maintainer convenience but must never be
# committed to the public repository.
data/private-source/

# Local NLP work (see docs/local_nlp_*.md). Commit the .gitkeep
# placeholders so the directory layout exists on a fresh clone, ignore
# everything else underneath.
checkpoints/**
!checkpoints/**/
!checkpoints/**/.gitkeep
data/labeled/.proposals_raw/
data/labeled/**/.tui_state.json
data/labeled/**/.tui_wal.jsonl
data/labeled/.tui_snapshot.json
# LLM-generated proposal staging files (regenerable via scripts/bootstrap_proposals.py)
data/labeled/spans/_*.jsonl
data/labeled/sections/_*.jsonl

# High-churn derived data is distributed through versioned GitHub
# Releases. Hydrate this directory with scripts/data_release.py
# download-dev when working from a fresh checkout.
data/derived/*.json

# Generated constellation HTML — rebuilt by
# scripts/scaffold_constellation_pages.py from canonical inputs in
# data/constellations/<slug>/{current.svg,metadata.json} plus three
# derived JSONs. The deploy workflows regenerate before staging web/.
web/constellations/index.html
web/constellations/*/index.html

# macOS finder noise under the constellation dirs
data/constellations/.DS_Store
data/constellations/**/.DS_Store
web/constellations/.DS_Store
web/constellations/**/.DS_Store

# uv / Hugging Face caches
.uv/
.hf_cache/

*.cast
data/screenshots/
data/manual/.session_journals/
data/manual/.forge_curator_state.json
data/manual/.forge_curator_snapshot.json
/docs/superpowers/
