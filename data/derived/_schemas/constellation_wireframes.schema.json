{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bcf-visualization/derived/constellation_wireframes.schema.json",
  "title": "Constellation Wireframes",
  "description": "2D star-pattern wireframes for the 14 named cluster-constellations and every per-jump sub-constellation. Each cluster shape is themed by the constellation's name (e.g. Toolkits = wrench outline) with hand-authored marker positions and silhouette polylines extracted from data/constellations/NN-<slug>/current.svg. Each per-jump shape is themed by the jump's source media as an abstract star pattern, never a literal logo. Coordinates are normalized to the unit square [0,1]; the renderer applies 3D sphere positioning. Star size encodes perk cost (small for 100 CP, ~1.0 for 800+ CP).",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "_source", "_count", "_jumps_count", "_note", "cluster_constellations", "jump_constellations"],
  "properties": {
    "schema_version": { "const": 2 },
    "_source": { "type": "string" },
    "_count":  { "type": "integer", "minimum": 0 },
    "_jumps_count": { "type": "integer", "minimum": 0 },
    "_note":   { "type": "string" },
    "cluster_constellations": {
      "type": "array",
      "minItems": 14,
      "maxItems": 14,
      "items": { "$ref": "#/$defs/cluster" }
    },
    "jump_constellations": {
      "type": "array",
      "items": { "$ref": "#/$defs/jump" }
    }
  },
  "$defs": {
    "cluster": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name", "slug", "slot_position", "shape_concept", "vertex_source",
        "revealed_at_chapter", "completed_at_chapter", "entered_pool_at_chapter",
        "marker_positions", "silhouette"
      ],
      "properties": {
        "name": {
          "enum": [
            "Toolkits", "Knowledge", "Vehicles", "Time", "Crafting",
            "Clothing", "Magic", "Quality", "Size",
            "Resources and Durability", "Magitech", "Alchemy",
            "Capstone", "Personal Reality"
          ]
        },
        "slug":          { "type": "string", "minLength": 1 },
        "slot_position": { "type": "integer", "minimum": 1, "maximum": 14 },
        "shape_concept": { "type": "string", "minLength": 1 },
        "vertex_source": { "enum": ["jumps", "perks"] },
        "revealed_at_chapter":     { "type": ["string", "null"] },
        "completed_at_chapter":    { "type": ["string", "null"] },
        "entered_pool_at_chapter": { "type": ["string", "null"] },
        "marker_positions": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "array",
            "minItems": 2,
            "maxItems": 2,
            "items": { "type": "number" }
          }
        },
        "silhouette": {
          "type": "array",
          "minItems": 0,
          "items": {
            "type": "array",
            "minItems": 2,
            "items": {
              "type": "array",
              "minItems": 2,
              "maxItems": 2,
              "items": { "type": "number" }
            }
          }
        }
      }
    },
    "jump": {
      "type": "object",
      "additionalProperties": false,
      "required": ["constellation", "jump", "shape_concept", "stars"],
      "properties": {
        "constellation": {
          "enum": [
            "Toolkits", "Knowledge", "Vehicles", "Time", "Crafting",
            "Clothing", "Magic", "Quality", "Size",
            "Resources and Durability", "Magitech", "Alchemy",
            "Capstone", "Personal Reality"
          ]
        },
        "jump":          { "type": "string", "minLength": 1 },
        "shape_concept": { "type": "string", "minLength": 1 },
        "stars": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "id", "perk_name", "cost", "size", "x", "y",
              "status", "acquired_chapter_num", "acquired_epub_sequence",
              "instances_count"
            ],
            "properties": {
              "id":        { "type": "string", "minLength": 1 },
              "perk_name": { "type": "string", "minLength": 1 },
              "cost":      { "type": ["integer", "null"], "minimum": 0 },
              "size":      { "type": "number", "minimum": 0.0, "maximum": 1.0 },
              "x":         { "type": "number" },
              "y":         { "type": "number" },
              "status": {
                "enum": ["Obtained", "Available", "Partial", "Locked", "Unknown", "Repeatable"]
              },
              "acquired_chapter_num": {
                "anyOf": [
                  { "type": "null" },
                  { "type": "string", "pattern": "^\\d+(\\.\\d+)?$" }
                ]
              },
              "acquired_epub_sequence": { "type": ["integer", "null"], "minimum": 0 },
              "instances_count":        { "type": "integer", "minimum": 1 }
            }
          }
        },
        "edges": {
          "type": "array",
          "items": {
            "type": "array",
            "minItems": 2,
            "maxItems": 2,
            "items": { "type": "integer", "minimum": 0 }
          }
        }
      }
    }
  }
}
