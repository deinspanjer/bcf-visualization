---
phase: 03-landscape-layout
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - web/app.js
  - web/mobile.css
  - tests/test_mobile_landscape.py
autonomous: true
requirements: [MOBL-03, MOBL-04]
user_setup: []

must_haves:
  truths:
    - "Rotating mid-playback preserves every field MOBL-03 names — word position, play state, speed, timeline zoom, and every preference toggle (tap-to-pause, haptics, on-roll behavior, view mode) — because all of them live on app.* and survive a structural rebuild (D-20)."
    - "Rotation is a full render() rebuild, not a cross-fade and not a live-DOM re-parent: no interpolated transition state is built and ROTATION_ANIM stays unused this phase (D-20, RESEARCH A3)."
    - "An open Settings, About or Help surface SURVIVES rotation and re-renders in the new layout's position — rotation is not a dismissal gesture — and the history sentinel stays balanced across the swap with history.length unchanged (D-21, D-16)."
    - "Rotating with a pointer down aborts the in-flight drag cleanly (the frozen pan offset is cleared by the gesture teardown) and never defers or blocks the layout swap; a playing roll cinematic continues from state because it is driven by word position, not by DOM identity (D-22)."
    - "Arriving in landscape shows the chrome and starts the idle timer, so a reader who just rotated sees the controls they rotated into and the cinema view still appears for someone who then simply watches (D-31)."
    - "Rotating away from landscape leaves no armed idle timer and no stale scrub width: the gesture teardown clears the timer and the layout-tagged width guard forces a layout-appropriate default until the new ResizeObserver measures (Pitfall 6)."
    - "Layout mode is resolved by the single detectLayoutMode()/MOBILE_LAYOUT_QUERY source of truth at every viewport this phase touches — no landscape-specific media query variant and no hand-rolled width comparison exists anywhere in the landscape code (D-06, D-36)."
    - "Landscape flyouts dismiss on a backdrop tap: the backdrop spans the whole landscape surface including the right rail, so tapping next to the control-dock button that opened a flyout closes it (MOBL-04, the shape Phase 2's device defect #3 arrived at)."
    - "Landscape flyouts trap focus while open — Tab and Shift+Tab wrap at the last and first focusable inside the panel and focus can never land on the sky, rail or dock behind it (MOBL-04)."
    - "The mobile back gesture closes a landscape flyout instead of leaving the app: a popstate consumes the single sentinel and history.length is unchanged across open-then-back (MOBL-04, D-16)."
    - "Pressing the control that opened a landscape flyout a second time closes it, and swapping Settings to About reuses the outstanding sentinel rather than stacking a second one."
    - "Every external story link in the landscape About flyout carries a rel attribute containing noopener (T-03-03)."
    - statement: "Rotation at the exact routing boundaries behaves per the Phase 1 boundary matrix: a 1100x900 landscape viewport stays desktop, a 900x1100 portrait viewport is mobile portrait, and a 900x600 landscape viewport is mobile landscape — the landscape arm never mounts at a viewport the matrix routes to desktop."
      verification: backstop
    - statement: "Rotating mid-drag (pointer down on the scrub track when the viewport swaps) leaves no frozen pan offset behind: the next drag in the new layout is monotonic from its first sample and emits no console error."
      verification: backstop
    - statement: "Rotating with a flyout open leaves exactly one history sentinel outstanding: history.length after open-rotate-close equals history.length before open."
      verification: backstop
    - statement: "On real iOS Safari in landscape, the OS edge-swipe-back gesture dismisses an open flyout rather than leaving the app. WebKit bug 248303 documents a historical regression where that gesture did not fire popstate; Phase 2 verified this in PORTRAIT only, so landscape must be re-exercised on hardware at the Phase C gate."
      verification: backstop

  prohibitions:
    - "MUST NOT make a physical rotation feel like a freeze: the layout swap must never be deferred, debounced past a frame, or blocked while a pointer is down — an ignored rotation reads to the reader as the app having hung."
    - "MUST NOT treat rotation as a dismissal: a reader who opened Settings and turned the phone must find Settings still open, not silently closed with their place in the panel lost."
    - "MUST NOT let a modal surface be escapable only by a route a touch-only reader cannot reach — backdrop tap, the opening control, and the system back gesture must each independently close it."
    - "MUST NOT silently degrade the frozen desktop experience or the Phase 1 gesture-probe contract to make landscape work — tests/test_mobile_plumbing.py, web/mobile-gestures.js and web/style.css stay byte-identical."

  flagged_assumptions:
    - id: FA-MOBL-03
      origin: edge-probe (spec-less fallback §A)
      requirement: MOBL-03
      status: unresolved
      note: "The deterministic edge probe returned category=unclassified for MOBL-03, so no edge criteria were auto-derived. The criteria below are the PLANNER'S OWN, authored from CONTEXT D-20..D-23/D-31, RESEARCH Pitfall 6, and the Phase 1 boundary matrix recorded in STATE.md. They are flagged, not probe-resolved."
      criteria:
        - "Rotation at the exact 900px/1100px routing boundaries: 1100x900 landscape stays desktop; 900x1100 portrait is mobile portrait; 900x600 landscape is mobile landscape."
        - "Rotation while a surface is open: the surface survives and re-renders in the new layout; history.length is unchanged."
        - "Rotation mid-drag: the drag aborts, the frozen pan offset is cleared, the next drag is monotonic."
        - "Rotation while the idle timer is armed: no stale timer fires into the new layout, no console error."
        - "Rotation at word position 0 and in a zero-roll story: no throw, field log and scrub both render their empty forms."
        - "Round trip portrait -> landscape -> portrait: every named field returns to its pre-rotation value and the portrait mini-rail binning is measured against the portrait width, not the landscape one."
    - id: FA-MOBL-04
      origin: edge-probe (spec-less fallback §A)
      requirement: MOBL-04
      status: unresolved
      note: "The deterministic edge probe returned category=unclassified for MOBL-04, so no edge criteria were auto-derived. The criteria below are the PLANNER'S OWN, authored from CONTEXT D-33/D-16/D-19 and RESEARCH Pitfall 5. They are flagged, not probe-resolved."
      criteria:
        - "Backdrop tap in the RAIL region (not just over the sky) closes an open flyout."
        - "Second press of the opening control closes it; Settings-to-About swap reuses the single sentinel."
        - "Tab past the last focusable and Shift+Tab past the first both wrap inside the panel."
        - "popstate closes the surface and history.length is unchanged across open-then-back."
        - "A flyout opened while chrome was hidden does not leave the reader with an unreachable close route."
        - "Every external anchor in the About flyout carries a rel containing noopener."

  artifacts:
    - "web/app.js: landscape-arrival handling in onLayoutMaybeChanged() (D-31 reveal + timer start, D-22 no deferral, Pitfall-6 width-layout reset)"
    - "web/mobile.css: landscape flyout/backdrop/help-overlay positioning rules under .mobile-app-landscape"
    - "tests/test_mobile_landscape.py: test_rotation_preserves_state, test_rotation_at_routing_boundaries, test_rotation_with_surface_open_and_mid_drag, test_landscape_surface_stack"

  key_links:
    - "onLayoutMaybeChanged() -> app.layoutMode -> render() (the ONE sanctioned rotation trigger; nothing else may swap layouts)"
    - "onLayoutMaybeChanged() landscape arrival -> revealMobileChrome() + resetMobileChromeHideTimer() AFTER render()/cachePlaybackDomRefs() (the scrub ref does not exist before the render)"
    - "renderMobileSurfaceBackdrop() as the LAST child of the landscape root -> backdrop spans sky AND rail"
    - "trapMobileSurfaceFocus() via render()'s non-desktop block -> landscape flyouts (wired by plan 02's Pitfall-3 fix)"
    - "popstate listener -> closeMobileSurface({fromPopstate:true}) -> balanced history (layout-agnostic, reused unmodified)"
---

<objective>
Close the phase's hard gate and its accessibility contract: prove that rotating mid-playback loses nothing, and that landscape's Settings/About/Help surfaces dismiss, trap focus and honor the system back gesture exactly as portrait's do.

Purpose: MOBL-03 and MOBL-04. Rotation is the behavior the roadmap names as this phase's hard gate; the surface stack is the one MOBL requirement whose machinery is entirely reused (D-33) and therefore needs proof rather than construction.

Output: the landscape-arrival handling in `onLayoutMaybeChanged()`, the landscape flyout positioning rules, and four new tests — one of which is the CI half of D-23's two-pronged rotation proof.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-UI-SPEC.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-01-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-02-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-05-SUMMARY.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...`.
- Commit signing is disabled repo-wide; `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`. Never `git add .`.
- `tests/test_mobile_plumbing.py`, `web/mobile-gestures.js` and `web/style.css` stay byte-identical.
</session_constraints>

## Artifacts this phase produces

Full phase-wide symbol inventory (this plan creates the rows marked **[03]**).

| Symbol / artifact | Kind | Plan |
|---|---|---|
| `renderMobileLandscape()`, `renderMobileSkyRegion(frame)`, `renderMobileLandscapeSidebar(frame)` | render functions (`web/app.js`) | 01 |
| `renderMobileFieldLog()`, `renderMobileFieldLogChildren(rows)`, `mobileFieldLogRows()` | render + model adapter | 01 |
| `mobileFieldLogQuoteText(roll)`, `mobileTruncate(s, n)` | helpers | 01 |
| `renderMobileCinemaScrub()`, `renderMobileCinemaScrubTrackChildren()` | render functions | 01 |
| `updateMobileLandscapeFrame()`, `updateMobileFieldLogFrame()` | incremental tier | 01 |
| `mobileScrubWidthDefaultForLayout()`, `app.mobileRailWidthLayout` | Pitfall-6 width guard | 01 |
| landscape arms in `render()` / `cachePlaybackDomRefs()` / `updatePlaybackFrame()`; `mobileChChipText()` branch | dispatch + copy | 01 |
| landscape CSS block in `web/mobile.css`; D-32 scroll-lock un-nesting | styles | 01 |
| `attachMobileGestures()` (D-34), `mobileScrubSurfaceEl()`, `rebuildMobileCinemaScrubTrack()` | gesture lifecycle | 02 |
| `resetMobileChromeHideTimer()`, `revealMobileChrome()`, `app.mobileChromeHideTimer` | auto-hide timer | 02 |
| `render()` mobile-block guard widened to every non-desktop layout (Pitfall 3); `togglePlayback()` wiring | dispatch edits | 02 |
| landscape-arrival handling in `onLayoutMaybeChanged()` (D-31, D-22, Pitfall-6 width reset) | dispatch edit | **[03]** |
| landscape flyout / backdrop / help-overlay CSS under `.mobile-app-landscape` | styles | **[03]** |
| `tests/test_mobile_landscape.py::test_rotation_preserves_state`, `::test_rotation_at_routing_boundaries`, `::test_rotation_with_surface_open_and_mid_drag`, `::test_landscape_surface_stack` | tests | **[03]** |
| `COVERAGE.md`, §6 acceptance-evidence table, freeze proof, Phase C gate | phase closure | 04 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Landscape surface stack — backdrop, focus trap, back gesture, and the landscape flyout anchor</name>

  <read_first>
    - `web/app.js:2991-3034` — `openMobileSurface()` (the allow-list guard, the same-kind toggle-shut branch, the single-sentinel `alreadyOpen` rule) and `closeMobileSurface()` (the `fromPopstate`-gated `history.back()` and the opener-refocus), plus the module-level `popstate` listener at `:3032-3034`. Confirm by reading that NONE of this branches on `app.layoutMode` — it is already layout-agnostic and is reused wholesale per D-33.
    - `web/app.js:3036-3090` — `MOBILE_FOCUSABLE_SELECTOR`, `mobileSurfaceFocusables()`, `teardownMobileSurfaceFocusTrap()`, `trapMobileSurfaceFocus()` and the comment explaining why the teardown handle lives on `app`.
    - `web/app.js:3415-3435` — `renderMobileSurface()` and `renderMobileSurfaceBackdrop()`, and the two comments recording Phase 2's device defects: why the PANEL lives inside `.mobile-sky` (D-19: it must not geometrically collide with the dock) and why the BACKDROP stays a child of `.mobile-app` (it must span the whole surface or tapping out becomes impossible).
    - `web/mobile.css` — the portrait `.mobile-flyout`, `.mobile-flyout-backdrop` and `.mobile-help-overlay` rules, including their positioning anchors and z-index ordering relative to `.mobile-dock-transport`.
    - `web/app.js` — `renderMobileSettingsFlyout()`, `renderMobileInfoFlyout()` and `renderMobileHelpOverlay()`; in particular every external anchor in the About flyout and the `rel` attribute each already carries.
    - `03-01-SUMMARY.md` — the landscape root/stage/sidebar class names and where `renderMobileSurface()` and `renderMobileSurfaceBackdrop()` were mounted in the landscape tree.
    - `tests/test_mobile_portrait.py` — `test_settings_about_help_persist_across_reload` and the surface-stack test group (from roughly line 934) — the assertion templates for backdrop tap, focus trap, opener toggle and popstate dismissal.
    - `02-05-SUMMARY.md` "iOS Safari pass — DONE" — the four device-found defects, especially #2 (dock buttons painting through a mis-anchored panel) and #3 (the backdrop no longer spanning the dock). Landscape must not re-earn either.
    - `03-RESEARCH.md` Pitfall 5 — WebKit bug 248303 and why the back-gesture route specifically must be re-exercised on hardware in landscape rather than inherited from portrait's pass.
  </read_first>

  <behavior>
    - At `PHONE_LANDSCAPE`, tapping the control-dock Settings button opens `.mobile-flyout` mounted inside `.mobile-sky`, with `.mobile-flyout-backdrop` present and spanning the full root width including the sidebar.
    - Tapping the backdrop at a point inside the sidebar's horizontal range closes the flyout.
    - Pressing the Settings dock button a second time closes the flyout; pressing About while Settings is open swaps to About without changing `history.length`.
    - `document.activeElement` moves into the panel on open; Tab from the last focusable wraps to the first; Shift+Tab from the first wraps to the last; focus never lands on a node outside `.mobile-flyout`.
    - `page.go_back()` closes the flyout and leaves the app on the same URL; `history.length` after open-then-back equals its value before the open.
    - The flyout panel's bounding box does not intersect the `.mobile-control-dock` button rects (the landscape analogue of Phase 2 defect #2).
    - Every anchor inside the About flyout with an `href` beginning `http` has a `rel` attribute containing `noopener`.
    - The Help overlay opens from the top-cluster help button in landscape and its call-to-action is reachable without the overlay exceeding the sky region's height.
  </behavior>

  <files>web/mobile.css, tests/test_mobile_landscape.py</files>

  <action>
The surface-stack JavaScript needs NO changes — `openMobileSurface`, `closeMobileSurface`, the `popstate` listener and the focus trap are already layout-agnostic, and plan 02 widened the one `render()`-side guard that was portrait-only. This task adds the landscape positioning rules and proves the whole contract.

**1. Landscape flyout and overlay positioning.** Add rules scoped under `.mobile-app-landscape` (inside the existing outer mobile media query in `web/mobile.css`) so the panel is legible at landscape's short height:
   - The panel keeps its D-33/D-19 mount inside `.mobile-sky` — do NOT constrain it to the right rail (only ~224px wide; the Settings segmented controls and the About link row would be badly cramped) and do NOT invent a third, dock-anchored anchoring scheme (a bespoke anchor is exactly what produced Phase 2's device defect #2).
   - Anchor the panel within the sky using a centred or inset-based position with a `max-height` bounded by the sky region's height minus the 12px cinema-scrub inset, and give its body the same internal-scroll-plus-static-footer treatment Phase 2 landed for the Help overlay when the content exceeded the region. Landscape is the SHORTER dimension, so a panel that fit portrait's 506px sky will not fit a 390px-tall landscape sky — assume it overflows and give it the scroll container rather than discovering it on device.
   - The backdrop stays a full-root overlay: confirm the existing `.mobile-flyout-backdrop` rule's positioning still spans the whole `.mobile-app-landscape` box (it should, being viewport-anchored) and add an explicit landscape rule only if a bounding-box measurement shows it stopping at the stage's edge. Its z-index must sit above the sidebar and below the panel.
   - Give the Help overlay the same sky-scoped bound (D-19) so the control dock stays operable while it auto-opens on a first landscape visit.

**2. Do not change any surface JavaScript.** If a landscape behavior appears broken, the cause is a CSS anchor or a stacking order, not the stack itself. Record any such finding in the summary rather than editing `openMobileSurface`/`closeMobileSurface`/`trapMobileSurfaceFocus`.

**3. Tests.** Add `test_landscape_surface_stack` to `tests/test_mobile_landscape.py`, covering every bullet in this task's `<behavior>` block. Structure it as one Playwright session per behavior group, mirroring the portrait surface-stack tests. Three assertions deserve explicit care:
   - The backdrop-tap dismissal must be driven at a coordinate whose x falls inside the sidebar's horizontal range, not merely somewhere over the sky — Phase 2's defect #3 was precisely a backdrop that had stopped covering the region where the opening button lives.
   - The focus-trap wrap must press Tab strictly more times than the panel has focusable children and then assert `document.activeElement.closest('.mobile-flyout')` is non-null.
   - The `rel` assertion must enumerate every `a[href^="http"]` inside the flyout and assert each one's `rel` contains the no-opener token, rather than checking only the first link.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py::test_landscape_surface_stack -x</automated>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py -x</automated>
  </verify>

  <acceptance_criteria>
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py::test_landscape_surface_stack -x` exits 0.
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py -x` exits 0 with no portrait regression.
    - With a landscape flyout open, the `.mobile-flyout-backdrop` rect's right edge is at or beyond the `.mobile-sidebar` rect's right edge, and a click at the sidebar's horizontal midpoint over the backdrop results in `.mobile-flyout` being absent.
    - After pressing Tab `n + 2` times where `n` is the count of focusables inside the open panel, `document.activeElement.closest('.mobile-flyout')` is non-null; the same holds for Shift+Tab.
    - `history.length` measured before opening Settings equals `history.length` measured after `page.go_back()` closes it, and `.mobile-flyout` is absent after the back navigation.
    - The open `.mobile-flyout` rect does not intersect any `.mobile-dock-btn` rect.
    - Every `.mobile-flyout a[href^="http"]` reports a `rel` attribute whose value contains the no-opener token; the assertion iterates all such anchors and the collected list is non-empty.
    - The open `.mobile-flyout` rect's height is at or below the `.mobile-sky` rect's height.
  </acceptance_criteria>

  <reversibility rating="reversible">CSS-only plus tests; the surface-stack JavaScript is untouched, so reverting is a `web/mobile.css` hunk.</reversibility>

  <done>Landscape Settings/About/Help open in the D-33 sky-scoped position, dismiss by backdrop tap anywhere including over the rail, trap keyboard focus, close on the system back gesture with a balanced history, and never overlap the control-dock buttons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rotation hand-off — state preservation, landscape arrival, mid-drag abort, and the boundary matrix</name>

  <read_first>
    - `web/app.js:2905-2933` — `onLayoutMaybeChanged()`, its rAF coalescing, the `next === app.layoutMode` early return, and the three listeners registered at `:2930-2932` (`MOBILE_MQ` change, `PORTRAIT_MQ` change, `orientationchange`). Note the existing comment that the ~100ms iOS re-settle re-check is deliberately NOT added until a real device shows the flap.
    - `web/app.js:73-80` — `MOBILE_LAYOUT_QUERY` and `detectLayoutMode()`, the D-06/D-36 single source of truth.
    - `web/app.js:120-160` — the `app` object's persisted/preference fields: `wordPos`, `playing`, `speed`, `mode`, `onRoll`, `tapToPause`, `haptics`, `helpSeen`, `mobileTimelineZoom`, `chromeHidden`, `mobileRailWidth`, `mobileRailWidthLayout`, `mobileChromeHideTimer`. These are the exact fields the rotation test enumerates.
    - `web/app.js:975-993` — `render()`'s reset of `app.dom` and `app.frameKeys`, which is what makes rotation a state-preserving rebuild rather than a state loss.
    - `web/app.js:3852-3876` (post-plan-02 state) — `attachMobileGestures()`'s teardown block, including the `app.mobileScrubPanPct = null` line and its comment explaining that a structural render tears listeners down mid-drag without ever firing `onScrubEnd` (this is D-22's mechanism, already present — verify rather than rebuild).
    - `03-02-SUMMARY.md` — the exact names and semantics of `revealMobileChrome()` and `resetMobileChromeHideTimer()`.
    - `tests/test_mobile_plumbing.py` — `test_layout_mode_matrix_matches_css_breakpoint`, `test_layout_mode_survives_rotation`, `test_gesture_attach_survives_forced_rerenders_without_double_fire`, `test_mid_drag_rerender_is_safe_and_fresh_probe_fires_once`. READ ONLY: these four are the Phase 1 gesture-probe contract, they assert only `window.__bcfLayoutMode` and the layout-agnostic probe, and they must stay green with ZERO edits. They are also the boundary-matrix reference the new rotation test mirrors.
    - `.planning/workstreams/mobile-ux/STATE.md` — the recorded 01-03 boundary-matrix decision: 1100x900 landscape stays desktop; only portrait crosses at 1100px.
    - `03-CONTEXT.md` D-20, D-21, D-22, D-23, D-31, D-36.
  </read_first>

  <behavior>
    - Portrait → landscape mid-playback: `wordPos` (within one playback frame's advance), `playing`, `speed`, `mobileTimelineZoom`, `tapToPause`, `haptics`, `onRoll` and `mode` all match their pre-rotation values; `window.__bcfLayoutMode` reads `landscape`; `.mobile-sidebar` is present and `.mobile-dock` is absent.
    - Landscape → portrait round trip: every field above returns to its pre-rotation value and `.mobile-dock` is present again.
    - Arriving in landscape: `.mobile-cinema-scrub` does NOT carry the hidden state immediately after the swap, and it DOES carry it 4600ms later if playback continues untouched (D-31 — chrome visible on arrival, timer armed).
    - Arriving in portrait from landscape: no idle timer remains armed — waiting 5000ms in portrait produces no class mutation and no console error.
    - Rotating with a Settings flyout open: the flyout is still present after the swap, mounted in the landscape sky, and `history.length` is unchanged.
    - Rotating with a pointer down on the scrub track: the swap happens on the same frame (the new layout's root class is present immediately after the viewport change resolves), and a fresh drag in the new layout is monotonic from its first sample with no console error.
    - Boundary matrix: 1100x900 reads `desktop`; 900x1100 reads `portrait`; 900x600 reads `landscape`; the landscape root never mounts at the viewport that reads `desktop`.
    - Rotating at word position 0 and on the `no-rolls` package throws nothing and renders the empty field log and zero-marker scrub in the new layout.
  </behavior>

  <files>web/app.js, tests/test_mobile_landscape.py</files>

  <action>
Rotation preservation is mostly a property of the existing design (D-20: every named field already lives on `app.*`). This task adds the one arrival behavior that is genuinely new, verifies the two behaviors that are claimed-but-untested, and builds the CI half of D-23's two-pronged proof.

**1. Landscape arrival in `onLayoutMaybeChanged()` (D-31 + Pitfall 6).** After the existing `app.layoutMode = next; window.__bcfLayoutMode = next; render();` sequence, add — strictly AFTER the `render()` call, because the cinema-scrub DOM ref does not exist until `cachePlaybackDomRefs()` has run inside it — a landscape-arrival branch that calls `revealMobileChrome()` and then `resetMobileChromeHideTimer()`. Rotation counts as activity, so the reader sees the controls they just rotated into, and the reset's own `app.playing` gate means the cinema view still appears for someone who then simply watches. Also reset the Pitfall-6 width guard on ANY layout change by setting `app.mobileRailWidthLayout = null` BEFORE the `render()` call, so plan 01's `mobileScrubWidthDefaultForLayout()` supplies the layout-appropriate default for the first frame instead of inheriting the outgoing layout's measurement. Do NOT add a `setTimeout`, a debounce, or a pointer-down check around any of this — an ignored or deferred rotation reads to the reader as a freeze (D-22), and the rAF coalescing already in place is the whole of the sanctioned debouncing.

**2. Verify, do not rebuild, the mid-drag abort (D-22).** `attachMobileGestures()`'s teardown already nulls `app.mobileScrubPanPct` on every structural render precisely because a render tears rail listeners down mid-drag without firing `onScrubEnd`. Confirm by reading that a rotation-triggered `render()` reaches that teardown, and add nothing if it does. If it does not, the fix belongs in the teardown block, never in a rotation-specific special case.

**3. Verify, do not rebuild, the surface survival (D-21).** `onLayoutMaybeChanged()` never touches `app.mobileSurface` and never touches history, so an open surface re-renders in the new layout with the single sentinel still outstanding. Confirm by reading and assert it in the test; add no bookkeeping.

**4. Do not build a cross-fade (D-20, RESEARCH A3).** `ROTATION_ANIM` (220ms) exists in the frozen gesture constants and stays unused this phase. Do not re-parent live DOM nodes, do not interpolate a transitional visual state, and do not add a transition class to the root on rotation. A single frame of rebuild is acceptable and is what keeps the phase inside D-18's single-render-tier discipline.

**5. Tests.** Add to `tests/test_mobile_landscape.py`:
   - `test_rotation_preserves_state` — the CI half of D-23. Start at `PHONE_PORTRAIT`, set a non-default speed, a non-default timeline zoom, toggle tap-to-pause and haptics, start playback, capture every field named in `<behavior>` (read through `window.__bcfPrefs` and the rendered DOM, plus the bookmark storage key for word position), `page.set_viewport_size(PHONE_LANDSCAPE)`, wait for `window.__bcfLayoutMode` to read `landscape`, then assert every field field-by-field. Then rotate back and assert the round trip. Assert word position monotonically advanced or held rather than reset to zero — do not assert exact equality across a live playback frame.
   - `test_rotation_at_routing_boundaries` — assert `window.__bcfLayoutMode` at 1100x900, 900x1100 and 900x600, and assert the landscape root is absent at the viewport that reads `desktop`. This mirrors `test_layout_mode_matrix_matches_css_breakpoint` in the plumbing file without editing it.
   - `test_rotation_with_surface_open_and_mid_drag` — (a) open Settings in portrait, rotate to landscape, assert the flyout is still present and `history.length` is unchanged, then close it and assert `history.length` returns to its pre-open value; (b) press the pointer down on the portrait mini-rail, rotate to landscape while it is down, release, then drive a fresh monotonic drag on the landscape scrub track and assert the sampled playhead sequence is non-decreasing with no console error; (c) rotate away from landscape while the idle timer is armed, wait past the window, and assert no console error and no hidden-state class anywhere.
   - Add a case (or extend `test_landscape_field_log_zero_and_empty_states`) rotating at word position 0 and on the `no-rolls` package.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py -x</automated>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py</automated>
    <automated>git diff --exit-code -- tests/test_mobile_plumbing.py web/mobile-gestures.js web/style.css</automated>
  </verify>

  <acceptance_criteria>
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py -x` exits 0 with `test_rotation_preserves_state`, `test_rotation_at_routing_boundaries` and `test_rotation_with_surface_open_and_mid_drag` all passing.
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py` exits 0 — the Phase 1 gesture-probe contract holds unmodified.
    - `git diff --exit-code -- tests/test_mobile_plumbing.py web/mobile-gestures.js web/style.css` exits 0.
    - Across a portrait→landscape rotation, each of `speed`, `mobileTimelineZoom`, `tapToPause`, `haptics`, `onRoll`, `mode` and `playing` compares equal to its captured pre-rotation value, asserted individually (not as a single aggregate), and the word position is greater than or equal to its pre-rotation value.
    - Immediately after the swap to landscape, `.mobile-cinema-scrub` lacks the hidden-state class; 4600ms later with playback untouched it carries it.
    - After rotating landscape→portrait, waiting 5000ms produces zero captured console messages and no element carries the hidden-state class.
    - `window.__bcfLayoutMode` reads `desktop` at 1100x900, `portrait` at 900x1100 and `landscape` at 900x600; `.mobile-app-landscape` is absent at 1100x900.
    - `history.length` before opening Settings equals `history.length` after an open → rotate → close sequence.
    - A fresh drag on the landscape scrub track after a mid-drag rotation yields a non-decreasing playhead sample sequence and zero captured console messages.
  </acceptance_criteria>

  <reversibility rating="reversible">The only production change is a short arrival branch in one existing function; everything else in this task is verification and tests.</reversibility>

  <done>Rotating mid-playback preserves every MOBL-03 field in both directions, arrives in landscape with chrome visible and the idle window started, aborts an in-flight drag cleanly without deferring the swap, keeps an open surface open with a balanced history, and respects the Phase 1 boundary matrix — with the CI half of D-23's two-pronged proof committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| History API → surface state | The `bcfMobileSurface` sentinel governs which flyout is open and is the sole mechanism behind back-gesture dismissal. |
| keyboard focus → modal containment | The focus trap is the only thing keeping Tab from reaching the sky, rail and dock behind an open panel. |
| outbound story links → new browsing context | The About flyout links to external story hosts. |
| viewport/orientation events → full rebuild | `matchMedia` and `orientationchange` are the only sanctioned triggers for a layout swap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Information Disclosure | external anchors in the landscape About flyout | low | mitigate | Task 1 asserts every `a[href^="http"]` inside the flyout carries a `rel` containing the no-opener token, so a linked page cannot reach back through `window.opener`. The flyout is reused unmodified from Phase 2; the assertion is new coverage in landscape. |
| T-03-04 | Denial of Service | history-stack growth across rotation with a surface open (D-21) | low | mitigate | `onLayoutMaybeChanged()` never touches history; `openMobileSurface` pushes at most one sentinel and reuses it on a Settings↔About swap. Task 2 asserts `history.length` is unchanged across open → rotate → close. |
| T-03-05 | Spoofing | a crafted history entry carrying a `bcfMobileSurface` value | low | accept | The `popstate` listener only ever CLOSES an already-open surface and never opens one from state; `openMobileSurface` allow-lists `kind` against `MOBILE_SURFACES`. No privileged data or action sits behind a surface. |
| T-03-09 | Elevation of Privilege | keyboard focus escaping an open landscape modal | low | mitigate | `trapMobileSurfaceFocus()` is re-attached for landscape by plan 02's Pitfall-3 fix; Task 1 asserts Tab and Shift+Tab both wrap inside the panel. |
| T-03-10 | Repudiation | claiming rotation is safe on hardware without hardware evidence | medium | mitigate | D-23 requires BOTH proofs. Task 2 delivers the Playwright viewport-swap half; plan 04's blocking gate delivers the real-device iOS half. Neither alone closes the requirement, and Phase 2 demonstrated a fully green suite still missing four device-only defects. |
| T-03-SC | Tampering | package-manager supply chain | high | accept | No package-manager install task exists in this plan or this phase; `web/` stays dependency-free and plan 04's freeze proof re-asserts it. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py` — full suite green.
2. `git diff --exit-code -- tests/test_mobile_plumbing.py web/mobile-gestures.js web/style.css` — the Phase 1 gesture-probe contract and both frozen web files untouched.
3. No cross-fade was built (D-20, RESEARCH A3): the landscape root's `className` read immediately after a rotation is byte-identical to the same read 500ms later — no transient transition/animation class is applied on the swap. (Asserted behaviorally rather than by grepping the constant's name, so an explanatory comment cannot invalidate the gate.)
4. Manual read-back: `onLayoutMaybeChanged()` contains no `setTimeout`, no pointer-state check and no early return other than the existing rAF coalescing and the `next === app.layoutMode` guard.
</verification>

<success_criteria>
- Every field MOBL-03 names survives rotation in both directions, asserted individually.
- Landscape arrival shows chrome and starts the idle window; leaving landscape leaves no armed timer.
- An open surface survives rotation with a balanced history sentinel.
- A mid-drag rotation aborts the drag cleanly without deferring the swap.
- Landscape flyouts dismiss three independent ways (backdrop, opening control, back gesture), trap focus, and never overlap the control dock.
- The Phase 1 boundary matrix still holds and `tests/test_mobile_plumbing.py` is unmodified.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-03-SUMMARY.md` when done.
</output>
