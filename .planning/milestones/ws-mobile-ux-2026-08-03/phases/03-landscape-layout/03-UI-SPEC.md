---
phase: 3
slug: landscape-layout
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-01
---

# Phase 3 — UI Design Contract: Landscape Layout

> Visual and interaction contract for `renderMobileLandscape()`. This document **locks** an already-approved design
> (`design/mobile-ux/prototype/layouts.jsx` `LandscapeF`, `scrubber.jsx` `CinemaScrub`, `panels.jsx` `FieldLog`,
> `styles.css` `@media (orientation: landscape)`) into concrete values for the planner and executor — it does not
> invent new visual design. **All design tokens (spacing scale, typography roles, color split, copywriting
> conventions) are inherited verbatim from `../02-portrait-layout/02-UI-SPEC.md` — this document adds
> landscape-specific layout tokens and copy only, and never forks the shared scale.** Generated by
> gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | **none** — no shadcn, no component framework, no bundler (unchanged from Phase 2) |
| Preset | not applicable |
| Component library | none — hand-authored DOM via `web/app.js`'s existing `el()` helper |
| Icon library | none — inline hand-drawn SVG, reused verbatim from `design/mobile-ux/prototype/panels.jsx` (`PlayIcon`, `GearIcon`, `InfoIcon`, `HelpIcon`); no new icon needed this phase — the landscape dock reuses the same Gear/Info icons Phase 2 already ported |
| Font | Reuse the three frozen `web/style.css:25-27` font-family variables — `--serif` (titles/headings), `--sans` (body), `--mono` (HUD labels/chips/meta). Unchanged from Phase 2. |

No shadcn gate applies — same `Tool: none` by-design contract as Phase 2 (`web/` is zero-dependency, no-build-step).

---

## Spacing Scale

**Inherited verbatim from `../02-portrait-layout/02-UI-SPEC.md`.** Any new spacing not covered by a locked landscape exception below MUST fall on Phase 2's 8-point scale (xs 4 / sm 8 / md 16 / lg 24 / xl 32 / 2xl 48 / 3xl 64). Do not re-declare or fork this table.

### Locked landscape exceptions (verbatim from `design/mobile-ux/prototype/styles.css`'s `@media (orientation: landscape)` block — do not re-grid to 8pt)

| Element | Value | Source |
|---------|-------|--------|
| Right rail width | `224px`, `flex-shrink: 0` | `.rail` (landscape) — the ~25% split is a fixed pixel rail against a fluid sky, not a percentage; at the phone-landscape widths this phase targets (≥568px per MOBF/D-36 routing) 224px lands close to 25–30% and stays legible at the field-log's 11px body size |
| Field log flex | `flex: 1 1 67%` of the rail's height | `.field-log` — MOBL-01's "top 2/3" acceptance bar |
| Control dock flex | `flex: 0 0 33%`, `min-height: 138px` | `.control-dock` — MOBL-01's "bottom 1/3" acceptance bar; the `min-height` floor keeps the Settings/About buttons + status line from collapsing on very short landscape heights (e.g. 320×568 rotated) |
| Field log padding | `12px 12px 8px` | `.field-log` |
| Control dock padding | `10px 12px` | `.control-dock` |
| Cinema-scrub pill inset | `left/right: 12px`, `bottom: 12px` within the sky region (absolute) | `.cinema-scrub` — same 12px inset convention as the portrait top-cluster |
| Cinema-scrub pill padding | `5px 8px 5px 5px` | `.cinema-scrub` |
| Cinema-scrub play button | 40×40 CSS px (`.fab-play.sm`) | `.cinema-scrub .fab-play.sm` — reuses the same `.fab-play` component Phase 2 already has, `sm` size variant |
| Cinema-scrub track height | `14px` (touch target is the whole pill, 40px min via the FAB + pill row height — see Tap Target Floor below) | `.cinema-scrub .scrub-track` |
| Cinema-scrub thumb | 12×12 CSS px, `scrub-progress` bar 2px, `scrub-rail` baseline 1px | `.cinema-scrub .scrub-thumb/.scrub-progress/.scrub-rail` |
| Cinema-scrub roll marker | 4×4 CSS px diamond | `.cinema-scrub .scrub-roll` — smaller than the portrait mini-rail's 6px dot; the pill is narrower and denser |
| Field-log live-roll card padding | `8px 10px`, `border-radius: 8px` | `.field-log .live` |
| Field-log entry padding | `5px 8px`, 2px left border accent | `.field-log .entry` |
| Field-log list gap | `5px` between entries | `.field-log .log-list` |
| Field-log evidence-quote hard clamp | see UI Considerations "long-text" row below — **new for this phase**, not in the prototype (Phase 3 addition per D-27) | N/A — Claude's Discretion CSS, `-webkit-line-clamp` or `max-height` |
| Dock quick-actions grid | 2-column grid, `gap: 8px`, buttons `min-height: 48px`, `padding: 9px 6px` | `.dock-grid .dock-btn` |
| Dock status row | single flex row, no extra padding beyond the `.control-dock` container's `10px 12px` | `.dock-status` |
| Rotation-arrival "chrome visible" grace | 0 (chrome renders visible synchronously on the arrival `render()`, D-31) — the auto-hide *timer* then starts, not the visibility state itself | `resetChromeHideTimer()` call sequence, Pattern 4 in RESEARCH.md |

**Tap target minimum:** 44×44 CSS px floor still applies project-wide (MOBX-03), same as Phase 2. Two exceptions carried forward from the prototype and Phase 2's own precedent (compact icon buttons at 36×36, the help/round icon-btn) plus one new landscape-specific exception:
- Cinema-scrub play FAB is `40×40` (`.fab-play.sm`) — below the 44px floor by prototype design. **Flagged, not silently accepted:** this is identical in kind to Phase 2's `.icon-btn.compact` (36×36) exception, which the Phase 2 UI-SPEC already priced in as a locked deviation for a small HUD control. Keep as 40×40 per the locked visual reference; if the Phase 4 accessibility pass (MOBX-03, Lighthouse ≥ 90) flags it, that is Phase 4's remediation, not this phase's — do not pre-emptively enlarge it and drift from the approved prototype.
- Dock quick-action buttons (`.dock-btn`) are `min-height: 48px` — already clears the 44px floor (their width, driven by the 2-col grid at 224px rail width minus padding/gap, is ≈96px — also clears).

### Safe-area insets (landscape puts the notch on a SHORT edge)

**Added after UI-checker review flagged its absence.** Portrait only ever had to clear the notch at the top and the home indicator at the bottom, so Phase 2 used `--safe-top` / `--safe-bottom`. Landscape rotates the notch onto a *side* edge, and which side depends on rotation direction — so both must be handled, not just one. Phase 1 already defined the variables for exactly this (`web/mobile.css:53-56`, commented "consumed by Phase 2/3 docks and rails"); they are currently unused and Phase 3 is their intended first consumer. Use the variables, never raw `env()` calls, so the fallbacks stay centralized.

| Surface | Inset contract | Why |
|---|---|---|
| `.mobile-app` landscape root | `padding-left: var(--safe-left); padding-right: var(--safe-right)` | Handles **both** rotation directions with one rule — whichever edge carries the notch gets a non-zero value and the other resolves to `0px`. Applying it at the root means the sky and rail inherit correct bounds without either needing its own rule. |
| Right rail (`.control-dock` bottom edge) | `padding-bottom: max(<locked dock padding>, var(--safe-bottom))` | The home indicator sits on the bottom edge in both landscape directions, same `max()` idiom Phase 2 used for the portrait dock (`mobile.css:226`). |
| Top chip cluster | `padding-top: max(<locked inset>, var(--safe-top))` | Small in landscape but non-zero on some devices; cheap insurance and consistent with portrait. |
| Cinema-scrub pill | none of its own | It is positioned inside the sky, which already sits inside the root's inset padding. Adding a second inset here would double-count. |

🧪 **backstop** — the inset values are `0px` in the simulator and in Chrome/Android, so this contract can only be confirmed on a real notched iPhone, in **both** rotation directions (notch-left and notch-right). Verify the rail is never clipped and the sky's focal label never slides under the notch.

---

## Typography

**Inherited verbatim from `../02-portrait-layout/02-UI-SPEC.md`'s 4-role table** (Micro-label / Body / Heading / Display), same fonts, same weights (400 regular, 700 bold — exactly 2). Landscape introduces no new role and no new weight. New landscape-only text elements map onto the existing roles as follows:

| Element | Role | Size | Weight | Line Height | Notes |
|---------|------|------|--------|--------------|-------|
| Top chip (landscape, `CH {num}` only — no word count) | Micro-label | 10px | 400 | 1.3 | Same `.chip` component as portrait; content differs (word count omitted per prototype `layouts.jsx:69` — a real, intentional content difference between the two `.top-cluster` variants, not an oversight) |
| Cinema-scrub roll-count (`{idx+1} / {total}`) | Micro-label | 10px | 400 | 1.3 | `.cinema-scrub .roll-count` |
| Field-log header (`Field Log · {constellation}`, count) | Micro-label | 9px | **600** — see flag below | 1.3 | `.field-log h3` — prototype sets `font-weight: 600` here, a THIRD weight value not present in Phase 2's locked 2-weight table. **Resolution: treat as bold (700) per the locked 2-weight contract, not a new 600 weight** — visually near-identical at 9px, and introducing a third weight value would violate the checker's typography dimension. Executor uses `font-weight: 700` for `.mobile-field-log-header`, not the prototype's literal 600. |
| Field-log live-roll row1 (`◢ ROLL {n} · {outcome}` / CP) | Micro-label | 8px | 400 | 1.3 | `.field-log .live .row1` |
| Field-log live-roll name (perk name / constellation) | Body (`--serif`) | 12px | 400 | 1.25 | `.field-log .live .name` |
| Field-log live-roll evidence quote (truncated ~100 char, italic) | Body (`--serif`, italicized) | 9.5px | 400 | 1.3 | `.field-log .live .sub` — the D-27 truncated quote; hard height clamp required, see UI Considerations |
| Field-log recent entry row1 (`Ch {n}` / outcome-CP) | Micro-label | 8px | 400 | 1.3 | `.field-log .entry .row1` |
| Field-log recent entry name | Body (`--serif`) | 11px | 400 | 1.2 | `.field-log .entry .name`, single-line ellipsis (long-text, see below) |
| Dock quick-action label (`Settings` / `About`) | Micro-label | 9px | 400 | 1.3 | `.dock-grid .dock-btn` |
| Dock status row (`{speed}× · {mode}` / `{pov} POV`) | Micro-label | 8px | 400 | 1.3 | `.dock-status` |

No display-role text is introduced by landscape beyond the shared sky focal label (already locked in Phase 2's spec, reused unmodified per D-35).

---

## Color

**Inherited verbatim from `../02-portrait-layout/02-UI-SPEC.md`** — all values reused from `web/style.css:9-27` frozen `:root` variables, never redefined in `web/mobile.css`. The 60/30/10 split and `--rose`/`--green`/`--amber` semantic reservations are unchanged. Landscape-specific additions to the **accent reserved-for list** (still `--cyan`, nothing new):

**New accent (`--cyan`) usages this phase — appended to Phase 2's reserved-for list, not a new color:**
- Cinema-scrub play/pause FAB fill (same `.fab-play` component as portrait)
- Cinema-scrub progress bar + thumb (`.scrub-progress`, `.scrub-thumb`) — the landscape equivalent of the portrait mini-rail's playhead
- Field-log header label text (`.field-log h3`)
- Active dock quick-action button state (`.dock-btn.is-active`) — same `.icon-btn.is-active`-family pattern as portrait's gear/info buttons

**New secondary-surface (`--panel`/`--edge` family) usages — appended, not new colors:**
- Right rail background gradient (`linear-gradient(270deg, rgba(5,29,38,0.96), rgba(5,29,38,0.55))`)
- Control dock background (`rgba(3,8,13,0.55)`)
- Cinema-scrub pill background/border (`rgba(5,29,38,0.85)` / `rgba(92,244,255,0.35)`)
- Field-log live-roll card border/background uses `--green` at 0.10/0.45 alpha (`rgba(123,255,189,...)`) — this is the existing **second semantic color** (Phase 2: "reserved for hit outcome markers only") extended to frame the live-roll card itself, since the live roll is definitionally the "in-progress / just landed" state, not a new use of green as an interactive accent. Flagged for checker awareness, not treated as a deviation requiring sign-off — it's the same semantic (in-play / active narrative moment), applied to a card instead of a dot.

No destructive actions exist in landscape (same as Phase 2: no delete/discard/reset UI). `--rose` remains reserved for "miss" outcome markers only (`.scrub-roll.miss`, `.roll-bin.miss` equivalents in the cinema-scrub and field-log entries).

---

## Copywriting Contract

All copy below is locked verbatim from `design/mobile-ux/prototype/panels.jsx` (`FieldLog`) / `layouts.jsx` (`LandscapeF`), reusing every Settings/About/Help string already locked in Phase 2's UI-SPEC unchanged (Settings and About/Help content and structure are identical across layouts per D-33 — only their mount position/CSS anchor differs, not their copy).

| Element | Copy |
|---------|------|
| Primary CTA | No new CTA this phase — landscape reuses Phase 2's Help overlay "Got it — read on" verbatim (same component, mounted per D-33/D-19 convention) |
| Top chip (CH, landscape variant) | `CH {chapterNum}` — **no word count**, unlike portrait's `CH {num} · {words}w`. This is an intentional, locked content difference (prototype `layouts.jsx:69`), not a truncation bug |
| Top chip (active roll, amber) | `◢ R{rollNumber ?? "—"}` — identical to portrait; omitted entirely when no roll active |
| Field-log header | `Field Log · {live?.constellation ?? "—"}` (left) · `{idx + 1} of {data.rolls.length}` (right, count in `--dim`) |
| Field-log live-roll row1 | `◢ ROLL {rollNumber ?? "—"} · {outcome}` (left) · `{perkCost != null ? perkCost + " CP" : "—"}` (right) |
| Field-log live-roll name | `{perkName || constellation || "—"}` |
| Field-log live-roll sub | `{perkJump}{perkJump && evidenceQuote ? " · " : ""}{evidenceQuote ? '"' + truncate(evidenceQuote, 100) + '"' : ""}` — truncate helper: `s.length > n ? s.slice(0, n-1).trim() + "…" : s` |
| Field-log recent entry row1 | `Ch {chapterNum}` (left) · `{outcome === "hit" ? perkCost + " CP" : outcome}` (right) |
| Field-log recent entry name | `{perkName || constellation || "—"}` |
| Cinema-scrub roll count | `{activeIdx + 1} / {data.rolls.length}` |
| Dock quick-action labels | `Settings` · `About` (with `GearIcon`/`InfoIcon`, reused verbatim) |
| Dock status row | `{speed}× · {mode}` (left) · `{pov || "Joe"} POV` (right, `.val` styled) |
| Settings/About/Help content | **Unchanged from Phase 2** — same group labels, option labels, value strings (including the mandatory `"detail"` singular correction Phase 2's UI-SPEC already locked), About/Help copy blocks. D-33 changes only the mount point, never the content. |
| Empty state (no active roll in field log) | The `.field-log .live` card is structurally omitted (`{live && (...)}` in the prototype) — same "absent element, not placeholder copy" convention as portrait's amber chip |
| Empty state (no recent rolls yet, e.g. word position 0) | `recentRolls()` returns `[]`; `.log-list` renders with zero `.entry` children below the (also possibly absent) live card — no "no rolls yet" copy exists in the prototype reference; **flagged as an explicit backstop below**, not silently assumed |
| Error state | **Not modified this phase.** Landscape reuses the same shared `renderLoadError()`/`renderLoading()` pre-branch contract as portrait (`render()` early-returns before the layout-mode branch) — no landscape-specific error/loading copy |
| Destructive confirmation | **Not applicable this phase.** No destructive actions in landscape (same as Phase 2) |

**Accessible names for icon-only controls** (added after UI-checker review). Every control whose visible content is an icon or glyph carries an explicit `aria-label`, matching the portrait convention:

| Control | `aria-label` |
|---|---|
| Cinema-scrub play/pause FAB (`.fab-play.sm`) | `"Play"` / `"Pause"`, toggled with state — same string pair the portrait dock FAB already uses, so screen-reader output is consistent across layouts |
| Top-cluster help button | `"Help"` |
| Dock quick-actions | `"Settings"` / `"About"` — these also carry visible text labels, so the `aria-label` is belt-and-braces rather than the only name |

This matters beyond hygiene: the cinema-scrub FAB is the only play/pause control visible in landscape once chrome is revealed, and Phase 4's MOBX-03 gate requires Lighthouse Accessibility ≥ 90.

---

## UI Considerations

> State coverage for the shape-rooted UI surfaces this phase touches (top chips, sky + cinema-scrub, right rail's
> field log + control dock, Settings/About/Help — reused). Empty/error COPY is defined above in Copywriting
> Contract; this section covers state coverage and references those rows. Probe run 2026-08-01
> (ui-consideration-probe over 6 landscape-specific elements plus the reused surface stack, collapsed below by
> shared resolution with Phase 2 where the surface stack is unchanged).

Applicable state considerations resolved: 10 covered, 5 backstop, 3 dismissed, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| Loading (whole landscape surface) | Whole landscape surface | ✅ covered | Same pre-branch contract as portrait: `app.error`/`!app.data` early-return the shared `renderLoading()`/`renderLoadError()` before the layout-mode branch — no landscape element ever renders without data (identical mechanism Phase 2 already locked, reused unchanged) |
| Empty (no active roll) | Cinema-scrub is unaffected (it always shows the track/progress/thumb regardless of an active roll); field-log live card | ✅ covered | `.field-log .live` card structurally omitted when `live` is null, matching the portrait amber-chip convention — see Copywriting Contract |
| Empty (unknown chapter/constellation) | Top chip, field-log header | ✅ covered | Field-log header falls back to `"—"` for the constellation when `live?.constellation` is falsy (Copywriting Contract row) |
| Empty (zero rolls in data) | Cinema-scrub bins, field log | ✅ covered | `binRolls([])` returns `[]` (same verbatim-ported function as portrait, D-35's shared math); field log renders header + empty `.log-list` with no crash — same null-safety pattern as portrait's mini-rail |
| Partial (missing roll fields) | Field-log rows, dock status | ✅ covered | Every field has an explicit fallback: `R{rollNumber ?? "—"}`, `{perkCost != null ? ... : "—"}`, `{pov || "Joe"} POV` — no `undefined` ever renders (same discipline as Phase 2) |
| Long-text | Field-log recent entry name, dock status row | ✅ covered | `.field-log .entry .name` gets `white-space: nowrap; overflow: hidden; text-overflow: ellipsis` (locked in prototype CSS) — never wraps or pushes layout |
| Long-text (evidence quote) | Field-log live-roll `.sub` (D-27's truncated quote) | ✅ covered | D-27 locks a ~100-char truncation at the data layer (`truncate(evidenceQuote, 100)`, same helper as the prototype) **plus** a CSS hard clamp is required as defense-in-depth: `.mobile-field-log-live-sub` gets `max-height: calc(1.3em * 3)` (or `-webkit-line-clamp: 3` with `display: -webkit-box; -webkit-box-orient: vertical; overflow: hidden`) so even a pathological 100-char quote with unusual line-wrapping cannot push `.log-list`'s recent entries out of the visible rail — this is the CONTEXT.md D-27 "MUST have a hard height clamp" requirement, made concrete |
| Zero-one-many | Field-log recent list (`recentRolls(wordPos, 6).slice(live ? 1 : 0)`) | ✅ covered | 0 recent → empty `.log-list` below the live card (or fully empty at word 0, see backstop below); 1–5 recent → one `.entry` per row; the `.slice(live?1:0)` avoids double-showing the live roll in both the live card and the recent list — same de-dup logic as the prototype, ported verbatim |
| Zero-one-many | Cinema-scrub roll markers (`.scrub-roll`, same `binRolls` as portrait) | ✅ covered | Same binning algorithm as portrait's mini-rail (D-35/RESEARCH "Don't Hand-Roll" table) — 0/1/many rolls all handled by the shared, already-tested `binRolls`/`finalizeBin` |
| Populated (normal playback) | All landscape surfaces | ✅ covered | The default state is the contract itself — chips/scrub/field-log/dock copy and tokens locked in the sections above |
| Overflow (short landscape viewport) | Right rail (field log + control dock) at short heights (e.g. 320×568 rotated, or a Chromebook window narrowed to the D-36 landscape boundary) | 🧪 backstop | `.control-dock`'s `min-height: 138px` floor plus `.field-log`'s `flex: 1 1 67%` could combine to overflow the rail's total height below some threshold — needs a Playwright bounding-box assertion (mirroring `test_portrait_layout_proportions_and_chip_overlap`) confirming no rail content is clipped/overlapping at the smallest in-scope landscape viewport (390×568, the rotated form of Phase 2's smallest portrait test viewport) |
| Overflow (field-log list itself) | `.log-list` when many recent entries render at a tall rail | 🧪 backstop | `.log-list` already has `overflow-y: auto` + `touch-action: pan-y` in the locked prototype CSS (internal scroll, not page scroll) — this is the *design*, but needs a Playwright assertion that the list scrolls internally rather than pushing `.control-dock` off-screen, since D-26 says the log "auto-follows the playhead; it does not scroll" for the *live* roll but the *recent* list is still a bounded, potentially-overflowing box |
| Empty (word position 0, no rolls have fired yet) | Field-log live card AND recent list both empty simultaneously | 🧪 backstop | Not explicitly covered by the prototype reference (its own demo data likely never starts at word 0 with zero prior rolls) — needs an explicit assumption: the header still renders (`Field Log · —`, `0 of {total}`), `.log-list` renders with zero children, no "nothing here yet" placeholder copy exists. **Assumption, not a locked design choice** — flagged for the executor to confirm this reads acceptably rather than as a broken/blank panel; if it reads as broken at the Phase C gate, routing a minimal placeholder string is a same-phase fix, not a re-plan |
| Overflow (top chip vs. cinema-scrub vertical collision) | Top-cluster chips vs. the cinema-scrub pill, both absolutely positioned inside the sky region | 🧪 backstop | Both are `position: absolute` within the same relatively-positioned sky container (top-cluster at `top:12px`, cinema-scrub at `bottom:12px`) — no *horizontal* overlap risk at typical phone-landscape heights (≥320px tall), but the shortest in-scope landscape height (320×568 rotated → 320px tall sky region minus rail... actually sky height = viewport height here since landscape stacks sky/rail horizontally) needs the same Playwright bounding-box non-overlap assertion Phase 2 already has for portrait's chip-vs-focal-label case, adapted to chip-vs-cinema-scrub |
| First reveal-tap suppressing pause (D-30) | Sky tap when chrome is hidden | 🧪 backstop | Not a visual/copy state but a *behavioral* state-coverage item that belongs in this section per the probe's scope: the "first tap reveals and returns, never falls through to togglePlayback" contract (RESEARCH.md Open Question 2) needs a Playwright assertion, not just a visual check — already flagged as a required test in RESEARCH.md's requirement→test map, restated here as the UI-state entry it is |
| Empty/error (per-flyout, Settings/About/Help while open) | Settings/About/Help in landscape | ✖ dismissed | Same reasoning as Phase 2: these read already-loaded in-memory `app.*` state only, unreachable before data load, perform no async work — D-33 changes only their mount point, not this analysis |
| Zero-one-many (static collections) | Settings segments, dock quick-action buttons (fixed at 2: Settings/About) | ✖ dismissed | Fixed-cardinality UI defined by the contract — cardinality cannot vary at runtime, same as Phase 2's dismissed row for this category |
| Rotation transition state | Mid-rotation frame (portrait → landscape DOM swap) | ✖ dismissed | Explicitly NOT a visual state to design for — D-20 mandates state preservation via a full `render()` rebuild, not a transitional/interpolated visual state; ROTATION_ANIM (220ms) is confirmed unused this phase (RESEARCH.md Assumption A3). This is a behavioral/architectural contract (MOBL-03), not a UI-considerations state — covered by the plan's Playwright rotation test and the D-23 device pass, not by this document |

---

## Registry Safety

Not applicable — no component registry (no shadcn, no npm packages in `web/`). All markup hand-authored; all visual reference material comes from this project's own approved prototype, not a third-party source. Unchanged from Phase 2.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: FLAG → **both findings resolved before planning.** The missing landscape safe-area contract is now specified (Spacing Scale § "Safe-area insets", covering both rotation directions via `--safe-left`/`--safe-right`), and accessible names for icon-only controls are now specified (Copywriting Contract § "Accessible names").
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: FLAG (non-blocking) — many within-role pixel sizes, identical in kind to Phase 2's already-accepted verbatim-port precedent. Exactly 2 weights preserved; the prototype's stray `600` is concretely mapped to `700`.
- [x] Dimension 5 Spacing: FLAG (non-blocking) — several non-4-multiple landscape values, each individually sourced to the approved prototype CSS through the same "locked exceptions" mechanism Phase 2 used.
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-08-01 (gsd-ui-checker, 6/6 dimensions, 3 FLAGs). The two actionable Dimension-2 findings were fixed before planning; the Typography and Spacing flags are accepted precedent carried forward from Phase 2.
