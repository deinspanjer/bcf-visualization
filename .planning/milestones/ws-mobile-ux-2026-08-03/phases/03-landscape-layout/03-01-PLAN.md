---
phase: 03-landscape-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app.js
  - web/mobile.css
  - tests/test_mobile_landscape.py
  - tests/test_mobile_portrait.py
  - tests/helpers/web_runtime_site.py
autonomous: true
requirements: [MOBL-01]
user_setup: []

must_haves:
  truths:
    - "At a landscape phone viewport the app mounts its own mobile landscape surface (.mobile-app root, .mobile-sidebar rail) and never mounts renderAppShell()'s .app / .portrait-banner markup (MOBL-01, retires the D-12 fallback)."
    - "The landscape right rail carries a field log built from recentRolls(wordPos, 6) — the D-24 model seam — and never from renderNarrativeReadout / renderRecentRolls (§0.2 frozen); .narrative-mount is absent at a landscape viewport."
    - "As playback advances in landscape the field log re-derives from the current word position so the live roll stays at top, driven by the existing updatePlaybackFrame dispatch with zero structural re-renders (D-18, D-26)."
    - "Landscape puts the sky region at ~75% of viewport width against a 224px fixed right rail; inside the rail the field log takes the top ~2/3 and the control dock the bottom ~1/3 with a 138px min-height floor (MOBL-01, UI-SPEC Spacing Scale)."
    - "The landscape sky region reuses ONE shared sky helper with portrait (viewport frame, camera layer, focal label, tap hint, Help overlay mount, Settings/About mount) — the markup exists in exactly one place in web/app.js (D-35)."
    - "Loading and error states are unreachable inside any landscape element: render() early-returns renderLoading()/renderLoadError() before the layout-mode branch (UI-SPEC covered)."
    - "The field log's live-roll card is structurally absent when no roll has fired at or before the current word position — no placeholder copy is rendered in its place (UI-SPEC covered)."
    - "The field-log header falls back to an em-dash for an unknown constellation and the recent-entry rows fall back to an em-dash for a missing perk name, so no field ever renders as the literal word undefined (UI-SPEC covered)."
    - "A story with zero rolls renders the field-log header plus an empty list and a cinema-scrub with zero roll markers, without throwing (UI-SPEC covered, exercised against the no-rolls package)."
    - "The recent-entry list excludes the live roll (it is sliced off the head of recentRolls' newest-first result) so the same roll never appears twice in the rail (UI-SPEC covered)."
    - "A recent-entry perk name that overflows the 224px rail is clipped with an ellipsis on one line and never wraps or widens the rail (UI-SPEC covered)."
    - "The live-roll evidence quote is truncated to ~100 characters at the data layer AND hard-clamped in CSS, so a long quote can never push the recent list out of the rail (D-27, UI-SPEC covered)."
    - "Evidence-quote text is rendered as text content, never as markup: a quote containing angle-bracket tag syntax renders as literal characters and produces no element nodes inside the quote node (T-03-01)."
    - "The landscape body scroll-lock applies to the whole mobile block, not just portrait, so the frozen style.css:45 min-height:100vh rule cannot make the landscape shell scrollable on iOS (D-32)."
    - statement: "At the smallest in-scope landscape viewport (568x320, the rotation of Phase 2's PHONE_PORTRAIT_SMALL) no rail content is clipped or overlapping: the field log and control dock bounding boxes both sit fully inside the .mobile-sidebar box and do not overlap each other."
      verification: backstop
    - statement: "The field-log list scrolls internally when many recent entries render at a tall rail: .mobile-field-log-list scrollHeight may exceed its clientHeight while the .mobile-control-dock bounding box stays fully inside the viewport."
      verification: backstop
    - statement: "At word position 0 with no roll yet fired, the field-log header still renders (constellation em-dash, count reading 0 of N) and the list renders with zero children — no nothing-here-yet placeholder copy exists; the executor confirms at the Phase C gate that this reads as an empty log rather than a broken panel."
      verification: backstop
    - statement: "The top chip cluster and the cinema-scrub pill, both absolutely positioned inside the same landscape sky stage, have non-overlapping bounding boxes at 844x390 and at 568x320."
      verification: backstop
    - statement: "Landscape safe-area insets are applied via the --safe-left/--safe-right/--safe-bottom/--safe-top variables (never raw env() calls outside the :root block) and can only be confirmed on a real notched iPhone in BOTH rotation directions — the rail must never be clipped and the sky's focal label must never slide under the notch."
      verification: backstop

  prohibitions:
    - "MUST NOT let a landscape surface look alive while being inert: every dispatch site that today tests only for portrait must cover landscape too, so a reader never sees a field log that silently stops advancing with no error anywhere."
    - "MUST NOT silently degrade the frozen desktop experience or the Phase 1 gesture-probe contract to make landscape work — tests/test_mobile_plumbing.py, web/mobile-gestures.js and web/style.css stay byte-identical, and a landscape failure is fixed in landscape code, never by relaxing a frozen file."
    - "MUST NOT render agent-curated or story-derived text (evidence quotes, perk names, chapter titles) as markup — a reader must never be able to receive executable or structural content through the evidence corpus."

  flagged_assumptions:
    - id: FA-MOBL-01
      origin: edge-probe (spec-less fallback §A)
      requirement: MOBL-01
      status: unresolved
      note: "The deterministic edge probe returned category=unclassified for MOBL-01, so no edge criteria were auto-derived. The criteria below are the PLANNER'S OWN, authored from RESEARCH.md Pitfalls 2/4/6 and UI-SPEC ## UI Considerations. They are flagged, not probe-resolved."
      criteria:
        - "Zero-roll story (no-rolls package): field-log header renders, list has zero children, cinema-scrub has zero roll markers, no console error."
        - "Word position 0 with rolls present elsewhere: live card absent, recent list empty, count reads 0 of N."
        - "Field log with exactly one roll at or before the playhead: live card present, recent list has zero entries (the slice removes the only row)."
        - "Smallest in-scope landscape viewport 568x320: rail children fully contained, no clipping."
        - "Evidence quote longer than 100 characters: truncated with an ellipsis at the data layer and clamped in CSS."

  artifacts:
    - "web/app.js: renderMobileLandscape(), renderMobileSkyRegion(frame), renderMobileLandscapeSidebar(frame), renderMobileFieldLog(), renderMobileFieldLogChildren(rows), mobileFieldLogRows(), mobileFieldLogQuoteText(roll), mobileTruncate(s, n), renderMobileCinemaScrub(), renderMobileCinemaScrubTrackChildren(), updateMobileLandscapeFrame(), updateMobileFieldLogFrame(), mobileScrubWidthDefaultForLayout()"
    - "web/app.js: landscape arms in render(), cachePlaybackDomRefs(), updatePlaybackFrame(); landscape branch in mobileChChipText()"
    - "web/app.js state: app.mobileRailWidthLayout, app.frameKeys.mobileFieldLog, app.frameKeys.mobileCinemaScrub, app.dom.mobileFieldLog*, app.dom.mobileCinemaScrub*"
    - "web/mobile.css: .mobile-app-landscape, .mobile-landscape-stage, .mobile-sky-landscape, .mobile-sidebar, .mobile-field-log, .mobile-field-log-header, .mobile-field-log-list, .mobile-field-log-live, .mobile-field-log-entry, .mobile-control-dock, .mobile-dock-grid, .mobile-dock-btn, .mobile-dock-status, .mobile-cinema-scrub and its track/inner/rail/progress/thumb/roll/fab/count children"
    - "tests/test_mobile_landscape.py (new)"
    - "tests/test_mobile_portrait.py: test_landscape_fallback_is_unchanged rewritten as test_landscape_no_longer_falls_back_to_desktop_shell"
    - "tests/helpers/web_runtime_site.py: one evidence_quotes entry added to a dense-rolls roll"

  key_links:
    - "render() landscape arm -> renderMobileLandscape() (the retirement of the D-12 fallback)"
    - "cachePlaybackDomRefs() landscape refs -> updateMobileLandscapeFrame() (without both, the field log mounts once and never advances)"
    - "recentRolls(wordPos, 6) -> mobileFieldLogRows() (D-24 model seam; the ONLY field-log data call)"
    - "renderMobileSkyRegion() -> renderMobilePortrait() AND renderMobileLandscape() (D-35 single sky implementation)"
    - "web/mobile.css outer mobile media query -> every new landscape rule (no rule escapes into the frozen desktop cascade)"
---

<objective>
Retire the D-12 interim landscape fallback and stand up the real landscape surface end to end: a shared sky region at ~75% width beside a 224px right rail carrying a live field log over a Settings/About control dock, wired through the existing render → cache-refs → incremental-update tier so the log follows the playhead with zero structural re-renders.

Purpose: MOBL-01. This is the phase's tracer — it proves the whole landscape architecture (routing, structural render, DOM-ref caching, incremental update, shared sky helper, D-24 data seam) on one working path before gestures, auto-hide, rotation and the surface stack expand out from it in plans 02 and 03.

Output: `renderMobileLandscape()` and its sub-renderers in `web/app.js`, the landscape CSS block in `web/mobile.css`, a new `tests/test_mobile_landscape.py`, and the rewritten landscape-fallback regression guard.

Phase goal (ROADMAP, verbatim — NOT in user-story form; see the planner note in the phase return): *"A phone turned sideways gives a cinema view with the field log, and rotating between layouts never loses your place."*
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-PATTERNS.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-UI-SPEC.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-VALIDATION.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through the main checkout's venv: `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...`. Baseline is 33 passing.
- Commit signing is disabled repo-wide (`commit.gpgsign=false`); commits need no auth. `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`.
- Two untracked paths are not yours: `.planning/workstreams/mobile-ux/config.json` and `design/mobile-ux/`. Stage specific files only; never `git add .`.
- `tests/test_mobile_plumbing.py`, `web/mobile-gestures.js` and `web/style.css` stay byte-identical. Research confirmed zero edits are needed in any of them.
</session_constraints>

## Artifacts this phase produces

Full phase-wide symbol inventory (this plan creates the rows marked **[01]**; later plans create the rest, listed here so no plan invents a competing name).

| Symbol / artifact | Kind | Plan |
|---|---|---|
| `renderMobileLandscape()` | render function (`web/app.js`) | **[01]** |
| `renderMobileSkyRegion(frame)` | shared sky helper (D-35) | **[01]** |
| `renderMobileLandscapeSidebar(frame)` | render function | **[01]** |
| `renderMobileFieldLog()` / `renderMobileFieldLogChildren(rows)` | render functions | **[01]** |
| `mobileFieldLogRows()` | model adapter over `recentRolls()` | **[01]** |
| `mobileFieldLogQuoteText(roll)` / `mobileTruncate(s, n)` | helpers | **[01]** |
| `renderMobileCinemaScrub()` / `renderMobileCinemaScrubTrackChildren()` | render functions | **[01]** |
| `updateMobileLandscapeFrame()` / `updateMobileFieldLogFrame()` | incremental tier | **[01]** |
| `mobileScrubWidthDefaultForLayout()` | helper (Pitfall 6) | **[01]** |
| landscape arms in `render()` / `cachePlaybackDomRefs()` / `updatePlaybackFrame()` | dispatch edits | **[01]** |
| landscape branch in `mobileChChipText()` | copy edit | **[01]** |
| `attachMobileGestures()` (rename+generalize of `attachMobilePortraitGestures`) | gesture lifecycle (D-34) | 02 |
| `resetMobileChromeHideTimer()` / `revealMobileChrome()` | auto-hide timer | 02 |
| `app.mobileChromeHideTimer` (new), `app.chromeHidden` (existing, first consumer) | state | 02 |
| `rebuildMobileCinemaScrubTrack()` | ResizeObserver rebuild target | 02 |
| landscape arrival handling in `onLayoutMaybeChanged()` (D-31, rail-width reset) | dispatch edit | 03 |
| `app.mobileRailWidthLayout` | state | **[01]** |
| `app.frameKeys.mobileFieldLog`, `app.frameKeys.mobileCinemaScrub` | state | **[01]** / 02 |
| `app.dom.mobileFieldLogList/Live/Header`, `app.dom.mobileCinemaScrubTrack/Inner/Thumb/Progress/Fab/Count` | state | **[01]** |
| CSS: `.mobile-app-landscape`, `.mobile-landscape-stage`, `.mobile-sky-landscape`, `.mobile-sidebar`, `.mobile-field-log*`, `.mobile-control-dock`, `.mobile-dock-grid`, `.mobile-dock-btn`, `.mobile-dock-status`, `.mobile-cinema-scrub*` | `web/mobile.css` | **[01]** |
| `tests/test_mobile_landscape.py` | new test file | **[01]** (extended by 02, 03) |
| `tests/test_mobile_portrait.py::test_landscape_no_longer_falls_back_to_desktop_shell` | rewritten regression guard | **[01]** |
| `COVERAGE.md` | phase artifact | 04 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end landscape slice — render arm, shared sky helper, live field log, incremental tier</name>

  <precondition>The main checkout's virtualenv exists and is runnable at `/Users/dre/src/bcf-visualization/.venv/bin/python` (this worktree has no `.venv`). Assert with `/Users/dre/src/bcf-visualization/.venv/bin/python -c "import playwright"` before running any test; halt if it fails.</precondition>

  <read_first>
    - `web/app.js:975-1017` — `render()`'s current body: the D-12 one-line portrait/else dispatch at `:991`, the `cachePlaybackDomRefs()`/`updatePlaybackFrame()` pair at `:992-993`, and the `layoutMode !== "desktop"` probe block at `:994-1016` whose inner `layoutMode === "portrait"` guard at `:1005` owns BOTH the gesture attach and the D-16 focus-trap re-attach (Pitfall 3 — plan 02 changes that inner guard, not this plan).
    - `web/app.js:3340-3413` — `renderMobilePortrait()`, the exact structural analog (PATTERNS.md §renderMobileLandscape). Lines `3348-3370` are the sky-region block this task extracts.
    - `web/app.js:791-798` — `recentRolls(wordPos, count)`, the D-24 model seam. Returns NEWEST-FIRST.
    - `web/app.js:2680-2724` — `cachePlaybackDomRefs()` and its portrait-only ref block at `:2701-2719`.
    - `web/app.js:2726-2747` — `updatePlaybackFrame()`; note the comment at `:2728-2732` explaining why the mobile early-return MUST precede the `!app.dom?.playhead` gate.
    - `web/app.js:2800-2810` — desktop's `narrativeKey`/`app.frameKeys.narrative` memoized-diff idiom, the pattern `updateMobileFieldLogFrame()` replicates.
    - `web/app.js:3742-3772` — `updateMobilePortraitFrame()`, the per-layout incremental analog.
    - `web/app.js:3624-3656` — `renderMobileTopCluster()` and `mobileChChipText()`.
    - `web/app.js:3661-3681` — `renderMobileFocalLabel()`: the exact principal-perk expression (`paidRollPerks(roll)[0] ?? (roll.free_perks || [])[0]` through `perkDisplayLabel`) the field-log name must REUSE, not re-derive.
    - `web/app.js:3226-3319` — `binRolls`/`finalizeBin`/`binSize`, `recomputeMobileRailBins()` (note its three-call-site rule) and `renderMobileRailRollsLaneChildren()`.
    - `web/app.js:3192-3224` — `panOffsetForPlayhead()` / `mobileInnerFraction()`.
    - `web/app.js:571-607` — the live roll schema (`roll_label`, `outcome`, `constellation`, `jump`, `word_position`, `chapter_num`, `evidence_quotes`, `free_perks`, `purchased_perks`, `uid`).
    - `web/app.js:2653-2679` — `mobileGearIcon()` / `mobileInfoIcon()` / `mobileHelpIcon()`.
    - `web/app.js:3966-4015` — the module-level `data-action` click delegation (already handles `toggle-playback`, `mobile-open-settings`, `mobile-open-info`, `mobile-open-help`, `mobile-close-surface`). Landscape buttons need matching attributes and NO new JS wiring.
    - `web/app.js:3415-3435` — `renderMobileSurface()` and `renderMobileSurfaceBackdrop()` and the D-19 comments explaining why the panel lives in the sky while the backdrop spans the app root.
    - `web/app.js:3119-3127` — `MOBILE_SPEED_RUNGS` / `mobileSpeedMultiplier()`.
    - `design/mobile-ux/prototype/layouts.jsx:56-113` — `LandscapeF`, the DOM reference.
    - `design/mobile-ux/prototype/panels.jsx:176-215` — `FieldLog`, the D-25 content reference including the `.slice(live ? 1 : 0)` de-dup and the `truncate(s, n)` helper.
    - `design/mobile-ux/prototype/scrubber.jsx:254-290` — `CinemaScrub`'s markup only (NOT its pointer wiring — plan 02 uses `attachRailScrub` instead, D-17).
    - `tests/test_mobile_portrait.py:1-70` — the harness (`_chromium_browser_or_skip`, `_page_with_console_capture`, `PHONE_PORTRAIT`, `PHONE_PORTRAIT_SMALL`, `PHONE_LANDSCAPE = {"width": 844, "height": 390}`, `DEFAULT_STORAGE`).
    - `tests/test_mobile_portrait.py:1264-1298` — `test_landscape_fallback_is_unchanged`, the test this task rewrites; its own comment anticipates this obsolescence.
    - `tests/test_mobile_portrait.py:190-205` — the `window.__bcfRenderStats` init-script/zero-structural-render idiom.
  </read_first>

  <behavior>
    - At `PHONE_LANDSCAPE` (844x390) with `tiny-default`: `document.querySelector(".mobile-app")` is non-null, `document.querySelector(".app")` is null, `document.querySelector(".portrait-banner")` is null, `document.querySelector(".narrative-mount")` is null, and `document.querySelector(".mobile-gesture-probe")` is still non-null.
    - The rail exists: `.mobile-sidebar` contains exactly one `.mobile-field-log` and exactly one `.mobile-control-dock`; the dock contains buttons carrying `data-action="mobile-open-settings"` and `data-action="mobile-open-info"`.
    - The cinema-scrub play button (`data-action="toggle-playback"`) toggles playback in landscape through the existing module-level delegation — no new click handler.
    - Playhead-following: with `window.__bcfRenderStats` pre-seeded and zeroed, advancing playback for ~1s at a landscape viewport leaves `window.__bcfRenderStats.structuralRenders === 0` while the `.mobile-field-log-header .count` text and/or the `.mobile-field-log-live .name` text change.
    - Zero rolls (`?dataPackage=no-rolls`): the header renders, `.mobile-field-log-list` has zero children, `.mobile-cinema-scrub-inner .mobile-cinema-scrub-roll` count is 0, and no console error is emitted.
    - Word position 0 on `tiny-default`: `.mobile-field-log-live` is absent, the header count reads `0 of {N}`.
    - `console_messages == []` on every landscape page in this file.
  </behavior>

  <files>web/app.js, tests/test_mobile_landscape.py, tests/test_mobile_portrait.py</files>

  <action>
Extract the shared sky region, add the landscape render arm, and generalize the two playback dispatch sites so the landscape surface is alive from its first frame. All new code lands in the existing `// ── Mobile UX ──` section of `web/app.js`, adjacent to the Phase 2 portrait code. No new source files.

**1. `renderMobileSkyRegion(frame)` (D-35).** Move the six children currently inlined in `renderMobilePortrait()` at `web/app.js:3348-3370` — `renderViewportFrame()`, the `.mobile-sky-camera-layer` wrapper around `renderSkyCamera(frame.lastRoll, frame.scene, frame.focusT)` (guarded on `frame.scene`), `renderMobileFocalLabel(frame)`, `renderMobileSkyTapHint()`, the `app.mobileSurface === "help"` Help-overlay slot, and `renderMobileSurface()` — into a new function returning them as an ARRAY. `renderMobilePortrait()` then spreads that array into its existing `el("div", { class: "mobile-sky mobile-sky-surface" }, ...)` wrapper, so portrait's rendered DOM is unchanged. Carry the existing D-19 explanatory comments with the moved block. `renderViewportFrame` and `renderSkyCamera` are §0.2 frozen — call them, never edit or copy them.

**2. `renderMobileLandscape()`.** Structural sibling of `renderMobilePortrait()`, in the same order: `maybeAutoOpenHelp()`, then `const frame = playthroughFrameState()`, then `app.mobileRailWidth = mobileScrubWidthDefaultForLayout()` (see step 6), then `recomputeMobileRailBins()` — this is a fourth sanctioned call site for bin recomputation alongside the three named in the comment at `web/app.js:3261-3267`; update that comment to name it. Return a root `el("div", { class: "mobile-app mobile-app-landscape" }, ...)` with three children:
   - `.mobile-landscape-stage` — a relatively-positioned flex child holding, in order: the sky wrapper `el("div", { class: "mobile-sky mobile-sky-surface mobile-sky-landscape" }, ...renderMobileSkyRegion(frame))`, then `renderMobileTopCluster()` reused verbatim, then `renderMobileCinemaScrub()`. The stage is the containing block for the absolutely-positioned chip cluster and scrub pill (the prototype's `LandscapeF` wraps them the same way).
   - `renderMobileLandscapeSidebar(frame)`.
   - `renderMobileSurfaceBackdrop()` as the LAST child of the root — same position as portrait, so a backdrop tap dismisses over the whole surface including the rail (this is the shape Phase 2's device defect #3 arrived at; do not scope it to the stage).

**3. Landscape chip copy.** Add a landscape branch inside `mobileChChipText()` (`web/app.js:3647`): when `app.layoutMode === "landscape"` return the chapter chip WITHOUT the word-count segment, per UI-SPEC's locked landscape top-chip row; otherwise return today's string unchanged. Branch inside the existing helper rather than passing a flag from `renderMobileTopCluster()`, so `updateMobileTopClusterFrame()` (`:3800`) picks the correct variant up for free in both layouts.

**4. `renderMobileLandscapeSidebar(frame)`.** `el("div", { class: "mobile-sidebar" }, renderMobileFieldLog(), controlDock)`. The control dock is `.mobile-control-dock` containing (a) a quick-actions group with the label copy `Quick actions` and a `.mobile-dock-grid` of two `.mobile-dock-btn` buttons — Settings (`data-action="mobile-open-settings"`, `aria-label="Settings"`, `mobileGearIcon()` plus a visible `Settings` span) and About (`data-action="mobile-open-info"`, `aria-label="About"`, `mobileInfoIcon()` plus a visible `About` span), each gaining `is-active` when `app.mobileSurface` matches, exactly as the portrait dock buttons do at `:3393-3406`; and (b) a `.mobile-dock-status` row whose left span reads `${mobileSpeedMultiplier() ?? "—"}× · ${app.mode}` and whose right `.val` span reads `${frame.chapter?.pov || "Joe"} POV`. No new click handler — the module-level delegation at `:3970` already owns both `data-action` values.

**5. Field log (D-24/D-25/D-26/D-27).**
   - `mobileFieldLogRows()` returns `{ live, recent, idx, total }`. Call `recentRolls(app.wordPos, 6)` ONCE; its result is newest-first, so `live` is element 0 (or `null` when the array is empty), `recent` is the same array sliced by `live ? 1 : 0` (the prototype's de-dup), `total` is `app.data.story.rolls.length`, and `idx` is `live ? app.data.story.rolls.indexOf(live) : -1`. Never call `renderNarrativeReadout` or `renderRecentRolls` — those are §0.2 frozen desktop views and their sizing assumptions do not fit a 224px rail.
   - `renderMobileFieldLog()` returns `.mobile-field-log` containing an `h3.mobile-field-log-header` with a left span reading `Field Log · ${live?.constellation || "—"}` and a right `span.count` reading `${idx + 1} of ${total}`, then `.mobile-field-log-list` holding `renderMobileFieldLogChildren(rows)`.
   - `renderMobileFieldLogChildren(rows)` returns an array: when `live` exists, a `.mobile-field-log-live` card first — `.row1` with a left span reading `◢ ROLL ${live.roll_label || "R—"} · ${live.outcome}` and a right span reading `${live.outcome === "hit" ? rollTotalCost(live) + " CP" : "—"}`; a `.name` div using the SAME principal-perk expression `renderMobileFocalLabel()` already uses at `web/app.js:3668-3669`, falling back through `live.rolled_perk_name`, `live.constellation`, then an em-dash; and a `.sub` div built from `live.jump` and the truncated quote (step below). Then one `.mobile-field-log-entry` per element of `recent`, each with a `.row1` (left `Ch ${r.chapter_num}`, right `${r.outcome === "hit" ? rollTotalCost(r) + " CP" : r.outcome}`) and a `.name` using the same principal-perk fallback chain. When `live` is null the array starts with the entries and the live card is structurally absent — never a blanked placeholder.
   - `mobileFieldLogQuoteText(roll)` reads `roll.evidence_quotes?.[0]?.text` (entries are objects with a `text` key — confirmed against `scripts/derive_roll_facts.py`'s `_evidence_quotes` payload shape) and returns an empty string when absent. `mobileTruncate(s, n)` mirrors the prototype's helper: return `s` unchanged when its length is at or below `n`, otherwise `s.slice(0, n - 1).trim()` plus a horizontal-ellipsis character. The `.sub` div composes `live.jump`, a middle-dot separator only when BOTH a jump and a quote exist, and the quote wrapped in straight double quotes inside an `em` node. Build every one of these nodes through the existing `el()` helper's `text` prop so story-derived text is set as text content, never parsed as markup (T-03-01).

**6. `mobileScrubWidthDefaultForLayout()` and `app.mobileRailWidthLayout` (Pitfall 6, RESEARCH A2 — recording the Claude's-Discretion choice explicitly).** Decision: `app.mobileRailWidth` and `app.mobileRailBins` STAY SHARED across both layouts, because exactly one layout is ever mounted. To stop a stale portrait measurement (350) from mis-binning the narrower landscape scrub pill for one frame after a rotation, add `app.mobileRailWidthLayout` (initialised `null` next to `mobileRailWidth` at `web/app.js:149`). `mobileScrubWidthDefaultForLayout()` returns `app.mobileRailWidth` unchanged when `app.mobileRailWidthLayout === app.layoutMode`; otherwise it returns 200 for landscape (the prototype's own cinema-scrub fallback) or 350 for portrait. Plan 02's ResizeObserver is what sets `app.mobileRailWidthLayout = app.layoutMode` when it writes a measured width; until then the layout-appropriate default is used. Wire the same guarded default into `renderMobilePortrait()` too so the reverse rotation is symmetric, and confirm the guard means an ordinary portrait re-render (opening Settings) never discards an already-measured width.

**7. Cinema-scrub markup (structure only — plan 02 attaches the drag).** `renderMobileCinemaScrub()` returns `.mobile-cinema-scrub` containing: a play button `.mobile-cinema-scrub-fab` with `data-action="toggle-playback"`, `type="button"`, `aria-label` toggling between the same `Play`/`Pause` strings the portrait FAB uses at `:3377`, and the same glyph text; a `.mobile-cinema-scrub-count` span reading `${idx + 1} / ${total}` from `mobileFieldLogRows()`; and a `.mobile-cinema-scrub-track` whose inner `.mobile-cinema-scrub-inner` carries `width: ${zoom * 100}%` and `transform: translateX(-${panPct}%)` computed with the existing `panOffsetForPlayhead()`. `renderMobileCinemaScrubTrackChildren()` returns the baseline rail div, the `.mobile-cinema-scrub-progress` div sized to the raw playhead percentage, one `.mobile-cinema-scrub-roll` marker per cached bin in `app.mobileRailBins` (positioned from `bin.midWord`, class-suffixed by `bin.dominant`), and the `.mobile-cinema-scrub-thumb` last. Reuse the cached bins — do NOT introduce a second binning call or a landscape-specific binning function.

**8. Dispatch generalization (Pitfall 2 — this is a hard requirement, not optional tidying).**
   - `render()` at `web/app.js:991`: replace the two-way ternary with a three-way dispatch that routes portrait to `renderMobilePortrait()`, landscape to `renderMobileLandscape()`, and everything else to `renderAppShell()`. Replace the D-12 comment above it with one recording that the interim fallback is retired. Leave the `:1005` guard alone — plan 02 owns it.
   - `cachePlaybackDomRefs()` at `web/app.js:2701`: keep the portrait ref block as-is and add an `else if` landscape branch caching `app.dom.mobileSky` (`.mobile-sky`), `app.dom.mobileSkyCameraLayer`, `app.dom.mobileFocalLabel`, `app.dom.mobileTopCluster`, `app.dom.mobileChipRow`, `app.dom.mobileFieldLog`, `app.dom.mobileFieldLogHeader`, `app.dom.mobileFieldLogHeaderCount`, `app.dom.mobileFieldLogList`, `app.dom.mobileCinemaScrub`, `app.dom.mobileCinemaScrubTrack`, `app.dom.mobileCinemaScrubInner`, `app.dom.mobileCinemaScrubProgress`, `app.dom.mobileCinemaScrubThumb`, `app.dom.mobileCinemaScrubFab`, `app.dom.mobileCinemaScrubCount`, `app.dom.mobileDockStatus`. Do NOT collapse the two branches into one non-desktop block — the ref sets genuinely differ.
   - `updatePlaybackFrame()` at `web/app.js:2733`: add a landscape early-return calling `updateMobileLandscapeFrame()`, placed immediately after the portrait early-return and BEFORE the `!app.dom?.playhead` gate. Extend the existing comment at `:2728-2732` to say landscape has no `#scrubber-playhead` either, so it needs the same ordering.
   - Extend `app.frameKeys`' initialiser at `web/app.js:978` with `mobileFieldLog: null` and `mobileCinemaScrub: null`.

**9. `updateMobileLandscapeFrame()` (D-18/D-26).** Mirror `updateMobilePortraitFrame()`'s shape: compute `const frame = playthroughFrameState()`, run the same sky-camera key-diff the portrait function does at `:3744-3750` (identical logic, identical `app.frameKeys.skyCamera` field — factor it into a small shared helper rather than pasting it twice), call `updateMobileTopClusterFrame()` and `updateMobileFocalLabelFrame(frame)` unchanged, update the cinema-scrub FAB's label/glyph and the `.mobile-dock-status` right span, move the scrub thumb/progress by style writes, and call `updateMobileFieldLogFrame()`. `updateMobileFieldLogFrame()` uses the memoized-key idiom from `web/app.js:2800-2810`: build a cheap key from the live roll's `uid` (or a none sentinel) joined with the recent rolls' `uid`s, compare it against `app.frameKeys.mobileFieldLog`, and only then `replaceChildren(...renderMobileFieldLogChildren(rows))` on `app.dom.mobileFieldLogList` plus a `textContent` write on the header's count span. Never rebuild the list every frame, never recompute bins here, and never call `render()` from this function.

**10. Rewrite the landscape regression guard.** In `tests/test_mobile_portrait.py`, rename `test_landscape_fallback_is_unchanged` to `test_landscape_no_longer_falls_back_to_desktop_shell` and invert its premise per RESEARCH.md's "Which assertions must change": `.app` and `.portrait-banner` must NOT be present at `PHONE_LANDSCAPE`; `.mobile-app` MUST be present; the `.mobile-gesture-probe` assertion and the `console_messages == []` assertion stay exactly as they are. Rewrite the docstring/comment to record that the D-12 fallback is retired on purpose, which is the fact Phase 4 needs before it deletes the portrait banner globally. Do not delete the test. Also delete the stray duplicated `browser.close()` at the end of the current body while rewriting it.

**11. Create `tests/test_mobile_landscape.py`.** Copy the harness conventions verbatim from `tests/test_mobile_portrait.py:1-70` — `_chromium_browser_or_skip`, `_page_with_console_capture`, `staged_web_runtime_site`, `DEFAULT_STORAGE`, and `PHONE_LANDSCAPE = {"width": 844, "height": 390}`. Add `PHONE_LANDSCAPE_SMALL = {"width": 568, "height": 320}` (the rotation of the existing `PHONE_PORTRAIT_SMALL`; note in a comment that UI-SPEC's "390x568" for this row reads as a transposition and 568x320 is the true rotation of Phase 2's smallest portrait viewport). Write `test_landscape_tracer_renders_and_follows_playhead` covering every bullet in this task's `<behavior>` block, plus `test_landscape_field_log_zero_and_empty_states` covering the `no-rolls` package and the word-position-0 case. Do NOT edit `tests/test_mobile_plumbing.py`.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py tests/test_mobile_plumbing.py -x</automated>
    <automated>git diff --exit-code -- tests/test_mobile_plumbing.py web/mobile-gestures.js web/style.css</automated>
  </verify>

  <acceptance_criteria>
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py tests/test_mobile_plumbing.py -x` exits 0 with at least 2 new landscape tests collected and no test deselected or skipped for reasons other than a missing Chromium.
    - `git diff --exit-code -- tests/test_mobile_plumbing.py web/mobile-gestures.js web/style.css` exits 0.
    - At `PHONE_LANDSCAPE` on `tiny-default`, `page.evaluate` returns non-null for `.mobile-app`, `.mobile-sidebar .mobile-field-log`, `.mobile-sidebar .mobile-control-dock`, `.mobile-cinema-scrub-track`, and `.mobile-gesture-probe`; and returns null for `.app`, `.portrait-banner`, and `.narrative-mount`.
    - With `window.__bcfRenderStats` seeded and zeroed at `PHONE_LANDSCAPE`, after a 1000ms playback window `window.__bcfRenderStats.structuralRenders` equals 0 AND the concatenation of `.mobile-field-log-header .count` textContent with `.mobile-field-log-live .name` textContent differs from its value at t=0.
    - At `?dataPackage=no-rolls` and `PHONE_LANDSCAPE`: `.mobile-field-log-header` is present, `.mobile-field-log-list` has `children.length == 0`, `document.querySelectorAll(".mobile-cinema-scrub-roll").length == 0`, and captured console messages are empty.
    - At `tiny-default` with the bookmark seeded to word position 0: `.mobile-field-log-live` is null and `.mobile-field-log-header .count` textContent starts with `0 of `.
    - `grep -c 'renderMobileSkyRegion' web/app.js` returns 3 or more (definition plus a call from each of the two layout renderers), proving the sky markup exists once and is shared.
  </acceptance_criteria>

  <reversibility rating="costly">Replacing the D-12 landscape fallback removes the "rotate to landscape" banner as landscape's safety net one phase before the Phase 4 cutover deletes it globally. Reverting after plans 02/03 build on the landscape arm would mean unwinding four files, so it is costly — but it is not one-way: the fallback is a single ternary branch and every replaced behavior is covered by the rewritten regression guard.</reversibility>

  <done>A landscape phone renders its own surface with a live field log that follows the playhead through the existing incremental tier, the desktop shell and portrait banner no longer mount there, the shared sky helper exists in exactly one place, and the full mobile suite is green with the three frozen files untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Landscape CSS — sky/rail split, field log, control dock, cinema-scrub pill, safe areas, D-32 scroll-lock</name>

  <read_first>
    - `web/mobile.css:1-68` — the file header, the outer `@media (max-width: 900px), (orientation: portrait) and (max-width: 1100px)` block at `:11`, the portrait-nested body scroll-lock at `:29-36` (its comment explicitly says "until Phase 3 replaces it"), the `.mobile-sky-surface` / `.mobile-rail-surface` touch-action utilities at `:38-48`, and the `:root` safe-area and viewport-unit variables at `:50-67`.
    - `web/mobile.css:69-120` — the portrait layout block: `.mobile-app`'s `height: var(--mobile-vh)` + `flex-direction: column`, `.mobile-top-cluster`'s absolute 12px inset and `pointer-events` convention, `.mobile-chip` tokens.
    - `web/mobile.css:220-240` — the portrait dock's `padding-bottom: max(...)` safe-area idiom to mirror for the control dock.
    - `design/mobile-ux/prototype/styles.css:228-264` — the `@media (orientation: landscape)` block: `.rail` 224px + gradient + left border, `.field-log` `flex: 1 1 67%` + `12px 12px 8px`, `.control-dock` `flex: 0 0 33%` + `min-height: 138px` + `10px 12px`, `.cinema-scrub` absolute `left/right/bottom: 12px` + `padding: 5px 8px 5px 5px` + pill radius + `is-hidden` opacity/translate transition.
    - `design/mobile-ux/prototype/styles.css:363-470` — cinema-scrub track/rail/progress/thumb/roll/count sizes and the field-log header/list/live/entry rules including the `.entry .name` ellipsis triple.
    - `.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-UI-SPEC.md` — the locked landscape exceptions table, the safe-area inset table, the Typography table (note the `600` → `700` mapping for the field-log header), the Color section, and the UI Considerations rows for long-text and overflow.
    - `tests/helpers/web_runtime_site.py:330-360, 480-500` — the `dense-rolls` fixture builder and the note at `:483-484` that only `tiny-default`/`tiny-alt` payloads must stay byte-identical.
  </read_first>

  <files>web/mobile.css, tests/test_mobile_landscape.py, tests/helpers/web_runtime_site.py</files>

  <action>
Add the landscape layout rules and prove the locked proportions and the two text-containment contracts.

**1. D-32 — un-nest the body scroll-lock.** In `web/mobile.css`, remove the `@media (orientation: portrait)` wrapper at `:29` so its `body` rule applies to the whole outer mobile block for both orientations, and rewrite the trailing sentence of the comment above it: the landscape fallback it was protecting no longer exists, so the lock must now cover landscape or the frozen `style.css:45` `min-height: 100vh` (the LARGE viewport on iOS) reintroduces the same 82px scroll defect Phase 2 fixed in portrait. Keep the rule body byte-identical; this is a scope change only.

**2. Landscape layout block.** All new rules land INSIDE the existing outer mobile media query — never a new top-level block, never an edit to `web/style.css`. Use a nested `@media (orientation: landscape)` only where a rule genuinely must differ by orientation (the sky/rail split does; the field-log and cinema-scrub internals do not and should sit outside the nested block, matching how the prototype separates its landscape layout block from its component blocks). Reproduce the UI-SPEC's locked exception values verbatim — do NOT re-grid them onto the 8-point scale:
   - `.mobile-app-landscape` gets `flex-direction: row`, plus `padding-left: var(--safe-left)` and `padding-right: var(--safe-right)` so BOTH notch rotations are handled with one rule and the sky and rail inherit correct bounds.
   - `.mobile-landscape-stage` gets `position: relative; flex: 1 1 auto; min-width: 0; display: flex;` — it is the containing block for the absolutely-positioned chip cluster and scrub pill, replacing `.mobile-app` in that role for landscape only.
   - `.mobile-sky-landscape` gets `flex: 1 1 auto; min-width: 0;`.
   - `.mobile-sidebar` gets `width: 224px; flex-shrink: 0; position: relative; display: flex; flex-direction: column;` plus the prototype's rail gradient and left border, expressed against the existing `--edge`/`--panel`-family variables where one already matches; never redefine a `:root` colour variable.
   - `.mobile-field-log` gets `flex: 1 1 67%; padding: 12px 12px 8px; overflow: hidden; display: flex; flex-direction: column;`.
   - `.mobile-control-dock` gets `flex: 0 0 33%; min-height: 138px; padding: 10px 12px;` a top border, the darker dock background, `display: flex; flex-direction: column; justify-content: space-between;` and `padding-bottom: max(10px, var(--safe-bottom))` using the same `max()` idiom as the portrait dock.
   - `.mobile-top-cluster` inside a landscape stage gets `padding-top: max(0px, var(--safe-top))`; do not change the portrait rule.

**3. Field-log component rules.** Header: mono, 9px, uppercase, letter-spacing per the prototype, colour `--cyan`, and `font-weight: 700` — the UI-SPEC explicitly overrides the prototype's stray `600` to keep exactly two weights across the design system. Its `.count` child stays `--dim` at weight 400. List: `flex: 1; overflow-y: auto; display: flex; flex-direction: column; gap: 5px; touch-action: pan-y;` plus the thin scrollbar treatment. Live card: `padding: 8px 10px; border-radius: 8px;` with the `--green`-family 0.10 background / 0.45 border (the same in-play semantic, applied to a card). Live `.row1` mono 8px in `--green`, `.name` serif 12px, `.sub` 9.5px italic `--muted`. Entry: `padding: 5px 8px;` with a 2px left accent border; `.row1` mono 8px `--dim`; `.name` serif 11px with the `white-space: nowrap; overflow: hidden; text-overflow: ellipsis` triple so a long perk name can never widen or wrap the 224px rail.

**4. D-27 hard clamp.** Give the live card's `.sub` node a CSS height clamp so even a pathological quote cannot push the recent list out of the rail: `display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 3; overflow: hidden;` with a matching `max-height` of three line-heights as the non-WebKit fallback. This is the concrete form of D-27's "MUST have a hard height clamp"; the 100-character truncation from Task 1 is the data-layer half and this is the defence-in-depth half.

**5. Control-dock button rules.** `.mobile-dock-grid` is a 2-column grid with an 8px gap. `.mobile-dock-btn` gets `padding: 9px 6px; min-height: 48px; border-radius: 10px;` a column flex with a 4px gap, mono 9px uppercase label, `--muted` text on the panel background with an `--edge` border, and an `is-active` variant using the `--cyan` fill/border/text triple that matches the portrait icon buttons. `.mobile-dock-status` is a single flex row justified between, mono 8px, with a `.val` child in the brighter ink colour.

**6. Cinema-scrub pill rules.** `.mobile-cinema-scrub` gets `position: absolute; left: 12px; right: 12px; bottom: 12px;` a flex row with an 8px gap, the pill background/border/radius/padding and shadow from the prototype, a 220ms opacity+transform transition, and a z-index above the sky camera layer but below the flyout stack. Add the `is-hidden` variant now (`opacity: 0; transform: translateY(12px); pointer-events: none;`) even though plan 02 is what toggles it, so the two halves cannot drift. `.mobile-cinema-scrub-fab` is 40x40 — record inline that this is the UI-SPEC's locked, flagged sub-44px exception carried from the approved prototype, remediated in Phase 4 if Lighthouse flags it, and NOT to be silently enlarged here. The track is `flex: 1; height: 14px; position: relative`, the thumb 12x12, the progress bar 2px, the baseline rail 1px, each roll marker a 4x4 rotated square colour-keyed by dominant outcome with `--green`/`--rose`/`--amber` used for hit/miss/unknown exactly as the portrait rail does. Apply the EXISTING `.mobile-rail-surface` utility class to the cinema-scrub track element rather than writing a second `touch-action: none` rule, and extend that utility's comment at `web/mobile.css:44-48` to name both consumers.

**7. Fixture: one evidence quote.** In `tests/helpers/web_runtime_site.py`, add an `evidence_quotes` list with a single `{"text": ..., "mention_chapter_num": ..., "mention_word_position": ...}` entry to exactly ONE roll in the `dense-rolls` package's chapter-facts builder. The text must be longer than 100 characters so the truncation is exercised, and must contain literal angle-bracket tag-looking characters so the markup-escaping assertion has a subject. Do not touch the `tiny-default` or `tiny-alt` payloads — the comment at `:483-484` requires they stay byte-identical. This is a deliberate, recorded addition to an existing fixture, not the new fixture VALIDATION.md's Wave 0 section says is unnecessary.

**8. Tests.** Add to `tests/test_mobile_landscape.py`:
   - `test_landscape_layout_proportions` at `PHONE_LANDSCAPE` and at `PHONE_LANDSCAPE_SMALL`: bounding-box assertions that the sidebar width is 224 CSS px, that the stage width is at least 70% of viewport width at 844x390, that the field-log and control-dock boxes are both fully contained within the sidebar box and do not overlap each other, that the control-dock box is fully inside the viewport, and that the top-cluster box and the cinema-scrub box do not overlap at either viewport. Mirror the assertion style of `test_portrait_layout_proportions_and_chip_overlap`.
   - `test_landscape_field_log_text_containment` at `?dataPackage=dense-rolls`: scrub or seek to the roll carrying the quote, then assert the `.sub` node's textContent length is at or below the truncation bound plus the surrounding punctuation, that its rendered box height is at or below the three-line clamp, that `.mobile-field-log-live .sub` contains no child element nodes other than the `em` wrapper (specifically `querySelectorAll(".mobile-field-log-live .sub b").length == 0`) while its textContent DOES contain the literal angle-bracket characters, that every `.mobile-field-log-entry .name` box width is at or below the list's client width, and that `.mobile-field-log-list` `scrollHeight >= clientHeight` with the control dock still fully on screen.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py -x</automated>
    <automated>git diff --exit-code -- web/style.css</automated>
  </verify>

  <acceptance_criteria>
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py -x` exits 0 with `test_landscape_layout_proportions` and `test_landscape_field_log_text_containment` both passing.
    - `git diff --exit-code -- web/style.css` exits 0.
    - At `PHONE_LANDSCAPE`, `document.querySelector(".mobile-sidebar").getBoundingClientRect().width` equals 224 and the stage's width is at least 0.70 of `window.innerWidth`.
    - At both landscape viewports the field-log and control-dock rects are inside the sidebar rect on all four edges and their vertical ranges do not intersect; the control-dock rect's bottom is at or above `window.innerHeight`.
    - At both landscape viewports the top-cluster rect and the cinema-scrub rect do not intersect.
    - `getComputedStyle(document.body).overflowY` equals `hidden` at `PHONE_LANDSCAPE` (proving the D-32 un-nesting took effect) and still equals `hidden` at `PHONE_PORTRAIT`.
    - `document.querySelectorAll(".mobile-field-log-live .sub b").length` equals 0 while `.mobile-field-log-live .sub` textContent contains the literal angle-bracket characters from the fixture quote.
    - Every `.mobile-field-log-entry .name` rect width is at or below `.mobile-field-log-list` clientWidth.
  </acceptance_criteria>

  <reversibility rating="reversible">Every rule is additive inside `web/mobile.css`'s existing mobile block; D-32 is a one-level scope change to an existing rule whose original nesting is a single line to restore.</reversibility>

  <done>The landscape surface is laid out to the locked contract — 224px rail, 2/3 field log over 1/3 control dock, floating cinema-scrub pill, safe-area padding on both notch edges — the body scroll-lock covers both orientations, and long perk names and long evidence quotes are provably contained rather than pushing the rail apart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static data payload → DOM | `visualization_facts.json` roll text (perk names, chapter titles, agent-curated evidence quotes) crosses into rendered nodes. The curation workstream will begin writing agent-generated text into this corpus, so it must be treated as untrusted content, not authored constants. |
| `localStorage` (`bcf:*`) → app state | Zoom, speed, mode, tap-to-pause, haptics and bookmark values are attacker-writable by anyone with console access on the origin, and drive layout and scrub math. |
| History API → surface state | `history.pushState`/`popstate` carry a `bcfMobileSurface` sentinel that governs which flyout is open. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | landscape field-log evidence quote / perk name / chapter title nodes | medium | mitigate | Every text node is built through `el()`'s `text` prop (which assigns `textContent`); Task 2's `test_landscape_field_log_text_containment` asserts a quote containing tag-looking characters produces zero element children and renders the characters literally. |
| T-03-02 | Tampering | `bcf:timeline-zoom` / `bcf:speed` / bookmark values feeding `panOffsetForPlayhead` and the bin math | low | mitigate | Landscape adds no new storage key and no new read path; it consumes the already-allow-listed `app.mobileTimelineZoom` and `app.speed` setters (`MOBILE_SPEED_RUNGS`, the zoom setter's rung table). No landscape code parses a raw `localStorage` value. |
| T-03-03 | Information Disclosure | outbound story links in the About flyout, reachable from the landscape control dock | low | mitigate | The About flyout is reused unmodified from Phase 2 (D-33); plan 03 asserts each external anchor carries `rel` containing `noopener` at a landscape viewport. No new outbound link is added by this plan. |
| T-03-04 | Denial of Service | `history.pushState` sentinel balance when a surface is open across a rotation (D-21) | low | mitigate | `openMobileSurface` pushes at most one sentinel and `closeMobileSurface` consumes it; rotation calls `render()` only and never touches history. Plan 03 asserts `history.length` is unchanged across an open-surface rotation. |
| T-03-05 | Spoofing | a crafted history entry carrying a `bcfMobileSurface` value | low | accept | The `popstate` listener only ever CLOSES an already-open surface; it never opens one from state, and `openMobileSurface` allow-lists `kind` against `MOBILE_SURFACES`. Opening Settings is not a privileged operation and gates no data. |
| T-03-SC | Tampering | package-manager supply chain | high | accept | No package-manager install task exists in this phase; `web/` remains dependency-free with no `package.json`, lockfile or bundler config, and plan 04's freeze proof re-runs the Phase 2 filesystem scan that asserts this. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py` — full suite green, at least 4 new landscape tests, no regression against the 33-test baseline.
2. `git diff --exit-code -- web/style.css web/mobile-gestures.js tests/test_mobile_plumbing.py` — the three frozen files untouched.
3. `grep -c 'renderMobileSkyRegion' web/app.js` returns 3 or more — the sky markup exists once and both layout renderers call it.
4. Manual read-back: no new rule in `web/mobile.css` sits outside the outer `@media (max-width: 900px), (orientation: portrait) and (max-width: 1100px)` block.
</verification>

<success_criteria>
- A landscape phone renders a real landscape surface — shared sky at ~75% width, 224px rail with a live field log over a Settings/About dock, floating cinema-scrub pill — instead of the desktop shell.
- The field log is derived from `recentRolls(wordPos, 6)` and follows the playhead through the existing incremental tier with zero structural re-renders.
- Long perk names ellipsize and long evidence quotes are truncated AND clamped; story-derived text never becomes markup.
- Zero-roll and word-position-0 stories render an empty log without throwing.
- The three frozen files are byte-identical and the full suite is green.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-01-SUMMARY.md` when done.
</output>
