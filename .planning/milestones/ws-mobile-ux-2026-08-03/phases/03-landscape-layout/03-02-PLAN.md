---
phase: 03-landscape-layout
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/app.js
  - tests/test_mobile_landscape.py
autonomous: true
requirements: [MOBL-02]
user_setup: []

must_haves:
  truths:
    - "There is exactly ONE mobile gesture-attach function: attachMobilePortraitGestures is renamed and generalized to attachMobileGestures, which branches on app.layoutMode for the scrub-surface selector only and keeps ONE set of teardown slots for both layouts (D-34)."
    - "Landscape sky gestures work: a tap toggles pause when tap-to-pause is on, a double-tap snaps to the last roll at or before the playhead and resumes, and a horizontal swipe steps one roll per 56px — the same callback bodies portrait already uses."
    - "The landscape cinema-scrub drag commits through window.attachRailScrub and the existing setWordPos path — there is no second scrub-input implementation anywhere in the codebase (D-17), and web/mobile-gestures.js stays byte-identical."
    - "A monotonic rightward drag on the landscape cinema-scrub moves the playhead monotonically forward at every zoom level (1x/2x/4x/8x), because the auto-pan offset is frozen at drag start and cleared at drag end — the Phase 2 device-found fix applies here from the start, not after a second device pass."
    - "render()'s gesture-attach and D-16 focus-trap re-attach block covers landscape as well as portrait — the guard is changed once, as a whole block, so the focus trap can never be left portrait-only while gesture attach is generalized (Pitfall 3)."
    - "Landscape chrome auto-hides after 4000ms of idle DURING PLAYBACK only; pausing reveals the chrome and stops the timer (D-28)."
    - "Only the cinema-scrub hides — the top chips and the right rail stay visible so the field log remains readable, which is the defining feature of landscape (D-29)."
    - "The first sky tap on hidden chrome ALWAYS reveals and returns immediately, regardless of the tap-to-pause preference; only a subsequent tap within the visible window pauses, and only when tap-to-pause is on — hidden chrome is never a trap (D-30)."
    - "Any sky or cinema-scrub interaction resets the idle timer: the reset is called from the existing gesture callback bodies (tap, double-tap, swipe step, swipe end, scrub, scrub end), never from a second raw pointer listener and never from a wordPos comparison (MOBL-02, Pitfall 1)."
    - "Auto-hide toggles visibility with a class mutation on the cached cinema-scrub node, never by calling render() — a hide/reveal cycle produces zero structural re-renders and therefore never tears down a live gesture listener (D-18, D-22)."
    - "The idle timer handle lives on app (not app.dom, which render() blanks) and is cleared unconditionally in attachMobileGestures's teardown block, so no stale timer survives a re-render or a layout transition away from landscape."
    - "The cinema-scrub track's pixel width is measured by a ResizeObserver using the same coalesced-rAF shape as the portrait rail, and the observer records which layout the measurement belongs to so a stale cross-layout width is never used for binning."
    - statement: "During unattended landscape playback the cinema-scrub actually disappears: at 3900ms after the last interaction it is still visible and at 4600ms it carries the hidden state — the 4000ms boundary is asserted from both sides, not only from the far side."
      verification: backstop
    - statement: "With tap-to-pause OFF and chrome hidden, one sky tap reveals the chrome and leaves playback running (the reveal must not fall through to a pause), and a second tap changes nothing about play state."
      verification: backstop

  prohibitions:
    - "MUST NOT ship an auto-hide that can never fire during playback. The prototype's reference effect re-arms its timer whenever the word position changes, and the word position changes on every animation frame while playing — porting that keying would silently deliver a cinema view that never appears, the exact opposite of what the reader was promised."
    - "MUST NOT leave a reader unable to recover hidden controls: revealing chrome must work independently of every preference toggle, so no combination of settings can strand someone with an invisible transport."
    - "MUST NOT introduce a second scrub-input implementation or a second animation-frame update tier. A parallel path silently re-acquires the already-fixed backward-drag defect on a real reader's device, where it is expensive to find and was already paid for once."
    - "MUST NOT silently degrade the frozen desktop experience or the Phase 1 gesture-probe contract to make landscape work — tests/test_mobile_plumbing.py, web/mobile-gestures.js and web/style.css stay byte-identical, and a landscape failure is fixed in landscape code."

  flagged_assumptions:
    - id: FA-MOBL-02
      origin: edge-probe (spec-less fallback §A)
      requirement: MOBL-02
      status: unresolved
      note: "The deterministic edge probe returned category=unclassified for MOBL-02, so no edge criteria were auto-derived. The criteria below are the PLANNER'S OWN, authored from RESEARCH.md Pitfall 1, Open Question 2 and CONTEXT D-28..D-31. They are flagged, not probe-resolved."
      criteria:
        - "Idle boundary from both sides: visible at 3900ms after the last interaction, hidden at 4600ms."
        - "One step either side of the boundary produced by a reset: an interaction at 3000ms must push the hide out past 6900ms."
        - "Reveal-tap when tap-to-pause is OFF: reveals, does not pause, does not require a preference change."
        - "Reveal-tap when tap-to-pause is ON: first tap reveals only (play state unchanged); the next tap pauses."
        - "Paused state: the timer never arms; chrome stays visible indefinitely while paused."
        - "A surface (Settings/About/Help) open: no gesture surface is attached, so the timer cannot be reset by a tap that the surface owns; chrome must not hide behind an open flyout."
        - "Zero-roll story: the cinema-scrub still reveals/hides and the play FAB still toggles, with no roll markers on the track."

  artifacts:
    - "web/app.js: attachMobileGestures() (renamed and generalized from attachMobilePortraitGestures, D-34)"
    - "web/app.js: resetMobileChromeHideTimer(), revealMobileChrome(), rebuildMobileCinemaScrubTrack(), mobileScrubSurfaceEl()"
    - "web/app.js state: app.mobileChromeHideTimer (new), app.chromeHidden (existing at :131, first consumer), app.mobileRailWidthLayout write site"
    - "web/app.js: render()'s mobile block guard widened from portrait-only to every non-desktop layout (Pitfall 3)"
    - "web/app.js: auto-hide wiring in togglePlayback()"
    - "tests/test_mobile_landscape.py: test_landscape_sky_gesture_contract, test_landscape_cinema_scrub_drag_is_monotonic_at_every_zoom, test_landscape_chrome_autohide_boundary, test_landscape_reveal_tap_semantics"

  key_links:
    - "render()'s non-desktop block -> attachMobileGestures() AND trapMobileSurfaceFocus() (one guard, both calls — Pitfall 3)"
    - "attachSkyGestures onTap -> revealMobileChrome() early return -> tap-to-pause branch (D-30 ordering is load-bearing)"
    - "every gesture callback body -> resetMobileChromeHideTimer() (the only sanctioned 'any touch' signal, since mobile-gestures.js cannot gain an onDown hook)"
    - "resetMobileChromeHideTimer() -> app.dom cinema-scrub node classList (never render())"
    - "attachMobileGestures teardown block -> clearTimeout(app.mobileChromeHideTimer) (no stale timer across renders or layout changes)"
---

<objective>
Make the landscape surface interactive and cinematic: generalize the single mobile gesture-attach lifecycle to cover landscape, wire the cinema-scrub drag through the existing single scrub-input path, and add the chrome auto-hide idle timer — the one genuinely novel piece of state this phase introduces.

Purpose: MOBL-02, plus the D-34 generalization and the Pitfall 3 focus-trap fix that MOBL-04 depends on in plan 03.

Output: `attachMobileGestures()` replacing `attachMobilePortraitGestures()`, the auto-hide timer helpers, and four new landscape behavior tests.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-PATTERNS.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-UI-SPEC.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-01-SUMMARY.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...`.
- Commit signing is disabled repo-wide; `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`. Never `git add .`.
- `tests/test_mobile_plumbing.py`, `web/mobile-gestures.js` and `web/style.css` stay byte-identical.
</session_constraints>

## Artifacts this phase produces

Full phase-wide symbol inventory (this plan creates the rows marked **[02]**).

| Symbol / artifact | Kind | Plan |
|---|---|---|
| `renderMobileLandscape()`, `renderMobileSkyRegion(frame)`, `renderMobileLandscapeSidebar(frame)` | render functions (`web/app.js`) | 01 |
| `renderMobileFieldLog()`, `renderMobileFieldLogChildren(rows)`, `mobileFieldLogRows()` | render + model adapter | 01 |
| `mobileFieldLogQuoteText(roll)`, `mobileTruncate(s, n)` | helpers | 01 |
| `renderMobileCinemaScrub()`, `renderMobileCinemaScrubTrackChildren()` | render functions | 01 |
| `updateMobileLandscapeFrame()`, `updateMobileFieldLogFrame()` | incremental tier | 01 |
| `mobileScrubWidthDefaultForLayout()`, `app.mobileRailWidthLayout` | Pitfall-6 width guard | 01 |
| landscape arms in `render()` / `cachePlaybackDomRefs()` / `updatePlaybackFrame()`; landscape branch in `mobileChChipText()` | dispatch + copy | 01 |
| landscape CSS block in `web/mobile.css`; D-32 scroll-lock un-nesting | styles | 01 |
| `attachMobileGestures()` (rename + generalize of `attachMobilePortraitGestures`, D-34) | gesture lifecycle | **[02]** |
| `mobileScrubSurfaceEl()` | selector helper | **[02]** |
| `rebuildMobileCinemaScrubTrack()` | ResizeObserver rebuild target | **[02]** |
| `resetMobileChromeHideTimer()`, `revealMobileChrome()` | auto-hide timer | **[02]** |
| `app.mobileChromeHideTimer` (new), `app.chromeHidden` (existing at `:131`, first consumer) | state | **[02]** |
| `render()` mobile-block guard widened to every non-desktop layout (Pitfall 3) | dispatch edit | **[02]** |
| auto-hide wiring in `togglePlayback()` | dispatch edit | **[02]** |
| landscape arrival handling in `onLayoutMaybeChanged()` (D-31, D-22, rail-width layout reset) | dispatch edit | 03 |
| `COVERAGE.md` | phase artifact | 04 |
| `tests/test_mobile_landscape.py` | new in 01, extended in **[02]** and 03 |
| `tests/test_mobile_portrait.py::test_landscape_no_longer_falls_back_to_desktop_shell` | rewritten regression guard | 01 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One gesture-attach lifecycle for both layouts — D-34 generalization and the landscape cinema-scrub drag</name>

  <read_first>
    - `web/app.js:3842-3964` — `attachMobilePortraitGestures()` in full: the four-part unconditional teardown block (`mobileSkyTeardown`, `mobileRailTeardown`, `mobileScrubPanPct = null`, `mobileRailResizeObserver`), the `if (app.mobileSurface) return;` guard and the comment explaining why an open overlay owns input, the four `attachSkyGestures` callbacks, the `attachRailScrub` pair with the frozen-pan comment at `:3912-3919` recording the Pixel 10 Pro XL 68k-word backward drag, and the ResizeObserver at `:3941-3963` with its coalesced-rAF and re-cache-the-active-dot discipline.
    - `web/app.js:994-1016` — `render()`'s non-desktop block. The OUTER guard at `:994` is already `layoutMode !== "desktop"`; the INNER guard at `:1005` is the portrait-only one that owns BOTH `attachMobilePortraitGestures()` and the D-16 focus-trap re-attach/teardown pair.
    - `web/mobile-gestures.js` in full (195 lines) — `attachSkyGestures(el, opts)` at `:35` and `attachRailScrub(el, opts)` at `:149`, the `G` constants at `:6-20` (`CHROME_AUTOHIDE: 4000`), and the `window.GestureConstants`/`window.attachSkyGestures`/`window.attachRailScrub` exports at `:195-198`. This file is FROZEN — read only.
    - `web/app.js:3192-3224` — `panOffsetForPlayhead()` / `mobileInnerFraction()`.
    - `web/app.js:834-860` — `setWordPos()`; `:766-790` — `lastRollAtWord()` / `rollStepFrom()`; `:367-380` — `persistBookmarkNow()`.
    - `web/app.js:3268-3279` — `recomputeMobileRailBins()` and its call-site rule (plan 01 added the landscape structural render as a fourth site).
    - `web/app.js:131` — `app.chromeHidden`, reserved since Phase 1 and unused until this plan.
    - `web/app.js:149` — `app.mobileRailWidth` and (after plan 01) `app.mobileRailWidthLayout`.
    - `03-01-SUMMARY.md` — the exact landscape class names plan 01 committed to, especially the cinema-scrub track and inner selectors and the `app.dom.mobileCinemaScrub*` ref names.
    - `tests/test_mobile_portrait.py` — `test_sky_gesture_contract`, `test_sky_tap_is_noop_when_tap_to_pause_off`, `test_rail_scrub_zoom_aware`, and `test_rail_drag_is_monotonic_at_every_zoom` (the `ed59087` regression proof written test-first against the pre-fix code). These four are the assertion templates for their landscape counterparts.
    - `03-PATTERNS.md` §`attachMobileGestures()` — the per-aspect portrait-vs-landscape divergence table and the "rail vs rail" naming-collision warning.
  </read_first>

  <behavior>
    - At `PHONE_LANDSCAPE` with tap-to-pause ON and chrome visible: one sky tap flips play state within 250ms; a second flips it back.
    - With tap-to-pause OFF and chrome visible: a sky tap leaves play state unchanged.
    - A double-tap on the landscape sky moves the playhead to the last roll at or before the current position and leaves playback running — it never teleports to the final roll in the story.
    - A horizontal swipe across the landscape sky steps exactly one roll per 56px of travel, in the direction the gesture contract locks.
    - A monotonic rightward pointer drag across `.mobile-cinema-scrub-track`, sampled at 30/40/50/60/70 percent of track width, produces a strictly non-decreasing playhead percentage at zoom 1, 2, 4 AND 8.
    - Releasing the drag persists the bookmark and clears the frozen pan offset, so the next drag re-captures.
    - Opening Settings in landscape detaches both gesture surfaces (a sky tap while a flyout is open does not change play state), and closing it re-attaches them.
    - After a structural re-render in landscape (e.g. a Settings open/close cycle), a single sky tap still fires exactly once — no double-binding.
    - Tab-cycling past the last focusable inside an open landscape flyout keeps focus inside the flyout (the Pitfall 3 proof).
  </behavior>

  <files>web/app.js, tests/test_mobile_landscape.py</files>

  <action>
Rename and generalize the single gesture-attach lifecycle, then wire the landscape scrub surface through it. This is an in-place edit of one existing function — do NOT create a landscape sibling function, and do NOT add a fourth teardown slot; either would duplicate the defensive teardown logic that D-07 and the frozen-pan fix both depend on.

**1. Widen `render()`'s inner guard (Pitfall 3).** At `web/app.js:1005`, change the inner `app.layoutMode === "portrait"` guard to cover every non-desktop layout, in ONE edit that moves the whole block — the gesture-attach call AND the `app.mobileSurface ? trapMobileSurfaceFocus(...) : teardownMobileSurfaceFocusTrap()` pair. Changing only the gesture-attach line would leave landscape flyouts opening correctly but trapping keyboard focus nowhere, which MOBL-04 explicitly forbids. Since the outer guard at `:994` is already non-desktop, the inner guard's condition becomes redundant — collapse the two rather than leaving a nested tautology, and keep the existing D-16 comment.

**2. Rename to `attachMobileGestures()` (D-34).** Rename `attachMobilePortraitGestures` at `web/app.js:3852` and update its single call site. Update the function's leading comment to say it serves both mobile layouts and to name the one hard branch point (the scrub-surface selector) and the one behavioral branch (the reveal-vs-pause tap ordering). Leave every teardown slot name as-is (`app.mobileSkyTeardown`, `app.mobileRailTeardown`, `app.mobileRailResizeObserver`) — a rename is optional and skipping it keeps the diff reviewable.

**3. Teardown block gains the timer.** Add `clearTimeout(app.mobileChromeHideTimer); app.mobileChromeHideTimer = null;` as a fourth teardown alongside the three existing ones, in the same unconditional position BEFORE the `if (app.mobileSurface) return;` guard. This is what stops a stale timer surviving a structural re-render inside landscape or a transition away from landscape. Initialise `app.mobileChromeHideTimer: null` next to `app.chromeHidden` at `web/app.js:131`.

**4. `mobileScrubSurfaceEl()`.** A one-line helper returning `document.querySelector(".mobile-cinema-scrub-track")` when `app.layoutMode === "landscape"` and `document.querySelector(".mobile-rail")` otherwise. Do NOT query `.mobile-rail` for landscape — that class is portrait's mini-rail, and the landscape sidebar is deliberately named `.mobile-sidebar` precisely so the two "rail" concepts cannot be conflated. Use this helper in both the `attachRailScrub` call and the ResizeObserver `observe()` call so the selector lives in exactly one place.

**5. Sky gesture callbacks.** Keep all four callback bodies as they are, with two additions:
   - `onTap` gains a NEW first branch, evaluated before the `if (!app.tapToPause) return;` line: when `app.layoutMode === "landscape"` and `app.chromeHidden` is true, call `revealMobileChrome()`, call `resetMobileChromeHideTimer()`, and `return` IMMEDIATELY. The early return is load-bearing — a tap that reveals must never fall through to `togglePlayback()` in the same invocation (D-30, RESEARCH Open Question 2). Placing this branch above the tap-to-pause check is what guarantees hidden chrome is recoverable with the preference off.
   - `onTap` (after the existing toggle), `onDoubleTap`, `onSwipeStep` and `onSwipeEnd` each end with a `resetMobileChromeHideTimer()` call. These callback bodies are the only sanctioned "any touch happened" signal, because `web/mobile-gestures.js` is frozen and cannot gain an `onDown` hook.

**6. Scrub callbacks.** Reuse the `onScrub`/`onScrubEnd` bodies verbatim for both layouts — same `app.mobileScrubPanPct` capture-on-first-callback / clear-on-scrub-end shape, same `mobileInnerFraction` math, same `setWordPos` commit, same `lastRollAtWord` return value. Only the queried element differs. Add `resetMobileChromeHideTimer()` to both bodies. Carry the existing frozen-pan comment forward and extend it to record that the landscape cinema-scrub inherits the same fix by construction rather than rediscovering it on a second device.

**7. ResizeObserver.** Keep the existing coalesced-rAF shape; observe `mobileScrubSurfaceEl()`'s element. In the rAF callback, after `app.mobileRailWidth = Math.max(50, cr.width)`, also set `app.mobileRailWidthLayout = app.layoutMode` so plan 01's cross-layout stale-width guard knows this measurement is current. Then `recomputeMobileRailBins()` and branch the rebuild target: portrait keeps its `app.dom.mobileRailRollsLane.replaceChildren(...renderMobileRailRollsLaneChildren())` plus the active-dot re-cache; landscape calls a new `rebuildMobileCinemaScrubTrack()` that does `app.dom.mobileCinemaScrubInner.replaceChildren(...renderMobileCinemaScrubTrackChildren())` and re-caches `app.dom.mobileCinemaScrubProgress` and `app.dom.mobileCinemaScrubThumb` from the fresh children, so the per-frame style writes never target a node this rebuild just detached. Guard every ref with an optional chain.

**8. Tests.** Add to `tests/test_mobile_landscape.py`:
   - `test_landscape_sky_gesture_contract` — tap toggle at 250ms, tap no-op with tap-to-pause off, double-tap snap-and-resume, and the 56px-per-roll swipe step, retargeted from `test_sky_gesture_contract` and `test_sky_tap_is_noop_when_tap_to_pause_off` to `PHONE_LANDSCAPE` and the landscape sky selector.
   - `test_landscape_cinema_scrub_drag_is_monotonic_at_every_zoom` — the landscape counterpart of `test_rail_drag_is_monotonic_at_every_zoom`. Drive a single pointer drag with successive `mouse.move` steps across `.mobile-cinema-scrub-track` at 30/40/50/60/70 percent and assert the sampled playhead percentage read from the live thumb marker (not the bookmark key — `persistBookmarkNow()` only runs at scrub end, and the defect this guards lives strictly mid-drag) is non-decreasing at zoom 1, 2, 4 and 8.
   - `test_landscape_gestures_survive_rerender_and_surface_toggle` — open Settings, assert a sky tap does not change play state; close it, assert a single sky tap fires exactly once; and Tab past the last focusable inside an open flyout asserting `document.activeElement` is still a descendant of `.mobile-flyout` (the Pitfall 3 proof).
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py tests/test_mobile_plumbing.py -x</automated>
    <automated>git diff --exit-code -- web/mobile-gestures.js tests/test_mobile_plumbing.py web/style.css</automated>
  </verify>

  <acceptance_criteria>
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py tests/test_mobile_plumbing.py -x` exits 0 with the three new landscape tests passing and every pre-existing portrait/plumbing test still passing.
    - `git diff --exit-code -- web/mobile-gestures.js tests/test_mobile_plumbing.py web/style.css` exits 0.
    - `grep -c 'function attachMobileGestures(' web/app.js` returns 1 and `grep -c 'function attachMobilePortraitGestures(' web/app.js` returns 0 — the rename is complete. (Declaration-form greps deliberately: prose comments never contain `function NAME(`, so this gate cannot be invalidated by a comment.)
    - `grep -c 'window.attachRailScrub(' web/app.js` returns 1 — exactly one scrub-input attach site serves both layouts.
    - At `PHONE_LANDSCAPE`, `.mobile-cinema-scrub-track` drag samples at 30/40/50/60/70 percent yield a non-decreasing playhead percentage sequence at each of zoom 1, 2, 4, 8.
    - With a landscape flyout open, `document.activeElement.closest('.mobile-flyout')` is non-null after pressing Tab more times than the flyout has focusable children.
    - With a landscape flyout open, a sky tap leaves `window.__bcfMobile`-observable play state unchanged; after closing the flyout a single sky tap changes it exactly once.
  </acceptance_criteria>

  <reversibility rating="reversible">A function rename plus one widened guard plus one selector branch, all inside `web/app.js`; the frozen gesture module is untouched, so the change is a single-file revert.</reversibility>

  <done>One `attachMobileGestures()` serves both mobile layouts through one teardown block and one scrub-input path; the landscape cinema-scrub drag is monotonic at every zoom from its first commit; and the D-16 focus trap re-attaches in landscape as well as portrait.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chrome auto-hide — the 4000ms idle timer, reveal-vs-pause semantics, and pause-reveals-and-holds</name>

  <read_first>
    - `web/app.js:905-930` — `stopPlayback()` and `togglePlayback()`, including the held-cinematic special case that returns early before the ordinary play/pause branch. The auto-hide wiring must cover BOTH exits.
    - `web/app.js:3852-3880` (post-Task-1 state) — `attachMobileGestures()`'s teardown block with the new `clearTimeout`, and the `onTap` reveal branch.
    - `web/app.js:131` and `:149` — `app.chromeHidden` and the neighbouring mobile state fields; add `app.mobileChromeHideTimer` here.
    - `web/mobile-gestures.js:6-20, 195` — the `G` constants object (`CHROME_AUTOHIDE: 4000`) and its `window.GestureConstants` export. Read the value from that export; do not restate 4000 as a second constant in `web/app.js`.
    - `design/mobile-ux/prototype/app.jsx` — the landscape auto-hide effect and the `onSky.tap` reveal branch. Read BOTH, then read RESEARCH.md Pitfall 1 before writing a line: the effect's dependency list re-arms on every word-position change, which happens every animation frame during playback, so a literal port can never fire. The reveal branch's early `return`, by contrast, IS the behavior to copy exactly.
    - `03-RESEARCH.md` Pattern 4 — the full lifecycle spec (where the handle lives, what starts it, what resets it, what tears it down) and the Security Domain row on a stale timer mutating a detached node.
    - `03-CONTEXT.md` D-28, D-29, D-30, D-31.
    - `web/app.js:2991-3034` — `openMobileSurface()` / `closeMobileSurface()`, to confirm both call `render()` (and therefore re-enter `attachMobileGestures()`'s teardown, which clears the timer).
    - `tests/test_mobile_portrait.py:190-205` — the `window.__bcfRenderStats` zero-structural-render idiom, reused here to prove auto-hide never calls `render()`.
  </read_first>

  <behavior>
    - Playing, landscape, no interaction: `.mobile-cinema-scrub` does NOT carry the hidden state at 3900ms after the last interaction, and DOES carry it at 4600ms.
    - Timer reset: an interaction at ~3000ms pushes the hide out — at 6900ms after the ORIGINAL start (i.e. 3900ms after the interaction) the scrub is still visible.
    - Paused, landscape: the scrub is visible and stays visible past 6000ms with no interaction; the timer never arms.
    - Pausing while chrome is hidden reveals it immediately and holds it revealed.
    - Reveal-vs-pause with tap-to-pause ON: from hidden+playing, one sky tap makes the scrub visible and leaves playback running; the NEXT sky tap pauses.
    - Reveal-vs-pause with tap-to-pause OFF: from hidden+playing, one sky tap makes the scrub visible and leaves playback running; a second tap changes nothing about play state (and the scrub stays visible).
    - Only the scrub hides: while `.mobile-cinema-scrub` carries the hidden state, `.mobile-top-cluster` and `.mobile-sidebar` are still visible with non-zero bounding boxes.
    - A full hide-then-reveal cycle produces `window.__bcfRenderStats.structuralRenders === 0`.
    - Rotating away from landscape (or opening a flyout) while the timer is armed does not later mutate anything: no console error is emitted and the scrub node in the new layout is unaffected.
  </behavior>

  <files>web/app.js, tests/test_mobile_landscape.py</files>

  <action>
Add the idle timer as a small piece of `app`-scoped state with a strict lifecycle. The timer is the only genuinely new machinery in this phase; every other landscape capability is reuse.

**1. State.** Add `mobileChromeHideTimer: null` to the `app` object beside `chromeHidden` at `web/app.js:131`. The handle lives on `app`, NOT on `app.dom` (which `render()` blanks on every structural rebuild) and NOT in a module-level closure (which would survive a landscape→portrait→landscape round trip without being clearable) — the same reasoning already documented inline for `app.mobileSurfaceFocusTrapTeardown`.

**2. `revealMobileChrome()`.** Sets `app.chromeHidden = false` and removes the hidden-state class from the cached cinema-scrub node via an optional chain (`app.dom.mobileCinemaScrub?.classList.remove(...)`). It performs no timer work of its own so callers stay explicit about whether the timer should re-arm. Do NOT call `render()`.

**3. `resetMobileChromeHideTimer()`.** Always begins by clearing the outstanding handle and nulling it. Then, ONLY when `app.layoutMode === "landscape"` AND `app.playing` AND no surface is open (`!app.mobileSurface`), it schedules a `setTimeout` for `window.GestureConstants?.CHROME_AUTOHIDE ?? 4000` milliseconds whose callback sets `app.chromeHidden = true` and adds the hidden-state class to the cached cinema-scrub node through an optional chain. The optional chain is the mitigation for a stale timer firing after its owning layout was torn down (T-03-06). Gating on `app.playing` is D-28: a paused reader is inspecting, which is exactly when the controls are wanted; gating on `!app.mobileSurface` stops the scrub from vanishing behind an open flyout. Do NOT call `render()` from the callback — a structural render tears down and reattaches every gesture listener, which would fire every 4 seconds of ordinary playback and makes the D-22 mid-drag guarantee impossible to reason about.

**4. Reset triggers.** In addition to the six gesture-callback sites Task 1 already wired, call the timer from:
   - `togglePlayback()`: on the transition to playing, call `resetMobileChromeHideTimer()` so the window starts. On the transition to NOT playing, call `revealMobileChrome()` and then `resetMobileChromeHideTimer()` — the reset's own `app.playing` gate turns it into a pure clear, which is exactly D-28's "pausing reveals chrome and keeps it revealed". Cover BOTH exits of the function, including the held-cinematic early return at `web/app.js:919-927`, so escaping a held cinematic also re-arms the window.
   - The cinema-scrub play FAB routes through the existing `toggle-playback` delegation into `togglePlayback()`, so it needs no separate wiring — confirm this rather than adding a second call site.

**5. Only the scrub hides (D-29).** The hidden-state class goes on `.mobile-cinema-scrub` alone. Do not add it to the top cluster or the sidebar: keeping the field log readable is the defining feature of landscape per MOBL-01, and hiding the rail would edge into the full-bleed cinematic D-01 deferred to v2.

**6. Do NOT reset the timer from `updateMobileLandscapeFrame()` or from any word-position comparison.** That is Pitfall 1: the word position advances on every animation frame during playback, so a reset keyed on it re-arms the timer roughly sixty times a second and the 4000ms window can never elapse. If a manual pass ever shows the scrub failing to fade during unattended playback, this is the first thing to check. Add a short comment at the definition recording the rejected keying and why, so a later reader does not "fix" it back.

**7. Tests.** Add to `tests/test_mobile_landscape.py`:
   - `test_landscape_chrome_autohide_boundary` — assert both sides of the 4000ms window (visible at 3900ms, hidden at 4600ms), the reset-pushes-it-out case (interaction at ~3000ms, still visible at 6900ms), the paused case (visible past 6000ms, timer never arms), the pause-while-hidden reveal, and the D-29 scope (top cluster and sidebar still visible while the scrub is hidden). Seed `window.__bcfRenderStats` and assert `structuralRenders === 0` across the whole hide/reveal cycle. Use `page.wait_for_timeout` for the window; if Chromium's clock proves too jittery for the 3900/4600 pair, use Playwright's `page.clock` fake-timer API rather than widening the tolerance — the point of the test is the boundary.
   - `test_landscape_reveal_tap_semantics` — parameterised over tap-to-pause ON and OFF. From hidden+playing: one sky tap reveals AND leaves play state unchanged in both cases; the next tap pauses only in the ON case and is a no-op in the OFF case. This is the D-30 no-trap guarantee and must be asserted, not merely reasoned about.
   - Extend `test_landscape_field_log_zero_and_empty_states` (or add a case) proving the scrub still reveals and hides and the play FAB still toggles on the `no-rolls` package.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py -x</automated>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py</automated>
  </verify>

  <acceptance_criteria>
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py -x` exits 0 with `test_landscape_chrome_autohide_boundary` and `test_landscape_reveal_tap_semantics` passing.
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py` exits 0 with no regression against the 33-test baseline.
    - At `PHONE_LANDSCAPE` while playing with no interaction: `.mobile-cinema-scrub` class list lacks the hidden state at t=3900ms and contains it at t=4600ms.
    - After an interaction at t≈3000ms the scrub still lacks the hidden state at t=6900ms.
    - While paused, the scrub lacks the hidden state at t=6000ms and `.mobile-cinema-scrub` remains hit-testable.
    - While the scrub carries the hidden state, `.mobile-top-cluster` and `.mobile-sidebar` both report a bounding rect with non-zero width and height.
    - Across a hide-then-reveal cycle, `window.__bcfRenderStats.structuralRenders` equals 0.
    - With tap-to-pause OFF, from hidden+playing, one sky tap results in the scrub visible AND play state unchanged; asserted as an explicit boolean pair, not inferred.
  </acceptance_criteria>

  <reversibility rating="reversible">The timer is three small functions plus two `app` fields and a handful of call sites; removing them restores always-visible chrome with no structural change.</reversibility>

  <done>Landscape chrome fades after 4000ms of genuine idleness during playback, pausing brings it back and holds it, the first tap on hidden chrome always reveals regardless of preferences, and the whole cycle runs through class mutation with zero structural re-renders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer input → app state | Sky and scrub gestures commit word-position changes through `setWordPos` and play-state changes through `togglePlayback`. |
| scheduled callback → DOM | The auto-hide `setTimeout` callback runs asynchronously and may fire after its owning render pass or layout has been torn down. |
| `localStorage` (`bcf:*`) → scrub math | `app.mobileTimelineZoom` feeds `panOffsetForPlayhead`/`mobileInnerFraction` on every drag callback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | stale auto-hide `setTimeout` mutating a detached or cross-layout DOM node | low | mitigate | The handle lives on `app` and is cleared unconditionally in `attachMobileGestures()`'s teardown block (which every structural render re-enters); the fired callback reaches the node through an optional chain so a null ref is a no-op rather than a throw. Task 2 asserts no console error after rotating away with the timer armed. |
| T-03-07 | Denial of Service | unbounded timer accumulation across repeated renders | low | mitigate | `resetMobileChromeHideTimer()` always clears before scheduling, and the teardown block clears again — at most one handle is outstanding. Asserted indirectly by the zero-structural-render + boundary tests. |
| T-03-02 | Tampering | `bcf:timeline-zoom` driving the landscape scrub mapping | low | mitigate | The zoom setter's rung allow-list is the only write path; the landscape drag reuses the identical `mobileInnerFraction` call portrait already uses, adding no new parse of a stored value. |
| T-03-08 | Elevation of Privilege | a gesture landing on the sky while a modal flyout is open | low | mitigate | `attachMobileGestures()`'s `if (app.mobileSurface) return;` guard leaves no gesture surface attached while a surface is open; Task 1 asserts a sky tap is inert with a landscape flyout open. |
| T-03-SC | Tampering | package-manager supply chain | high | accept | No package-manager install task exists in this plan or this phase; `web/` stays dependency-free and plan 04's freeze proof re-asserts it. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py` — full suite green.
2. `git diff --exit-code -- web/mobile-gestures.js web/style.css tests/test_mobile_plumbing.py` — frozen files untouched.
3. `grep -c 'function attachMobilePortraitGestures(' web/app.js` returns 0; `grep -c 'window.attachRailScrub(' web/app.js` returns 1 — one gesture-attach function, one scrub-input path.
4. Manual read-back: `resetMobileChromeHideTimer` is not referenced anywhere inside `updateMobileLandscapeFrame`, `updatePlaybackFrame`, or `setWordPos`.
</verification>

<success_criteria>
- One `attachMobileGestures()` serves both mobile layouts, with one teardown block, one scrub-input path and one focus-trap re-attach site.
- The landscape cinema-scrub drag is monotonic at 1x/2x/4x/8x from its first commit.
- Chrome auto-hides after 4000ms of real idleness during playback only, and pausing reveals and holds it.
- The first tap on hidden chrome always reveals, with any preference combination.
- Auto-hide never calls `render()`; a full hide/reveal cycle costs zero structural renders.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-02-SUMMARY.md` when done.
</output>
