---
phase: 01-mobile-state-gesture-plumbing
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified: []
autonomous: false
requirements: [MOBF-01, MOBF-06]
must_haves:
  truths:
    - "The full project verification gate (scripts/verify.py) is green over the completed Phase 1 changes (AGENTS.md standing requirement)"
    - "The INTEGRATION_PLAN §5 Phase A gate is reviewed with Dre and approved: desktop unchanged (scripted smoke test passes), layoutMode flips correctly on a phone viewport and survives rotation (Milestone Gate 2)"
    - "Decision conformance is presented for review: D-01 through D-11 each mapped to its implementation or its recorded later-phase binding (MOBF-01)"
  artifacts:
    - path: ".planning/phases/01-mobile-state-gesture-plumbing/01-04-SUMMARY.md"
      provides: "Gate evidence: verification output, decision-conformance table, Dre's approval"
  key_links:
    - from: "scripts/verify.py"
      to: "tests/test_desktop_smoke.py + tests/test_mobile_plumbing.py"
      via: "full pytest run inside the verification gate"
      pattern: "pytest"
---

## Phase Goal

**Goal (ROADMAP Phase 1, verbatim):** The app knows which layout it is in and can receive touch gestures safely, with zero new UI and zero desktop change.

<objective>
Close Phase 1 with the two mandated gates: the mechanical gate (full `scripts/verify.py` green, desktop smoke test passing) and the human gate (the INTEGRATION_PLAN §5 Phase A review with Dre, required by Milestone Gate 2 and CONTEXT.md's "Every Track A phase ends with the plan §5 gate review with Dre plus a passing run of the D-10 smoke test").

Purpose: a Track A phase that has not passed both gates is not done, by roadmap definition.

Output: gate evidence in the SUMMARY and Dre's approval to proceed to Phase 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mobile-state-gesture-plumbing/01-CONTEXT.md
@.planning/ROADMAP.md
@design/mobile-ux/INTEGRATION_PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full verification gate and assemble Phase A evidence</name>
  <precondition>data/derived/visualization_facts.json exists (hydrated working copy — scripts/verify.py's full pytest pass reads data/derived/*.json; hydrate via `.venv/bin/python scripts/data_release.py download-dev` if missing)</precondition>
  <files></files>
  <read_first>AGENTS.md (verification requirement and test-quality rules), design/mobile-ux/INTEGRATION_PLAN.md §5 Phase A gate and §0.5, .planning/phases/01-mobile-state-gesture-plumbing/01-CONTEXT.md (D-01..D-11)</read_first>
  <action>Run, in order, capturing output for the checkpoint: (1) `node --check web/app.js && node --check web/mobile-gestures.js`; (2) `.venv/bin/python -m pytest tests/test_desktop_smoke.py -x -q` (the D-10 gate command); (3) `.venv/bin/python scripts/verify.py` (full gate: git diff --check, data_release.py check-derived, full pytest). Then assemble the decision-conformance table for the checkpoint: D-01 mobile cinematic same-as-desktop (recorded; binds Phase 2 — no Phase 1 code), D-02 visibilitychange pause (implemented mobile-only, tested), D-03 viewport meta without zoom-disabling attributes (grep evidence from 01-01), D-04 provenance shape curated_by human|agent + run ledger (recorded; binds Phase 7 — no Phase 1 code, no re-interview needed), D-05 no mobile carousel in v1 (recorded), D-06 identical matchMedia string (grep evidence), D-07 per-render attach lifecycle (test evidence), D-08 svh/dvh + safe-area foundation (computed-style test evidence), D-09 STORAGE_VERSION 3 + purge incl. bcf:portrait-dismissed (test evidence), D-10 scripted smoke test (pass output), D-11 haptics decorative/feature-detected (byte-identical port retains the guard; bridge wired). Also confirm `git diff web/style.css` is empty and no §0.2 read-only function body changed (git diff review of web/app.js hunks).</action>
  <verify>
    <automated>.venv/bin/python scripts/verify.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/verify.py` exits 0
    - `.venv/bin/python -m pytest tests/test_desktop_smoke.py -x -q` exits 0
    - `git diff web/style.css` output is empty
    - The decision-conformance table lists all eleven decisions D-01..D-11 with evidence or recorded later-phase binding
  </acceptance_criteria>
  <done>All mechanical gates are green and the Phase A evidence pack (verification output + decision-conformance table + freeze confirmation) is ready for review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: INTEGRATION_PLAN §5 Phase A gate review with Dre</name>
  <what-built>Phase 1 mobile plumbing, end to end: layout-mode detection from the identical CSS breakpoint query (survives rotation), the verbatim gesture-helper port with a per-render attach lifecycle proven single-fire under mid-drag re-renders, the bcf:* v3 storage schema with purge and round-trip, the mobile CSS foundation (touch-action surfaces, svh/dvh, safe-area insets), pause-on-hidden for mobile layouts, and the scripted §0.5 desktop smoke test — with zero new UI and a provably unchanged desktop.</what-built>
  <how-to-verify>
    INTEGRATION_PLAN §5 Phase A gate: "running on desktop is unchanged. On a phone-sized viewport, app.layoutMode flips to portrait or landscape correctly and survives rotation."
    1. Review the Task 1 evidence: scripts/verify.py output, smoke-test pass, empty style.css diff, decision-conformance table (D-01..D-11).
    2. Optional hands-on desktop check (≥ 1100px window): open the app as usual — header, scrubber, carousel, field log identical to before; play/scrub/Details all behave; resize 1920 -> 1101 -> back with no flicker or layout flash.
    3. Optional hands-on phone-viewport check (devtools device toolbar, e.g. iPhone 390x844): in the console, `window.__bcfLayoutMode` reads "portrait"; rotate to 844x390 -> "landscape"; rotate back -> "portrait". The existing "best in landscape" banner still appears (it stays as the safety net until Phase 4). Page pinch-zoom still works (D-03).
    4. Storage spot-check (phone or desktop): `localStorage.getItem("bcf:preview-port-storage-version")` reads "3"; `localStorage.getItem("bcf:portrait-dismissed")` reads null on a profile that previously dismissed the banner.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 1 (Phase A gate passed), or describe issues to route back into gap closure</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

This plan produces no new code symbols (see 01-01-PLAN.md for the full phase-wide inventory). Its artifact is the gate evidence in `01-04-SUMMARY.md`: verification output, the D-01..D-11 decision-conformance table, and Dre's Phase A approval.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| completed phase -> milestone trust claim | the "desktop unchanged" core-value guarantee is asserted to the user here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Repudiation | phase-gate claim of desktop parity | high | mitigate | Dual gate: deterministic scripts/verify.py + smoke test evidence, plus the blocking human review against §5 Phase A before the phase may close |
</threat_model>

<verification>
- `.venv/bin/python scripts/verify.py` exits 0
- Checkpoint resolved with Dre's approval (or issues routed to gap closure)
</verification>

<success_criteria>
- Milestone Gate 2 satisfied for Phase 1: §5 Phase A review passed with Dre plus a passing scripted desktop smoke test (MOBF-06)
- MOBF-01 closed out: all interview decisions demonstrated as implemented or recorded for their binding phase
</success_criteria>

<output>
Create `.planning/phases/01-mobile-state-gesture-plumbing/01-04-SUMMARY.md` when done.
</output>
