---
phase: 04-mobile-cutover-accessibility
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - index.html
  - tests/helpers/web_runtime_site.py
  - tests/test_landing_page.py
autonomous: true
requirements: [MOBX-02]
user_setup: []

must_haves:
  truths:
    - "The repo-root landing page shows a story-title chip, an author credit, and a 44x44 `?` button that opens a help dialog — all three in one meta row placed above the Survey letter, never inside it (MOBX-02)."
    - "The Survey letter, its subject line, and the existing Open-visualization / View-source nav are byte-identical after this plan. MOBX-02's constraint is explicit: the letter stays verbatim."
    - "The `?` button is built at 44x44 from the start. It is brand-new markup with no pre-approved smaller visual constraining it, so unlike the in-app cinema-scrub FAB it needs no hit-area trick — build it correctly the first time (04-UI-SPEC.md)."
    - "The help dialog is a native `<dialog>` opened with showModal(). No focus trap, no Escape handler, no backdrop-click handler is hand-rolled — those are exactly the deceptively-complex problems the native element exists to solve, on a page that had zero JavaScript before this plan."
    - "The dialog carries a condensed credit block (title, byline, the three story source links, a pointer to the in-app Help, and the same Got-it CTA string the in-app overlay uses) — NOT the full 7-row gesture how-to, which is meaningless before the reader has entered the visualization."
    - "The landing page's help content is DUPLICATED from the in-app overlay, not shared. `index.html` has no bundler, no module graph and no build step; importing the app to render six lines of copy would drag the entire app bootstrap onto the page whose whole point is to be a fast dependency-free entry point (04-UI-SPEC.md § Copywriting Contract)."
    - "Every new external link carries target=_blank with rel=noopener noreferrer, mirroring the View-source link already correct in this same file."
    - "Every new element reuses `index.html`'s own existing local design tokens — the same hex values as the frozen app palette, declared a second time because the two files share no stylesheet. No new hex value, no new font, no third font weight is introduced."
    - "MOBX-02 edge (no-JS): with JavaScript disabled the `?` button is not merely inert, it is absent from the accessibility tree — a `<noscript>` rule hides it so no reader is offered a dead affordance. The page's core function, the Open-visualization link, is a plain anchor and is unaffected."
    - "MOBX-02 edge (dialog unsupported): the open handler feature-checks before calling showModal, so on a browser without the native element the failure mode is a silent no-op rather than a console error. No polyfill is added; this is supplementary content and the visualization link remains fully usable without it."
    - "MOBX-02 edge (long title): the meta row wraps rather than truncating. The chip's text is fixed static copy naming the actual work, and the landing page has vertical room to spare, so wrapping reads better than the in-app dock's ellipsis convention — a deliberate divergence, not an oversight."
    - "The dialog is explicitly sized rather than relying on user-agent defaults, so it cannot overflow horizontally at the narrowest supported viewport."
    - "Focus returns to the `?` button when the dialog closes. Native `<dialog>` does not restore focus to the invoker in every browser; this is the one behavior it does not give for free, and it is the only reason the page gains a script at all."
    - statement: "On a real iPhone, the landing page's `?` opens the dialog and it reads correctly at phone width. Static-file dialog behavior on iOS Safari differs from headless Chromium, and the landing page is the first thing a reader arriving from a story link sees."
      verification: backstop
    - statement: "The dialog does not overflow or clip at a 320px-wide viewport. Native `<dialog>`'s default sizing has never been measured in this codebase; an explicit max-width is specified for exactly that reason, and the measurement confirms it."
      verification: backstop
    - statement: "With JavaScript disabled the `?` button is genuinely absent from the accessibility tree rather than present-but-broken — asserted in a JS-disabled browser context, since a `<noscript>` rule cannot be observed in a normal one."
      verification: backstop

  prohibitions:
    - "MUST NOT edit the Survey letter, its subject line, or the existing nav. MOBX-02 names the letter as verbatim; it is the voice of the piece and the one part of this page that is not ours to revise."
    - "MUST NOT couple the landing page to the app bundle to avoid duplicating six lines of copy. That trades a small honest duplication for the exact new dependency this codebase's zero-build-step rule exists to prevent, on the page whose value is loading instantly."
    - "MUST NOT ship the `?` button below 44x44 or reach 44 with a hit-area trick. This is new markup with no locked visual to preserve; a control that is born under-sized and then padded is a control that was designed wrong."
    - "MUST NOT leave the no-JS reader looking at a button that does nothing. An affordance that is visible and dead is worse than no affordance — it reads as the page being broken."

  flagged_assumptions:
    - id: FA-MOBX-02
      origin: planner (edge-probe returned `unclassified` for MOBX-02; never probe-resolved, must not be closed by an agent)
      requirement: MOBX-02
      status: unresolved
      note: "The deterministic edge probe could not classify MOBX-02 into a shape category, so no acceptance criterion was auto-derived for its degradation behavior. The criteria below are planner-authored and carried to the Phase D+E gate. They are NOT a probe result."
      criteria:
        - "No-JS: in a browser context with JavaScript disabled, the `?` control is absent from the accessibility tree, and the Open-visualization anchor still navigates."
        - "Dialog unsupported: with the native dialog constructor removed from the page before interaction, pressing `?` produces no console error and no visible breakage."
        - "Long title: at the narrowest supported width the chip, credit and `?` either fit on one line or wrap onto a second — never truncated, never overlapping."
        - "Narrow viewport: at 320px the dialog's rendered box lies entirely within the viewport horizontally."
        - "Focus return: after closing the dialog by Escape, by the CTA, and by the backdrop, the active element is the `?` button in all three cases — or, where a route is not natively supported, that route's actual behavior is recorded rather than assumed."
        - "For Dre at the gate: whether the condensed credit-only dialog is the right scope, or whether he wants the gesture how-to duplicated after all. 04-RESEARCH.md Open Question 1 assigned this to Claude's discretion for v1; it is static HTML and cheap to expand either way."
    - id: FA-MOBX-06
      origin: 04-UI-SPEC.md § Copywriting Contract (flagged for the executor, carried by the planner)
      requirement: MOBX-02
      status: unresolved
      note: "The three story source URLs now exist as two independent hardcoded copies — one in `web/app.js`'s STORY_LINKS constant, one in this dialog — because the two files share no module. If a story link ever changes, BOTH must be updated together. This is the same class of duplication already accepted for the title and author strings appearing in both files today, and it is accepted here rather than solved, but it is recorded so a future reader finds it deliberately."
      criteria:
        - "The dialog's three hrefs are character-identical to `web/app.js`'s STORY_LINKS entries at the time of writing — verified by comparing them, not by retyping from memory."
        - "The duplication is named in the summary and on the gate agenda so Dre rules on accepting it rather than inheriting it silently."

  artifacts:
    - "index.html: .landing-meta-row wrapper with .landing-title-chip, .landing-author-credit and .landing-help-btn"
    - "index.html: #landing-help-dialog with its ::backdrop rule, explicit max-width, and condensed credit content"
    - "index.html: the <noscript> rule hiding the ? control, and the first inline <script> this file has ever carried (focus return on close)"
    - "tests/helpers/web_runtime_site.py: repo-root landing-page staging so a Playwright fixture can serve it over http"
    - "tests/test_landing_page.py: a genuinely new test surface — every existing fixture targets /web/, none targets the repo root"

  key_links:
    - "the staged site root -> the landing page's ./web/ relative link; the fixture must stage BOTH so the primary nav link resolves rather than 404ing in the test"
    - "web/app.js's STORY_LINKS -> the dialog's three hrefs; no module connects them, so the link is a human convention recorded in FA-MOBX-06"
    - "the ? button -> #landing-help-dialog via a feature-checked showModal call, and back again via the close listener that restores focus"
    - "index.html's existing local :root tokens -> every new element's color; these are the same values as the frozen app palette, declared twice because the files share no stylesheet"
---

<objective>
Give the repo-root landing page a story-title chip, an author credit, and a `?` button that opens a condensed help-and-credits dialog — without touching a word of the Survey letter, and without dragging the app bundle onto a page that has been zero-JavaScript until now.

Purpose: MOBX-02. The landing page is the first thing a reader arriving from a story link sees, and today it names neither the story nor its author and offers no help at all.

Output: an oriented landing page, a new Playwright fixture that can serve the repo root, and `tests/test_landing_page.py`.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-PATTERNS.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-UI-SPEC.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-VALIDATION.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...` — this worktree has NO `.venv` of its own.
- Commit signing is disabled repo-wide; `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`. Never `git add .`.
- This plan touches NOTHING under `web/`. `web/app.js`, `web/style.css`, `web/mobile.css` and `web/mobile-gestures.js` are all owned by sibling plans in this phase or frozen outright; keeping this plan's file set disjoint is what lets it run in parallel with plan 04-02.
- The repo root gains no `package.json`, no bundler, no build step. The only script is a handful of inline vanilla DOM calls.
</session_constraints>

## Artifacts this phase produces

Phase-wide symbol inventory, creations AND deletions. This plan owns the rows marked **[03]**. The complete table, including plan 01's seven deletions, is in `04-01-PLAN.md`.

| Symbol / artifact | Kind | Plan |
|---|---|---|
| **DELETED** the banner render function, call site, storage constant, state field, migration entry, CSS block and stale media wrapper | removals | 01 |
| `tests/test_freeze_proof.py` | new test file | 01 |
| the `aria-live="polite"` region, `mobileRollAnnouncement()`, `announceMobileRoll()`, the mobile keydown branch, `.mobile-live-region` | live region + keyboard | 02 |
| `.landing-meta-row` | new wrapper (`index.html`) | **[03]** |
| `.landing-title-chip`, `.landing-author-credit`, `.landing-help-btn` | new elements + CSS (`index.html`) | **[03]** |
| `#landing-help-dialog` + its `::backdrop` rule | new native dialog (`index.html`) | **[03]** |
| the `<noscript>` rule hiding the help control | progressive-enhancement guard (`index.html`) | **[03]** |
| the inline focus-return `<script>` — the FIRST JavaScript this file has ever carried | script (`index.html`) | **[03]** |
| repo-root landing-page staging in `tests/helpers/web_runtime_site.py` | fixture extension | **[03]** |
| `tests/test_landing_page.py` | new test file (new fixture target) | **[03]** |
| `.mobile-cinema-scrub-fab::before` + `position: relative`; the reduced-motion timer branch | hit area + reduced motion | 04 |
| `tests/test_lighthouse_accessibility.py` | new test file | 05 |
| `COVERAGE.md`, the §6 evidence table, the gate rulings | phase artifacts | planner / 05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Landing-page meta row, help dialog, and a fixture that can serve the repo root</name>

  <read_first>
    - `index.html` in full (359 lines) — read once. Extract in that single pass: the local `:root` token block (lines 14-23), the `.message-panel` / `.constellation` / `.subject` / `.letter` / `.actions` / `.secondary-link` rules and their exact spacing values, the `@media (max-width: 700px)` overrides, the markup insertion point at line 323-341, and the already-correct external-link pattern at line 353.
    - `04-UI-SPEC.md` § "New locked value: landing page additions" — the locked spacing, sizes, colors and typography roles for all four new elements plus the dialog; and § "Copywriting Contract → Landing page (MOBX-02)" — the exact copy strings for every element, which are locked, not suggestions.
    - `04-PATTERNS.md` § "index.html — Landing-page chip, credit, `?` dialog (MOBX-02)" — the locked insertion point, the markup shape, and the external-link precedent to mirror.
    - `web/app.js` lines 113-124 — the STORY_LINKS constant whose three URLs are copied literally into the dialog (read-only; this plan does not touch `web/`).
    - `web/app.js` lines 3725-3766 — `renderMobileHelpOverlay()`, the content source being condensed. Note the byline construction and the Got-it CTA string, both of which the dialog reuses verbatim for cross-surface consistency.
    - `tests/helpers/web_runtime_site.py` lines 1-70 and 565-700 — `WEB_FILES`, `_copy_web_files`, `_serve`, `StagedWebRuntimeSite.url_for` and `staged_web_runtime_site`'s signature. The server already serves `site_root` at `/`, so the landing page needs staging, not a second server.
    - `tests/test_mobile_portrait.py` lines 24-76 — the `_page_with_console_capture` harness shape to mirror for a new test module.
  </read_first>

  <files>index.html, tests/helpers/web_runtime_site.py, tests/test_landing_page.py</files>

  <behavior>
    - Test 1: the landing page renders a title chip whose text is the story title and an author credit naming the author, both above the letter.
    - Test 2: a 44x44-or-larger help control exists, carries an accessible name, and is reachable by keyboard.
    - Test 3: activating it opens a modal dialog; the dialog is open according to the element's own open state.
    - Test 4: the dialog contains the story title, the byline, three source links, a pointer to the in-app help, and the Got-it CTA.
    - Test 5: every anchor in the dialog that targets a new browsing context carries both noopener and noreferrer.
    - Test 6: the Survey letter's paragraphs, the subject line, and both existing nav links are unchanged — asserted against their exact current text so an accidental edit fails.
    - Test 7: the Open-visualization link resolves to a real page in the staged site rather than 404ing.
    - Test 8: closing the dialog by the CTA leaves focus on the help control.
    - Edge case: the page still loads and renders with zero console errors — the harness already captures console output; assert it is empty.
  </behavior>

  <action>
Extend the fixture first, then write the tests, then the markup — so the tests fail for the right reason.

**1. Fixture.** `staged_web_runtime_site()` already stages a site root and serves it over a real local http port, with the app under `/web/`. Add repo-root landing-page staging: copy the repository's root `index.html` into the staged site root alongside the existing `web/` directory, so `site.url_for("/")` serves the landing page AND its relative `./web/` link resolves to the already-staged app. Do this inside the existing staging path rather than adding a second fixture or a second server — the whole point is that the landing page's primary nav link is exercised against a real staged app. Keep the change additive: no existing test may change behavior because of it.

**2. Markup, at the locked insertion point.** Insert the meta row as the FIRST child of the introductory-note section, immediately before the decorative constellation block and therefore above the subject line. It functions as a masthead above the letter and must never be placed inside the letter itself. It holds three siblings — the title chip, the author credit, the help button — plus the dialog. Use the copy strings from `04-UI-SPEC.md`'s Copywriting Contract exactly as written, including the accessible name and tooltip on the button and the dialog's own accessible label.

**3. The dialog's content**, condensed per the locked contract: the story title as a heading, the byline with the author's name emphasised followed by the genre note, a row of three source links whose hrefs are copied literally from `web/app.js`'s STORY_LINKS (compare them side by side; do not retype from memory — see FA-MOBX-06), the one-sentence pointer telling the reader that full gestures, settings and playback help are available once they open the visualization, and the Got-it CTA using the identical string the in-app overlay already uses. Do NOT duplicate the seven-row gesture how-to. Apply the external-link attributes that the existing View-source anchor in this same file already demonstrates.

**4. Styling**, all inside the page's existing `<style>` block, following its existing local idiom rather than re-gridding it. Take every value from `04-UI-SPEC.md`'s locked table: the meta row is a wrapping flex row with the specified gap and bottom margin; the chip reuses the app's chip padding and pill radius with the panel/edge surface pairing and accent text; the credit uses the page's existing secondary-text token; the help button is a native 44x44 square with the same panel/edge pairing as every in-app icon button and an accent glyph; the dialog gets the specified padding, an explicit max-width so it cannot overflow a narrow viewport, and a backdrop derived from the page's own darkest token. Introduce no new hex value, no new font family, and no weight beyond the two the page already uses.

**5. Behavior.** Open the dialog from an inline handler on the button that feature-checks for the native modal method before calling it, so an unsupported browser gets a silent no-op instead of a thrown error. Add one small inline script that restores focus to the help button when the dialog closes — this is the single behavior the native element does not provide reliably, and it is the only reason this file gains a script. Immediately after the button, add a `<noscript>` block whose style rule hides the help control, so a reader with scripting off is never shown an affordance that cannot work.

**6. Tests.** Create `tests/test_landing_page.py` implementing the behaviors above, mirroring `tests/test_mobile_portrait.py`'s console-capturing page helper but pointing at the staged site root rather than the app path. Test 6 is the important one: assert the letter's paragraph text, the subject line and both nav link labels against their exact present values so any future edit to the Survey letter fails loudly.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_landing_page.py -x -q</automated>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py tests/test_freeze_proof.py -q</automated>
    <automated>test "$(git diff 57d2768 HEAD -- index.html | grep -c '^-[^-]')" = "0"</automated>
    <automated>test "$(grep -c 'target="_blank"' index.html)" = "$(grep -c 'rel="noopener noreferrer"' index.html)"</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_landing_page.py -x -q` exits 0 and collects at least 8 items.
    - `git diff 57d2768 HEAD -- index.html` shows ZERO removed content lines — this plan is purely additive to the landing page, which is the mechanical proof that the Survey letter is verbatim.
    - The count of `target="_blank"` occurrences in `index.html` equals the count of `rel="noopener noreferrer"` occurrences.
    - A Playwright measurement reports the help button's rendered box at 44 CSS px or greater in both dimensions.
    - The button has a non-empty accessible name, and the dialog has a non-empty accessible label.
    - The dialog's three source hrefs are asserted equal to the three values read out of `web/app.js`'s STORY_LINKS at test time, so the two copies cannot silently diverge without a red test.
    - The staged `Open visualization` link returns a 200 and loads the app, proving the fixture stages both surfaces.
    - Console output captured during the page load is empty.
    - The four-file mobile/desktop suite plus the freeze proof are still green — this plan touches nothing under `web/`.
  </acceptance_criteria>

  <reversibility rating="costly">The three story URLs now exist as two hardcoded copies with no module connecting them (FA-MOBX-06); a future link change requires a coordinated edit in `index.html` and `web/app.js` together, and nothing in the build will catch a half-done change except the test added here.</reversibility>

  <done>The landing page names the story and its author, offers a working 44x44 help control that opens a condensed credit-and-help dialog with safe external links, leaves the Survey letter provably untouched, and is covered by a new test module served from a fixture that stages both the landing page and the app.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the landing page's four degraded states — no JS, no native dialog, narrowest viewport, focus return</name>

  <precondition>Task 1's markup is committed and `pytest tests/test_landing_page.py -x -q` is green. These are hardening assertions over existing markup; running them before the markup exists would only prove the elements are missing.</precondition>

  <read_first>
    - `04-UI-SPEC.md` § "UI Considerations" — the four rows this task closes (no-JS, dialog unsupported, narrow viewport, focus return), each with its stated resolution and the reason it is a backstop rather than a plain assertion.
    - `tests/test_landing_page.py` (Task 1's output) — the module you are extending and its page helper.
    - `tests/test_mobile_portrait.py` lines 24-76 — the browser-context creation pattern, which is where a JavaScript-disabled context has to be configured; the existing helper takes a browser and builds a context, so a second variant or an extra keyword is needed rather than a per-page toggle.
    - `04-VALIDATION.md` § "Manual-Only Verifications" — items 4 and the device-pass framing, so the automated coverage here is scoped to what a headless browser can honestly prove and the rest is left to the gate.
  </read_first>

  <files>tests/test_landing_page.py</files>

  <behavior>
    - Test 1 (no-JS): in a browser context created with JavaScript disabled, the page still renders the letter and the Open-visualization anchor, and the help control is absent from the accessibility tree — asserted via the accessibility snapshot or computed visibility, not merely by the element being present-but-inert.
    - Test 2 (dialog unsupported): with the native modal method removed from the dialog element before interaction, activating the help control produces no page error and no uncaught console error; the page remains usable.
    - Test 3 (narrow viewport): at 320 CSS px wide, the opened dialog's bounding box lies entirely within the viewport horizontally, and the meta row's three children neither overlap nor truncate — the row wraps instead.
    - Test 4 (focus return, CTA): closing via the Got-it CTA leaves the help button as the active element.
    - Test 5 (focus return, Escape): closing via the Escape key leaves the help button as the active element.
    - Edge case: the long-title check is a wrapping assertion, not a width assertion — assert the chip's full text is rendered (no ellipsis, no clipped scrollWidth) at the narrowest viewport.
  </behavior>

  <action>
Extend `tests/test_landing_page.py` with the four degraded states. No production edit is expected — but if a test surfaces a genuine gap (for example, the narrow-viewport dialog overflowing despite its explicit max-width, or a focus route not restoring), fix it in `index.html` in this task's commit and say so in the summary rather than relaxing the assertion.

**No-JS** needs a browser context created with scripting disabled, which is a context-creation option, not a page setting — extend the module's page helper with a flag rather than duplicating it. Assert the help control is hidden as the `<noscript>` rule intends: check its computed visibility or its absence from the accessibility snapshot, because "the element is in the DOM" would pass even when the affordance is dead, which is exactly the failure this row exists to prevent. Also assert the letter and the primary nav anchor are both still present, since the page's core function must survive.

**Dialog unsupported** is simulated by deleting the native modal method from the dialog instance in the page before clicking, then asserting no page error fires and the console stays clean. The point of the feature check is that the failure is silent, so the assertion is about the absence of an error, not about any visible outcome.

**Narrow viewport** runs at 320 CSS px. Open the dialog and compare its bounding box against the viewport width; assert the left edge is at or above zero and the right edge is at or below the viewport width. Separately assert the meta row wrapped rather than truncated: the chip's rendered text is its full copy and its scroll width does not exceed its client width.

**Focus return** is asserted on two routes — the CTA and Escape. Record what the backdrop-click route actually does rather than assuming it; if the native element does not fire the same close path there, note the real behavior in the summary and carry it to the gate rather than adding a third handler to force symmetry.

Every one of these four rows is marked as a backstop in `must_haves.truths` because the honest limit of a headless browser is what it can observe, not what a reader on a real phone experiences. The device pass at plan 04-05 confirms the remainder.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_landing_page.py -x -q</automated>
    <automated>test "$(grep -c 'noscript' index.html)" -ge "1"</automated>
    <automated>test "$(git diff 57d2768 HEAD -- index.html | grep -c '^-[^-]')" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_landing_page.py -x -q` exits 0 and collects at least 13 items (Task 1's 8 plus these 5).
    - The no-JS test creates its context with scripting disabled — asserted by the test itself failing if run in a scripted context, demonstrated once during execution.
    - The no-JS assertion is about accessibility-tree absence or computed hiding, not mere DOM presence.
    - At 320 px the dialog's bounding box satisfies `x >= 0` and `x + width <= 320`.
    - At 320 px the title chip's `scrollWidth` is no greater than its `clientWidth` — proving it wrapped rather than clipped.
    - Focus is on the help control after both the CTA close and the Escape close.
    - `index.html` still shows zero removed content lines against the milestone base commit.
    - If any production fix was needed, it is named in the summary with the observed failure that prompted it.
  </acceptance_criteria>

  <reversibility rating="reversible">Test-only, with any incidental `index.html` fix confined to the new elements.</reversibility>

  <done>All four degraded states are covered by committed assertions: the help control is genuinely absent without JavaScript, an unsupported dialog fails silently, the dialog fits a 320 px viewport with the chip wrapping rather than clipping, and focus returns to the invoker on both tested close routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| landing page → external story hosts | Three new outbound links open new browsing contexts to third-party forum/archive sites. |
| reader's browser capabilities → page behavior | The page must stay functional with scripting off and without native dialog support; both are capability boundaries the page cannot control. |
| `web/app.js`'s STORY_LINKS → the dialog's hardcoded copies | No module connects them; correctness is a human convention, recorded rather than enforced. |
| the Survey letter → this plan's edits | The letter is authored voice under an explicit verbatim constraint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | reverse tabnabbing via the three new `target="_blank"` story links | medium | mitigate | Both `noopener` and `noreferrer` on every new external anchor, mirroring the existing View-source link in the same file. Gated by an acceptance criterion asserting the two attribute counts are equal across the whole file, so a new link cannot be added without the guard. |
| T-04-11 | Denial of Service | an unguarded `showModal()` throwing on a browser without the native element, breaking the page's script | low | mitigate | The handler feature-checks before calling; a dedicated test removes the method and asserts no error fires. |
| T-04-12 | Tampering | an accidental edit to the Survey letter, which is under an explicit verbatim constraint | medium | mitigate | `git diff` against the milestone base commit must show zero removed content lines in `index.html`, plus exact-text assertions on the letter, subject and nav labels. |
| T-04-13 | Spoofing | a visible help affordance that silently does nothing for a no-JS reader, reading as a broken page | low | mitigate | A `<noscript>` rule removes the control entirely, asserted in a scripting-disabled browser context rather than inferred. |
| T-04-14 | Tampering | the two hardcoded copies of the story URLs drifting apart, sending readers to a stale destination | medium | mitigate | A test reads the URLs out of `web/app.js` and asserts equality with the dialog's hrefs, converting a silent drift into a red test. Recorded as FA-MOBX-06 for the gate. |
| T-04-SC | Tampering | package-manager supply chain | high | accept | No package-manager install occurs in this plan and the repo root gains no `package.json`. The one npm tool this phase uses is introduced in plan 04-05 behind its own blocking legitimacy checkpoint. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_landing_page.py -x -q` — green, >= 13 items.
2. `git diff 57d2768 HEAD -- index.html | grep -c '^-[^-]'` — returns 0.
3. `target="_blank"` and `rel="noopener noreferrer"` occurrence counts in `index.html` are equal.
4. The help button measures >= 44x44; the dialog fits a 320 px viewport; the chip wraps rather than clips.
5. The four-file app suite plus the freeze proof stay green — this plan touches nothing under `web/`.
</verification>

<success_criteria>
- The landing page names the story and its author and offers working help, above a verbatim Survey letter.
- The `?` control is 44x44 by construction, accessibly named, and keyboard reachable.
- The dialog is native, feature-checked, explicitly sized, and returns focus to its invoker.
- Every new external link is safe.
- A new test module covers both the happy path and all four degraded states, served from a fixture that stages the landing page and the app together.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-03-SUMMARY.md` when done.
</output>
