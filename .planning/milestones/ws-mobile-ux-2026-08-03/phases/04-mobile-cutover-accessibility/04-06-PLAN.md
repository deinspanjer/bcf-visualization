---
phase: 04-mobile-cutover-accessibility
plan: 06
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - .planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/COVERAGE.md
  - .planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-06-SUMMARY.md
autonomous: false
requirements: [MOBX-01, MOBX-02, MOBX-03, MOBX-04, MOBX-05]
user_setup: []

must_haves:
  truths:
    - "The whole suite — the four pre-existing files plus the three new ones — passes in ONE invocation, with the 53-test Phase 3 baseline intact and no pre-existing test deleted rather than renamed."
    - "The whole-MILESTONE freeze proof is green: the desktop stylesheet's diff against the pre-Phase-1 base is deletion-only, the gesture file is byte-identical to that same base, and `web/` still carries no package manifest, lockfile or bundler config. This is MOBX-01's closing claim and it is measured against the commit before ANY mobile work started, not against this phase's own base."
    - "COVERAGE.md exists carrying the exact API-coverage declaration for this phase, with no fabricated capability matrix."
    - "An INTEGRATION_PLAN.md §6 acceptance-checklist evidence table covers every applicable bullet across every group — including the Accessibility group that Phases 2 and 3 both marked out-of-scope, which this phase owns and must now cite for the first and last time."
    - "Every flagged assumption raised anywhere in this phase — FA-MOBX-01 through FA-MOBX-07 — is on the gate agenda with its criteria, unanswered. Four of them originate from edge-probe rows the deterministic probe returned as unclassified and were never probe-resolved; none may be closed by an agent."
    - "The phase ends with the §5 Phase D+E gate review ruled on by Dre, a passing desktop smoke test, and a real-device pass on iOS Safari specifically."
    - "The device pass carries D-49's measurement: with Safari's Request Desktop Website turned on, the actual innerWidth and layoutMode are READ off the device. This replaces an unverified documentation figure with a number, and it is the input to D-50's ruling."
    - "The device pass carries the VoiceOver check: Dre enables the screen reader, performs one gesture, and reports whether the announcement is spoken and whether it stays silent through playback. A live region present in the DOM but never announced passes every automated check while delivering nothing, and this is the last phase that will look."
    - "MOBX-04's throw-decay clause is presented to Dre as a requirements-accuracy note, not as a satisfied item. The feature it names was never built."
    - "Anything not landed at this gate ships as v2. This is the final phase of the mobile-ux workstream, and the deferral list is stated explicitly rather than left implicit."
    - statement: "The banner is genuinely gone on real hardware in BOTH orientations — the milestone's headline claim, checked where it matters. The banner was the mobile safety net for three phases and its absence has only ever been asserted in emulation."
      verification: backstop
    - statement: "The landing page's help control opens correctly on a real iPhone. Static-file dialog behavior on iOS Safari differs from headless Chromium, and this is the first thing a reader arriving from a story link sees."
      verification: backstop
    - statement: "The expanded hit area around the cinema-scrub play button registers reliably under a real thumb and creates no dead zone against the adjacent progress track."
      verification: backstop

  prohibitions:
    - "MUST NOT auto-approve, self-approve, or infer approval of the Phase D+E gate under any mode, including autonomous and auto-advance. This is the milestone's closing gate; the whole point is that a human rules on judgments no agent can make from emulation."
    - "MUST NOT declare the phase done on a green suite alone. Three phases running, a fully green suite has hidden a defect a reader would have hit on the first day — and Phase 3's two were structural, not cosmetic. The hardware pass is the evidence, not a formality."
    - "MUST NOT close any FA-MOBX flagged assumption without Dre's explicit ruling. Four of them exist precisely because the deterministic probe declined to classify them; an agent closing them would substitute its own judgment for the one the probe refused to make."
    - "MUST NOT fabricate an external-API capability matrix in COVERAGE.md. This phase integrates no external API; the honest declaration is the deliverable and an invented matrix would be a false record."
    - "MUST NOT mark MOBX-04 satisfied on the strength of the throw-decay clause. Presenting a vacuous clause as met would put a false statement into the requirements record on the last day anyone will check."

  flagged_assumptions:
    - id: FA-GATE-04
      origin: planner (carries FA-MOBX-01..07 from plans 04-01 through 04-05)
      requirement: MOBX-01, MOBX-02, MOBX-03, MOBX-04, MOBX-05
      status: unresolved
      note: "Every flagged assumption authored in this phase is carried onto this gate's agenda for Dre's ruling. FA-MOBX-01 through FA-MOBX-04 originate from edge-probe rows the deterministic probe returned as `unclassified`; per protocol they stay unresolved and are surfaced rather than auto-backstopped or auto-dismissed. FA-MOBX-05 through FA-MOBX-07 originate from the UI spec and the package legitimacy seam. None may be closed by an agent."
      criteria:
        - "FA-MOBX-01 (MOBX-01, banner absence): both phone orientations and desktop width, plus the reader-facing question of whether portrait genuinely stands on its own now that the rotation suggestion is gone."
        - "FA-MOBX-02 (MOBX-02, landing-page degradation): no-JS, unsupported dialog, long title, 320px sizing, focus return on every close route — plus whether the condensed credit-only dialog is the right scope or the gesture how-to should be duplicated after all."
        - "FA-MOBX-03 (MOBX-04, reduced-motion timing): the in-flight timer case, the module-load snapshot meaning an OS toggle takes effect next load, the paused and surface-open cases, and the throw-decay wording."
        - "FA-MOBX-04 (MOBX-05, hidden-page pause): portrait, landscape, desktop-unaffected, bookmark write intact, and whether pause-without-auto-resume is still the wanted behavior now that the mobile app actually exists."
        - "FA-MOBX-05 (MOBX-03, untracked-acquisition phrasing): zero live examples; confirm the row stays dead code rather than carrying invented copy. Carried with it: the whole announcement phrasing table is proposed copy that Dre has not yet approved."
        - "FA-MOBX-06 (MOBX-02, duplicated story URLs): two hardcoded copies with no module connecting them; confirm accepting the duplication rather than inheriting it silently."
        - "FA-MOBX-07 (MOBX-03, audit tool legitimacy): the SUS verdict ruling and the version pin, already taken at plan 04-05 Task 1 — restated here so the phase record carries it."

  artifacts:
    - "The full-suite run record: one invocation, seven files, the exact pass count against the 53-test baseline"
    - "The whole-milestone freeze-proof record measured against the pre-Phase-1 base commit"
    - "COVERAGE.md, verified in place"
    - "The INTEGRATION_PLAN.md §6 acceptance-evidence table, including the Accessibility group for the first time"
    - "The Phase D+E gate agenda, assembled and unanswered, and then Dre's rulings"
    - "The v2 deferral list, stated explicitly because this is the final phase"

  key_links:
    - "commit 57d2768 (pre-Phase-1) -> the closing freeze claim; a phase-local base certifies a strictly weaker statement than MOBX-01 makes"
    - "the D-49 device measurement -> D-50's ruling on the desktop-view escape hatch; without the number the decision stays an inference"
    - "the VoiceOver check -> the live region's only real proof; DOM presence and computed style are not evidence that anything is spoken"
    - "this gate -> the mobile-ux milestone's close; anything not landed here is v2"
---

<objective>
Close the milestone: prove the whole suite green in one run, prove the desktop surface byte-identical to before any mobile work started, assemble the §6 acceptance evidence, and take the `INTEGRATION_PLAN.md` §5 Phase D+E gate to Dre with a real-device iOS pass that carries two measurements no emulator can produce.

Purpose: MOBX-01 through MOBX-05 closure, and the end of the mobile-ux workstream. Three phases running, hardware has found defects a green suite missed every single time.

Output: the phase's evidence records and Dre's gate rulings.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-VALIDATION.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-UI-SPEC.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-01-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-02-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-03-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-04-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-05-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-04-SUMMARY.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...` — this worktree has NO `.venv` of its own.
- Commit signing is disabled repo-wide; `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`. Never `git add .`.
- This plan writes no production code. If the gate surfaces a defect, it routes to `/gsd-plan-phase 4 --gaps`, not to an in-place edit of a committed plan.
</session_constraints>

## Artifacts this phase produces

Phase-wide symbol inventory, creations AND deletions. This plan owns the rows marked **[06]**.

| Symbol / artifact | Kind | Plan |
|---|---|---|
| **DELETED** `renderPortraitBanner()` and its call site in `renderAppShell()` | render removal (`web/app.js`) | 01 |
| **DELETED** `LS_PORTRAIT_DISMISSED`, `app.portraitDismissed`, its migration-clear entry | state/storage removal (`web/app.js`) | 01 |
| **DELETED** the `.portrait-banner` rule block and its child selectors | frozen-CSS removal (`web/style.css`) | 01 |
| **DELETED** the stale `@media (max-width: 900px), …` wrapper | frozen-CSS removal (`web/style.css`) | 01 |
| `tests/test_freeze_proof.py` | new test file | 01 |
| the `aria-live="polite"` region node + `app.dom.mobileLiveRegion` | live-region DOM (`web/app.js`) | 02 |
| `mobileRollAnnouncement(roll)`, `announceMobileRoll()` | live-region helpers (`web/app.js`) | 02 |
| `rollMarkerModel` added to the `viz-model.js` import block | import edit (`web/app.js`) | 02 |
| the mobile `keydown` early branch (Arrow / Home / `?`) | controller edit (`web/app.js`) | 02 |
| `.mobile-live-region` visually-hidden rule | style (`web/mobile.css`) | 02 |
| `.landing-meta-row`, `.landing-title-chip`, `.landing-author-credit`, `.landing-help-btn` | static markup + CSS (`index.html`) | 03 |
| `#landing-help-dialog`, its `<noscript>` guard, and the first inline `<script>` in that file | static markup (`index.html`) | 03 |
| `tests/test_landing_page.py` + repo-root staging in `tests/helpers/web_runtime_site.py` | new test file + fixture | 03 |
| `.mobile-cinema-scrub-fab::before` + `position: relative` on its host | style (`web/mobile.css`) | 04 |
| the `PREFERS_REDUCED_MOTION` branch in `resetMobileChromeHideTimer()` | timer edit (`web/app.js`) | 04 |
| the MOBX-04 throw-decay requirements-accuracy note | phase record | 04 |
| `tests/test_lighthouse_accessibility.py` + the recorded score, form factor and version | new test file + record | 05 |
| the audit tool's package legitimacy ruling | gate record | 05 |
| `COVERAGE.md` | phase artifact (written at planning, verified here) | planner / **[06]** |
| the §6 acceptance-evidence table, the whole-milestone freeze record, the v2 deferral list, and Dre's Phase D+E rulings | phase closure records | **[06]** |

<tasks>

<task type="auto">
  <name>Task 1: Full-suite sweep, whole-milestone freeze proof, COVERAGE verification, §6 evidence table, and the gate agenda</name>

  <precondition>All five prior plans in this phase have committed summaries on disk. If any is missing, halt — the §6 evidence table cites real test names from those summaries and cannot be assembled from planned ones.</precondition>

  <read_first>
    - `04-01-SUMMARY.md` through `04-05-SUMMARY.md` — every test name this phase added, every decision recorded, every deviation, and the recorded Lighthouse score. The evidence table must cite real names, not planned ones.
    - `.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-04-SUMMARY.md` — the sweep/freeze-proof/COVERAGE precedent to mirror, its §6 evidence-table format, and its Accessibility row that explicitly deferred to this phase.
    - `design/mobile-ux/INTEGRATION_PLAN.md` §6 (the full acceptance checklist, all groups), §5 phases D and E (this phase merges them), and §0.5 (the desktop smoke checklist).
    - `.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/COVERAGE.md` — already written during planning; verify it in place rather than rewriting a file that already satisfies the grep.
    - `04-VALIDATION.md` § "Per-Task Verification Map" and § "Manual-Only Verifications" — the rows this sweep closes and the four items that route to Task 2.
    - `.planning/workstreams/mobile-ux/REQUIREMENTS.md` MOBX-01..05 — the exact wording each evidence row is answering, including MOBX-04's throw-decay clause.
  </read_first>

  <files>.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/COVERAGE.md</files>

  <action>
Produce the phase's evidence, not new functionality.

**1. Full-suite sweep.** Run all seven test files in ONE invocation — the four pre-existing ones plus `test_freeze_proof.py`, `test_landing_page.py` and `test_lighthouse_accessibility.py` — and record the exact pass count. Compare against the 53-test Phase 3 baseline and confirm no pre-existing test was deleted rather than renamed. Note the Lighthouse run's contribution to wall-clock time so a future reader knows why the full suite is slower than it was.

**2. Whole-milestone freeze proof.** Run `tests/test_freeze_proof.py` and additionally record the raw numbers by hand: the deletion-only diff of the desktop stylesheet against the pre-Phase-1 base, the byte-identical gesture file against that same base, and the dependency scan. Record the base commit SHA explicitly in the summary and state in words that it is the pre-Phase-1 commit, not this phase's own base — MOBX-01's claim is whole-milestone and a phase-local base would certify something weaker. Also record the diff of `tests/test_desktop_smoke.py` across this phase and justify plan 04-01's single edit the way Phase 2 justified its own.

**3. COVERAGE.md verification.** The file already exists from planning with the exact declaration sentence. Verify it in place with an exact-match grep; do not rewrite a file that already satisfies its own acceptance check.

**4. §6 acceptance-evidence table.** Assemble it in this plan's summary, one row per applicable checklist bullet across every group, each citing the automated test that proves it by file and test name, or marked hardware-only with a pointer to Task 2, or marked out-of-scope with the owning phase named. The Accessibility group is the important one: Phases 2 and 3 both marked it out-of-scope and deferred it here, so every one of its bullets must now carry a real citation — reduced motion, keyboard equivalents, aria-live announcements, and 44x44 tap targets. Add the Help-and-credits group's landing-page bullet, which Phase 3 also deferred. Never silently omit a bullet.

**5. Requirement-by-requirement closure record.** For each of MOBX-01 through MOBX-05, state what closed it and how. Two entries need care: MOBX-03 has FOUR independent clauses (announcements, keyboard, 44x44, Lighthouse score) and each needs its own citation — in particular, record that the Lighthouse score and the 44x44 bar were verified separately and that neither is evidence for the other. MOBX-04's throw-decay clause is vacuous and must be recorded as a requirements-accuracy note, not as satisfied.

**6. Gate agenda.** Assemble the Phase D+E agenda for Task 2, itemising: the seven carried flagged assumptions FA-MOBX-01 through FA-MOBX-07 with their criteria; the announcement phrasing table awaiting Dre's copy approval; D-49's desktop-mode measurement and the D-50 ruling that depends on it; the VoiceOver check; the MOBX-04 throw-decay wording; the two hardcoded copies of the story URLs; the landing-page dialog's content scope; and every deviation recorded in plans 04-01 through 04-05. Do not pre-answer any of them.

**7. The v2 deferral list.** This is the final phase of the workstream, so state explicitly what is NOT shipping and why: the in-app desktop-view toggle pending D-49's measurement and a further ruling; the spreadsheet export as the real artifact for non-sighted readers; large-tablet-portrait ergonomics; and the plan §8 backlog items. Anything a reader might expect and not find should be findable here.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py tests/test_freeze_proof.py tests/test_landing_page.py tests/test_lighthouse_accessibility.py</automated>
    <automated>test "$(git diff --numstat 57d2768 HEAD -- web/style.css | cut -f1)" = "0" && git diff --exit-code 57d2768 HEAD -- web/mobile-gestures.js</automated>
    <automated>test "$(grep -F -c 'No external API integration: cutover and accessibility work touches only local DOM, localStorage, static outbound links, and a local Lighthouse CLI invocation.' .planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/COVERAGE.md)" = "1"</automated>
    <automated>test ! -e web/package.json && test ! -e web/package-lock.json && test ! -e web/yarn.lock && test ! -e package.json</automated>
  </verify>

  <acceptance_criteria>
    - The seven-file pytest invocation exits 0 in a single run, and the recorded pass count is at least the 53-test Phase 3 baseline plus the number of new tests named across the 04-01..04-05 summaries.
    - `git diff --numstat 57d2768 HEAD -- web/style.css` reports 0 added lines, and `git diff --exit-code 57d2768 HEAD -- web/mobile-gestures.js` exits 0. Both SHAs are recorded in the summary with a sentence stating why this base and not a phase-local one.
    - `grep -F -c` for the COVERAGE declaration sentence returns 1.
    - No package manifest, lockfile or bundler config exists under `web/` or at the repo root.
    - The §6 evidence table has a row for every applicable checklist bullet across every group, and every row is a `tests/...::test_name` citation, an explicit hardware-only marker, or an explicit out-of-scope marker naming the owning phase. No blank cells. The Accessibility group's four bullets all carry real citations.
    - The requirement closure record has a separate citation for each of MOBX-03's four clauses, and explicitly states that the Lighthouse score is not evidence for the 44x44 bar.
    - MOBX-04's closure record carries the throw-decay requirements-accuracy note rather than marking the clause satisfied.
    - The gate agenda lists FA-MOBX-01 through FA-MOBX-07 by id with their criteria, and no agenda item carries a pre-supplied answer.
    - The v2 deferral list names the desktop-view toggle, the spreadsheet export, large-tablet ergonomics and the §8 backlog items.
  </acceptance_criteria>

  <reversibility rating="reversible">Documentation and evidence only; no source change.</reversibility>

  <done>The whole suite is green in one run, the desktop surface is proven byte-identical to before any mobile work started, COVERAGE.md is verified in place, the §6 evidence table covers every bullet including the Accessibility group for the first time, and the gate agenda plus the v2 deferral list are assembled and unanswered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: INTEGRATION_PLAN.md §5 Phase D+E gate review with Dre — desktop smoke, real-device iOS pass, D-49 measurement, and the VoiceOver check</name>

  <precondition>A real iOS device running Safari is available to Dre, reachable over `ios-webkit-debug-proxy` plus the target-based WebKit inspector protocol (Safari's own Inspector window must be closed — the session is single-client). Phases 2 and 3 both completed this exact workflow. If no iOS hardware is available, halt and report rather than substituting Android or emulation: this milestone's own record is that hardware found defects a green suite missed in every single phase, two of them structural, and this is the last phase that will look.</precondition>

  <read_first>
    - `04-06-SUMMARY.md` (Task 1's output) — the §6 evidence table, the freeze record, the requirement closure record, the gate agenda and the v2 deferral list.
    - `design/mobile-ux/INTEGRATION_PLAN.md` §5 phases D and E — the two gates this phase merges, and their exit criteria.
    - `.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-04-SUMMARY.md` § "Phase C Gate Agenda" — the iOS pass workflow, the two structural defects hardware found, and the methodology note on item 4 showing how an ambiguous first measurement was redone properly rather than accepted.
    - `04-CONTEXT.md` D-49 and D-50 — the desktop-mode measurement and the conditional ruling that depends on it.
    - `04-UI-SPEC.md` § "Live region → Announcement phrasing" — the proposed copy table awaiting approval.
  </read_first>

  <what-built>
    Mobile is now the real experience. The rotate-to-landscape banner is deleted from JavaScript and CSS and the stale second breakpoint went with it, leaving exactly one breakpoint pair in the repo. The landing page names the story and its author and offers a working 44x44 help control opening a condensed credit-and-help dialog, above a verbatim Survey letter. Both mobile layouts carry a visually-hidden polite live region that announces outcome-aware roll text on user-caused position changes only and stays silent through playback; arrows step one roll, Home snaps to the live edge, `?` toggles Help, and Space already worked. The cinema-scrub play button is touchable at 48x48 while painting exactly 40x40. Under reduced motion the landscape auto-hide window doubles to 8000ms and every transition is already silenced. Playback still pauses when the page is hidden, on both mobile layouts and not on desktop. Lighthouse Accessibility scores at or above 90 at the mobile form factor from a committed, pinned, re-runnable gate. The desktop surface is proven byte-identical to the commit before any mobile work started.
  </what-built>

  <how-to-verify>
1. **Desktop smoke (required, non-negotiable).** Run `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py` and confirm it passes. A phase that fails the §0.5 smoke test is not done. Then open the app in a desktop browser at 1100px or wider and walk the §0.5 checklist by eye. This is the milestone's closing freeze claim — pay attention to the header region where the banner used to sit and confirm nothing shifted.

2. **Serve the app locally and open it on a real iPhone over Safari** (the Phase 2/3 workflow: `ios-webkit-debug-proxy` plus the target-based WebKit inspector protocol; close Safari's own Inspector window first).

3. **Banner absence, both orientations (MOBX-01).** Hold the phone upright, then sideways. In each, confirm no rotation-suggestion banner appears, and take a screenshot. Over the CDP bridge, confirm the element query for it returns null in both. This was the mobile safety net for three phases; its absence is the headline claim.

4. **Landing page on a real phone (MOBX-02).** Open the repo-root landing page on the device. Confirm the story title chip and author credit read correctly at phone width, tap the `?`, and screenshot the opened dialog. Confirm the three source links open, and that the Survey letter reads exactly as it always has.

5. **D-49 — the desktop-mode measurement.** In Safari, turn on Request Desktop Website for the app. Over the CDP bridge, READ and record two numbers: `window.innerWidth` and the app's own layout mode. The prediction from documentation is a ~980px layout viewport, which our portrait clause would still match — meaning the app would render the MOBILE layout scaled down rather than switching to desktop. That prediction has never been measured. One tap replaces it with a fact.
   **Then rule on D-50:** if browser-native desktop mode does not serve as the escape hatch, the in-app toggle drops to v2 with the recorded rationale (both alternatives would break either the iPad-portrait rule or the single-source-of-layout-truth discipline that has held for four phases). If you want it built anyway, say so — that is a scope decision, not something an agent may infer.

6. **VoiceOver — the live region's only real proof (MOBX-03).** Turn VoiceOver on. Perform ONE gesture that moves the playhead — a swipe-step is the clearest. Report: **is the announcement spoken at all**, and does it read sensibly? Then start playback and let several rolls pass. Report: **does it stay silent?** A live region that is present but never announced passes every automated check while delivering nothing, and no headless browser runs a screen reader.
   While you are here, **rule on the announcement wording**. The phrasing table is Claude's proposed copy grounded in the real dataset, not approved copy. Misses are 410 of 670 rolls and currently read as a terse "Miss." — the common case, deliberately not padded with a cost estimate. Multi-grab rolls name the first perk and a count of the rest. Each row is a one-line string edit if you want it different.
   **A scope note you should push back on if you disagree:** this region is deliberately minimal. It exists because MOBX-03 asks for it and it genuinely helps a reader with residual vision confirm where a gesture landed. It is explicitly NOT a claim that the visualization is usable unsighted — the right artifact for that is the spreadsheet export you have planned.

7. **Keyboard (MOBX-03).** If you have an iPad with a keyboard, or simply narrow a desktop browser window until it crosses the breakpoint: confirm the arrow keys step one roll at a time rather than jumping thousands of words, Home snaps to the live edge and resumes, `?` opens Help and a second `?` closes it, and Space toggles playback. Then widen the window past 1100px and confirm the arrows go back to their desktop word-stepping.

8. **Tap targets (MOBX-03).** Tap the cinema-scrub play button at its very edge, repeatedly, with a thumb rather than a fingertip. Confirm it registers reliably and that you do not accidentally grab the progress track next to it. The painted circle is unchanged at 40x40; only the touchable area grew.

9. **Reduced motion (MOBX-04).** Turn on Reduce Motion in iOS accessibility settings and reload. Start playback in landscape and put the phone down: the cinema-scrub pill should now take about eight seconds to fade rather than four. Confirm transitions elsewhere are already still. **Note the requirements-accuracy item:** MOBX-04 also names throw decay, which was never implemented — rule on amending the wording rather than treating it as unmet forever.

10. **Hidden-page pause (MOBX-05).** With playback running, switch to another app or lock the screen, wait, and come back. Confirm playback stopped, your place was kept, and it did not silently resume while you were away. This behavior shipped in Phase 1 and was decided before any mobile UI existed — say if living with the app has changed your mind about it.

11. **Rule on the gate agenda** in `04-06-SUMMARY.md`: FA-MOBX-01 through FA-MOBX-07 with their criteria, the announcement copy, the two hardcoded copies of the story URLs, the landing dialog's content scope, the throw-decay wording, the audit tool's pinned version, and every deviation plans 04-01 through 04-05 recorded.

12. **Rule on the v2 deferral list.** This is the final phase of the workstream — anything not landed here ships as v2. Confirm the list is what you expect, or name what is missing.

13. If anything is wrong, report it here. Fixes route through `/gsd-plan-phase 4 --gaps` rather than in-place plan edits. **Expect to need a second device pass after any fix** — in Phase 2 the fix for one device defect introduced another that only a second pass caught, and in Phase 3 both defects were structural.
  </how-to-verify>

  <acceptance_criteria>
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py` exits 0 and the result is recorded in the summary.
    - A real iOS Safari device pass has been run, with the device model and iOS/Safari version recorded.
    - Banner absence is recorded with a screenshot per orientation AND a CDP null assertion — both, not one.
    - D-49's measurement is recorded as two actual numbers read off the device (`innerWidth` and the app's layout mode with desktop mode on), and D-50 is ruled on the basis of those numbers rather than the documentation inference.
    - The VoiceOver check is recorded as two explicit answers: whether the announcement is spoken, and whether it stays silent through playback. "Present in the DOM" is not an acceptable answer to either.
    - Dre has ruled on the announcement phrasing table — approved as proposed, or with the specific rows he wants changed.
    - Every numbered step above has an explicit outcome recorded — pass, fail with detail, or explicitly deferred with an owner. No step left blank.
    - Every gate-agenda item, including FA-MOBX-01 through FA-MOBX-07, carries an explicit ruling from Dre.
    - The gate's disposition is recorded verbatim as Dre stated it. This checkpoint is NOT auto-approvable under any configuration, including autonomous mode and auto-advance; an agent may not infer, assume or self-issue approval.
  </acceptance_criteria>

  <resume-signal>Type "approved" to close the Phase D+E gate and the mobile-ux milestone, or describe the issues found (each will be routed to `/gsd-plan-phase 4 --gaps`).</resume-signal>

  <done>Dre has walked the §5 Phase D+E gate and the §6 acceptance checklist on real iOS Safari hardware, recorded D-49's measurement as a number and ruled on D-50, answered the VoiceOver check in both directions, ruled on the announcement copy and on every carried flagged assumption, confirmed the v2 deferral list, and either approved the gate or enumerated the defects to route to gap closure — with the desktop smoke test recorded as passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent claims → the milestone record | This summary is the durable evidence a future reader inherits; an unverified claim here becomes an unexamined fact there, and there is no later phase to correct it. |
| emulation results → the shipping decision | Headless Chromium cannot run a screen reader, cannot reproduce Safari's desktop-mode viewport, cannot produce non-zero safe-area insets, and cannot reproduce a real thumb. |
| repository → the frozen desktop surface | MOBX-01's closing claim rests on a diff against the commit before any mobile work started. |
| requirement wording → the requirements record | Marking a vacuous clause satisfied would put a false statement into the record on the last day anyone checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-25 | Repudiation | declaring the milestone done on a green suite without hardware evidence | high | mitigate | Task 2 is a blocking human gate that is never auto-approvable. Every numbered step must carry an explicit outcome, and the device model and OS version are recorded. Three prior phases each found hardware defects a green suite missed. |
| T-04-26 | Spoofing | claiming an accessible live region on the strength of DOM presence alone | high | mitigate | The VoiceOver check demands two explicit spoken/silent answers, and "present in the DOM" is named as an unacceptable answer. This is the only proof available for the feature and this is the last phase that will look. |
| T-04-27 | Tampering | a desktop regression hidden behind a green mobile suite | high | mitigate | The freeze proof diffs against the pre-Phase-1 base rather than the working tree or a phase-local base, so a mid-milestone edit that was later reverted-and-re-added is still caught; the desktop smoke test is a non-negotiable gate step. |
| T-04-28 | Repudiation | closing an unclassified flagged assumption by agent judgment | high | mitigate | Four of the seven exist precisely because the deterministic probe declined to classify them. They are prohibited from agent closure and are listed by id on the gate agenda with their criteria. |
| T-04-29 | Repudiation | marking MOBX-04 satisfied on a clause whose feature was never built | medium | mitigate | The throw-decay note is a named deliverable of Task 1's closure record and a named agenda item for Task 2; the requirement is presented for amendment rather than marked met. |
| T-04-30 | Repudiation | a fabricated external-API capability matrix in COVERAGE.md | low | mitigate | The declaration sentence is fixed and asserted by exact-match grep; the file already exists from planning and is verified in place rather than rewritten. |
| T-04-SC | Tampering | package-manager supply chain | high | accept | No package-manager install occurs in this plan. The one npm tool this phase uses was ruled on by a human at plan 04-05's blocking legitimacy checkpoint and pinned to the reviewed version; the dependency scan re-asserts that `web/` and the repo root carry no manifest, lockfile or bundler config. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py tests/test_freeze_proof.py tests/test_landing_page.py tests/test_lighthouse_accessibility.py` — one invocation, all green.
2. `git diff --numstat 57d2768 HEAD -- web/style.css` reports 0 added lines; `git diff --exit-code 57d2768 HEAD -- web/mobile-gestures.js` exits 0.
3. `COVERAGE.md` carries the exact declaration sentence.
4. No package manifest, lockfile or bundler config under `web/` or at the repo root.
5. Task 2's blocking gate is resolved by Dre, with the device model, OS version, D-49's two numbers and the VoiceOver answers all recorded, and every agenda item ruled on.
</verification>

<success_criteria>
- The whole suite passes in one run with no pre-existing test lost.
- The desktop surface is byte-identical to the commit before any mobile work started, measured whole-milestone.
- COVERAGE.md carries the exact declaration; the §6 evidence table has no blank or omitted row, including the Accessibility group.
- Each of MOBX-03's four clauses has its own citation and the score is not offered as evidence for the tap-target bar.
- MOBX-04's throw-decay clause is recorded as a requirements-accuracy note.
- Dre has ruled on the §5 Phase D+E gate on real iOS hardware, with D-49 settled by measurement and the VoiceOver check answered in both directions.
- The v2 deferral list is explicit, because there is no next phase in this workstream.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-06-SUMMARY.md` when done.
</output>
