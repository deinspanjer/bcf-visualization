---
phase: 04-mobile-cutover-accessibility
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - web/mobile.css
  - web/app.js
  - tests/test_mobile_portrait.py
  - tests/test_mobile_landscape.py
autonomous: true
requirements: [MOBX-03, MOBX-04, MOBX-05]
user_setup: []

must_haves:
  truths:
    - "Every tappable control in both mobile layouts is touchable at 44x44 CSS px or larger. The compact icon buttons ALREADY clear it via Phase 2's content-box-plus-padding technique, proven by an existing passing assertion — this plan verifies that and changes only the cinema-scrub play button, the one remaining sub-44 control (D-39)."
    - "The cinema-scrub play button's PAINTED box stays exactly 40x40 with its glow pixel-identical. Its hit area is expanded by a transparent absolutely-positioned pseudo-element, because padding would grow the visible circle — background and box-shadow paint through the padding box, so the padding technique that is correct for the icon buttons is wrong for this one control (RESEARCH Pitfall 3)."
    - "The 44x44 requirement and the Lighthouse Accessibility score are TWO INDEPENDENT checks and both are required. Lighthouse's tap-target audit is axe-core's target-size rule at a 24x24 default — a lower, different success criterion — so a passing score is blind to the 44 bar and can never be offered as evidence for it (RESEARCH Pitfall 1)."
    - "The cinema-scrub play button's 44x44 is verified by clicking at an offset outside its painted box and asserting its handler fired. A rect measurement on that element reports 40x40 forever, because a pseudo-element's absolute-position overflow never changes its host's own layout box."
    - "With prefers-reduced-motion set to reduce, the landscape chrome auto-hide window is 8000ms instead of 4000ms, and the doubling reads the reduced-motion preference from the ONE module-level constant the desktop cinematic already computes — never a second matchMedia query (MOBX-04, RESEARCH Pattern 1)."
    - "With prefers-reduced-motion set to reduce, mobile transitions and animations are already silenced by a pre-existing unscoped global rule in the frozen desktop stylesheet. This plan VERIFIES that with a test rather than adding a duplicate mobile-scoped block; the auto-hide doubling is the only genuinely new reduced-motion code in the phase."
    - "MOBX-04's throw-decay clause is VACUOUS and is recorded as a requirements-accuracy note rather than satisfied. Throw-to-scrub inertia was never implemented — it is a v2 backlog item and the swipe-end consumer ignores velocity by design. There is nothing to disable."
    - "Playback pauses when the page is hidden, on BOTH mobile layouts, with word position and every preference intact and no auto-resume when the page becomes visible again. This is Phase 1 behavior that this phase VERIFIES; no implementation is written (MOBX-05, D-51)."
    - "The desktop path is unaffected by the hidden-page pause: the existing guard keys on the layout mode not being desktop, so a hidden desktop page keeps playing exactly as it did before any mobile work started."
    - "web/mobile-gestures.js is still byte-identical (D-17) — the auto-hide constant is DEFINED there and READ through the global bridge, so the doubling belongs entirely in the app module."
    - statement: "The expanded hit area around the cinema-scrub play button does not create a dead zone or a double-registration against the progress track 8px to its right. The overlay reaches 4px into that gap; the arithmetic says it leaves 4px clear, but it has never been measured on a real pill layout under a real thumb."
      verification: backstop
    - statement: "On a real iPhone, tapping the very edge of the cinema-scrub play button registers reliably. Hit-area expansion is a touch-ergonomics change and a synthetic offset click is a proxy for a thumb, not a substitute."
      verification: backstop

  prohibitions:
    - "MUST NOT report a passing Lighthouse Accessibility score as evidence that the 44x44 tap-target requirement is met. The audit measures 24x24; conflating the two would publish a conformance claim the code has not earned, on the one requirement whose whole purpose is to be measurable."
    - "MUST NOT substitute a bounding-rect measurement for the cinema-scrub play button's offset-click assertion, and MUST NOT relax the 44 bar to 40 to make a check pass. A rect assertion on that element fails by construction; the tempting fix is to lower the number, which silently drops the requirement for exactly the control it was written for."
    - "MUST NOT re-apply a hit-area fix to the compact icon buttons. Phase 2 already shipped a working, tested solution there; layering a second technique on top is churn that risks regressing a control that is currently correct."
    - "MUST NOT implement throw-to-scrub inertia in order to then disable it under reduced motion. The requirement names a feature that was never built; building it to satisfy the wording would be fabricating scope, and the honest response is to record the requirement as inaccurate."
    - "MUST NOT enlarge the cinema-scrub play button's painted circle or its glow. The visual is a locked prototype reference carried through the Phase C gate; growing it to reach 44 would trade an approved design for a number that a transparent overlay delivers without cost."

  flagged_assumptions:
    - id: FA-MOBX-03
      origin: planner (edge-probe returned `unclassified` for MOBX-04; never probe-resolved, must not be closed by an agent)
      requirement: MOBX-04
      status: unresolved
      note: "The deterministic edge probe could not classify MOBX-04, so no acceptance criterion was auto-derived for its timing edges. The criteria below are planner-authored and carried to the Phase D+E gate. Note in particular that the reduced-motion preference is snapshotted ONCE at module load, so an in-flight timer cannot be re-armed by a preference change mid-session — that is a real behavioral limit, not a test artifact."
      criteria:
        - "Timer mid-flight: with reduced motion active from page load, an auto-hide timer armed by a gesture uses the 8000ms window; a timer already armed BEFORE the doubling logic ran keeps its original window until the next reset call. Confirm this is acceptable rather than silently accepting it."
        - "Preference toggled mid-session: because the preference is read once at module load, toggling the OS setting while the app is open does not re-arm existing timers and takes effect on the next page load. Confirm at the gate whether that is acceptable for v1 or a v2 item."
        - "Reduced motion plus paused: the auto-hide timer does not run at all while playback is stopped, so the doubling has no observable effect in that state — confirm it does not read as the setting being ignored."
        - "Reduced motion plus an open surface: the timer is suppressed while a flyout is open under both windows."
        - "Throw decay: confirm with Dre that recording the clause as vacuous, rather than building the feature to disable it, is the right call, and that MOBX-04's wording gets a requirements-accuracy note."
    - id: FA-MOBX-04
      origin: planner (edge-probe returned `unclassified` for MOBX-05; never probe-resolved, must not be closed by an agent)
      requirement: MOBX-05
      status: unresolved
      note: "The deterministic edge probe could not classify MOBX-05, so no acceptance criterion was auto-derived. This requirement is VERIFY-ONLY: the behavior shipped in Phase 1 and this plan must not re-implement it. The criteria below are planner-authored and carried to the Phase D+E gate."
      criteria:
        - "Portrait: hiding the page while playing stops playback, preserves word position, and does not auto-resume on return to visible."
        - "Landscape: identical behavior — the landscape arm was added in Phase 3 and is covered by the same guard only because that guard keys on the layout mode rather than naming portrait; confirm rather than assume."
        - "Desktop: hiding the page while playing does NOT stop playback, proving the mobile-only scoping is intact and no frozen behavior changed."
        - "The bookmark write that also fires on hide is unaffected and still persists the position in every layout."
        - "For Dre at the gate: whether pause-without-auto-resume is still the wanted behavior after living with the mobile app, since the Phase 1 interview decided it before any mobile UI existed."

  artifacts:
    - "web/mobile.css: .mobile-cinema-scrub-fab gains position:relative, plus a new transparent ::before hit-area overlay rule"
    - "web/app.js: the reduced-motion branch inside the existing resetMobileChromeHideTimer()"
    - "tests/test_mobile_landscape.py: the offset-click hit-area assertion, the doubled auto-hide boundary tests, and the landscape hidden-page pause test"
    - "tests/test_mobile_portrait.py: the existing-44px verification, the transitions-already-silenced test, and the portrait and desktop hidden-page tests"
    - "A requirements-accuracy note recording MOBX-04's throw-decay clause as vacuous"

  key_links:
    - "the ::before overlay -> its host's new position:relative; without that declaration the overlay resolves against some further ancestor and lands in the wrong place entirely"
    - "the module-level reduced-motion constant -> the auto-hide delay; a second matchMedia call would be two independent snapshots of one OS preference"
    - "web/mobile-gestures.js's auto-hide constant -> the app module's global-bridge read; the doubling multiplies the read value and never edits the frozen file"
    - "the existing layout-mode guard on the hidden-page handler -> MOBX-05 covering landscape for free; that is the fact this plan verifies rather than assumes"
---

<objective>
Close the three requirements that are mostly already true: expand the one remaining sub-44 tap target without changing a pixel of what it paints, double the landscape auto-hide window under reduced motion, and prove that hidden-page pausing works on both mobile layouts and leaves desktop alone.

Purpose: MOBX-03's tap-target clause, MOBX-04, and MOBX-05. Research established that the compact icon buttons, the global reduced-motion silencing, and the hidden-page pause are all already shipped — so most of this plan is verification, and the small amount of new code is one CSS rule and one ternary.

Output: a 44x44-clean mobile surface, a reduced-motion-aware auto-hide, committed proof for all three, and an honest note that one clause of MOBX-04 describes a feature that never existed.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-PATTERNS.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-UI-SPEC.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-VALIDATION.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-02-SUMMARY.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...` — this worktree has NO `.venv` of its own.
- Commit signing is disabled repo-wide; `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`. Never `git add .`.
- `web/mobile-gestures.js` stays BYTE-IDENTICAL (D-17). `web/style.css` is finished for this milestone and `tests/test_freeze_proof.py` gates it — this plan adds ZERO lines to it, including no mobile-scoped reduced-motion block.
- `tests/test_desktop_smoke.py` must stay at plan 04-01's single justified edit. The desktop-unaffected assertion in this plan goes in `tests/test_mobile_portrait.py` driven at a desktop viewport, following the existing precedent of the desktop-shell regression guard already living in that file.
- All new CSS goes in `web/mobile.css`, inside its single existing `@media` block.
- Playwright 1.59.0 is installed in the shared venv and supports reduced-motion at BOTH context creation and page level — verified during planning. Use the CONTEXT-level option; see Task 2.
</session_constraints>

## Artifacts this phase produces

Phase-wide symbol inventory, creations AND deletions. This plan owns the rows marked **[04]**. The complete table, including plan 01's seven deletions, is in `04-01-PLAN.md`.

| Symbol / artifact | Kind | Plan |
|---|---|---|
| **DELETED** the banner render function, call site, storage constant, state field, migration entry, CSS block and stale media wrapper | removals | 01 |
| `tests/test_freeze_proof.py` | new test file | 01 |
| the `aria-live="polite"` region, `mobileRollAnnouncement()`, `announceMobileRoll()`, the mobile keydown branch, `.mobile-live-region` | live region + keyboard | 02 |
| the landing-page meta row, dialog, `<noscript>` guard and inline script; `tests/test_landing_page.py` | landing page | 03 |
| `.mobile-cinema-scrub-fab::before` transparent hit-area overlay | new style rule (`web/mobile.css`) | **[04]** |
| `position: relative` added to the existing `.mobile-cinema-scrub-fab` rule | style edit (`web/mobile.css`) | **[04]** |
| the reduced-motion branch inside `resetMobileChromeHideTimer()` | timer edit (`web/app.js`) | **[04]** |
| offset-click hit-area assertion, doubled-auto-hide boundary tests, landscape hidden-page test | tests (`tests/test_mobile_landscape.py`) | **[04]** |
| existing-44px verification, transitions-already-silenced test, portrait and desktop hidden-page tests | tests (`tests/test_mobile_portrait.py`) | **[04]** |
| the MOBX-04 throw-decay requirements-accuracy note | phase record | **[04]** |
| `tests/test_lighthouse_accessibility.py` | new test file | 05 |
| `COVERAGE.md`, the §6 evidence table, the gate rulings | phase artifacts | planner / 05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the cinema-scrub play button's hit area to 44x44 without changing what it paints</name>

  <read_first>
    - `04-UI-SPEC.md` § "New locked value: cinema-scrub FAB hit-area overlay (D-39)" — the locked inset value, the exact set of properties the overlay must NOT carry, the one declaration the host rule gains, and the verification note explaining why a rect assertion is wrong here.
    - `04-PATTERNS.md` § "web/mobile.css — `.mobile-cinema-scrub-fab` hit-area (D-39) — CONTRASTING pair" — both analogs side by side: the padding technique that is correct for the compact icon buttons and wrong for this one, and why.
    - `web/mobile.css` lines 296-312 — the compact icon button rule, so you can see the already-working technique and confirm you are NOT touching it.
    - `web/mobile.css` lines 660-716 — the cinema-scrub pill and its play button. Note the pill's padding on the button's side; the overlay's negative inset must fit inside it, and this is what makes an offset click land on the overlay rather than outside the pill.
    - `tests/test_mobile_portrait.py` lines 515-540 — the existing, currently-passing 44px rect assertion. This is the pattern to extend for every OTHER control and the explicit anti-pattern for this one.
    - `tests/test_mobile_landscape.py` — the landscape harness, the viewport constants, and the existing chrome-visibility handling; the Phase 3 summary records that force-clicking an auto-hidden pill hits the sky underneath, so chrome state matters in this test.
    - `04-VALIDATION.md` findings 1 and 3 — the two independent checks, and the explicit instruction not to let a rect check be substituted.
  </read_first>

  <files>web/mobile.css, tests/test_mobile_landscape.py, tests/test_mobile_portrait.py</files>

  <behavior>
    - Test 1: with the landscape chrome visible and the sky-tap preference off, a click 2 CSS px outside the play button's painted left edge, at its vertical midpoint, toggles playback — proving the overlay dispatches to its host.
    - Test 2 (control): a click 10 CSS px outside that same edge does NOT toggle playback — proving test 1's result came from the overlay and not from something else catching the click.
    - Test 3: the play button's own bounding rect still measures 40x40, asserted deliberately as documentation of why a rect check cannot verify this control.
    - Test 4: the computed style of the overlay reports a box at least 44 px in both dimensions, and carries no background image, no background color other than fully transparent, and no box shadow.
    - Test 5: every OTHER tappable control in both layouts still measures at least 44x44 by rect — the existing portrait assertion extended to the landscape dock and top cluster, with the play button explicitly excluded and the exclusion justified in a comment pointing at the offset-click test.
    - Edge case: the assertion runs with chrome revealed and playback stopped, so the pill is not in its hidden, pointer-events-disabled state.
  </behavior>

  <action>
Two CSS changes and a test set. No JavaScript.

**1. The host rule gains exactly one declaration:** a relative position, so the overlay resolves against the button itself rather than against whatever ancestor happens to be positioned. The button currently declares no position at all, which is why this is required and not optional. Change nothing else in that rule — the width, the height, the radius, the background and the box shadow are the locked prototype visual carried through the Phase C gate.

**2. Add one new rule for the button's `::before`** with empty content, absolute positioning, and the negative inset locked in `04-UI-SPEC.md`, producing a 48x48 hit-testable box around a 40x40 painted one. Give it no background, no box shadow and no radius. A transparent positioned pseudo-element still dispatches clicks to its host but paints nothing, which is the entire reason this technique — and not padding — is correct here: this button's background and glow paint through the padding box, so padding would visibly grow the glowing circle.

Update the existing explanatory comment on the host rule: it currently says the sub-44 exception would be remediated in Phase 4 if the accessibility pass flagged it. Replace that with what is now true — the hit area is expanded here, the painted size is unchanged, and rect-based verification does not work for this control.

**3. Verify, do not rebuild, the compact icon buttons.** Run the existing portrait size assertion first and confirm it passes before writing anything. Phase 2 already solved that case with a content-box override plus padding, which grows the element's own hit-tested box and IS correctly verified by a rect measurement. Adding a second technique on top would be churn on a control that is currently correct.

**4. Tests.** Put tests 1-4 in the landscape file where the pill lives, and extend test 5 across both files. For tests 1 and 2, seed the sky-tap preference OFF so a click that misses the overlay and lands on the sky is a genuine no-op rather than a second route to toggling playback — otherwise the control assertion cannot fail. Ensure the chrome is revealed and playback is stopped before measuring, so the pill is not hidden with pointer events disabled. Drive the clicks by coordinate, not by element handle, because the whole point is to hit a point outside the element's own box.

For test 5, extend the existing rect-based pattern to the landscape dock and top-cluster controls. Exclude the play button by selector and put the reason in a comment that names the offset-click test, so a future reader does not "fix" the exclusion by relaxing the threshold.

**5. Record the independence explicitly.** In the summary, state that this task satisfies MOBX-03's 44x44 clause and that plan 04-05's Lighthouse gate satisfies a different clause of the same requirement, and that neither is evidence for the other. That sentence exists because the two live in one requirement's text and the audit's real threshold is lower.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py -x -q</automated>
    <automated>test "$(awk '/\.mobile-cinema-scrub-fab::before/,/\}/' web/mobile.css | grep -Ec 'background|box-shadow|border-radius')" = "0"</automated>
    <automated>test "$(awk '/\.mobile-cinema-scrub-fab[[:space:]]*\{/,/\}/' web/mobile.css | grep -Ec 'width:[[:space:]]*40px')" = "1"</automated>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_freeze_proof.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - The awk-extracted `::before` rule body contains no background, box-shadow or border-radius declaration, and does contain a negative inset.
    - The awk-extracted host rule body still declares a 40 px width and a 40 px height, and now also declares a relative position.
    - A click 2 px outside the painted left edge toggles playback; a click 10 px outside it does not. Both assertions are in the same test run with the sky-tap preference off.
    - The play button's bounding rect is asserted to be exactly 40x40 — a deliberate assertion, not an oversight.
    - The overlay's computed box measures at least 44 px in both dimensions.
    - Every other tappable control in both mobile layouts measures at least 44x44 by rect, with the play button excluded by selector and the exclusion commented with a pointer to the offset-click test.
    - `pytest tests/test_freeze_proof.py -x -q` exits 0 — no frozen file was touched.
    - The summary states in plain words that the 44x44 clause and the Lighthouse clause are independent and that neither result may be reported as satisfying the other.
  </acceptance_criteria>

  <reversibility rating="reversible">One added declaration and one added CSS rule, both scoped to a single selector.</reversibility>

  <done>The cinema-scrub play button is touchable at 48x48 while painting exactly 40x40 with an unchanged glow; the compact icon buttons are verified still passing untouched; every other mobile control clears 44x44 by rect; and the phase record states that the Lighthouse score is not evidence for any of it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Double the auto-hide window under reduced motion, and verify the reduced-motion and hidden-page behaviors that already ship</name>

  <precondition>`window.GestureConstants` exposes the auto-hide constant at runtime (the global bridge Phase 1 established so `web/mobile-gestures.js` could stay byte-frozen). If the app module cannot read it, halt rather than hardcoding a duplicate value or editing the frozen gesture file.</precondition>

  <read_first>
    - `04-PATTERNS.md` § "web/app.js — Auto-hide 8000ms doubling under reduced motion (MOBX-04)" — the exact edit shape and the named precedent, plus the explicit instruction not to add a second media query.
    - `web/app.js` lines 233-245 — the module-level reduced-motion constant and the desktop cinematic branch that already consumes it. This is the single computation; read it so you reuse rather than re-derive.
    - `web/app.js` lines 4330-4350 — `resetMobileChromeHideTimer()` in full, including the conditions under which it arms at all.
    - `web/app.js` lines 4588-4598 — the hidden-page handler that already satisfies MOBX-05, including its layout-mode guard and the bookmark write that shares the same event.
    - `web/style.css` lines 58-68 — the pre-existing unscoped global reduced-motion rule. Read it to confirm it is unscoped and therefore already covers every mobile transition. Do NOT edit this file.
    - `web/mobile.css` — grep for transition and animation declarations so the verification test targets a real one (the cinema-scrub fade is the most legible).
    - `tests/test_mobile_landscape.py` — the existing auto-hide boundary tests and the seeded-slow-speed technique Phase 3 introduced so timing-sensitive tests do not race the end of the story.
    - `04-VALIDATION.md` finding 2 and the "already satisfied, do not rebuild" paragraph — the throw-decay vacuity and the global-rule finding.
  </read_first>

  <files>web/app.js, tests/test_mobile_landscape.py, tests/test_mobile_portrait.py</files>

  <behavior>
    - Test 1: in a browser context created with reduced motion set to reduce, the landscape chrome is STILL VISIBLE at a point past the normal 4000ms window and is HIDDEN after the doubled window elapses.
    - Test 2 (control, normal motion): the chrome is hidden by the normal window, proving test 1's later hide is caused by the preference and not by a slow test.
    - Test 3: in a reduced-motion context, the computed transition duration on the cinema-scrub pill is zero — verifying the pre-existing global rule already silences mobile transitions, with no new mobile-scoped block.
    - Test 4 (MOBX-05, portrait): with playback running, making the page hidden stops playback; the word position is unchanged; returning to visible does not resume.
    - Test 5 (MOBX-05, landscape): identical behavior in the landscape layout.
    - Test 6 (MOBX-05, desktop): at a desktop viewport, making the page hidden leaves playback running.
    - Edge case: the reduced-motion preference is snapshotted once at module load, so the context option must be set at context creation, before the app module evaluates — a page-level call made after navigation would silently not affect the constant.
  </behavior>

  <action>
One production edit, four verifications.

**1. The one production edit.** Inside the existing `resetMobileChromeHideTimer()`, take the base auto-hide delay from the global gesture-constants bridge exactly as it does today, then multiply it by two when the module-level reduced-motion constant is true, and pass the result to the timer. Reuse that existing constant — it is already computed once at module load and already consumed by the desktop cinematic. Do not add a second media query anywhere; two independent snapshots of one OS preference is the parallel-implementation failure this project's rules forbid, and the two could disagree after a preference change.

Do not touch `web/mobile-gestures.js`. The base constant is defined there and that file is byte-frozen; the app module already reads it through the global bridge, and the doubling multiplies the read value.

**2. Verify the global reduced-motion rule rather than duplicating it.** The frozen desktop stylesheet already carries an unscoped rule that zeroes every animation and transition under the preference, which silences every mobile transition today with no Phase 4 edit. Prove it with test 3 instead of adding a mobile-scoped block that would be a second, redundant source of the same behavior — and which would also mean adding lines to a file this plan must not touch.

**3. Record the throw-decay clause as vacuous.** MOBX-04 names throw decay as something reduced motion must disable. Throw-to-scrub inertia was never implemented: it is a v2 backlog item and the swipe-end consumer ignores velocity by design. Do NOT build it in order to disable it. Write a short requirements-accuracy note in the summary stating that the clause has no referent in the shipped code, and put it on the Phase D+E gate agenda so Dre rules on amending the wording rather than inheriting a requirement that reads as unmet forever.

**4. Verify the hidden-page pause; do not re-implement it.** The behavior shipped in Phase 1 and its guard keys on the layout mode not being desktop, which is why the landscape arm added in Phase 3 is covered without any edit. Write tests 4, 5 and 6 to prove exactly that, and confirm the pre-existing handler is unchanged by this plan.

To drive the hidden state in a test, redefine the document's visibility-state property in the page as configurable, set it to hidden, and dispatch the visibility-change event — the handler reads that property directly, so this exercises the real code path. Assert on both the play state and the preserved word position, and assert the absence of auto-resume by restoring the property to visible, dispatching again, and confirming playback is still stopped.

Put tests 1-3 and 5 in the landscape file, and tests 4 and 6 in the portrait file — test 6 driven at a desktop viewport, following the existing precedent of the desktop-shell regression guard that already lives there. Do NOT add anything to `tests/test_desktop_smoke.py`; that file's diff for this milestone is plan 04-01's single justified edit and it should stay that way.

**5. Timing discipline.** Tests 1 and 2 span the auto-hide windows, so seed a slower playback speed the way the Phase 3 auto-hide tests already do, otherwise a short fixture story finishes before the window elapses and the chrome hides for the wrong reason. Set the reduced-motion option when creating the browser context, never after navigation — the constant is read once at module load.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_landscape.py tests/test_mobile_portrait.py -x -q</automated>
    <automated>test "$(grep -c 'matchMedia("(prefers-reduced-motion' web/app.js)" = "1"</automated>
    <automated>test "$(grep -c 'PREFERS_REDUCED_MOTION' web/app.js)" -ge "4"</automated>
    <automated>test "$(grep -c 'prefers-reduced-motion' web/mobile.css)" = "0"</automated>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_freeze_proof.py tests/test_desktop_smoke.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'matchMedia("(prefers-reduced-motion' web/app.js` returns exactly 1 — the pre-existing single query, with no second one added.
    - `grep -c 'PREFERS_REDUCED_MOTION' web/app.js` returns at least 4 — the declaration plus its three consumers, one of which is new.
    - `grep -c 'prefers-reduced-motion' web/mobile.css` returns 0 — no duplicate mobile-scoped block was added.
    - `git diff --exit-code HEAD~ HEAD -- web/style.css web/mobile-gestures.js` is clean for this task's commit.
    - Under reduced motion the landscape chrome is asserted visible past the normal window and hidden after the doubled one; under normal motion it is asserted hidden by the normal window. Both boundaries are asserted, not just the doubled one.
    - The reduced-motion context option is applied at context creation; a comment in the test names the reason (the preference is snapshotted at module load).
    - The computed transition duration on the cinema-scrub pill is zero under reduced motion.
    - Hidden-page pausing is asserted in portrait AND landscape, with word position preserved and no auto-resume; and asserted NOT to occur at a desktop viewport.
    - `tests/test_desktop_smoke.py` has no diff from this task's commit.
    - The summary carries the throw-decay requirements-accuracy note and it appears on the gate agenda.
  </acceptance_criteria>

  <reversibility rating="reversible">A single ternary inside one existing function; everything else in the task is a test.</reversibility>

  <done>The landscape auto-hide window doubles under reduced motion using the one existing preference constant; the global transition silencing is proven rather than duplicated; hidden-page pausing is proven on both mobile layouts and proven absent on desktop; and MOBX-04's throw-decay clause is recorded as vacuous instead of being fabricated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS accessibility preferences → app behavior | Reduced motion is a client-only signal read once at module load; the app must not fork it into two disagreeing snapshots. |
| an automated check → an accessibility conformance claim | The two MOBX-03 clauses measure different things at different thresholds; reporting one as the other publishes a claim the code has not earned. |
| page lifecycle → playback state | A hidden page must stop playback on mobile and must not change desktop behavior. |
| this plan's edits → the frozen surface | `web/style.css` and `web/mobile-gestures.js` must both remain untouched even though this plan's subject matter lives partly in the former. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Repudiation | reporting the Lighthouse score as evidence for the 44x44 clause | high | mitigate | Recorded as an explicit prohibition; the two clauses are verified by two independent checks in two different plans, and the summary must state the independence in words. The audit's real 24 px threshold was read from the bundled rule source, not inferred. |
| T-04-16 | Repudiation | a rect assertion silently relaxed from 44 to 40 so the play button passes | high | mitigate | The offset-click assertion is mandated, the rect is asserted to be exactly 40x40 as documentation, and the exclusion in the general rect test carries a comment naming the offset-click test so a future reader cannot mistake it for an oversight. |
| T-04-17 | Tampering | a second reduced-motion media query producing two disagreeing snapshots of one preference | medium | mitigate | Grep gates pin the query count in the app module to one and the count in the mobile stylesheet to zero; the new branch consumes the existing constant. |
| T-04-18 | Tampering | an edit to the byte-frozen gesture file while chasing the auto-hide constant | high | mitigate | The constant is read through the existing global bridge; `tests/test_freeze_proof.py` asserts the gesture file byte-identical to the milestone base on every run. |
| T-04-19 | Repudiation | fabricating throw-to-scrub inertia so MOBX-04's wording can be marked satisfied | medium | mitigate | Prohibited outright; the honest response — a requirements-accuracy note plus a gate agenda item — is a named deliverable of Task 2. |
| T-04-SC | Tampering | package-manager supply chain | high | accept | No package-manager install occurs in this plan; `web/` remains dependency-free and the freeze proof re-asserts it. The one npm tool this phase uses is introduced in plan 04-05 behind its own blocking legitimacy checkpoint. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_portrait.py tests/test_mobile_landscape.py tests/test_desktop_smoke.py tests/test_freeze_proof.py` — all green.
2. The awk-scoped `::before` rule carries no paint declaration; the host rule still paints at 40x40 and now positions relatively.
3. An offset click 2 px outside the painted edge fires the handler; one 10 px outside does not.
4. Exactly one reduced-motion media query exists in the app module and none in the mobile stylesheet.
5. Both auto-hide boundaries are asserted, and hidden-page pausing is asserted in portrait, landscape and (as a non-effect) desktop.
</verification>

<success_criteria>
- Every mobile tap target is touchable at 44x44 or larger, with the one locked-visual control verified by offset click rather than by rect.
- The Lighthouse gate and the 44 px gate are recorded as independent, with neither offered as evidence for the other.
- The auto-hide window doubles under reduced motion from the single existing preference constant.
- Global transition silencing is verified, not duplicated.
- Hidden-page pausing is proven on both mobile layouts and proven absent on desktop.
- MOBX-04's throw-decay clause is recorded as vacuous, not fabricated.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-04-SUMMARY.md` when done.
</output>
