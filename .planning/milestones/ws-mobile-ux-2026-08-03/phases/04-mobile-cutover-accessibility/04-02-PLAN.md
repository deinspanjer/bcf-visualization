---
phase: 04-mobile-cutover-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - web/app.js
  - web/mobile.css
  - tests/test_mobile_portrait.py
  - tests/test_mobile_landscape.py
autonomous: true
requirements: [MOBX-03]
user_setup: []

must_haves:
  truths:
    - "A single visually-hidden aria-live=\"polite\" region exists in the mobile shell and mounts on BOTH mobile layouts. It never mounts on the desktop path, which is frozen (D-44)."
    - "The live region is hidden with the UI-SPEC's locked visually-hidden property set — a 1px clipped box, never a technique that removes the node from the accessibility tree. This is the single most failure-prone value in the phase: the wrong technique passes every DOM assertion and announces nothing."
    - "Announcements fire on exactly five user-caused position changes and no others: double-tap, swipe-step, rail-scrub RELEASE, and the two mobile keyboard steps. The per-move scrub callback, plain tap, surface open/close, and play/pause never announce (D-42/D-44, UI-SPEC trigger table)."
    - "The live region stays silent for the entire duration of free playback. At 25000 words/sec a roll lands every few seconds and screen readers speak serially, so announcing every roll would produce continuous interruption a user would simply switch off (D-42/D-45)."
    - "Announcement phrasing is outcome-aware and branches on outcome BEFORE touching any perk field. Misses are 410 of 670 rolls — the common case, not an edge case — and read as a terse Miss line with no cost estimate (D-43, RESEARCH Pitfall 4)."
    - "Outcome classification reuses viz-model.js's existing rollMarkerModel(roll); no second miss/multi-grab/untracked classifier is written (no-parallel-implementations)."
    - "Roll numbering reuses the indexOf-plus-one over app.data.story.rolls that the field-log header and cinema-scrub count already display. Never roll_label (null on 669 of 670 rolls) and never any *_ordinal field (three divergent numbering sequences that must never be joined)."
    - "Every announcement string reaches the DOM as a text node, never as markup. Perk display labels and quote text can contain literal angle brackets; the el() helper's text prop is the established convention and the only path used here."
    - "web/app.js still registers exactly ONE window-level keydown listener. The mobile branch lives inside the existing handler at the point after its editable guard and before its desktop word-stepping, so a focused text control still blocks every mobile key exactly as it blocks every desktop key (D-47, D-07)."
    - "Desktop keyboard behavior at >=1100px is byte-identical: arrows still step 10000 words (2000 with shift), Home still goes to word 0, End still goes to the story's last word. Only the non-desktop branch is new."
    - "Arrow keys step +/-1 ROLL on mobile, reusing the same rollStepFrom() the swipe callback already calls; Home mirrors the double-tap body (snap to the last roll at or before the playhead, then resume) rather than desktop's jump-to-word-0 (D-46)."
    - "Space needs no new code: the existing handler's Space case has no layout gate and already toggles playback identically on every layout. This requirement is closed by a test, not by an edit."
    - "`?` routes through the same openMobileSurface(\"help\") call the on-screen help button makes, so a second press closes it — the toggle convention every surface control gained in Phase 2 (D-48)."
    - "MOBX-03 edge (empty): when no roll exists at or before the target position, NOTHING is announced — silence, not a placeholder string, matching this project's established absent-element-over-placeholder-copy convention."
    - "MOBX-03 edge (ordering): landing again on the roll you were already on still re-announces. Screen readers only fire on a text mutation, so the node's text is cleared and re-set in the same tick; without that, scrubbing away and back is silent."
    - "MOBX-03 edge (ordering): a rail scrub announces once, at release. The per-pointermove callback stays silent — announcing every drag frame is exactly the continuous interruption D-42 forbids, and a throttle is not the answer because a throttle drops announcements and misreports the roll count."
    - "MOBX-03 edge (adjacency): stepping past either end of the roll list holds at the first/last roll (rollStepFrom clamps) and re-announces that same roll, rather than wrapping, going silent, or throwing."
    - "MOBX-03 edge (adjacency): pressing `?` while a surface is already open behaves as the surface stack already defines — a second `?` with Help open closes it; `?` with Settings or About open swaps to Help reusing the single outstanding history sentinel. No second sentinel is pushed and no new code path is added."
    - statement: "A screen reader actually SPEAKS the region on a real device, and stays silent through playback. DOM presence and computed style are assertable in Playwright; whether VoiceOver announces is not — no headless browser runs a screen reader, and an element that is present but unannounced passes every automated check while delivering nothing."
      verification: backstop

  prohibitions:
    - "MUST NOT hide the live region with display:none, visibility:hidden, the hidden attribute, aria-hidden, content-visibility:hidden, or a zero-width/zero-height box. Each of these removes the node from the accessibility tree, shipping a feature that satisfies every presence assertion while announcing nothing — a silent failure that passes CI, which is precisely how this milestone has been bitten three times on hardware."
    - "MUST NOT announce during free playback, and MUST NOT introduce a throttle as a compromise. Continuous announcement makes the app hostile to the exact users this feature exists for; a throttle silently DROPS announcements, so a jump from one roll number to a much later one would misreport the count in the one channel whose only job is to be truthful."
    - "MUST NOT present this live region — in code comments, in the summary, or at the gate — as making the visualization usable without sight. D-45 records it as genuine help for users with residual vision who navigate by gesture and want confirmation of where they landed; the right artifact for non-sighted readers is the planned spreadsheet-export workstream, and overclaiming here would misrepresent the app's accessibility."
    - "MUST NOT add a second window-level keydown listener. Two listeners competing for the same keys is the double-binding class of bug the per-render attach discipline exists to prevent, and it would make desktop's arrow behavior depend on registration order."
    - "MUST NOT build a phrasing branch for an outcome shape with zero instances in the dataset. Dead code written against an imagined case cannot be reviewed against reality; see FA-MOBX-05."

  flagged_assumptions:
    - id: FA-MOBX-05
      origin: 04-UI-SPEC.md § UI Considerations, row marked unresolved (carried, not planner-invented)
      requirement: MOBX-03
      status: unresolved
      note: "The `untracked_acquisition` evidence kind has ZERO instances in the current 670-roll dataset, so no announcement wording is proposed and none should be invented. rollMarkerModel already computes the flag; routing it to distinct phrasing is a one-line addition IF the shape ever appears. Carried to the Phase D+E gate for Dre's ruling — an agent must not close it by writing speculative copy."
      criteria:
        - "Confirm at the gate that the dataset still contains zero rolls with this evidence kind; if it does, the row stays dead and the code carries a pointer comment naming 04-UI-SPEC.md rather than invented copy."
        - "If the shape has appeared, route it through the same terse outcome-first style as the Miss row and have Dre approve the exact wording at the gate — not before."
      criteria_note: "Also carried: RESEARCH Assumption A3 — the full announcement phrasing table is Claude's proposed copy, grounded in the real dataset but NOT yet approved by Dre. Confirm or adjust every row at the gate; each is a one-line string edit, not a structural rework."

  artifacts:
    - "web/app.js: the aria-live region node mounted in render()'s non-desktop block, plus app.dom.mobileLiveRegion"
    - "web/app.js: mobileRollAnnouncement(roll) — the outcome-aware string builder"
    - "web/app.js: announceMobileRoll() — the clear-then-set writer, called from the five trigger sites"
    - "web/app.js: rollMarkerModel added to the existing viz-model.js import block"
    - "web/app.js: the mobile keydown early branch inside the one existing window-level handler"
    - "web/mobile.css: the .mobile-live-region visually-hidden rule"
    - "tests/test_mobile_portrait.py and tests/test_mobile_landscape.py: aria-live and keyboard assertions (no analog exists — grep confirms zero aria-live and zero mobile keyboard assertions in the suite today)"

  key_links:
    - "the five gesture/keyboard trigger sites -> announceMobileRoll(); the rAF playback tier is deliberately NOT wired, and that omission is the feature"
    - "rollMarkerModel(roll) (web/viz-model.js) -> the announcement's outcome branch; the import block edit is what makes this reuse possible instead of a second classifier"
    - "the existing keydown handler's editable guard -> the mobile branch's placement; inserted before it, a focused Settings control would swallow arrow keys into a roll step"
    - "openMobileSurface(\"help\") -> the `?` key; reusing it is what gives the key its toggle and history-sentinel behavior for free"
---

<objective>
Give the mobile layouts their accessibility spine: one visually-hidden `aria-live="polite"` region that announces where a user-caused gesture landed, and keyboard equivalents for the gestures — arrows step one roll, Home snaps to the live edge, `?` toggles Help, Space already works.

Purpose: MOBX-03's two behavioral clauses. Both edit the same file and the keyboard branch calls the announcement helper, so they ship together rather than as two plans racing for `web/app.js`.

Output: an announcing, keyboard-drivable mobile app with the trigger set locked to user-caused changes only.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-PATTERNS.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-UI-SPEC.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-VALIDATION.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-01-SUMMARY.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...` — this worktree has NO `.venv` of its own.
- Commit signing is disabled repo-wide; `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`. Never `git add .`.
- `web/mobile-gestures.js` stays BYTE-IDENTICAL (D-17). `web/style.css` is finished for this milestone — plan 04-01 made its one sanctioned edit and `tests/test_freeze_proof.py` now gates it; this plan adds ZERO lines to it.
- All new CSS goes in `web/mobile.css`, inside its single existing `@media` block (lines 19-1084), which is character-identical to `MOBILE_LAYOUT_QUERY`.
- `web/` gains no `package.json`, no bundler, no build step.
</session_constraints>

## Artifacts this phase produces

Phase-wide symbol inventory, creations AND deletions. This plan owns the rows marked **[02]**. The full table (including plan 01's five deletions and the frozen-CSS block removal) is in `04-01-PLAN.md`; the rows below are this plan's own.

| Symbol / artifact | Kind | Plan |
|---|---|---|
| **DELETED** the banner render function, call site, storage constant, state field, migration entry, CSS block and stale media wrapper | removals | 01 |
| `tests/test_freeze_proof.py` | new test file | 01 |
| the `aria-live="polite"` region node + `app.dom.mobileLiveRegion` | live-region DOM (`web/app.js`) | **[02]** |
| `mobileRollAnnouncement(roll)` | outcome-aware string builder (`web/app.js`) | **[02]** |
| `announceMobileRoll()` | clear-then-set writer (`web/app.js`) | **[02]** |
| `rollMarkerModel` added to the `viz-model.js` import block | import edit (`web/app.js`) | **[02]** |
| announce calls at `onDoubleTap` / `onSwipeStep` / `onScrubEnd` | gesture wiring (`web/app.js`) | **[02]** |
| the mobile `keydown` early branch (ArrowRight / ArrowLeft / Home / `?`) | controller edit (`web/app.js`) | **[02]** |
| `.mobile-live-region` visually-hidden rule | style (`web/mobile.css`) | **[02]** |
| aria-live + keyboard assertions in both mobile test files | tests | **[02]** |
| `.landing-meta-row` / `.landing-title-chip` / `.landing-author-credit` / `.landing-help-btn` / `#landing-help-dialog` + `<noscript>` + inline `<script>` | static markup (`index.html`) | 03 |
| `tests/test_landing_page.py` + landing-page fixture support | new test file + fixture | 03 |
| `.mobile-cinema-scrub-fab::before` + `position: relative` on its host | style (`web/mobile.css`) | 04 |
| the `PREFERS_REDUCED_MOTION` branch in `resetMobileChromeHideTimer()` | timer edit (`web/app.js`) | 04 |
| `tests/test_lighthouse_accessibility.py` | new test file | 05 |
| `COVERAGE.md`, the §6 evidence table, the gate rulings | phase artifacts | planner / 05 |

<!-- planner-discipline-allow: aria-live -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The visually-hidden live region, its outcome-aware announcement, and its three gesture triggers</name>

  <precondition>Plan 04-01's freeze proof is green — `pytest tests/test_freeze_proof.py -x -q` exits 0. This plan adds no line to `web/style.css` and needs that gate already standing so any accidental touch is caught by an existing test rather than discovered at the phase gate.</precondition>

  <read_first>
    - `04-UI-SPEC.md` § "Live region (MOBX-03 / D-42..D-45)" — the LOCKED trigger table (which call sites fire and which explicitly do not), the phrasing table per outcome shape, the update-mechanism note, and the "Locked hiding technique — `.mobile-live-region`" property table. Use that property table verbatim; it is a locked value, not executor inference.
    - `04-PATTERNS.md` § "web/app.js — Live region DOM node + update helper (D-42..D-45)" — the two structural analogs to combine (per-render node placement with an `app.dom.*` ref; incremental text write), the exact trigger call sites with line numbers, and the numbering expression to reuse verbatim.
    - `web/viz-model.js` lines 40-100 — `paidRollPerks`, `perkDisplayLabel`, `rollTotalCost`, and `rollMarkerModel`'s four booleans/counts. Read once; extract everything the string builder needs in that pass.
    - `web/app.js` lines 1-48 (the `viz-model.js` import block — three of the four helpers are already imported, one is not), lines 1040-1075 (`render()`'s non-desktop block, where the gesture probe already mounts a node and is the natural mount point), and lines 4396-4475 (the three gesture callbacks). Read as separate non-overlapping ranges.
    - `web/app.js` lines 4015-4035 — `mobileFieldLogRows()`'s roll-numbering expression, the one this announcement reuses.
    - `web/mobile.css` lines 19-24 — the single `@media` block opener your new rule must live inside.
    - `tests/test_mobile_portrait.py` lines 31-76 — the `_page_with_console_capture(browser, site, storage=..., viewport=...)` harness and viewport constants; the assertions are new but the harness pattern is reusable.
  </read_first>

  <files>web/app.js, web/mobile.css, tests/test_mobile_portrait.py, tests/test_mobile_landscape.py</files>

  <behavior>
    - Test 1: on a phone-portrait viewport the mobile shell contains exactly one element with a polite live-region role, and its computed style matches the locked visually-hidden property set (1px box, clipped, non-zero size).
    - Test 2: at a desktop viewport (>=1100px) no live-region element exists anywhere in the document.
    - Test 3: a swipe-step gesture writes an announcement naming the roll number, the total, and either the perk with its CP or the terse miss form; assert against a fixture whose roll outcomes are known.
    - Test 4: a rail-scrub drag across many rolls writes NOTHING until pointerup; on release exactly one announcement appears, naming the settled position.
    - Test 5: free playback across at least two roll crossings leaves the region's text unchanged — captured before, compared after.
    - Test 6 (empty edge): with the playhead scrubbed before the first roll, no announcement is written; the region's text stays empty.
    - Test 7 (ordering edge): landing on the same roll twice in a row produces a second announcement — proven by observing a mutation, not by comparing two equal strings.
    - Test 8: a plain single tap writes no announcement (it changes play state or reveals chrome; it never moves the playhead).
    - Test 9 (landscape): the region mounts and announces identically in the landscape layout — the double-tap trigger is the cheapest one to drive there.
    - Edge case: `perkDisplayLabel` output containing literal angle brackets reaches the DOM as text, not markup — assert the region's `textContent` and `innerHTML`-free construction.
  </behavior>

  <action>
Build the region, the string, and the wiring — in that order, so the node exists before anything writes to it.

**1. Mount the node.** `render()` already has one `app.layoutMode !== "desktop"` block that appends the gesture probe and runs the attach lifecycle; mount the live region there, in the same block, so it exists on both mobile layouts and on neither the desktop path nor the loading/error paths. Build it with the existing `el()` helper: a `div` carrying the class, `role="status"`, `aria-live="polite"`, and `aria-atomic="true"`. Cache the node on `app.dom` immediately after appending — that block runs AFTER `cachePlaybackDomRefs()` replaces `app.dom`, so a ref set inside `cachePlaybackDomRefs()` would be fine but a ref set before it would be discarded; setting it at the mount site keeps the two facts adjacent.

**2. Style it.** Add one rule to `web/mobile.css` inside the existing media block using the property table locked in `04-UI-SPEC.md` § "Locked hiding technique". Copy that table's seven properties and values exactly. The prohibited alternatives are enumerated in `must_haves.prohibitions` — every one of them leaves the node present in the DOM and absent from the accessibility tree, which is a green-CI silent failure, so do not substitute, simplify, or "modernise" the technique.

**3. Add the missing import.** `web/app.js`'s `viz-model.js` import block already pulls `paidRollPerks`, `perkDisplayLabel` and `rollTotalCost`; add `rollMarkerModel` to the same block, keeping the block's existing alphabetical-ish ordering. This one-line edit is what makes outcome classification a reuse instead of a second implementation.

**4. Write `mobileRollAnnouncement(roll)`** — a pure function returning a string, no DOM access. Compute the roll's position as its index in `app.data.story.rolls` plus one, over that array's length — the identical expression `mobileFieldLogRows()` already uses for the field-log header count. Then branch on `rollMarkerModel(roll)`, checking the miss-like flag FIRST, before touching any perk field: misses are 410 of 670 rolls and a perk-first template renders them as a string of dashes. Implement exactly the four reachable rows of `04-UI-SPEC.md`'s phrasing table — miss, single paid perk, multi-grab (first perk name plus a count of the remainder, so it stays correct at three or more), and the free-only fallback the existing principal-name chain already has. Do not add a fifth branch for the zero-instance evidence kind; leave a one-line pointer comment naming `04-UI-SPEC.md` instead (see FA-MOBX-05). Every substring is composed with ordinary string interpolation and returned as a plain string.

**5. Write `announceMobileRoll()`** — resolve the roll at the current playhead with the existing `lastRollAtWord(app.wordPos)`; if it returns nothing, return immediately and write nothing (silence is the specified behavior, not a placeholder). Otherwise write the built string into the cached node by first assigning an empty string and then assigning the message, two synchronous assignments in the same tick. Both assignments go through the node's text property; no markup path is used anywhere. The double write is load-bearing: assistive tech fires on a text mutation, so re-landing on the same roll would otherwise be silent.

**6. Wire exactly three call sites** in `attachMobileGestures()`, each immediately after the existing `setWordPos(...)` call so the announcement reflects the committed position: the double-tap callback, the swipe-step callback, and the scrub-END callback. Do NOT wire the per-pointermove scrub callback — it fires on every move and must stay silent. Do NOT wire the plain-tap callback: it never moves the playhead, so there is no position change to announce (this resolves an ambiguity in D-42's literal phrasing and is flagged for gate confirmation in `04-UI-SPEC.md`; adding it later is one line, not a re-plan). Do NOT wire anything in the rAF playback tier.

**7. Tests.** Add the behaviors above to `tests/test_mobile_portrait.py` (tests 1-8) and `tests/test_mobile_landscape.py` (test 9), using the existing `_page_with_console_capture` harness and viewport constants. For test 7, prove the re-announcement by observing a mutation rather than comparing strings — a `MutationObserver` installed in the page that counts character-data changes on the region is the direct way, and it is the only way two identical announcements are distinguishable. For test 5, seed a slower playback speed the way the landscape auto-hide tests already do, so the story does not finish before the observation window.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_portrait.py tests/test_mobile_landscape.py -x -q</automated>
    <automated>test "$(grep -c 'aria-live' web/app.js)" = "1"</automated>
    <automated>test "$(grep -c 'innerHTML' web/app.js)" = "0"</automated>
    <automated>test "$(awk '/\.mobile-live-region[[:space:]]*\{/,/\}/' web/mobile.css | grep -Ec 'display:[[:space:]]*none|visibility:[[:space:]]*hidden|content-visibility|aria-hidden')" = "0"</automated>
    <automated>test "$(grep -c 'rollMarkerModel' web/app.js)" -ge "2"</automated>
    <automated>test "$(awk '/^function mobileRollAnnouncement/,/^}/' web/app.js | grep -Ec 'roll_label|_ordinal|setTimeout|setInterval')" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'aria-live' web/app.js` returns exactly 1 — one region, not one per layout branch.
    - `grep -c 'innerHTML' web/app.js` returns 0.
    - The awk-extracted `.mobile-live-region` rule body in `web/mobile.css` contains none of the accessibility-tree-removing declarations, and DOES contain a non-zero `width` and `height`.
    - `grep -c 'rollMarkerModel' web/app.js` returns at least 2 (the import and at least one use).
    - The awk-extracted body of the announcement string builder references no `roll_label`, no `*_ordinal` field, and no timer function. (File-wide greps for those names are meaningless here — `web/app.js` legitimately carries 10 pre-existing `roll_label`/ordinal references in the desktop roll log and 4 pre-existing `setTimeout` calls; only the new function's body is in scope.)
    - A Playwright assertion confirms the region's computed `width` and `height` are each greater than 0 and no greater than 2 CSS px, and that its computed `display` is not `none` and `visibility` is not `hidden`.
    - At a >=1100px viewport, `document.querySelector` for the live-region class evaluates to `None`.
    - A drag across the rail produces exactly ONE character-data mutation on the region, observed at pointerup — not one per pointermove.
    - Playback across at least two roll crossings produces ZERO character-data mutations on the region.
    - Scrubbing to a position before the first roll leaves the region's `textContent` empty.
    - Landing twice on the same roll produces two character-data mutations.
    - No throttle was introduced: the awk-extracted announcement-function bodies contain no timer call, and the zero-mutation-during-playback test is the behavioral proof.
    - `pytest tests/test_mobile_portrait.py tests/test_mobile_landscape.py -x -q` exits 0 with more collected items than before this task.
  </acceptance_criteria>

  <reversibility rating="reversible">One new node, one new CSS rule, one import line and three call-site additions; every announcement string is a one-line edit if Dre's gate review adjusts the wording (RESEARCH Assumption A3).</reversibility>

  <done>A single visually-hidden polite live region mounts on both mobile layouts and never on desktop; it announces outcome-aware roll text on double-tap, swipe-step and scrub-release only; it re-announces a repeat landing; it stays empty when no roll is in range and silent throughout playback; and outcome classification and roll numbering both reuse the existing single implementations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mobile keyboard equivalents as an early branch inside the one existing keydown handler</name>

  <precondition>`announceMobileRoll()` from Task 1 exists and is callable — the keyboard steps announce through the same helper, and a branch that moves the playhead without announcing would make the keyboard the one navigation route that goes silent.</precondition>

  <read_first>
    - `04-PATTERNS.md` § "web/app.js — Keyboard early-branch (D-46/D-47/D-48)" — the existing handler quoted in full with the insertion point marked, plus the named precedent (the mobile early-return already used by the playback frame updater).
    - `web/app.js` lines 4560-4590 — the one and only window-level keydown handler, read directly to confirm the line numbers before editing.
    - `web/app.js` lines 795-825 — `lastRollAtWord()` and `rollStepFrom()`, including the comment explaining that `rollStepFrom` CLAMPS at both ends rather than wrapping or returning null. That clamping behavior is the adjacency edge this task must encode, not work around.
    - `web/app.js` lines 4416-4432 — the double-tap callback body that the Home key must mirror (snap to the last roll at or before the playhead, then resume if paused) rather than desktop's jump-to-word-0.
    - `web/app.js` lines 3097-3114 — `openMobileSurface()`, whose re-request-closes behavior is what gives the `?` key its toggle for free.
    - `04-CONTEXT.md` D-46, D-47, D-48 — the three decisions this task implements verbatim.
  </read_first>

  <files>web/app.js, tests/test_mobile_portrait.py, tests/test_mobile_landscape.py</files>

  <behavior>
    - Test 1: on a phone-portrait viewport, ArrowRight advances the playhead to the next roll's word position (not by a word count), and writes an announcement.
    - Test 2: ArrowLeft steps back one roll and announces.
    - Test 3: Home snaps to the last roll at or before the current playhead and starts playback if it was paused — matching double-tap, NOT jumping to word 0.
    - Test 4: `?` opens the Help overlay; a second `?` closes it.
    - Test 5 (adjacency): ArrowLeft repeatedly at the first roll holds there — the playhead does not go negative, does not wrap to the end, and the same roll re-announces each press.
    - Test 6 (adjacency): ArrowRight repeatedly at the last roll holds there with the same behavior.
    - Test 7: at a >=1100px viewport, ArrowRight still steps 10000 words and shift+ArrowRight still steps 2000; Home still goes to word 0 and End to the story's last word — desktop is untouched.
    - Test 8: Space toggles playback on portrait, on landscape and on desktop — a verification of existing behavior, with no production edit.
    - Test 9: with focus inside a form control, none of the mobile keys fire — the existing editable guard still shields them.
    - Test 10 (landscape): ArrowRight and `?` behave identically in the landscape layout.
  </behavior>

  <action>
Insert ONE branch into the existing window-level keydown handler. Do not register a second listener anywhere — see `must_haves.prohibitions`.

**Placement is the whole decision.** Put the branch after the handler's existing editable-element guard and after its Space case, and before its desktop word-stepping chain. After the editable guard means a focused Settings control still swallows the keys exactly as it swallows desktop's. After the Space case means Space keeps its current layout-agnostic behavior with no edit — it already toggles playback on every layout, which is why MOBX-03's Space clause needs a test and no code. Before the desktop chain means desktop never sees these keys on mobile and mobile never falls through to word-stepping. This mirrors the mobile early-return shape already proven across two phases in the playback frame updater.

**Gate the branch on the layout mode being anything other than desktop** — one condition, matching the existing single-source layout signal. Do not introduce a second layout test, a viewport width read, or a touch-capability check.

Inside it, handle four keys, each preventing the default and returning so nothing falls through:

- Forward arrow: take the next roll from `rollStepFrom(app.wordPos, 1)`, commit it with `setWordPos`, then announce. Note `rollStepFrom` clamps at the list ends rather than returning nothing, so at the last roll this re-commits the same position and re-announces it — that is the specified adjacency behavior, not a bug to guard against.
- Back arrow: the same with a step of minus one.
- Home: mirror the double-tap callback's body exactly — resolve the last roll at or before the current playhead, commit it if one exists, announce, and start playback if it is currently stopped. This deliberately differs from desktop's Home, which stays a jump to word 0 two lines below in the same handler.
- The question-mark key: call `openMobileSurface` for the help surface. It already closes on a re-request, so a second press toggles it shut and no second history sentinel is pushed. Handle only the key value the browser reports for the shifted glyph; do not add a shift-plus-slash fallback unless a test proves the primary value is not delivered.

**Tests.** Add tests 1-9 to `tests/test_mobile_portrait.py` and test 10 to `tests/test_mobile_landscape.py`. Drive keys with Playwright's keyboard API against the page body (not a focused control) so the handler's own guard is exercised honestly; test 9 focuses a real Settings input first. For test 7, assert the desktop step sizes numerically against the pre-existing constants rather than asserting "position changed" — a mobile branch leaking into desktop would still change the position, just by the wrong amount. For test 8, assert Space at all three layouts in one test so a future layout gate on Space cannot pass by covering only one.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_portrait.py tests/test_mobile_landscape.py tests/test_desktop_smoke.py -x -q</automated>
    <automated>test "$(grep -c 'window.addEventListener("keydown"' web/app.js)" = "1"</automated>
    <automated>test "$(grep -c 'matchMedia("(prefers-reduced-motion' web/app.js)" = "1"</automated>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_freeze_proof.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'window.addEventListener("keydown"' web/app.js` returns exactly 1.
    - `grep -c 'document.addEventListener("keydown"' web/app.js` returns exactly 1 (the pre-existing surface focus trap) — no new document-level listener was added either.
    - Desktop arrow stepping is asserted numerically: at >=1100px, ArrowRight moves the playhead by exactly 10000 words and shift+ArrowRight by exactly 2000, and Home lands on word 0.
    - On mobile, ArrowRight lands the playhead exactly on the next roll's `word_position` — an equality assertion against the fixture's roll list, not a "moved forward" assertion.
    - Home on mobile lands on the last roll at or before the prior playhead AND leaves playback running; it does NOT land on word 0.
    - Pressing `?` twice returns the surface state to closed, and `history.length` is unchanged across the pair.
    - Repeated ArrowLeft at the first roll leaves the playhead at that roll's word position and produces one character-data mutation per press on the live region.
    - With focus inside a form control, ArrowRight leaves the playhead unchanged on every layout.
    - `pytest tests/test_freeze_proof.py -x -q` still exits 0 — `web/style.css` and the gesture file are untouched by this plan.
  </acceptance_criteria>

  <reversibility rating="reversible">One branch inside one existing handler; removing it restores the prior behavior exactly, since desktop's chain was never modified.</reversibility>

  <done>Arrows step one roll on mobile and announce; Home snaps to the live edge and resumes; `?` toggles Help through the existing surface call; Space is proven to already work on all three layouts; desktop keyboard behavior is numerically unchanged; and `web/app.js` still registers exactly one window-level keydown listener.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived roll data → live-region DOM text | Perk display labels and evidence text originate in the derived data bundle and can contain literal angle brackets; they cross into the DOM here. |
| keyboard input → playback/position state | A window-level key handler mutates the reader's position and play state; a focused form control must not have its keys stolen. |
| accessibility tree → the app's accessibility claim | An element present in the DOM but absent from the accessibility tree satisfies every automated check while delivering nothing. |
| this plan's edits → the frozen desktop surface | `web/style.css` is finished; desktop's keyboard chain must remain byte-identical in behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | DOM-based XSS via announcement text built from derived perk/quote strings | medium | mitigate | Every announcement reaches the DOM through the node's text property; no markup path is used. Enforced by an acceptance criterion asserting `innerHTML` appears zero times in `web/app.js`, and by the existing project-wide `el()`-helper text-only convention. Data is author-controlled/derived rather than user-submitted, which is why this is medium and not high. |
| T-04-06 | Spoofing | a live region that is present but silent — an accessibility claim the code does not deliver | high | mitigate | The hiding technique is a locked property table, not executor inference; the awk-scoped rule-body gate rejects every accessibility-tree-removing declaration; a Playwright assertion pins the computed box to a non-zero 1px. Residual: whether a screen reader actually speaks it is a device-pass backstop (`must_haves.truths`, verified at the plan 04-05 gate). |
| T-04-07 | Denial of Service | continuous announcement during playback rendering the app unusable for screen-reader users | high | mitigate | No trigger is wired into the rAF playback tier; the trigger set is the five user-caused sites and is asserted by a zero-mutation test across two roll crossings. A throttle is prohibited rather than merely discouraged. |
| T-04-08 | Elevation of Privilege | a second keydown listener stealing keys from a focused form control or from the desktop chain | medium | mitigate | Exactly one window-level listener is asserted by grep; the branch is placed after the existing editable guard; desktop step sizes are asserted numerically rather than directionally. |
| T-04-09 | Repudiation | overclaiming the app is usable unsighted on the strength of this region | medium | mitigate | Recorded as an explicit prohibition and carried to the Phase D+E gate; D-45's scope rationale is restated in the summary rather than assumed. |
| T-04-SC | Tampering | package-manager supply chain | high | accept | No package-manager install occurs in this plan; `web/` remains dependency-free and `tests/test_freeze_proof.py` re-asserts it on every run. The one npm tool this phase uses is introduced in plan 04-05 behind its own blocking legitimacy checkpoint. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_portrait.py tests/test_mobile_landscape.py tests/test_desktop_smoke.py tests/test_freeze_proof.py` — all green.
2. `grep -c 'aria-live' web/app.js` = 1; `grep -c 'innerHTML' web/app.js` = 0.
3. `grep -c 'window.addEventListener("keydown"' web/app.js` = 1.
4. The awk-scoped `.mobile-live-region` rule body carries no accessibility-tree-removing declaration and a non-zero box.
5. Zero live-region mutations across two roll crossings of free playback; exactly one at scrub release; two on a repeat landing.
</verification>

<success_criteria>
- One polite live region, mobile-only, hidden by the locked technique, announcing on exactly the five user-caused triggers.
- Miss phrasing is the first branch, not an afterthought; outcome classification and roll numbering both reuse existing single implementations.
- Arrows step one roll, Home snaps to the live edge, `?` toggles Help, Space is proven already working.
- Desktop keyboard behavior is numerically identical to before.
- No throttle, no second listener, no second layout signal, no second matchMedia query.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-02-SUMMARY.md` when done.
</output>
