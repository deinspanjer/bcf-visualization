---
phase: 04-mobile-cutover-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app.js
  - web/style.css
  - tests/test_desktop_smoke.py
  - tests/test_freeze_proof.py
autonomous: true
requirements: [MOBX-01]
user_setup: []

must_haves:
  truths:
    - "No rotate-to-landscape banner element exists in the rendered DOM at ANY viewport — phone portrait, phone landscape, or desktop width — because the render function that produced it and the CSS that styled it are both deleted, not merely hidden (MOBX-01, D-37)."
    - "Exactly ONE breakpoint definition pair remains in the repo after this plan: the JS query at web/app.js:91 and its lockstep copy in web/mobile.css. The stale copy that web/style.css carried since Phase 3 is gone, restoring D-06's single-source-of-layout-truth intent (D-38)."
    - "web/style.css's diff against the whole-milestone base commit 57d2768 is deletion-only: zero added lines. Any tombstone comment, reformat, or unrelated tweak in that file fails the gate, which is the point — this is the milestone's one sanctioned frozen-CSS edit (D-37)."
    - "The scripted desktop smoke test passes after the deletion, and the desktop shell above 1100px renders exactly as it did before it. This is the milestone's closing freeze claim and the deletion is the single change most likely to break it."
    - "web/mobile-gestures.js is byte-identical to commit 57d2768, unbroken since Phase 1 (D-17)."
    - "STORAGE_VERSION stays at its current value. The orphaned bcf:portrait-dismissed localStorage key is inert dead data once nothing reads it; purging it would cost 34 fixture edits across 4 test files for zero behavioral gain (RESEARCH Pitfall 5)."
    - "A committed pytest module re-runs the whole-milestone freeze proof on demand, so the closing claim survives as a repeatable gate instead of a one-off shell command that rots the moment this phase closes."

  prohibitions:
    - "MUST NOT edit any part of web/style.css beyond the single sanctioned banner block deletion. The frozen desktop experience is this milestone's stated core value; a second, unnoticed edit in the same commit would ship a desktop regression behind a green mobile suite, and this file has been byte-identical for three phases."
    - "MUST NOT bump STORAGE_VERSION to purge the now-orphaned dismissal key. A bump makes migratePreviewStorage() wipe every bcf:* key on first load, destroying a returning reader's bookmark, playback speed, zoom and comfort toggles — real user-data loss traded for a cosmetic cleanup nobody can observe."
    - "MUST NOT diff the freeze proof against Phase 3's base commit (22bdd8c) or any other phase-local base. MOBX-01's claim is whole-milestone: the desktop UI is byte-identical to what it was BEFORE any mobile work started. A phase-local base would silently certify only the last phase's slice of the freeze."

  flagged_assumptions:
    - id: FA-MOBX-01
      origin: planner (edge-probe returned `unclassified` for MOBX-01; never probe-resolved, must not be closed by an agent)
      requirement: MOBX-01
      status: unresolved
      note: "The deterministic edge probe could not classify MOBX-01 into a shape category, so no acceptance criterion was auto-derived for its boundary behavior. The criteria below are authored by the planner and carried to the Phase D+E gate for Dre's ruling. They are NOT a probe result and must not be recorded as one."
      criteria:
        - "Banner absent in phone PORTRAIT: document.querySelector('.portrait-banner') is null at 390x844 and at 320x568."
        - "Banner absent in phone LANDSCAPE: same null assertion at 844x390, 956x390 and 568x320 — the banner's stale media query used to match some of these viewports and its removal must not be inferred from the portrait case alone."
        - "Banner absent at DESKTOP width: same null assertion at >=1100px, where the element was never visible but was still constructed by renderAppShell() on every render before this plan."
        - "No dismissal state survives anywhere in the render path: the app state object no longer carries a dismissal field, and no code reads or writes the bcf:portrait-dismissed key."
        - "The reader-facing consequence Dre rules on at the gate: with the banner gone there is no longer any in-app affordance suggesting rotation. Confirm on device that portrait genuinely stands on its own, in both orientations, with no residual gap where the banner used to sit."

  artifacts:
    - "tests/test_freeze_proof.py — the committed whole-milestone freeze gate"
    - "web/app.js with the banner render function, its call site, its storage constant, its state field and its migration entry all removed"
    - "web/style.css with the banner rule block and the stale breakpoint media wrapper removed, and nothing else changed"
    - "tests/test_desktop_smoke.py with its banner assertion restated as a deletion assertion"

  key_links:
    - "commit 57d2768 (pre-Phase-1) -> the freeze-proof diff target; a phase-local base or a bare working-tree diff proves a strictly weaker claim than MOBX-01 makes"
    - "renderAppShell()'s banner call site -> the ONE sanctioned edit to a plan §0.2 read-only function this milestone; every other §0.2 function stays untouched"
    - "web/style.css's deleted media block -> web/app.js:91's MOBILE_LAYOUT_QUERY, whose own comment documents the divergence this deletion resolves (D-38)"
    - "this plan's green freeze proof -> every later plan in the phase; if the freeze breaks here, the rest of the phase is building on a broken premise"
---

<objective>
Delete the rotate-to-landscape banner from the app end-to-end — render function, call site, storage constant, state field, migration entry, and its CSS — and prove in one committed gate that the desktop experience is byte-identical to what it was before any mobile work started.

Purpose: MOBX-01 is the milestone's closing proof. The freeze has held perfectly through three phases; D-37's deletion is the one change that could break it, and it touches the two files the freeze protects. Doing it FIRST, on the phase's best context, means an architectural surprise costs one commit instead of five.

Output: a banner-free app, a deletion-only diff on frozen CSS, and `tests/test_freeze_proof.py` — a repeatable whole-milestone freeze gate every later plan and phase inherits.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-PATTERNS.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-VALIDATION.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-04-SUMMARY.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...` — this worktree has NO `.venv` of its own. Baseline at Phase 3 close: 53 passing.
- Commit signing is disabled repo-wide; `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`.
- Never `git add .` — these are not yours: `.planning/workstreams/mobile-ux/config.json`, `design/mobile-ux/`, `.planning/research/.cache/`.
- `web/mobile-gestures.js` must stay BYTE-IDENTICAL (D-17). `web/` gains no `package.json`, no bundler, no build step.
- Re-grep every cited line number before editing. RESEARCH cited them against `ef21947`; they were re-verified at `cdbcb55` during planning and all still match, but the branch may move again.
</session_constraints>

## Artifacts this phase produces

Phase-wide symbol inventory, creations AND deletions. This is the milestone's only phase with deletions. This plan owns the rows marked **[01]**.

| Symbol / artifact | Kind | Plan |
|---|---|---|
| **DELETED** `renderPortraitBanner()` (`web/app.js:1228-1233`) | render function removal | **[01]** |
| **DELETED** its call site in `renderAppShell()` (`web/app.js:1098`) | dispatch removal | **[01]** |
| **DELETED** `LS_PORTRAIT_DISMISSED` (`web/app.js:62`) | storage constant removal | **[01]** |
| **DELETED** `app.portraitDismissed` (`web/app.js:145`) | state field removal | **[01]** |
| **DELETED** its entry in `migratePreviewStorage()`'s clear-list (`web/app.js:428`) | migration removal | **[01]** |
| **DELETED** the `.portrait-banner` rule block + child selectors (`web/style.css:325-359`) | frozen-CSS removal | **[01]** |
| **DELETED** the stale `@media (max-width: 900px), …` wrapper (`web/style.css:360-362`) | frozen-CSS removal | **[01]** |
| `tests/test_freeze_proof.py` | new test file (new shape: wraps `git diff`) | **[01]** |
| `mobileRollAnnouncement(roll)` + `announceMobileRoll(roll)` | live-region helpers (`web/app.js`) | 02 |
| the `aria-live="polite"` region node + `app.dom.mobileLiveRegion` | live-region DOM (`web/app.js`) | 02 |
| `rollMarkerModel` added to the `viz-model.js` import block | import edit (`web/app.js`) | 02 |
| the mobile `keydown` early branch (Arrow/Home/`?`) inside the existing handler | controller edit (`web/app.js`) | 02 |
| `.mobile-live-region` hiding rule | style (`web/mobile.css`) | 02 |
| `.landing-meta-row`, `.landing-title-chip`, `.landing-author-credit`, `.landing-help-btn` | static markup + CSS (`index.html`) | 03 |
| `#landing-help-dialog` + its `<noscript>` guard + the focus-return inline `<script>` | static markup + first JS in that file (`index.html`) | 03 |
| `tests/test_landing_page.py` + repo-root landing-page fixture support in `tests/helpers/web_runtime_site.py` | new test file + fixture | 03 |
| `.mobile-cinema-scrub-fab::before` hit-area overlay + `position: relative` on its host | style (`web/mobile.css`) | 04 |
| the `PREFERS_REDUCED_MOTION` branch inside `resetMobileChromeHideTimer()` | timer edit (`web/app.js`) | 04 |
| `tests/test_lighthouse_accessibility.py` | new test file (new shape: shells to a CLI) | 05 |
| `COVERAGE.md` | phase artifact (written at planning, verified at close) | planner / 05 |
| the §6 acceptance-evidence table, freeze-proof record and Phase D+E gate rulings | phase closure records | 05 |

<!-- planner-discipline-allow: renderPortraitBanner -->
<!-- planner-discipline-allow: portrait-banner -->
<!-- planner-discipline-allow: LS_PORTRAIT_DISMISSED -->
<!-- planner-discipline-allow: portraitDismissed -->

<tasks>

<task type="tracer">
  <name>Task 1: Delete the rotate banner end-to-end — JS render path, storage state, and the one sanctioned frozen-CSS edit</name>

  <precondition>`git rev-parse 57d2768` resolves (the pre-Phase-1 base commit, `docs(01): create phase plan`), and `git diff --exit-code 57d2768 HEAD -- web/style.css` is currently EMPTY. If web/style.css already differs from that base before this task starts, halt: the freeze this task is about to make its single sanctioned exception to has already been broken by something else, and that must be understood before adding a second delta.</precondition>

  <read_first>
    - `web/app.js` lines 55-70, 140-150, 420-435, 1090-1105, 1225-1240 — the five deletion sites, read as five distinct non-overlapping ranges rather than a whole-file read. `04-PATTERNS.md` § "web/app.js — Banner deletion (D-37/D-38)" lists all five verbatim with line numbers.
    - `web/app.js` lines 85-95 — `MOBILE_LAYOUT_QUERY` and the comment that documents the divergence this deletion resolves (D-38). Do not edit this; read it to confirm nothing depends on the CSS block surviving.
    - `web/style.css` lines 318-368 — the deletion range plus the rule that must survive immediately after it untouched. `04-PATTERNS.md` § "web/style.css — the one sanctioned frozen-CSS deletion" pins the exact boundaries.
    - `tests/test_desktop_smoke.py` lines 110-130 and 320-340 — the two banner assertion sites and their now-stale explanatory comments (`04-RESEARCH.md` § "The two tests/test_desktop_smoke.py sites needing an update").
    - `04-CONTEXT.md` D-37 and D-38 — the authority for this edit and its scope limit.
  </read_first>

  <files>web/app.js, web/style.css, tests/test_desktop_smoke.py</files>

  <action>
Per D-37 and D-38, remove the pre-Phase-2 rotation-suggestion fallback completely. This is a pure subtraction task: there is no analog to follow and no replacement element to design. Nothing mounts where it used to sit — its absence IS the acceptance bar (04-UI-SPEC.md § Deletions).

**1. `web/app.js` — five removals, no rewrites.** Delete, in this order so the file never transiently references a symbol it no longer defines: the call site inside `renderAppShell()` (line 1098, the ternary that conditionally emitted the element); the render function itself (lines 1228-1233); the state field on the `app` object literal (line 145); the entry in `migratePreviewStorage()`'s clear-list (line 428); and the module-level storage-key constant (line 62). Delete only these — `renderAppShell()`'s remaining body, and every other function in plan §0.2's read-only list, stays byte-identical. Leave no tombstone comment naming the removed symbols: the git history is the record, and a lingering mention would defeat the absence gates below.

**2. `web/style.css` — the milestone's one sanctioned frozen-CSS edit.** Delete the contiguous block from its leading explanatory comment (line 325) through the closing brace of the media wrapper (line 362), inclusive. That range holds the base rule, its `strong` / `button` / `button:hover` / `svg` child selectors, and the stale `@media (max-width: 900px), (orientation: portrait) and (max-width: 1100px)` wrapper that Phase 3 deliberately left diverged from the real breakpoint. The rule immediately following (`.app-main`) must survive with no whitespace or formatting change. This deletion is what restores D-06's single-source-of-layout-truth: after it there is exactly one breakpoint pair in the repo, `web/app.js:91` and its lockstep copy in `web/mobile.css`. Add nothing to this file — not a comment, not a blank line. The gate is deletion-only.

**3. Do NOT bump `STORAGE_VERSION`.** The dismissal key is now orphaned in returning readers' localStorage and that is the correct outcome. A version bump would make `migratePreviewStorage()` wipe every `bcf:*` key on next load — the reader's bookmark, speed, zoom and comfort toggles — and would force 34 fixture edits across 4 test files, all to purge inert data nothing reads. See `must_haves.prohibitions`.

**4. `tests/test_desktop_smoke.py` — restate two assertions.** Both current sites assert the element is hidden. Playwright treats zero matching elements as hidden, so those assertions keep passing after deletion while their neighbouring comments (which cite the now-nonexistent source lines) become factually wrong. Replace each with the absence idiom the other three mobile test files already use — evaluate `document.querySelector` for the class in the page and assert the result is `None` — and rewrite the accompanying comments to describe deletion rather than CSS-driven hiding. This is a deliberate, justified edit to a file Phase 3 kept at zero diff; record the justification in the summary the way Phase 2 recorded its single assertion swap.

**5. Confirm the absence across the whole viewport envelope,** not just the one you happened to test: add or extend assertions so the element's absence is proven at phone portrait, phone landscape, and desktop width. FA-MOBX-01's criteria list the exact viewports; the mobile test files already define every one of them as module constants.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py</automated>
    <automated>test "$(git diff --numstat 57d2768 HEAD -- web/style.css | cut -f1)" = "0"</automated>
    <automated>test "$(grep -c 'renderPortraitBanner\|LS_PORTRAIT_DISMISSED\|portraitDismissed' web/app.js)" = "0"; test "$(grep -c 'portrait-banner' web/style.css)" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - The full four-file suite exits 0 with at least the 53-test Phase 3 baseline still passing and no pre-existing test deleted.
    - `git diff --numstat 57d2768 HEAD -- web/style.css` reports `0` in its added-lines column, and a non-zero deleted-lines column. Deletion-only is the gate; a single added line — including a comment — fails it.
    - `git diff --exit-code 57d2768 HEAD -- web/mobile-gestures.js` exits 0.
    - `grep -c 'renderPortraitBanner\|LS_PORTRAIT_DISMISSED\|portraitDismissed' web/app.js` returns 0 and `grep -c 'portrait-banner' web/style.css` returns 0.
    - `grep -c 'STORAGE_VERSION' web/app.js` still finds the constant and its value is unchanged from HEAD~ — verified by `git diff 57d2768 HEAD -- web/app.js | grep -c 'STORAGE_VERSION'` returning 0.
    - `tests/test_desktop_smoke.py` contains no `to_be_hidden()` assertion for the removed class; both sites assert null via `page.evaluate`.
    - Absence is asserted at phone portrait, phone landscape and desktop width — three distinct viewports, not one.
  </acceptance_criteria>

  <reversibility rating="reversible">Two deletion hunks in two files; undoing is a `git revert` of one commit, and the freeze proof in Task 2 makes any drift immediately visible.</reversibility>

  <done>The banner render function, call site, storage constant, state field and migration entry are gone from `web/app.js`; the rule block and stale media wrapper are gone from `web/style.css` with zero added lines; the desktop smoke test asserts deletion rather than hiding; and the full suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit the whole-milestone freeze proof as a repeatable pytest gate</name>

  <read_first>
    - `.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-04-SUMMARY.md` § Accomplishments — the Phase 3 freeze-proof shell commands this test formalises, and the recorded reasoning for why the diff targets a BASE commit rather than the working tree.
    - `04-VALIDATION.md` § "Wave 0 Requirements" — the requirement that this be a committed test, and the explicit base-commit correction (whole-milestone `57d2768`, NOT Phase 3's `22bdd8c`).
    - `04-PATTERNS.md` § "tests/test_freeze_proof.py — no analog" — confirmation that no existing test in the suite wraps a `git diff`; this is a new test shape, not an extension of the Playwright harness.
    - `tests/test_mobile_plumbing.py` (first 40 lines) — the module-level import and naming conventions to match; do NOT import the Playwright harness here, this test needs no browser.
  </read_first>

  <files>tests/test_freeze_proof.py</files>

  <behavior>
    - Test 1: `web/mobile-gestures.js` is byte-identical to the whole-milestone base commit — `git diff --exit-code` against `57d2768` returns no output. Red if the file is ever touched.
    - Test 2: `web/style.css`'s diff against `57d2768` is deletion-only — the `--numstat` added-lines column is exactly `0`. Red before Task 1 lands (numstat returns nothing at all, so the test must distinguish "no diff" from "deletion-only diff" and fail the former, because MOBX-01 requires the deletion to have happened). Red again if anyone later adds a line to that file.
    - Test 3: the deleted selector no longer appears anywhere in the served stylesheet set — a source-level absence check across `web/style.css` and `web/mobile.css`, so a re-add in either file is caught.
    - Test 4: `web/` is still dependency-free and build-step-free — no `package.json`, no lockfile, no bundler config anywhere under `web/`, and `web/index.html`'s script/stylesheet tag set is byte-identical to `57d2768`.
    - Edge case: the base commit must be resolvable. If `git rev-parse` for it fails, the test fails loudly with the SHA in the message rather than skipping — a silently-skipped freeze proof is worse than none.
  </behavior>

  <action>
Write `tests/test_freeze_proof.py`, a browser-free pytest module that turns Phase 3's one-off shell commands into the milestone's standing gate.

Pin the base commit as a module-level constant with the value `57d2768` and a comment stating what it is (the pre-Phase-1 commit, `docs(01): create phase plan`) and why it is not a phase-local base: MOBX-01's claim is that desktop is byte-identical to before ANY mobile work started, which a per-phase base would silently weaken. Resolve the repo root from the test file's own path (`Path(__file__).resolve().parents[1]`) rather than assuming a cwd, and run every `git` invocation with `cwd` set to it — this suite runs from a worktree whose interpreter lives in a different checkout.

Implement the four behaviors above as separate test functions so a failure names which invariant broke. Use `subprocess.run` with `check=False` and assert on `returncode`/`stdout` explicitly, so a git failure surfaces as an assertion message carrying the command and its stderr, never as a bare non-zero exit or a skip. For the deletion-only test, parse `--numstat`'s tab-separated first column as an integer and assert it equals zero, and separately assert the deleted-lines column is greater than zero so an unmodified file cannot masquerade as a passing deletion-only diff.

For the served-stylesheet absence test, read both stylesheet files as text and assert the class-selector substring is absent from each. Keep the searched literal in exactly one module-level constant so the two call sites cannot drift.

For the dependency scan, mirror the checks Phase 3 ran by hand: assert none of `package.json`, `package-lock.json`, `yarn.lock`, `vite.config.js`, `webpack.config.js`, `rollup.config.js` exists under `web/`, and assert `git diff --exit-code 57d2768 HEAD -- web/index.html` is clean.

Do not add this module to any existing test file's imports and do not make any other test depend on it — it is a standalone gate, invoked in the phase's full-suite command alongside the four existing files.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_freeze_proof.py -x -q</automated>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py tests/test_freeze_proof.py</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_freeze_proof.py -x -q` exits 0 and collects at least 4 test items.
    - `grep -c '57d2768' tests/test_freeze_proof.py` returns at least 1, and `grep -c '22bdd8c' tests/test_freeze_proof.py` returns 0 — the phase-local base must not appear anywhere in the file.
    - The module imports no Playwright symbol: `grep -c 'playwright' tests/test_freeze_proof.py` returns 0.
    - No test in the module can pass by skipping: `grep -c 'pytest.skip\|@pytest.mark.skip' tests/test_freeze_proof.py` returns 0.
    - Running the module with the base commit temporarily set to an unresolvable SHA produces a FAILURE naming that SHA, not a skip or an error swallow — demonstrate this once in the summary and revert.
    - The five-file suite passes in one invocation.
  </acceptance_criteria>

  <reversibility rating="reversible">A new standalone test file with no production dependents.</reversibility>

  <done>`tests/test_freeze_proof.py` exists, is green, pins the whole-milestone base commit, proves the gesture file byte-identical and the stylesheet diff deletion-only, re-checks the dependency scan, and fails loudly rather than skipping when git cannot answer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → frozen desktop surface | Every change in this plan must be provably confined to the sanctioned deletion hunks; `web/style.css` is the file the milestone's closing claim rests on. |
| agent claim → phase record | "Desktop is byte-identical" written in a summary is not evidence; only a diff against the correct base commit is. |
| returning reader's localStorage → app state | Existing `bcf:*` keys carry a reader's bookmark and preferences; the migration path this plan edits is the one code path that can destroy them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `web/style.css` — an unintended second edit riding along with the sanctioned deletion | high | mitigate | The gate is `--numstat` added-lines `== 0` against `57d2768`, not a subjective review. Any added line, including a comment, fails. Enforced by `tests/test_freeze_proof.py` on every later run in this phase. |
| T-04-02 | Tampering | `web/mobile-gestures.js` — the byte-frozen gesture contract (D-17) | high | mitigate | `git diff --exit-code 57d2768 HEAD` on that path, committed as a test so it re-runs rather than being asserted once. |
| T-04-03 | Denial of Service | returning readers' `bcf:*` preferences wiped by an unnecessary `STORAGE_VERSION` bump | medium | mitigate | Explicit prohibition plus an acceptance criterion asserting the constant is unchanged against the base commit. The orphaned key is left inert by design. |
| T-04-04 | Repudiation | a freeze proof that silently skips (git unavailable, SHA unresolvable) and is read as a pass | medium | mitigate | The test module contains no skip markers and must fail loudly naming the SHA; demonstrated once during execution and recorded in the summary. |
| T-04-SC | Tampering | package-manager supply chain | high | accept | No package-manager install occurs in this plan. `tests/test_freeze_proof.py` re-asserts that `web/` carries no `package.json`, lockfile or bundler config. The one npm tool this phase uses (`lighthouse`) is introduced in plan 04-05 behind its own blocking legitimacy checkpoint. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py tests/test_freeze_proof.py` — one invocation, all green, >= 53 pre-existing tests intact.
2. `git diff --numstat 57d2768 HEAD -- web/style.css` — added-lines column is `0`, deleted-lines column is non-zero.
3. `git diff --exit-code 57d2768 HEAD -- web/mobile-gestures.js` — empty.
4. `git diff 57d2768 HEAD -- web/app.js | grep -c 'STORAGE_VERSION'` — returns 0.
5. The banner class is absent from both stylesheets and every banner symbol is absent from `web/app.js`.
</verification>

<success_criteria>
- No rotate-to-landscape banner exists in the DOM at phone portrait, phone landscape, or desktop width.
- `web/style.css`'s whole-milestone diff is deletion-only.
- Exactly one breakpoint definition pair remains in the repo.
- `STORAGE_VERSION` is unchanged and no reader preference is purged.
- The whole-milestone freeze proof is a committed, repeatable, non-skippable pytest module.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-01-SUMMARY.md` when done.
</output>
