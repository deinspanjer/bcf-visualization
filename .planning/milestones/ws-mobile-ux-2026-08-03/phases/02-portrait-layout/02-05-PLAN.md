---
phase: 02-portrait-layout
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - .planning/workstreams/mobile-ux/phases/02-portrait-layout/COVERAGE.md
  - tests/test_mobile_portrait.py
autonomous: false
requirements: [MOBP-01, MOBP-02, MOBP-03, MOBP-04, MOBP-05]

must_haves:
  truths:
    - "The full web suite — desktop smoke, mobile plumbing and mobile portrait — passes in one run from the worktree."
    - "The desktop experience is provably untouched: web/style.css and web/mobile-gestures.js have no diff, and the desktop smoke test passes with only the one superseded portrait-crossing assertion changed."
    - "The landscape fallback still renders the desktop shell plus the portrait banner and still attaches the Phase-1 gesture probe — Phase 3's surface is untouched by this phase (D-12)."
    - "The phase's API-coverage declaration exists in the phase directory and states that no external API integration is involved."
    - "Dre has reviewed the portrait surface against the INTEGRATION_PLAN.md §5 Phase B gate and the §6 acceptance checklist, on real iOS Safari hardware, and has ruled on every flagged decision this phase raised."
  artifacts:
    - path: ".planning/workstreams/mobile-ux/phases/02-portrait-layout/COVERAGE.md"
      provides: "API coverage declaration for a phase with no external API integration"
      contains: "No external API integration"
    - path: "tests/test_mobile_portrait.py"
      provides: "test_landscape_fallback_is_unchanged regression guard"
      contains: "def test_landscape_fallback_is_unchanged"
  key_links:
    - from: "tests/test_mobile_portrait.py"
      to: "web/app.js render() landscape branch"
      via: "PHONE_LANDSCAPE assertions that the desktop shell, banner and probe all still mount"
      pattern: "PHONE_LANDSCAPE"
  prohibitions:
    - "MUST NOT close the phase on emulation alone — the toolbar, tap latency and letterboxing questions this phase raised are only answerable on real hardware, and signing off without that check would report a confidence the evidence does not support."
    - "MUST NOT edit any rule or variable in web/style.css, and MUST NOT change what the desktop layout mode renders at >=1100px."
---

<objective>
Close the phase: prove the whole suite green, prove the frozen surfaces frozen, record the API-coverage declaration, and take the portrait surface to Dre for the INTEGRATION_PLAN.md §5 Phase B gate on real hardware.

Purpose: this workstream's gate rule is that a phase is not done until it passes both the scripted desktop smoke test and a human review against the corresponding §5 gate. This plan is that gate.
Output: a green full suite, COVERAGE.md, and Dre's ruling on every flagged decision.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-VALIDATION.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-01-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-02-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-03-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite sweep, landscape/desktop freeze proof, and the coverage declaration</name>
  <precondition>`/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest --version` succeeds from this worktree (no local .venv here).</precondition>
  <files>.planning/workstreams/mobile-ux/phases/02-portrait-layout/COVERAGE.md, tests/test_mobile_portrait.py</files>
  <read_first>
    - .planning/workstreams/mobile-ux/phases/02-portrait-layout/02-VALIDATION.md — the per-requirement test map and the sampling contract this sweep has to satisfy
    - design/mobile-ux/INTEGRATION_PLAN.md — §0.5 smoke checklist, §5 Phase B steps and gate, §6 acceptance checklist (Layout / Gestures / Scrubber / Persistence / Help), §7 things not to do
    - web/app.js — the `render()` landscape branch as it stands after this phase, to confirm the fallback path is still the Phase-1 one
    - tests/test_mobile_plumbing.py and tests/test_desktop_smoke.py — the two files whose green status is the freeze proof
  </read_first>
  <action>
1. Write `.planning/workstreams/mobile-ux/phases/02-portrait-layout/COVERAGE.md` containing exactly this declaration line, with a short heading above it and nothing invented beyond it:

   `No external API integration: portrait UI port touches only local DOM/localStorage and static outbound links.`

   Do not fabricate a capability matrix; this phase integrates no SDK, service or external API.

2. Add `test_landscape_fallback_is_unchanged` to tests/test_mobile_portrait.py: at `PHONE_LANDSCAPE`, assert the desktop shell root is present, the portrait banner element is present and visible, the `.mobile-gesture-probe` element is present, and no `.mobile-app` element exists — proving D-12's interim fallback survived the phase and Phase 3 still has the surface it expects.

3. Run the full suite in one invocation and record the result in the summary: `tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py`.

4. Run the freeze proof and record it: `git diff --exit-code web/style.css web/mobile-gestures.js tests/test_mobile_plumbing.py`, plus `git diff --numstat tests/test_desktop_smoke.py` showing the single superseded portrait-crossing assertion and nothing else.

5. Confirm no dependency crept in: there is no `package.json`, lockfile or bundler config anywhere under `web/`, and `web/index.html` still loads exactly the same script and stylesheet set it did before this phase.

6. Assemble the §6 acceptance-checklist evidence table (Layout / Gestures / Scrubber / Persistence / Help) into the plan summary, one row per checklist item with the automated test that proves it or the note that it is a hardware-only item for Task 2.
  </action>
  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py && git diff --exit-code web/style.css web/mobile-gestures.js tests/test_mobile_plumbing.py</automated>
  </verify>
  <acceptance_criteria>
    - The full-suite command above exits 0 with zero failures and zero errors.
    - `git diff --exit-code web/style.css web/mobile-gestures.js tests/test_mobile_plumbing.py` exits 0.
    - `git diff --numstat tests/test_desktop_smoke.py` reports at most 3 changed lines.
    - `COVERAGE.md` exists in the phase directory and contains the declaration sentence verbatim.
    - `test_landscape_fallback_is_unchanged` passes: at 844x390 the portrait banner is visible, the gesture probe exists, and `document.querySelector(".mobile-app")` is null.
    - `find web -name package.json -o -name package-lock.json -o -name yarn.lock -o -name vite.config.js` returns nothing.
    - The summary contains a §6 acceptance-checklist table with a named test or a hardware-only marker for every row.
  </acceptance_criteria>
  <done>The whole web suite is green in one run, the frozen files are provably untouched, the landscape fallback is intact, and the coverage declaration is on disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: INTEGRATION_PLAN.md §5 Phase B gate review with Dre</name>
  <what-built>
Portrait layout is complete and automated-green: a phone held upright renders the real sky over an always-visible mini-rail dock; sky tap / double-tap / swipe follow the locked gesture contract; the mini-rail scrubs accurately at 1x / 2x / 4x / 8x with cluster-binned, counted diamonds and a separate cyan active marker; Settings, About and Help all work with focus trap and back-gesture dismissal; first-run help auto-opens once; every preference survives reload. Desktop and the landscape fallback are provably unchanged.
  </what-built>
  <how-to-verify>
Serve the app locally (`python3 -m http.server 8001` from the repo root, then open `http://<mac-lan-ip>:8001/web/` on the phone) and walk the INTEGRATION_PLAN.md §6 acceptance checklist on **real iOS Safari hardware** — emulation cannot reproduce the dynamic toolbar, the `svh` settle, or real touch latency.

**Layout** — sky reads as roughly 60% of the screen with the dock always visible; nothing clips under the notch or home indicator; the top chips are clearly clear of the sky's focal label with a long perk name; scroll the page and confirm the dock does not jump when the Safari toolbar collapses or expands.

**Gestures** — tap pauses and resumes; turn tap-to-pause off in Settings and confirm tap does nothing; double-tap in the middle of the story returns you to the roll you were on (NOT the end of the story) and resumes; swipe left and right step one roll per thumb-width of travel; confirm haptics are simply absent (this is expected on iOS) and that every gesture still gives visible feedback.

**Scrubber** — drag the rail at 1x and confirm the clusters read as counted diamonds rather than a smear; confirm the current roll is always visible as its own cyan diamond; step through 2x / 4x / 8x in Settings and confirm the drag lands where your finger is at each level.

**Persistence** — set mode, on-roll, speed, zoom and both comfort toggles; force-quit Safari; reopen and confirm every one of them came back, along with your reading position.

**Help** — confirm the help sheet appeared by itself on your very first visit, that dismissing it sticks across a reload, and that the `?` and `ⓘ` buttons reopen it on demand; confirm the phone's back gesture closes an open flyout instead of leaving the app.

**Rulings needed** — please decide on each of these, all raised during planning:
1. **D-15** — portrait always renders the playthrough presentation; the Settings "Details" option persists the choice and takes effect on desktop but has no distinct mobile view in v1. Confirm, or redirect to building a real mobile details view (currently a deferred idea).
2. **Speed rungs (F-04)** — the ½× / 1× / 2× / 4× labels map to 2500 / 5000 / 10000 / 25000 words-per-second, chosen so a phone-set speed always matches an existing desktop rung. The exact-4x alternative (20000) is not a desktop rung and would render the frozen desktop speed control blank. Confirm the chosen mapping.
3. **Rail drag at zoom > 1 (F-06)** — the auto-pan recomputes from the live position during a drag, faithfully to the prototype, so the mapping shifts under your finger at 2x and above. Does that feel right on hardware, or should the pan freeze for the duration of a drag?
4. **Help geometry (F-07 / D-19)** — the Help sheet is scoped to the sky region rather than full-bleed, so the dock stays operable and the Phase-1 plumbing test keeps passing. Confirm or redirect.
5. **Sky letterboxing (RESEARCH A1)** — the sky camera fits a 1.6:1 world stage into a tall phone frame, so there are bars above and below. Does that read as cinematic framing or as a bug that needs a mobile-specific crop?
6. **Test-contract changes (F-01 / F-02)** — the Phase-1 gesture probe is deliberately retained in portrait, and one superseded portrait-banner assertion in the desktop smoke test was updated to assert the portrait surface. Confirm both.

The scripted gate — `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py` — is already green from Task 1; re-run it if you want it fresh.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 2, or describe what needs to change plus your rulings on items 1-6.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase artifacts → downstream phases | Phase 3 builds on the attach/detach and surface conventions this phase establishes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Repudiation | Phase sign-off without hardware evidence | medium | mitigate | The gate checkpoint is blocking and names the real-device walkthrough explicitly; the six rulings are recorded in the plan summary so a later phase can trace why each default stands |
| T-02-17 | Spoofing | Desktop regression hidden behind a passing suite | medium | mitigate | The freeze proof is a diff assertion on the frozen files plus a bounded diff on the gate test, not just a green test run |
| T-02-SC | Tampering | Supply chain | low | mitigate | The sweep asserts no `package.json`, lockfile or bundler config exists under `web/`; the phase installed nothing |
</threat_model>

## Artifacts this phase produces

See the full phase inventory in `02-01-PLAN.md`. Added by THIS plan: `.planning/workstreams/mobile-ux/phases/02-portrait-layout/COVERAGE.md` and `test_landscape_fallback_is_unchanged` in `tests/test_mobile_portrait.py`.

<verification>
- `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py` exits 0.
- `git diff --exit-code web/style.css web/mobile-gestures.js tests/test_mobile_plumbing.py` exits 0.
- Dre's §5 Phase B gate approval is recorded in the plan summary along with rulings on all six flagged items.
</verification>

<success_criteria>
- Full web suite green in one run; frozen files provably untouched; landscape fallback intact.
- COVERAGE.md on disk with the exact declaration.
- Phase B gate approved by Dre after a real-device walkthrough, with every flagged decision ruled on.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-05-SUMMARY.md` when done
</output>
