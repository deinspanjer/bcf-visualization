---
phase: 02-portrait-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app.js
  - web/mobile.css
  - tests/test_mobile_portrait.py
  - tests/helpers/web_runtime_site.py
  - tests/test_desktop_smoke.py
autonomous: true
requirements: [MOBP-01, MOBP-03]

must_haves:
  truths:
    - "On a phone-portrait viewport the app renders a sky region over an always-visible dock instead of the desktop shell, and the desktop shell still renders unchanged at >=1100px landscape (D-12)."
    - "Portrait playback runs for several seconds with zero structural re-renders and zero recursion — updatePlaybackFrame's portrait branch returns before the desktop #scrubber-playhead gate (D-18, RESEARCH Pitfall 2)."
    - "Dragging the mini-rail moves the word position and the playhead follows, committing through the existing setWordPos path (MOBP-03 at zoom 1)."
    - "The sky region measures ~60% of the portrait viewport height and the dock is fully visible without scrolling at 390x844 and at 320x568 (MOBP-01)."
    - "The top chip cluster's bounding box never intersects the sky focal label's bounding box at 390x844 or at 320x568, including a long multibyte perk name (MOBP-01 adjacency + encoding edges)."
    - "The top chip cluster renders the CH chip first and the amber roll chip second; with no active roll the amber chip element is absent from the DOM, not blanked (MOBP-01 ordering + empty edges; UI-SPEC empty-state row)."
    - "With a null/unknown chapter the dock title renders the literal em-dash fallback and never renders the string undefined (UI-SPEC partial-data row)."
    - "The dock now-title and meta line stay on one line and ellipsize rather than wrapping, at 320px width (UI-SPEC long-text row)."
    - "Before app.data resolves the shared loading screen renders and no portrait element mounts; on load failure the shared error screen renders (UI-SPEC loading/error rows — the pre-branch early returns in render() are untouched)."
    - "Normal portrait playback shows chips, sky, dock meta and rail playhead all tracking the same wordPos (UI-SPEC populated row)."
    - "tests/test_mobile_plumbing.py passes unmodified after the portrait branch lands — the diagnostic probe still mounts and attaches once per structural render in portrait, exactly one aria-label Play/Pause button exists, and exactly one .readout-meta element tracks wordPos."
    - statement: "On a real iPhone in Safari the top chips read as clearly separated from the sky focal label at the device's real aspect ratio and toolbar state, not merely non-overlapping in emulation."
      verification: backstop
    - statement: "renderSkyCamera's xMidYMid-meet letterboxing at a phone portrait aspect reads as intentional cinematic framing rather than a broken viewport (RESEARCH Assumption A1)."
      verification: backstop
  artifacts:
    - path: "web/app.js"
      provides: "renderMobilePortrait, updateMobilePortraitFrame, attachMobilePortraitGestures, panOffsetForPlayhead, fractionFromPointer, mobileInnerFraction; render()/cachePlaybackDomRefs()/updatePlaybackFrame() portrait branches"
      contains: "renderMobilePortrait"
    - path: "web/mobile.css"
      provides: "Portrait layout rules — .mobile-app column flex on --mobile-vh, .mobile-sky flex 1 1 60%, .mobile-dock safe-area padding, .mobile-rail 70px"
      contains: ".mobile-app"
    - path: "tests/test_mobile_portrait.py"
      provides: "Portrait tracer, no-recursion regression, and MOBP-01 layout/overlap tests"
      contains: "def test_portrait_playback_has_no_recursive_renders"
    - path: "tests/helpers/web_runtime_site.py"
      provides: "dense-rolls staged package (clustered rolls + one long multibyte perk name) for MOBP-01 encoding and MOBP-04 binning tests"
      contains: "dense-rolls"
  key_links:
    - from: "web/app.js render()"
      to: "web/app.js renderMobilePortrait()"
      via: "app.layoutMode === 'portrait' branch replaces renderAppShell()"
      pattern: "layoutMode === \"portrait\""
    - from: "web/app.js updatePlaybackFrame()"
      to: "web/app.js updateMobilePortraitFrame()"
      via: "portrait early return placed before the desktop app.dom.playhead gate"
      pattern: "updateMobilePortraitFrame"
    - from: "web/mobile-gestures.js attachRailScrub"
      to: "web/app.js setWordPos"
      via: "onScrub callback converts viewport fraction to inner fraction, then commits"
      pattern: "attachRailScrub"
  prohibitions:
    - "MUST NOT edit any rule or variable in web/style.css, and MUST NOT change what the desktop layout mode renders at >=1100px — the desktop experience is frozen and pixel parity is the acceptance bar."
    - "MUST NOT render the prototype's procedural placeholder sky in the portrait sky region — mobile readers must see the same truth-bearing cinematic the desktop shows, never a decorative stand-in (D-13)."
    - "MUST NOT add an npm dependency, a package.json, a bundler, or any build step to web/ — the app stays a zero-dependency static site readers can open directly."
    - "MUST NOT modify web/mobile-gestures.js — it stays byte-identical to the verbatim prototype port (D-07/D-17)."
---

<objective>
Phase goal (ROADMAP, verbatim — not in user-story form, see the plan-set note): **A phone held upright is a complete, usable visualization — sky, scrubbing, settings, and help.**

This plan is the phase's tracer: the thinnest path that wires every layer portrait will ever touch — render dispatch → portrait DOM → frozen sky primitives → incremental playback tier → pointer gestures → shared model setters → bookmark persistence — and proves it end to end. Everything later in the phase expands outward from this slice.

Purpose: portrait rendering and the incremental-update branch must land together or the first portrait render recurses into `render()` forever (RESEARCH Pitfall 2). Landing them as one proven slice also pins the two Phase-1 regression contracts discovered during planning (see `<flagged_assumptions>`), which are invisible from the plan documents alone.

Output: a phone-portrait viewport renders a real sky over a working dock, playback ticks incrementally, and dragging the mini-rail scrubs the story.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-PATTERNS.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-UI-SPEC.md
@.planning/workstreams/mobile-ux/ROADMAP.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end portrait slice — render branch, real sky, dock, mini-rail scrub</name>
  <precondition>`/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest --version` succeeds from this worktree (this worktree has no local .venv; the main checkout's venv is the only interpreter with Playwright installed).</precondition>
  <files>web/app.js, web/mobile.css, tests/test_mobile_portrait.py, tests/test_desktop_smoke.py</files>
  <read_first>
    - web/app.js — read `render()` (~903-929), `renderAppShell()` (~950), `playthroughFrameState()`/`renderPlaythrough()`/`renderViewportFrame()` (~1403-1463), `renderSkyCamera()` signature (~1621), model setters `setWordPos`/`togglePlayback`/`lastRollAtWord`/`chapterAtWord`/`formatWords`/`persistBookmarkNow` (~317-800), `cachePlaybackDomRefs`/`updatePlaybackFrame`/`updateScrubberFrame`/`updatePlaybackControlsFrame`/`updatePlaythroughFrame` (~2562-2665), and the whole `// ── Mobile UX ──` section (~2762-2927)
    - web/mobile-gestures.js — the exact `attachRailScrub(el, opts)` contract (lines 149-193): `onScrub(fraction)` receives a viewport-relative 0..1 fraction and its RETURN VALUE is compared by `!==` to fire the per-roll haptic
    - web/mobile.css — the Phase 1 foundation: `.mobile-sky-surface`, `.mobile-rail-surface`, `--safe-*`, `--mobile-vh`
    - design/mobile-ux/prototype/layouts.jsx — `PortraitC` DOM order (top cluster, sky, dock: transport row + MiniRail + hint row)
    - design/mobile-ux/prototype/scrubber.jsx lines 51-68 and 137-196 — `panOffsetForPlayhead`, `fractionFromPointer`, and the MiniRail lane/playhead markup
    - design/mobile-ux/prototype/styles.css — `.sky`, `.dock`, `.mini-rail` and its lane/tick/band/dot/playhead rules
    - tests/test_mobile_plumbing.py — lines 129-245 and 380-424: the portrait-viewport assertions this task must keep green UNMODIFIED
    - tests/test_desktop_smoke.py — lines 252-299: the portrait-crossing assertion this task must update
    - tests/helpers/web_runtime_site.py — `staged_web_runtime_site`, `PHONE_PORTRAIT` conventions, fixture roll/chapter field names
  </read_first>
  <action>
Wire one portrait path through every layer, production quality, in a single change.

1. `render()` (web/app.js ~903): branch on `app.layoutMode === "portrait"` to append `renderMobilePortrait()`; every other mode keeps appending `renderAppShell()` byte-identically (desktop AND the landscape fallback, per D-12). Keep `cachePlaybackDomRefs(); updatePlaybackFrame();` in place after the append. Leave the existing `app.layoutMode !== "desktop"` probe block that mounts `.mobile-gesture-probe` and calls `attachMobileGestureProbes()` running for BOTH portrait and landscape — Phase 1's plumbing tests drive it at PHONE_PORTRAIT and must stay green unmodified. Add `z-index:2147483647;` to that probe element's existing inline style so the portrait DOM cannot steal its hit target. After the probe block, call the new `attachMobilePortraitGestures()` when `app.layoutMode === "portrait"`.

2. `updatePlaybackFrame()` (~2589): insert `if (app.layoutMode === "portrait") { updateMobilePortraitFrame(); return; }` as the FIRST statement after the `!app.data` guard and BEFORE the `!app.dom?.playhead` gate. Everything below that gate stays byte-identical. Landing this in the same change as step 1 is mandatory: portrait DOM has no `#scrubber-playhead`, so without it the first render recurses to a stack overflow on load.

3. `cachePlaybackDomRefs()` (~2562): keep the existing desktop ref object untouched and append a portrait-only block that additionally caches `mobileSkyCameraLayer`, `mobileFocalLabel`, `mobileDockMeta`, `mobileDockTitle`, `mobileFab`, `mobileRail`, `mobileRailInner`, `mobilePlayhead`, `mobileActiveDot`, `mobileTopCluster` when `app.layoutMode === "portrait"`.

4. `renderMobilePortrait()` — new function at the end of the `// ── Mobile UX ──` section. Root `el("div", { class: "mobile-app" })`, children in this order:
   - `.mobile-top-cluster` — leave as an empty positioned container in this task (Task 2 fills it).
   - `.mobile-sky mobile-sky-surface` — `position:relative; overflow:hidden`. Children: `renderViewportFrame()` called as-is (frozen, do not copy), then `el("div", { class: "mobile-sky-camera-layer" }, frame.scene ? renderSkyCamera(frame.lastRoll, frame.scene, frame.focusT) : null)` where `frame = playthroughFrameState()`. Never call `renderPlaythrough`, `renderCarousel`, `createCarouselSlot` or `renderConstellationCard` (desktop fixed-pixel geometry). The mount class must be `mobile-sky-camera-layer`, never the desktop `sky-camera-layer` name, so the desktop updater can never resolve to it (RESEARCH Pitfall 5).
   - `.mobile-dock` — `.mobile-dock-transport` containing: the play/pause FAB `el("button", { class: "mobile-fab-play", "data-action": "toggle-playback", "aria-label": app.playing ? "Pause" : "Play", title: ... })` (delegated through the existing module-level `data-action` click handler — do NOT add an onClick, matching the desktop hot-control convention); `.mobile-dock-now` holding `.mobile-dock-meta readout-meta` (dual class, see acceptance criteria) and `.mobile-dock-title`; then `renderMobileScrubber()`; then an empty `.mobile-hint-row` placeholder (Task 2 fills it).
   - Speed / gear / info buttons are NOT part of this task.
5. `renderMobileScrubber()` — new function producing `.mobile-rail mobile-rail-surface` wrapping `.mobile-rail-inner` (style `width: ${zoom*100}%`, `transform: translateX(-${panPct}%)`), which contains: a chapters lane of `.mobile-ch-tick` (major tick when `chapter_num % 10 === 0` or chapter_num is "1") positioned at `(chapter.word_start / total) * 100`%; a rolls lane with `.mobile-pov-band` per chapter (left/width from `word_start`/`word_end`, POV class `mc` for Joe, `aisha` for Aisha, else `other`) and one `.mobile-roll-dot` per roll at `(roll.word_position / total) * 100`% carrying its `outcome` class; and `.mobile-playhead` at `playheadPctRaw`. Cluster bins are Plan 02-03's job — one dot per roll here. Read every field from the LIVE schema (`roll.word_position`, `roll.outcome`, `chapter.word_start`, `chapter.word_end`, `chapter.chapter_num`, `chapter.pov`), not the prototype's camelCase names.

6. Port `panOffsetForPlayhead(playheadPctRaw, zoom)` and `fractionFromPointer(el, e, zoom, panPct)` verbatim from prototype/scrubber.jsx (algorithm unchanged), and add `mobileInnerFraction(viewportFraction, zoom, panPct)` returning `Math.max(0, Math.min(1, (viewportFraction + panPct / 100) / zoom))` — the single conversion both the rail callback and later plans use.

7. `attachMobilePortraitGestures()` — mirror `attachMobileGestureProbes`'s lifecycle exactly (teardown stored on `app`, invoked defensively before re-attach, called only from inside the render pass). Use NEW teardown slots `app.mobileSkyTeardown` and `app.mobileRailTeardown`; never touch `app.mobileGestureTeardown`, which the Phase-1 probe owns — sharing that slot would drop one attachment's teardown and double-bind. In this task attach only the rail: `window.attachRailScrub(railEl, { onScrub, onScrubEnd })` where `onScrub(f)` computes `playheadPctRaw` from the live `app.wordPos`, derives `panPct` via `panOffsetForPlayhead(playheadPctRaw, app.mobileTimelineZoom)`, converts with `mobileInnerFraction`, commits `setWordPos(Math.round(innerFrac * app.data.story.total_words))`, and RETURNS `lastRollAtWord(target)` so the module's built-in per-roll haptic fires; `onScrubEnd` calls `persistBookmarkNow()`. Do not port the prototype MiniRail's second raw pointer-listener effect — that would be a parallel scrub path (D-17).

8. `updateMobilePortraitFrame()` — mirror `updatePlaythroughFrame`'s key-diff shape: recompute `playthroughFrameState()`, `replaceChildren` on `app.dom.mobileSkyCameraLayer` only when `frame.scene` is live or the `scene:${uid}` key changed (store under `app.frameKeys.skyCamera`), set the FAB's `aria-label`/`title`/icon from `app.playing`, write `app.dom.mobileDockMeta.textContent` and `app.dom.mobileDockTitle.textContent`, and move `app.dom.mobilePlayhead.style.left` plus `app.dom.mobileRailInner.style.transform` from the current wordPos/zoom. Mutate text and style only — never rebuild the rail's lanes here.

9. `web/mobile.css`: append portrait rules inside the existing media block. `.mobile-app { height: var(--mobile-vh); display:flex; flex-direction:column; overflow:hidden; }`, `.mobile-sky { flex: 1 1 60%; min-height: 0; position:relative; overflow:hidden; }`, `.mobile-dock { flex: 0 0 auto; padding: 14px 12px max(18px, var(--safe-bottom)); }`, `.mobile-dock-transport { display:flex; align-items:center; gap:10px; }`, `.mobile-rail { position:relative; height:70px; padding:4px 8px; overflow:hidden; }`, plus the lane/tick/band/dot/playhead rules adapted from prototype styles.css. Use only new `.mobile-*` class names and only existing `--cyan`/`--green`/`--amber`/`--edge`/`--panel`/`--ink`/`--muted`/`--void` variables; never `100vh`, never a desktop structural class name, never a redefinition of a `:root` color.

10. Tests — create `tests/test_mobile_portrait.py` reusing `staged_web_runtime_site`, `_chromium_browser_or_skip`, `_page_with_console_capture` and `PHONE_PORTRAIT` conventions copied from tests/test_mobile_plumbing.py. Add `test_portrait_tracer_renders_sky_dock_and_rail_scrub_commits` and `test_portrait_playback_has_no_recursive_renders`.

11. `tests/test_desktop_smoke.py` — at the 1100x1300 portrait crossing (~line 289) the banner-visible assertion now encodes superseded Phase-1 behavior: replace that ONE assertion with an assertion that the portrait surface renders (`.mobile-app` visible). Change nothing else in that file; the 1920x1080 desktop assertions and the render-count assertions stay exactly as they are.
  </action>
  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_portrait.py -x && /Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_plumbing.py tests/test_desktop_smoke.py -x && git diff --exit-code web/style.css web/mobile-gestures.js</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_mobile_portrait.py::test_portrait_tracer_renders_sky_dock_and_rail_scrub_commits -x` passes: at 390x844 `.mobile-app`, `.mobile-sky`, `.mobile-dock` and `.mobile-rail` are visible; `document.querySelector(".app")` is null; a pointerdown at 25% of the rail's width changes `window.__bcfPrefs`-adjacent state such that the `.mobile-dock-meta` text and `.mobile-playhead` inline `left` both change, and `localStorage.getItem("bcf:bookmark:word_position")` is updated after pointerup.
    - `pytest tests/test_mobile_portrait.py::test_portrait_playback_has_no_recursive_renders -x` passes: with `window.__bcfRenderStats = { structuralRenders: 0 }` injected, clicking play at 390x844 and waiting 1500ms leaves `window.__bcfRenderStats.structuralRenders === 0`, the captured console/pageerror list empty, and the dock meta text different from its pre-play value.
    - `pytest tests/test_mobile_plumbing.py -x` passes with `git diff --exit-code tests/test_mobile_plumbing.py` clean.
    - `pytest tests/test_desktop_smoke.py -x` passes and `git diff --numstat tests/test_desktop_smoke.py` shows at most 3 changed lines.
    - `git diff --exit-code web/style.css web/mobile-gestures.js` exits 0.
    - In portrait, `document.querySelectorAll('button[aria-label="Play"], button[aria-label="Pause"]').length === 1` and `document.querySelectorAll(".readout-meta").length === 1`, and the `.readout-meta` element's text changes while playing and holds steady while paused.
    - In portrait, `document.querySelector(".mobile-app .viewport")` and `document.querySelector(".mobile-app .app")` are both null, and `document.querySelector(".mobile-sky-camera-layer svg")` is non-null while a cinematic is firing.
    - `document.querySelector(".mobile-gesture-probe")` is non-null at PHONE_PORTRAIT and its computed `z-index` is `2147483647`.
    - `getComputedStyle(document.querySelector(".mobile-sky")).touchAction === "pan-y"` and `getComputedStyle(document.querySelector(".mobile-rail")).touchAction === "none"`.
  </acceptance_criteria>
  <done>A phone-portrait viewport shows the real sky over a working dock, playback advances the dock readout and playhead with zero structural re-renders, dragging the rail scrubs the story and persists the bookmark, and both Phase-1 test files still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Top chips, focal label and hint row without overlap at 320px</name>
  <files>web/app.js, web/mobile.css, tests/test_mobile_portrait.py, tests/helpers/web_runtime_site.py</files>
  <read_first>
    - web/app.js — the `renderMobilePortrait()` written in Task 1; `chapterAtWord`, `lastRollAtWord`, `formatWords`, `rollTotalCost`, `paidRollPerks`, `perkDisplayLabel` (imported from viz-model.js at the top of the file); `renderNarrativeReadout`'s roll-label/cost conventions (~2263-2290)
    - .planning/workstreams/mobile-ux/phases/02-portrait-layout/02-UI-SPEC.md — Copywriting Contract rows for the CH chip, amber roll chip, dock now meta/title, hint row; Typography Display row (focal label `clamp(18px, 4.5vw, 26px)`); Color accent list
    - design/mobile-ux/prototype/styles.css lines 85-160 — `.focal-label` kicker/name/sub, `.chip`, `.top-cluster`
    - design/mobile-ux/prototype/layouts.jsx lines 6-54 — chip cluster and hint row shape
    - tests/helpers/web_runtime_site.py — `_roll_hit`, `_roll_miss`, `_chapter`, `_chapter_facts`, `_visualization_facts`, `staged_web_runtime_site` package registration
  </read_first>
  <action>
Fill the two placeholders left by Task 1 and prove MOBP-01's acceptance bar.

1. `renderMobileTopCluster()` — `.mobile-top-cluster` absolutely positioned 12px inset from the sky's top/left/right, containing a flex row (8px gap) of chips followed by the top-right help button slot (leave the help button out until Plan 02-04; reserve its 44x44 area with an empty spacer so the layout does not shift later). Chip 1 is `.mobile-chip` with text `CH ${chapter.chapter_num} · ${formatWords(app.wordPos)}w`. Chip 2 is `.mobile-chip amber` with text `◢ ${roll.roll_label || "R—"}` and is appended ONLY when `lastRollAtWord(app.wordPos)` is non-null — when there is no active roll the element must not exist at all.

2. `renderMobileFocalLabel(frame)` — `.mobile-focal-label` inside `.mobile-sky`, rendered only while `frame.firing && frame.lastRoll`. Three children: `.kicker` = `roll.constellation || "—"`; `.name` = for a hit, `perkDisplayLabel(paidRollPerks(roll)[0] ?? roll.free_perks[0])` falling back to `roll.rolled_perk_name || roll.constellation || "—"`, and for a non-hit the literal `Miss`; `.sub` = `${rollTotalCost(roll)} CP · ch ${roll.chapter_num}` for a hit, else `miss ${roll.miss_cost_estimate ?? "?"} · ch ${roll.chapter_num}`. Position it per the prototype at `left:50%; top:38%; transform:translate(-50%,-50%)` with `pointer-events:none`, and size `.name` with `clamp(18px, 4.5vw, 26px)` per the UI-SPEC Display role. `.mobile-focal-label` must sit below `.mobile-top-cluster` in the sky's stacking order and the sky must reserve top padding at least equal to the chip cluster's height so the two can never collide at narrow widths.

3. Dock now-line copy: `.mobile-dock-meta readout-meta` text = `${formatWords(app.wordPos)} / ${formatWords(total)} words · ${Math.round(app.wordPos / total * 100)}%`; `.mobile-dock-title` text = `chapterAtWord(app.wordPos)?.title || "—"`. Both get `white-space:nowrap; overflow:hidden; text-overflow:ellipsis` and a `min-width:0` flex parent so they ellipsize instead of wrapping. Override the inherited desktop `.readout-meta` type rule with a `.mobile-dock .readout-meta` selector in mobile.css (higher specificity, no edit to web/style.css).

4. `.mobile-hint-row` — left span the literal `tap rail → jump · drag → scrub`; right span `${app.mobileTimelineZoom}× zoom · ` only when the zoom is above 1, followed by `${chapterAtWord(app.wordPos)?.pov || "Joe"} POV`.

5. Wire the chips, focal label and dock title into `updateMobilePortraitFrame()`'s incremental pass: chip and hint text are `textContent` writes; the amber chip and the focal label are structural presence changes, so key them (`roll uid`/`firing` composite) and `replaceChildren` their container only when the key changes — never every frame.

6. `tests/helpers/web_runtime_site.py` — add a THIRD staged package `dense-rolls`, registered in `packages.json` alongside `tiny-default`/`tiny-alt` and staged via `_stage_package`. Its chapter facts contain at least 12 rolls, of which at least 4 fall within a few hundred words of each other (so they merge into one bin at 390px width and 1x zoom) and at least 2 are >5% of total words apart, with mixed `hit`/`miss` outcomes, and one purchased perk named with a long multibyte string (>= 60 characters including non-ASCII, e.g. an em-dash and accented letters). `tiny-default` and `tiny-alt` payloads must be byte-identical to today — branch on the package id, do not mutate the shared builders in place.

7. Tests — add `test_portrait_layout_proportions_and_chip_overlap` to tests/test_mobile_portrait.py, run at 390x844 and again at 320x568 against the `dense-rolls` package, plus `test_portrait_empty_and_partial_states`.
  </action>
  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_portrait.py -x && /Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_plumbing.py tests/test_desktop_smoke.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_portrait_layout_proportions_and_chip_overlap` asserts, at BOTH 390x844 and 320x568: `.mobile-sky` bounding-box height is between 50% and 70% of viewport height; `.mobile-dock`'s bottom edge is within the viewport; and the `.mobile-top-cluster` box and the `.mobile-focal-label` box do not intersect (`cluster.y + cluster.height <= label.y`) while the long multibyte perk name from the `dense-rolls` package is the active roll's perk.
    - `test_portrait_empty_and_partial_states` asserts: with the playhead before the first roll, `document.querySelector(".mobile-chip.amber")` is null and `.mobile-focal-label` is null; the `.mobile-dock-title` text is never the string `undefined`; and with a chapter whose title is absent the dock title reads the single-character em-dash fallback.
    - At 320px width, `.mobile-dock-title` and `.mobile-dock-meta` each report `scrollHeight <= clientHeight + 1` (single line) and `scrollWidth > clientWidth` for the long title (ellipsized, not wrapped).
    - `document.querySelectorAll(".mobile-top-cluster .mobile-chip")[0].textContent` starts with `CH ` and, when a roll is active, index 1 starts with the `◢` glyph.
    - `pytest tests/test_mobile_plumbing.py tests/test_desktop_smoke.py -x` still passes.
    - Loading the `tiny-default` package produces a `visualization_facts.json` byte-identical to the pre-change output (assert via a checksum comparison in the test or by the untouched Phase-1 suites passing).
  </acceptance_criteria>
  <done>Portrait shows the CH and roll chips, the sky focal label and the hint row; chips never overlap the focal label at 390 or 320 px even with a long multibyte perk name; empty and partial data render structural omissions and the em-dash fallback, never `undefined`.</done>
</task>

</tasks>

<flagged_assumptions>
Two conflicts found during planning that no source artifact records. Both are load-bearing and are resolved here by planner decision; both go on the Phase B gate agenda.

**F-01 — Phase-1 tests drive portrait, not landscape.** RESEARCH.md Pattern 2 states the Phase-1 gesture assertions run at `PHONE_LANDSCAPE`. They do not: `test_gestures_fire_exactly_once_without_structural_renders`, `test_gesture_attach_survives_forced_rerenders_without_double_fire`, `test_mid_drag_rerender_is_safe_and_fresh_probe_fires_once` and `test_visibilitychange_pauses_playback_on_mobile_only` all start at `PHONE_PORTRAIT` (tests/test_mobile_plumbing.py:138, 190, 223, 393). Since that file must stay green unmodified, portrait must keep mounting `.mobile-gesture-probe` and calling `attachMobileGestureProbes()` once per structural render, must expose exactly one `aria-label="Play"/"Pause"` button, and must expose exactly one `.readout-meta` element that tracks `wordPos`. The probe is Phase-1 diagnostic scaffolding retained deliberately; retiring it is a Phase-4 cleanup candidate, not this phase's call.

**F-02 — the desktop smoke test asserts the portrait banner.** `tests/test_desktop_smoke.py:289` asserts `.portrait-banner` is visible at 1100x1300, which is portrait layout mode. D-12 replaces that surface, so this one assertion is superseded and is updated to assert the portrait surface instead. The freeze is about desktop-mode rendering at >=1100px; that assertion is a mobile-mode assertion living in the gate file. No other line of that file changes.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app state | `bcf:*` values are attacker/user-editable and drive render math |
| pointer events → model setters | gesture callbacks write word position into shared state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | `updatePlaybackFrame()` desktop `app.dom.playhead` gate | high | mitigate | Land the portrait early-return before the gate in the same change as the `render()` branch; `test_portrait_playback_has_no_recursive_renders` proves zero structural renders and an empty console over a live playback window |
| T-02-02 | Tampering | `bcf:bookmark:word_position`, `bcf:timeline-zoom` read at init | low | mitigate | Reuse the existing `readStoredNumber`/`readStoredChoice` allow-list readers and the `clamp` inside `setWordPos`; the rail callback commits only through `setWordPos`, never by writing `app.wordPos` directly |
| T-02-03 | Tampering | Portrait DOM construction from roll/chapter data fields | medium | mitigate | Build every node with the existing `el()` helper and `textContent`/`text:` props; no `innerHTML` anywhere in the portrait renderers, so a hostile perk/chapter string cannot inject markup |
| T-02-04 | Spoofing | Frozen desktop surface | low | accept | No desktop code path changes; `git diff --exit-code web/style.css` is the gate |
| T-02-SC | Tampering | Supply chain | low | mitigate | This phase installs no packages; `web/` remains dependency-free with no `package.json` and no build step (prohibition-enforced) |
</threat_model>

## Artifacts this phase produces

**New `web/app.js` functions:** `renderMobilePortrait`, `renderMobileTopCluster`, `renderMobileFocalLabel`, `renderMobileScrubber`, `updateMobilePortraitFrame`, `attachMobilePortraitGestures`, `panOffsetForPlayhead`, `fractionFromPointer`, `mobileInnerFraction` (this plan); `rollStepFrom`, `setMobileSpeedMultiplier`, `mobileSpeedMultiplier` (02-02); `binRolls`, `finalizeBin`, `binSize`, `recomputeMobileRailBins` (02-03); `openMobileSurface`, `closeMobileSurface`, `renderMobileSurface`, `renderMobileSettingsFlyout`, `renderMobileInfoFlyout`, `renderMobileHelpOverlay`, `trapMobileSurfaceFocus`, `maybeAutoOpenHelp`, `mobileSeg`, `mobilePlayIcon`, `mobileGearIcon`, `mobileInfoIcon`, `mobileHelpIcon` (02-04).
**Modified existing functions:** `render()`, `cachePlaybackDomRefs()`, `updatePlaybackFrame()` (this plan); `setMode()` (02-04).
**New `app.*` state:** `mobileSkyTeardown`, `mobileRailTeardown` (this plan); `mobileSkyHintShown` (02-02); `mobileRailBins`, `mobileRailWidth`, `mobileRailResizeObserver` (02-03); `mobileSurface`, `mobileSurfaceOpener`, `mobileHelpAutoOpened` (02-04).
**New constants:** `MIN_DOT_SPACING_PX` (02-03), `MOBILE_SPEED_RUNGS` (02-02), `MOBILE_SURFACES` (02-04).
**New CSS classes in `web/mobile.css`:** `.mobile-app`, `.mobile-top-cluster`, `.mobile-chip`, `.mobile-sky`, `.mobile-sky-camera-layer`, `.mobile-focal-label`, `.mobile-dock`, `.mobile-dock-transport`, `.mobile-fab-play`, `.mobile-dock-now`, `.mobile-dock-meta`, `.mobile-dock-title`, `.mobile-rail`, `.mobile-rail-inner`, `.mobile-lane`, `.mobile-ch-tick`, `.mobile-pov-band`, `.mobile-roll-dot`, `.mobile-playhead`, `.mobile-hint-row` (this plan); `.mobile-sky-tap-hint`, `.mobile-icon-btn` (02-02); `.mobile-roll-bin`, `.mobile-roll-bin .count` (02-03); `.mobile-flyout`, `.mobile-flyout-backdrop`, `.mobile-seg`, `.mobile-help-overlay`, `.mobile-got-it`, `.mobile-source-row` (02-04).
**New files:** `tests/test_mobile_portrait.py` (this plan), `.planning/workstreams/mobile-ux/phases/02-portrait-layout/COVERAGE.md` (02-05).
**Modified test infrastructure:** `tests/helpers/web_runtime_site.py` (`dense-rolls` package, this plan), `tests/test_desktop_smoke.py` (one portrait-crossing assertion, this plan).

<verification>
- `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_mobile_portrait.py tests/test_mobile_plumbing.py tests/test_desktop_smoke.py -x` is green.
- `git diff --exit-code web/style.css web/mobile-gestures.js tests/test_mobile_plumbing.py` exits 0.
- No `package.json`, lockfile or bundler config exists anywhere under `web/`.
</verification>

<success_criteria>
- Portrait renders its own surface; desktop and landscape still render `renderAppShell()` unchanged.
- Playback in portrait produces zero structural re-renders and no recursion.
- Rail drag scrubs and persists the bookmark through the existing shared setters.
- Chips never overlap the focal label at 390 or 320 px; empty/partial states omit structurally and never print `undefined`.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-01-SUMMARY.md` when done
</output>
