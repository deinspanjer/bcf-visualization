---
phase: 02-mechanical-verifier
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/manual/perk_aliases.json
  - data/derived/perk_directory.json
autonomous: false
requirements: [CINF-03]

must_haves:
  truths:
    - "The 14 perk names measured in 02-RESEARCH.md's Perk Resolution Reality Check as unresolvable through perk_name_resolver's ladder (jump=None) are re-confirmed against the live repo state before any data edit is proposed."
    - "Every addition to data/manual/perk_aliases.json is Dre-approved via an explicit checkpoint before being written — an executor never writes this hand-curated file autonomously (D-11)."
    - "Derived artifacts affected by an alias change (data/derived/perk_directory.json) are regenerated through scripts/build_perk_directory.py, never hand-edited."
    - "If any of the 14 names still fails to resolve after Dre's approved additions, that residual is surfaced as a new checkpoint for Dre — never silently accepted, never worked around by weakening perk_name_resolver's ladder or editing data/manual/chapter_roll_overrides.json (D-08, D-11)."
  artifacts:
    - path: "data/manual/perk_aliases.json"
      provides: "Dre-approved alias additions closing some or all of the 14 D-11 perk-name gaps, in the existing flat {canonical: [alias, ...]} shape"
  key_links:
    - from: "data/manual/perk_aliases.json"
      to: "scripts/perk_name_resolver.py"
      via: "load_perk_aliases() reads this file fresh at DirectoryMatchIndex construction time — no perk_directory.json regen is required for a pure alias-to-existing-canonical fix"
      pattern: "load_perk_aliases"

  prohibitions:
    - requirement_id: CINF-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT write additions to data/manual/perk_aliases.json without Dre's explicit checkpoint approval — an executor never writes this hand-curated file autonomously (D-11, curation authority)."
    - requirement_id: CINF-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit data/manual/chapter_roll_overrides.json or hand-edit data/derived/perk_directory.json to paper over a residual unresolved perk name — regenerate perk_directory.json only through scripts/build_perk_directory.py, and route any true catalog gap to a checkpoint, not a workaround (D-08)."
---

<objective>
Re-confirm and then close (as far as Dre approves) the 14 perk-name resolution gaps measured in 02-RESEARCH.md, via a Dre-approved addition to `data/manual/perk_aliases.json` — BEFORE the corpus-wide baseline (Plan 02-03) runs, so success criterion 1's "100%" is literally true rather than allowlisted around.

Purpose: D-11 is Dre's own post-research decision — fix the data first, since these are genuine `perk_directory.json`/`perk_aliases.json` coverage gaps (perks that exist in the story but were never catalogued), not a resolver-logic bug. Because `data/manual/perk_aliases.json` is hand-curated, curation authority applies: an executor must never write it autonomously.

Output: an updated, Dre-approved `data/manual/perk_aliases.json`; a regenerated `data/derived/perk_directory.json`; a re-confirmed resolution count (ideally 14/14, otherwise an explicit, named residual documented for Dre in this plan's SUMMARY).
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/phases/02-mechanical-verifier/02-CONTEXT.md
@.planning/workstreams/curation/phases/02-mechanical-verifier/02-RESEARCH.md
@scripts/perk_name_resolver.py
@data/manual/perk_aliases.json

<measured_baseline>
Re-measured directly against the live repo in this planning session (2026-07-26), matching 02-RESEARCH.md's own numbers exactly: of the corpus's 120 distinct perk-name mentions, 97/120 (80.8%) resolve with `jump=None` (the only info an override roll object itself provides — roll objects carry no `jump` field). The 14 that do NOT resolve, with their `(chapter_num, perk_name, constellation, jump)`:

`36 / "Weapon & Item Storage Chest" / None / jump="Monster Hunter"`
`79 / "Core Ability - Amp Core" / "Knowledge" / jump="Titanfall"`
`79 / "Oi!" / "Knowledge" / jump="Titanfall"`
`81 / "ARM SLAVE M6 Bushnell" / "Knowledge" / jump=None`
`82 / "Farming tool" / "Quality" / jump="Viking Saga"`
`91 / "Primo Victoria" / "Toolkits" / jump="Sabaton"`
`92 / "Altmode" / "Size" / jump="Transformers"`
`92 / "Decepticon Terrorize!" / "Size" / jump="Transformers"`
`92 / "Energon" / "Size" / jump="Transformers"`
`92 / "Energon Battle Pistol" / "Size" / jump="Transformers"`
`92 / "Energon Melee Weapon" / "Size" / jump="Transformers"`
`92 / "ROBOTS IN DISGUISE! - Medium Chassis" / "Size" / jump="Transformers"`
`93 / "The Village" / "Personal Reality" / jump="Personal Reality"`
`95 / "Gladius-class Heavy Corvette" / "Knowledge" / jump="Halo"`

Additionally measured in this planning session: `data/derived/perk_directory.json` has ZERO rows under jump="Transformers" resembling any of the 6 unresolved Transformers names — the jump's only 6 catalogued rows are `"All Spark Chosen"`, `"Matrix of Leadership"`, `"Cybertronian Forge"`, `"Master Builder"`, `"Science! Engineering"`, `"Science! Mechanics"`. Similarly, jump="Titanfall" has only `"Companion Import"`, `"What's This Do?"`, `"Titan Engineering"`; jump="Sabaton" has only `"Firestorm"`, `"Panzerkampf"`, `"The Toolkit"`; jump="Viking Saga" has only `"Craftsman of the Gods"`; jump="Halo" has only `"Engineer"`, `"Erudition"`. None of these existing rows are an obvious alias target for the corresponding unresolved name by substring/prefix — Task 1 of this plan must broaden the search (drop the jump filter, try normalized substring across ALL jumps/constellations) before concluding no candidate exists, since a same-name generic-tech row filed under a different jump is possible and this planning pass did not do that broader search.
</measured_baseline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm the 14 unresolved perk names and search for alias candidates</name>
  <read_first>
    scripts/perk_name_resolver.py (`DirectoryMatchIndex.lookup` :279-376 — note `jumps_to_try` at :294-299 stays empty when `jump` is falsy, so a no-jump lookup only ever reaches Step 4's name-only/constellation-scoped fallback);
    data/manual/chapter_roll_overrides.json (source of the 120 perk-name mentions, `rolls[].perks[]` across all chapters);
    data/derived/perk_directory.json (`perks[]` — the canonical rows the ladder resolves against);
    data/derived/obtained_perks.json (jump cross-reference source, coverage stops at chapter 119.5 per its own `_note`/`_coverage` field — not usable for chapters past that point, but usable here since all 14 gaps are in chapters ≤95)
  </read_first>
  <precondition>data/derived/perk_directory.json and data/derived/obtained_perks.json exist (produced by Phase 1's pipeline regen; confirmed present in this planning session).</precondition>
  <files>(none committed — /tmp/perk_resolution_spike.py is a throwaway script, deliberately outside the repo)</files>
  <action>
    Write a throwaway Python script at the fixed path `/tmp/perk_resolution_spike.py` (not committed to the repo) that: (1) loads `data/manual/chapter_roll_overrides.json` and collects every `(chapter_num, perk_name, constellation)` triple from every roll's `perks[]` array across all chapters; (2) builds a `DirectoryMatchIndex` from the live `data/derived/perk_directory.json`'s `perks` array and `data/manual/perk_aliases.json` via `build_directory_match_index`; (3) for each triple, calls `.lookup(perk_name, jump=None, constellation=constellation)` and records which fail to resolve; (4) cross-references `data/derived/obtained_perks.json` by `(chapter_num, normalized perk_name)` to see if a `jump` value is available there, and re-tries `.lookup(perk_name, jump=<that value>, constellation=constellation)` for anything still unresolved. Confirm the resulting unresolved list matches the 14-name list in `<measured_baseline>` above (or note exactly which names differ, if the corpus changed since this planning pass).

    For each of the confirmed-unresolved names, run a SECOND, broader search: iterate ALL rows in `data/derived/perk_directory.json`'s `perks` array (no jump filter, no constellation filter this time) and surface any row whose `name` shares a normalized substring/prefix with the unresolved name (reuse `perk_name_resolver._normalize` for the comparison) as a candidate alias target, plus any row under the SAME constellation regardless of jump. Print a report: for each of the 14 names, the chapter, constellation, jump, and either "no candidate found" or the list of candidate directory rows (with their own name/jump/constellation) found by the broadened search. This report is the evidence for Task 2's checkpoint — do not propose a specific alias mapping yet, only surface candidates.
  </action>
  <verify>
    <automated>.venv/bin/python /tmp/perk_resolution_spike.py 2>&1 | tee /tmp/perk_resolution_report.txt | grep -c "no candidate found\|candidate"</automated>
  </verify>
  <acceptance_criteria>
    The script runs to completion and prints a per-name report for all confirmed-unresolved perk names (expected 14, per `<measured_baseline>`); for each, either a broadened-search candidate row or an explicit "no candidate found" line is printed — no name is silently omitted from the report.
  </acceptance_criteria>
  <done>A complete, printed report exists covering every currently-unresolved perk name with its broadened-search findings, ready to hand to the Task 2 checkpoint verbatim.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>How should each of the 14 unresolved perk names (measured in Task 1) be closed in data/manual/perk_aliases.json — as an alias to an existing directory row, or flagged as needing a new catalog entry?</decision>
  <context>
    D-11 (your own post-research decision): the missing perks/aliases must be fixed in the data before the verifier's 100% baseline is established (Plan 02-03), not allowlisted around. `data/manual/perk_aliases.json` is hand-curated — you approve every addition; an executor never writes it autonomously. Task 1 re-confirmed the 14 unresolved names and ran a broadened (non-jump-filtered) search of `data/derived/perk_directory.json` for plausible alias targets. Six of the 14 are Transformers "sub-instance" tech (Altmode, Decepticon Terrorize!, Energon, Energon Battle Pistol, Energon Melee Weapon, ROBOTS IN DISGUISE! - Medium Chassis) — the Transformers jump's own catalogued rows (`All Spark Chosen`, `Matrix of Leadership`, `Cybertronian Forge`, `Master Builder`, `Science! Engineering`, `Science! Mechanics`) show no obvious substring match to any of them, meaning these may genuinely be perks that exist in the story but were never added to the Unabridged List catalog `perk_directory.json` is built from — in which case a JSON alias alone cannot manufacture a canonical row that doesn't exist; that would require adding a row to `data/raw/Brocktons_Celestial_Forge_Reference.xlsx` and re-running `scripts/build_perk_directory.py`, which is a larger, separate data-curation task this plan does not attempt inline.

    Task 1's printed report (see its `<verify>` output) is the evidence for this decision, name by name.
  </context>
  <options>
    <option id="approve-aliases">
      <name>Approve the alias additions as I specify per-name</name>
      <pros>Closes each name with a plain, additive, reversible perk_aliases.json row (no derived-artifact schema change); matches D-11's stated primary fix mechanism.</pros>
      <cons>Only works for names where an existing canonical directory row is the correct target — you supply that mapping per name.</cons>
    </option>
    <option id="defer-uncataloged">
      <name>Mark any name with no plausible existing-row target as a documented, named exception for now</name>
      <pros>Does not block this phase or force a risky same-session xlsx edit; mirrors the existing ch104 precedent (a documented, human-approved exception, not a silently-passing test).</pros>
      <cons>Plan 02-03's corpus baseline will need to encode this exact, named exception explicitly (chapter + perk name + your rationale) rather than reaching literal 100% with zero exceptions.</cons>
    </option>
    <option id="mixed">
      <name>Mixed: approve aliases for some names, defer others</name>
      <pros>Matches reality — some names may resolve to an existing row, others may not.</pros>
      <cons>Requires you to go through the list once, per-name.</cons>
    </option>
  </options>
  <resume-signal>For each of the 14 names in Task 1's report: reply with either the canonical directory-row name to alias it to, or "defer" with a one-line reason. Reply "approved as proposed" only if Task 1's report already contains a single unambiguous candidate per name you're happy with.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply Dre's approved aliases, regenerate perk_directory.json, and re-verify resolution</name>
  <read_first>
    data/manual/perk_aliases.json (exact existing shape: flat `{canonical: [alias, ...]}`, no metadata wrapper — confirmed via direct read, 23 canonical keys currently);
    scripts/build_perk_directory.py (`main()` :618 — the regeneration entrypoint, reads `data/raw/Brocktons_Celestial_Forge_Reference.xlsx` + `data/manual/perk_aliases.json`, writes `data/derived/perk_directory.json`)
  </read_first>
  <files>data/manual/perk_aliases.json, data/derived/perk_directory.json</files>
  <action>
    For every name Dre approved in Task 2's checkpoint response, add an entry to `data/manual/perk_aliases.json` in the existing flat shape (`"<canonical directory row name>": ["<raw curated perk_name string>", ...]`) — additive only, never remove or restructure an existing key. Do not touch any name Dre marked "defer". Re-run `.venv/bin/python scripts/build_perk_directory.py` to regenerate `data/derived/perk_directory.json` through the existing pipeline (never hand-edit the derived file). Re-run Task 1's measurement script (or an equivalent fresh invocation) to confirm the approved names now resolve. If any approved name STILL fails to resolve after the alias addition (indicating an alias-only fix was not actually sufficient for that name), STOP — do not mark this task done, and surface a new checkpoint to Dre naming exactly which approved name still fails and why, rather than silently accepting the gap or loosening the resolver (D-08). If Dre marked any names "defer" in Task 2, record those explicitly (chapter, perk name, jump, constellation, Dre's stated reason) in this plan's SUMMARY.md so Plan 02-03 can encode them as a named, non-fatal exception in the corpus-baseline test rather than reaching them for the first time mid-Plan-02-03.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_perk_directory.py && .venv/bin/python -c "
import sys
sys.path.insert(0, 'scripts')
from perk_name_resolver import build_directory_match_index
import json
directory = json.loads(open('data/derived/perk_directory.json').read())['perks']
idx = build_directory_match_index(directory)
print('perk_directory.json regenerated and DirectoryMatchIndex builds cleanly:', len(directory), 'rows')
"</automated>
  </verify>
  <acceptance_criteria>
    `data/manual/perk_aliases.json` contains only additive new entries for Dre-approved names (existing 23 canonical keys unchanged); `data/derived/perk_directory.json` regenerates cleanly via `scripts/build_perk_directory.py` with no errors; a fresh resolution measurement shows every Dre-approved name now resolving with `jump=None`; any Dre-deferred names are explicitly listed, with rationale, in this plan's SUMMARY.md.
  </acceptance_criteria>
  <done>All Dre-approved perk names resolve through the unmodified ladder; `perk_directory.json` was regenerated through the pipeline, never hand-edited; any deferred names are named and explained for Plan 02-03 to consume, not silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor -> hand-curated data files | `data/manual/perk_aliases.json` and (transitively, via chapter_roll_overrides.json cross-reference) `data/manual/chapter_roll_overrides.json` are hand-curated; only Dre-approved writes may land |
| Task 1's measurement script -> repo state | Read-only against all inputs; never mutates anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | `data/manual/perk_aliases.json` | high | mitigate | Task 3 only writes entries Dre explicitly approved in Task 2's checkpoint; Task 1 and Task 2 perform zero writes; the plan's `autonomous: false` frontmatter and blocking checkpoint gate enforce this at the workflow level |
| T-02-07 | Tampering | `data/derived/perk_directory.json` | medium | mitigate | Task 3 regenerates this file exclusively via `scripts/build_perk_directory.py` — no direct JSON edit is ever specified or permitted |
| T-02-08 | Repudiation | A residual unresolved name silently treated as "fixed" | high | mitigate | Task 3's action explicitly halts and creates a new checkpoint if a Dre-approved name still fails to resolve post-fix, rather than marking the task done |

</threat_model>

<verification>
After Task 3, run the perk-resolution measurement one final time and confirm: (a) every Dre-approved name resolves, (b) any deferred names are explicitly enumerated with rationale in the SUMMARY, (c) `data/manual/perk_aliases.json`'s existing 23 canonical keys are unchanged (only additive rows), (d) `.venv/bin/python scripts/build_perk_directory.py` exits 0.
</verification>

<success_criteria>
Every perk-name gap Dre chose to fix now resolves through the unmodified `perk_name_resolver.py` ladder; every gap Dre chose to defer is named and explained, ready for Plan 02-03 to encode as a documented exception rather than discover mid-baseline-run; `data/manual/perk_aliases.json` was never written without Dre's approval.
</success_criteria>

<output>
Create `.planning/workstreams/curation/phases/02-mechanical-verifier/02-02-SUMMARY.md` when done
</output>
