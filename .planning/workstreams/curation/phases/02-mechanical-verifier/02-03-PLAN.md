---
phase: 02-mechanical-verifier
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - scripts/mechanical_verifier.py
  - tests/test_mechanical_verifier.py
autonomous: true
requirements: [CINF-03]

must_haves:
  truths:
    - "scripts/mechanical_verifier.py's verify_chapter(), looped over all 118 hand-curated chapters in data/manual/chapter_roll_overrides.json against the real epub, reports zero `fail` outcomes (`no_evidence` permitted per D-05, and any Plan-02-02-documented, Dre-approved exception permitted and visibly logged) — success criterion 1."
    - "The corpus-baseline test (test_verifier_passes_full_hand_curated_corpus) skips with an explicit reason when data/raw/Brocktons_Celestial_Forge.epub is absent, but this plan's own phase-gate run executes it for real, with the epub present, and records the pass/no_evidence/fail counts."
    - "The CLI (scripts/mechanical_verifier.py's __main__ block) writes a JSON report and is NOT wired into scripts/pipeline.py and NOT manifest-registered (D-09) — it is a QA instrument invoked out-of-band."
    - "Running the full test suite after this plan shows no NEW failures beyond the 5 known-accepted baseline (4 in tests/test_forge_curator.py, 1 in tests/test_roll_ordinal_contract.py::test_chapter_55_1_source_roll_six_borrows_first_56_prediction)."
  artifacts:
    - path: "scripts/mechanical_verifier.py"
      provides: "verify_chapter() aggregation + CLI (parse_args/main) writing a JSON report to data/derived/mechanical_verification_report.json by default"
      exports: ["verify_chapter", "parse_args", "main"]
    - path: "tests/test_mechanical_verifier.py"
      provides: "test_verifier_passes_full_hand_curated_corpus — the D-10 corpus-wide baseline test, skip-if-epub-absent"
  key_links:
    - from: "tests/test_mechanical_verifier.py"
      to: "scripts/mechanical_verifier.py"
      via: "corpus-baseline test imports verify_chapter directly and loops all 118 chapter_roll_overrides.json entries"
      pattern: "from scripts.mechanical_verifier import verify_chapter"
    - from: "scripts/mechanical_verifier.py (CLI)"
      to: "data/derived/mechanical_verification_report.json"
      via: "main() writes the aggregated pass/no_evidence/fail report; not manifest-registered, not pipeline-wired (D-09)"
      pattern: "mechanical_verification_report"

  prohibitions:
    - requirement_id: CINF-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit data/manual/chapter_roll_overrides.json to make the corpus-baseline test pass — a hand-curated-chapter failure means the verifier is wrong, never the corpus (D-08)."
    - requirement_id: CINF-03
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT introduce any fuzzy/similarity/edit-distance matching path to raise the pass rate — any residual failure is either fixed at its true source (verifier bug) or routed to an explicit, named, Dre-approved exception, never fuzzy-accepted."
    - requirement_id: CINF-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT wire scripts/mechanical_verifier.py into scripts/pipeline.py or the runtime manifest — it is explicitly a QA instrument, not a pipeline input (D-09)."
---

<objective>
Expand the tracer's `verify_roll()` into `verify_chapter()` (loops every roll in a chapter, aggregates counts), add the CLI wrapper that writes a JSON report, and land the D-10 corpus-wide baseline test that proves success criterion 1 — 100% over all 118 hand-curated chapters — for real, against the real epub.

Purpose: this is where the phase's actual success bar gets measured. Everything in Plan 02-01/02-02 exists to make this run meaningful: if it passes, agent output (Phase 3) has a real, proven bar to clear; if it fails, per ROADMAP's own principle, the verifier — not the corpus — is wrong, and must be fixed here before the phase closes.

Output: `scripts/mechanical_verifier.py` extended with `verify_chapter()` + CLI; `tests/test_mechanical_verifier.py` extended with the skip-guarded corpus-wide baseline test; a recorded pass/no_evidence/fail count for the SUMMARY.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/phases/02-mechanical-verifier/02-CONTEXT.md
@.planning/workstreams/curation/phases/02-mechanical-verifier/02-RESEARCH.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/deferred-items.md
@scripts/build_exemplar_index.py
@.planning/workstreams/curation/phases/02-mechanical-verifier/02-01-SUMMARY.md
@.planning/workstreams/curation/phases/02-mechanical-verifier/02-02-SUMMARY.md

<interfaces>
From `scripts/mechanical_verifier.py` (built in Plan 02-01 — do not re-derive, use exactly):

`verify_roll(chapter_num: str, roll_index: int, roll: dict, *, prose_loader, directory_index) -> dict` returns `{"chapter_num": str, "roll_index": int, "status": "pass"|"fail"|"no_evidence", "issues": [{"code": str, "severity": "error", "message": str}, ...]}`.

`prose_loader` is a caller-supplied callable `chapter_num -> (chapter_html, word_index)`; `directory_index` is a `perk_name_resolver.DirectoryMatchIndex` built once via `build_directory_match_index`.

`POSITION_TOLERANCE_WORDS = 50` — the named tolerance constant, already defined.

`cp_word_index.py` (Plan 02-01) exports `_chapter_word_index(chapter_html, section_classifications, chapter_num) -> list[int]` and `load_chapter_html(epub_path, epub_href) -> str`.
</interfaces>

<known_accepted_baseline>
5 pre-existing pytest failures predate this phase (documented in Phase 1's deferred-items.md): 4 in `tests/test_forge_curator.py` (`test_stats_click_selects_actual_visible_roll_line`, `test_stats_click_refocuses_prose_for_motion_and_space_chords`, `test_stats_click_jumps_prose_to_selected_roll_location`, `test_click_roll_then_space_p_opens_perk_picker_for_selected_roll`) and 1 in `tests/test_roll_ordinal_contract.py::test_chapter_55_1_source_roll_six_borrows_first_56_prediction`. `scripts/verify.py` exits 1 for this reason alone. This plan's Task 3 judges by "no NEW failures," not absolute green.
</known_accepted_baseline>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verify_chapter() aggregation and the CLI report writer</name>
  <read_first>
    scripts/mechanical_verifier.py (as written in Plan 02-01 — `verify_roll`, `POSITION_TOLERANCE_WORDS`);
    scripts/build_exemplar_index.py (`parse_args` :280-306, `main` :309-335 — the argparse + stdout-summary CLI shape to mirror; note it does NOT use `_common.write_validated_json`, it writes plain `json.dumps` — mirror that, not the schema-validated pattern, since D-09 explicitly frames this report as a convenience, not a pipeline artifact);
    scripts/data_paths.py (`RAW`/`MANUAL`/`DERIVED`);
    data/derived/chapters.json (`chapters[]` — `chapter_num`/`epub_href` map needed to build the CLI's `prose_loader`)
  </read_first>
  <behavior>
    - `verify_chapter("67", override_entry, prose_loader=..., directory_index=...)` returns a per-chapter result with `rolls` = a list of 5 `verify_roll` results (chapter 67 has 5 roll entries) and `counts` reflecting however many of those are pass/fail/no_evidence.
    - The CLI, run with no arguments against the real repo data, writes `data/derived/mechanical_verification_report.json` and prints a stdout summary with `pass`/`no_evidence`/`fail` totals across all chapters in `chapter_roll_overrides.json` (not just the hand-curated 118 — the CLI is a general-purpose instrument Phase 3 can also point at agent-curated chapters later).
  </behavior>
  <files>scripts/mechanical_verifier.py</files>
  <action>
    Add `verify_chapter(chapter_num: str, override_entry: dict, *, prose_loader, directory_index) -> dict` to `scripts/mechanical_verifier.py`: loop `enumerate(override_entry.get("rolls") or [])`, call `verify_roll(chapter_num, roll_index, roll, prose_loader=prose_loader, directory_index=directory_index)` for each, collect the per-roll results into a `rolls` list, and tally a `counts` dict (`{"pass": n, "fail": n, "no_evidence": n}`). Return `{"chapter_num": chapter_num, "rolls": rolls, "counts": counts}`.

    Add the CLI as an `if __name__ == "__main__":` block plus `parse_args(argv=None)` and `main(argv=None)` functions, mirroring `build_exemplar_index.py`'s structure (argparse with `--overrides` defaulting to `data/manual/chapter_roll_overrides.json`, `--chapters` defaulting to `data/derived/chapters.json`, `--perk-directory` defaulting to `data/derived/perk_directory.json`, `--perk-aliases` defaulting to `data/manual/perk_aliases.json`, `--epub` defaulting to `data_paths.RAW / "Brocktons_Celestial_Forge.epub"`, `--output` defaulting to `data/derived/mechanical_verification_report.json`). `main()` loads all inputs, builds a `chapter_num -> epub_href` dict from `chapters.json`, builds a lazy-caching `prose_loader` closure (cache `{chapter_num: (html, word_index)}`, populate on first access via `cp_word_index.load_chapter_html` + `cp_word_index._chapter_word_index`), builds `directory_index` via `build_directory_match_index(perk_directory_doc["perks"], load_perk_aliases())`, loops every chapter key in the overrides doc's `chapter_roll_overrides` dict calling `verify_chapter`, aggregates phase-wide counts, writes the full per-chapter results as plain `json.dumps(payload, indent=2, ensure_ascii=False)` to the output path (no `_common.write_validated_json`/schema call — this artifact is deliberately not schema-registered per D-09), and prints a stdout summary (`wrote <path>` then `pass: N`, `no_evidence: N`, `fail: N` lines, mirroring `build_exemplar_index.py:323-332`'s print pattern).
  </action>
  <verify>
    <automated>.venv/bin/python scripts/mechanical_verifier.py && test -f data/derived/mechanical_verification_report.json && .venv/bin/python -c "import json; d = json.loads(open('data/derived/mechanical_verification_report.json').read()); assert 'chapters' in d or isinstance(d, dict); print('report shape ok')"</automated>
  </verify>
  <acceptance_criteria>
    Running `.venv/bin/python scripts/mechanical_verifier.py` with no arguments against the real repo exits 0, writes `data/derived/mechanical_verification_report.json`, and prints `pass:`/`no_evidence:`/`fail:` count lines to stdout. `grep -n "write_validated_json\|pipeline.py\|refresh_current_runtime_manifest" scripts/mechanical_verifier.py` returns nothing (confirms D-09's not-pipeline-wired, not-manifest-registered, not-schema-registered constraints).
  </acceptance_criteria>
  <done>`verify_chapter()` and the CLI both exist and run cleanly against the real repo data, producing a JSON report and a stdout summary; the module remains unregistered with the pipeline/manifest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The D-10 corpus-wide baseline test</name>
  <read_first>
    scripts/mechanical_verifier.py (as extended in Task 1 of this plan — `verify_chapter`);
    data/manual/chapter_roll_overrides.json (the 118-chapter `chapter_roll_overrides` dict to loop);
    .planning/workstreams/curation/phases/02-mechanical-verifier/02-02-SUMMARY.md (read this FIRST — it records whether Plan 02-02 closed all 14 perk-name gaps or left any as a Dre-approved, documented residual; this determines whether Task 2's assertion is a plain zero-fail check or must exempt named residual pairs)
  </read_first>
  <behavior>
    - With the real epub present, looping `verify_chapter` over all 118 entries in `chapter_roll_overrides.json`'s `chapter_roll_overrides` dict produces zero `fail` statuses across all rolls (permitting `no_evidence`), MINUS any exact `(chapter_num, perk_name)` pair Plan 02-02's SUMMARY explicitly documents as a Dre-approved residual.
    - When `data/raw/Brocktons_Celestial_Forge.epub` is absent, the test reports SKIPPED with an explicit reason, never ERROR and never a false PASS.
  </behavior>
  <files>tests/test_mechanical_verifier.py</files>
  <action>
    Add a module-level `EPUB_AVAILABLE = (RAW / "Brocktons_Celestial_Forge.epub").exists()` constant (via `from data_paths import RAW`, matching this file's existing import style from Plan 02-01) and decorate the new test with `@pytest.mark.skipif(not EPUB_AVAILABLE, reason="epub is gitignored; corpus baseline needs local source")`. Write `test_verifier_passes_full_hand_curated_corpus`: build the same `prose_loader`/`directory_index` construction Task 1's CLI `main()` uses (reuse the CLI's own helper functions if Task 1 factored them out separately from `main()` itself — if not, inline the equivalent construction here rather than shelling out to the CLI), loop every chapter key in `data/manual/chapter_roll_overrides.json`'s `chapter_roll_overrides` dict, call `verify_chapter` for each, and collect every `fail`-status roll's issues across the whole corpus. Read `.planning/workstreams/curation/phases/02-mechanical-verifier/02-02-SUMMARY.md`: if it documents zero deferred perk-name residuals, assert the collected fail count is exactly 0. If it documents one or more `(chapter_num, perk_name)` residual pairs, define a small, explicit, named module-level constant (e.g. a set of tuples) with an inline comment citing the 02-02 SUMMARY and Dre's stated rationale for each, and assert the fail count is exactly 0 AFTER excluding failures whose `perk_unresolved` issue names exactly one of those documented pairs — every excluded pair must still appear in the printed/collected results (never silently absorbed out of the report, only out of the pass/fail assertion). Print the final `{pass, no_evidence, fail}` counts so they can be copied into this plan's SUMMARY.md.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_mechanical_verifier.py -k corpus_baseline -x -q -s</automated>
  </verify>
  <acceptance_criteria>
    `.venv/bin/python -m pytest tests/test_mechanical_verifier.py -k corpus_baseline -x -q -s` passes with the real epub present (not skipped), and its printed output shows the exact `{pass, no_evidence, fail}` counts with `fail == 0` (after any Dre-approved, explicitly-named exclusion documented in 02-02's SUMMARY). Running the same command with `BCF_DATA_DIR` pointed at a directory whose `raw/` subfolder lacks the epub reports SKIPPED, not ERROR.
  </acceptance_criteria>
  <done>The corpus-wide baseline test exists, is skip-guarded correctly, and passes for real against the real epub with the recorded counts; any documented residual exception is named, cited, and still visible in the report.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression check and phase-closing verification</name>
  <read_first>
    .planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/deferred-items.md (the exact 5 known-accepted failure test IDs, quoted verbatim in this plan's `<known_accepted_baseline>` context block above)
  </read_first>
  <files>(none — verification only; results recorded in .planning/workstreams/curation/phases/02-mechanical-verifier/02-03-SUMMARY.md)</files>
  <action>
    Run the full pytest suite (`.venv/bin/python -m pytest -q`) and `scripts/data_release.py check-derived`. Confirm every `FAILED` line in the pytest output matches one of the 5 known-accepted test IDs listed in `<known_accepted_baseline>` — any additional failing test name is a genuine NEW regression this phase introduced and must be fixed before this task is done, not documented away. Record the final `{pass, no_evidence, fail}` corpus-baseline counts (from Task 2) and the full-suite pass/fail totals in this plan's SUMMARY.md.
  </action>
  <verify>
    <automated>OUT=$(mktemp) && .venv/bin/python -m pytest -q 2>&1 | tee "$OUT"; NEW=$(grep '^FAILED' "$OUT" | grep -Ev "test_forge_curator\.py::(test_stats_click_selects_actual_visible_roll_line|test_stats_click_refocuses_prose_for_motion_and_space_chords|test_stats_click_jumps_prose_to_selected_roll_location|test_click_roll_then_space_p_opens_perk_picker_for_selected_roll)|test_roll_ordinal_contract\.py::test_chapter_55_1_source_roll_six_borrows_first_56_prediction"); if [ -z "$NEW" ]; then echo "OK: no new failures"; else echo "NEW FAILURES:"; echo "$NEW"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    The full pytest suite's `FAILED` lines are a subset of exactly the 5 known-accepted test IDs (no new failures introduced by this phase's work). `scripts/data_release.py check-derived` reports "local derived data ok" (or the same pre-existing informational lines Phase 1 documented, e.g. the `multi_grab: ch 95 override drops paid perk(s)` note), with no new error.
  </acceptance_criteria>
  <done>Full-suite regression confirmed clean relative to the known-accepted baseline; the corpus-baseline counts and full-suite totals are recorded in 02-03-SUMMARY.md for the phase record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Corpus-baseline test -> hand-curated data | `data/manual/chapter_roll_overrides.json` is read-only input; the test must never propose editing it to pass |
| CLI report -> disk | Writes only structured pass/fail/reason data, never raw copyrighted prose |
| This plan's assertion -> phase gate | The 100% claim in the phase SUMMARY must be backed by a REAL run (epub present), not a skipped one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | The corpus-baseline test's pass/fail assertion | critical | mitigate | Task 2's action forbids editing `chapter_roll_overrides.json`; any exclusion of a specific `(chapter_num, perk_name)` pair must be sourced from Plan 02-02's SUMMARY (Dre-approved), named explicitly in a code comment, and still visible in the collected report — never a blanket loosening |
| T-02-10 | Repudiation | A skipped-not-failed corpus-baseline run being mistaken for a passing 100% baseline | high | mitigate | Task 2's acceptance criteria require the phase-gate run to execute with the epub present (not skipped), and Task 3 records the real counts in the SUMMARY |
| T-02-11 | Information Disclosure | `data/derived/mechanical_verification_report.json` | low | accept | The report contains reason codes/messages/positions derived from already-committed evidence-quote text conventions, never new raw prose; it is gitignored (`data/derived/*.json`) like every other derived artifact |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new package installs in this plan |

</threat_model>

<verification>
Run `.venv/bin/python -m pytest tests/test_mechanical_verifier.py -x -q -s` (all of it, including the corpus baseline) with the real epub present, then the full suite per Task 3. Confirm the CLI (`.venv/bin/python scripts/mechanical_verifier.py`) runs standalone and writes a report.
</verification>

<success_criteria>
`verify_chapter()` and the CLI exist and work against real data; the D-10 corpus-wide baseline test passes for real with recorded `{pass, no_evidence, fail}` counts (fail == 0 modulo any explicitly Dre-approved, named exception); the full test suite shows no new failures beyond the 5 known-accepted baseline; `scripts/mechanical_verifier.py` remains unregistered with the pipeline/manifest.
</success_criteria>

<output>
Create `.planning/workstreams/curation/phases/02-mechanical-verifier/02-03-SUMMARY.md` when done
</output>
