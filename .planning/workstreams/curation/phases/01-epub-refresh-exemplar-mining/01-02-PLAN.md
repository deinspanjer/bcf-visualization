---
phase: 01-epub-refresh-exemplar-mining
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - data/manual/chapter_roll_overrides.json
autonomous: false
requirements: [EPUB-02]

must_haves:
  truths:
    - "Running scripts/verify.py (git diff --check, data_release.py check-derived, full pytest) exits 0 against the force-regenerated derived-data tree from Plan 01-01."
    - "The manifest entry for perk_directory.json no longer reports a stale sha256 (scripts/data_release.py manifest has re-stamped it against the regenerated file)."
    - "The 24 previously-documented Track B test failures (test_chapter_alignment_fingerprints.py, test_data_package_contract.py, test_forge_curator.py, test_model_validation.py, test_roll_ordinal_contract.py, test_roll_position_invariants.py, test_web_data_contract.py — per .planning/workstreams/mobile-ux/phases/01-mobile-state-gesture-plumbing/deferred-items.md) no longer occur in a full, unfiltered pytest run."
  artifacts:
    - path: "data/derived/data_package.json"
      provides: "Manifest with fresh sha256 stamps for every data/derived/*.json file, including perk_directory.json"
  key_links:
    - from: "scripts/data_release.py manifest"
      to: "data/derived/data_package.json"
      via: "re-stamps sha256 for every _top_level_json_files() entry"
      pattern: "manifest|sha256"
    - from: "scripts/chapter_alignment.py check"
      to: "data/manual/chapter_roll_overrides.json[*]['_fingerprint']"
      via: "compares stored fingerprint to a fresh re-simulation per chapter"
      pattern: "_fingerprint|chapter_alignment"

  prohibitions:
    - statement: "MUST NOT run scripts/realign_chapters.py with --yes — every fingerprint re-stamp requires an explicit, per-chapter accept/skip/abort decision from Dre after reviewing the stored-vs-current predicted-roll diff."
      category: values
      status: unverified
      flagged: true
    - statement: "MUST NOT achieve a 'green' verify.py / pytest run by skipping, deleting, or marking failing tests xfail/skip instead of fixing the underlying staleness — a failure means investigate the real cause, not silence the signal."
      category: values
      status: unverified
      flagged: true
    - statement: "MUST NOT hand-patch any data/derived/*.json file directly (e.g. editing a stored sha256 or fingerprint value by hand) instead of legitimately regenerating it via the pipeline/manifest scripts."
      category: values
      status: unverified
      flagged: true
---

<objective>
Clear the residual staleness that the full pipeline force-regen (Plan 01-01) does not automatically fix on its own — a stale manifest sha256 stamp and any post-refresh chapter-alignment fingerprint drift — then run the phase's actual green-gate proof, `scripts/verify.py`, to completion.

Purpose: EPUB-02 requires the full pipeline to complete green: predicted rolls extend into new chapters (proven in Plan 01-01), previously curated chapters still validate, and `visualization_facts.json` rebuilds. This plan is the remaining half of that proof — re-stamping the manifest, handling any (expected rare-to-zero) chapter realignment, and running the actual `scripts/verify.py` gate.
Output: A green `scripts/verify.py` run (git diff --check + check-derived + full pytest, zero failures) against the Plan 01-01 force-regenerated derived-data tree.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/ROADMAP.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-CONTEXT.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/01-mobile-state-gesture-plumbing/deferred-items.md
@scripts/chapter_alignment.py
@scripts/realign_chapters.py
@scripts/verify.py
</context>

<interfaces>
<!-- scripts/chapter_alignment.py (already read in full) -->
check() -> list[Mismatch]                     # Mismatch(chapter_num, stored, current)
model_issues_by_chapter() -> dict[str, list[dict]]
chapters_overridden_but_unstamped() -> list[str]
fail_if_misaligned() -> None                  # raises SystemExit on any mismatch

<!-- scripts/realign_chapters.py CLI (already read in full) -->
python scripts/realign_chapters.py            # interactive: accept/skip/abort per chapter
python scripts/realign_chapters.py --yes      # FORBIDDEN in this plan (see prohibitions)

<!-- scripts/verify.py (already read in full) -->
verification_commands() -> [
  ["git", "diff", "--check"],
  [python, "scripts/data_release.py", "check-derived"],
  [python, "-m", "pytest"],
]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-stamp the manifest and check for post-refresh alignment drift</name>
  <files>data/derived/data_package.json</files>
  <action>
    Run `.venv/bin/python scripts/data_release.py manifest` to re-stamp sha256 hashes
    for every file in data/derived/ against the Plan 01-01 force-regenerated tree —
    this clears the "perk_directory has stale sha256" failure without any hand-edit,
    since `manifest` recomputes hashes from the actual on-disk files. Then run
    `.venv/bin/python scripts/chapter_alignment.py check` (read-only) to detect whether
    any of the 118 hand-curated chapters now has a predicted-roll shape that disagrees
    with its stamped `_fingerprint` — per RESEARCH.md, this is expected to be rare or
    zero (new chapters appended at the story's end should not shift earlier chapters'
    word counts/roll shapes), but must be checked, not assumed. Capture the check's
    exit code and any listed chapter numbers for Task 2.
  </action>
  <verify>
    <automated>test -f data/derived/data_package.json && python3 -c "import json; d=json.load(open('data/derived/data_package.json')); print('files' in d and 'perk_directory.json' in d.get('files', {}))"</automated>
  </verify>
  <done>
    scripts/data_release.py manifest has run successfully and re-written
    data/derived/data_package.json with fresh sha256 entries; the chapter_alignment
    check's result (clean, or a specific list of mismatched chapters) is recorded for
    Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm chapter-alignment status and resolve any drift</name>
  <what-built>
    Task 1 ran `scripts/chapter_alignment.py check` against the freshly regenerated
    derived-data tree.
  </what-built>
  <how-to-verify>
    If Task 1 reported ZERO mismatches: this checkpoint auto-clears — record "0
    mismatches, no re-alignment needed" and proceed directly to Task 3 without pausing.

    If Task 1 reported ONE OR MORE mismatched chapters: STOP. For each mismatched
    chapter, run `.venv/bin/python scripts/realign_chapters.py` INTERACTIVELY (never
    with `--yes` — see must_haves.prohibitions) and present Dre with the printed
    stored-vs-current predicted-roll shape diff for that chapter. Dre must explicitly
    choose accept / skip / abort per chapter before any `_fingerprint` is re-stamped.
    Do not bulk-accept on Dre's behalf.
  </how-to-verify>
  <resume-signal>Type "no mismatches, continuing" (auto-clear case) or, after realign_chapters.py's interactive session completes, describe the outcome per chapter (accepted/skipped/aborted)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the full verify.py green gate</name>
  <files>none (verification only)</files>
  <action>
    Run `.venv/bin/python scripts/verify.py` (git diff --check, then
    scripts/data_release.py check-derived, then a full unfiltered
    `.venv/bin/python -m pytest`) to completion. This is the actual EPUB-02 acceptance
    proof — a non-zero exit or any test failure means the phase is not done; investigate
    and resolve the real cause (do not filter, skip, or delete a failing test to force a
    green result — see must_haves.prohibitions). Confirm specifically that none of the
    previously-documented 24 Track B failures (test_chapter_alignment_fingerprints.py,
    test_data_package_contract.py, test_forge_curator.py, test_model_validation.py,
    test_roll_ordinal_contract.py, test_roll_position_invariants.py,
    test_web_data_contract.py) appear in the failure output.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/verify.py</automated>
  </verify>
  <done>
    scripts/verify.py exits 0. The full pytest run reports zero failures, including
    zero occurrences of any of the 24 previously-documented Track B failures.
  </done>
</task>

</tasks>

## Flagged Assumptions (unresolved edge-probe items — not silently dropped)

- **EPUB-02 (unclassified edge-probe category):** the automated edge-probe classified
  EPUB-02 as "unclassified — review manually" and, per D-06, this stays explicitly
  **unresolved** rather than being auto-backstopped. The natural acceptance surface is
  ROADMAP.md's Phase 1 Success Criterion 2 ("A full pipeline re-run completes green:
  predicted rolls extend into the new chapters, all previously curated chapters still
  validate, and visualization_facts.json rebuilds") — Task 3's `scripts/verify.py` run
  is this plan's concrete proof of that criterion, but the edge-probe's own
  classification gap is recorded here rather than silently dropped.

## Artifacts this plan produces

- No new source files. Re-executes existing scripts (`data_release.py manifest`,
  `chapter_alignment.py check`, optionally `realign_chapters.py` interactively,
  `verify.py`) against the Plan 01-01 output tree.
- `data/derived/data_package.json` — re-stamped manifest (fresh sha256 for every
  `data/derived/*.json` file)
- `data/manual/chapter_roll_overrides.json` — zero or more `_fingerprint` re-stamps,
  only via Dre's explicit interactive accept per chapter (never `--yes`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Regenerated derived data -> manifest | data_release.py manifest recomputes hashes from on-disk files only; no external input |
| Realignment decision -> chapter_roll_overrides.json | Only an explicit, per-chapter interactive human decision may re-stamp a `_fingerprint` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Tampering | scripts/realign_chapters.py fingerprint re-stamping | high | mitigate | Never run with `--yes`; each chapter requires an explicit accept/skip/abort choice at the Task 2 checkpoint, with the diff shown before any stamp |
| T-01-02-02 | Repudiation | "green" claimed without the real pytest output | medium | mitigate | Task 3's `<verify>` runs the actual `scripts/verify.py` command; acceptance requires the real exit code and output, not an assertion |
| T-01-02-03 | Denial of Service (of trust) | Failing tests silently skipped/deleted to force green | high | mitigate | Explicit prohibition (must_haves.prohibitions); Task 3 explicitly checks for the 24 named Track B failures by name |
| T-01-02-SC | Tampering | Supply chain (package installs) | n/a | mitigate | No new pip/npm/cargo packages introduced by this plan |
</threat_model>

<verification>
Automated:
- `.venv/bin/python scripts/data_release.py manifest` runs successfully.
- `.venv/bin/python scripts/chapter_alignment.py check` result recorded (clean or mismatch list).
- `.venv/bin/python scripts/verify.py` exits 0.

Manual: Task 2's checkpoint is resolved (auto-cleared on zero mismatches, or Dre's per-chapter accept/skip/abort recorded).
</verification>

<success_criteria>
- Manifest sha256 stamps are fresh (no stale perk_directory entry).
- Any chapter-alignment drift is resolved only via Dre's explicit interactive decision.
- `scripts/verify.py` exits 0 with a full, unfiltered pytest run reporting zero failures.
</success_criteria>

<output>
Create `.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-02-SUMMARY.md` when done
</output>
