---
phase: 01-epub-refresh-exemplar-mining
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - .planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/corpus-analysis-report.md
autonomous: true
requirements: [CINF-02]

must_haves:
  truths:
    - "A human-readable corpus-analysis-report.md exists documenting the corpus's observed evidence-quote patterns, roll-shape distribution (including multi-grab frequency), and perk-link/naming conventions, sourced from data/derived/exemplar_index.json's statistics block."
    - "The report quotes example text only via evidence_quotes already present in data/manual/chapter_roll_overrides.json — no epub prose is read or quoted."
    - "scripts/verify.py exits 0 as the phase's final closing gate, confirming Plan 01-03's pipeline.py edit did not regress the green state established in Plan 01-02."
  artifacts:
    - path: ".planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/corpus-analysis-report.md"
      provides: "Human-readable corpus characterization for Phase 3 prompt design (D-05)"
      min_lines: 40
  key_links:
    - from: "corpus-analysis-report.md"
      to: "data/derived/exemplar_index.json['statistics']"
      via: "report's numeric tables are sourced from the built index's statistics block, not recomputed independently"
      pattern: "statistics|roll_shape_distribution|evidence_quote_stats"

  prohibitions:
    - statement: "MUST NOT quote or paraphrase any epub prose text in corpus-analysis-report.md beyond text already present verbatim in an existing chapter_roll_overrides.json evidence_quotes[].text field."
      category: privacy
      status: unverified
      flagged: true
    - statement: "MUST NOT report the phase as green/complete without pasting or observing scripts/verify.py's actual exit code and output — never assert success without running the command."
      category: values
      status: unverified
      flagged: true
---

<objective>
Author the human-readable corpus-analysis report (D-05's second output) from the exemplar index's already-computed statistics, then run the phase's final closing verification to confirm nothing regressed across all four plans.

Purpose: Satisfies ROADMAP Phase 1 Success Criterion 4 ("The index documents the corpus's observed evidence-quote patterns, roll-shape distribution, and perk-link conventions") — this report is what Phase 3's prompt design will read to understand the corpus it is mining exemplars from.
Output: `corpus-analysis-report.md` and a final green `scripts/verify.py` run closing out the phase.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/ROADMAP.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-CONTEXT.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-RESEARCH.md
</context>

<interfaces>
<!-- data/derived/exemplar_index.json shape, produced by Plan 01-03 -->
{
  "schema_version": 1,
  "exemplars": [{"chapter_num": ..., "regime_tags": [...], "is_boundary": ..., "rolls": [...]}],
  "statistics": {
    "chapter_count": ..., "regime_distribution": {...}, "boundary_chapters": [...],
    "roll_shape_distribution": {...}, "display_position_policy_distribution": {...},
    "evidence_quote_stats": {"min": ..., "max": ..., "mean": ..., "total": ...},
    "cp_ledger_checkpoint_usage_count": ...
  }
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the human-readable corpus-analysis report</name>
  <files>.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/corpus-analysis-report.md</files>
  <action>
    Read data/derived/exemplar_index.json's "statistics" block (built in Plan 01-03)
    and write corpus-analysis-report.md with these sections: (1) Overview — total
    curated chapter count, regime distribution table; (2) Roll-shape distribution —
    perks-per-roll frequency table including multi-grab frequency (rolls with 2+
    perks); (3) Evidence-quote patterns — count/length/position-policy distribution
    (display_position_policy_distribution), min/max/mean evidence quotes per roll; (4)
    cp_ledger_checkpoint usage — report the exact count from the index's statistics
    (do not hardcode a number from RESEARCH.md's earlier snapshot — recompute-derived
    figures only); (5) Perk-link/naming conventions — how the "perks" array's string
    values are expected to resolve against perk_directory.json names (reference the
    existing convention, do not invent a new one); (6) Regime-boundary handling — the
    ch97 dual-tag example (regime_tags [2, 3], is_boundary true), explained for a
    Phase 3 prompt-design reader. Where an illustrative quote is useful, copy the
    verbatim text of an existing evidence_quotes[].text entry from
    data/manual/chapter_roll_overrides.json — never read or quote the epub file
    itself.
  </action>
  <verify>
    <automated>test -f .planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/corpus-analysis-report.md &amp;&amp; grep -ci "regime" .planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/corpus-analysis-report.md</automated>
  </verify>
  <done>
    corpus-analysis-report.md exists, covers all six required sections, and every
    quoted example traces to an existing chapter_roll_overrides.json evidence_quotes
    entry rather than the epub.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final phase-closing verification</name>
  <files>none (verification only)</files>
  <action>
    Run `.venv/bin/python scripts/verify.py` (git diff --check, check-derived, full
    pytest) one more time as the phase's closing gate — this confirms Plan 01-03's
    scripts/pipeline.py edit and the new exemplar_index.json artifact did not regress
    the green state Plan 01-02 established. Additionally confirm
    data/derived/exemplar_index.json is present in data/derived/data_package.json's
    manifest "files" list with a schema_version recorded, closing out CINF-02's
    manifest-registration requirement (D-03).
  </action>
  <verify>
    <automated>.venv/bin/python scripts/verify.py &amp;&amp; python3 -c "import json,sys; d=json.load(open('data/derived/data_package.json')); sys.exit(0 if 'exemplar_index.json' in d.get('files', {}) else 1)"</automated>
  </verify>
  <done>
    scripts/verify.py exits 0 and data/derived/exemplar_index.json is confirmed present
    in the manifest's files list — the phase's full success criteria (ROADMAP Phase 1,
    criteria 1-4) are all satisfied and observable.
  </done>
</task>

</tasks>

## Flagged Assumptions (carried from Plan 01-03 — not silently dropped)

- **CINF-02 (unclassified edge-probe category):** as recorded in Plan 01-03, CINF-02's
  edge-probe classification stays explicitly unresolved rather than auto-backstopped.
  This plan's Task 1 is the concrete proof of ROADMAP Success Criterion 4 (corpus
  documentation); Task 2's manifest check plus Plan 01-03's retrieval tests are the
  concrete proof of Success Criterion 3. The edge-probe's own classification gap is
  recorded here, not silently dropped.

## Artifacts this plan produces

- `.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/corpus-analysis-report.md` — new file, six sections (Overview, Roll-shape distribution, Evidence-quote patterns, cp_ledger_checkpoint usage, Perk-link/naming conventions, Regime-boundary handling)
- No new scripts or tests — this plan only authors documentation and re-runs existing verification.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Already-computed statistics -> markdown report | Report authoring reads only data/derived/exemplar_index.json and data/manual/chapter_roll_overrides.json; never the epub |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | Epub prose text leaking into the committed corpus-analysis-report.md | high | mitigate | Report copies text only from existing evidence_quotes[].text fields; epub file is never opened for this task (must_haves.prohibitions) |
| T-01-04-02 | Repudiation | Phase reported "done" without observing the real scripts/verify.py output | medium | mitigate | Task 2's <verify> runs the actual command; must_haves.prohibitions forbids asserting success without running it |
| T-01-04-SC | Tampering | Supply chain (package installs) | n/a | mitigate | No new pip/npm/cargo packages introduced by this plan |
</threat_model>

<verification>
Automated:
- `test -f corpus-analysis-report.md` and it references "regime" (sanity check it covers regime-boundary handling).
- `.venv/bin/python scripts/verify.py` exits 0.
- `exemplar_index.json` present in `data_package.json`'s manifest files list.
</verification>

<success_criteria>
- corpus-analysis-report.md documents evidence-quote patterns, roll-shape distribution, and perk-link conventions (ROADMAP Success Criterion 4).
- scripts/verify.py is green as the phase's final closing gate, confirming all four plans compose cleanly.
</success_criteria>

<output>
Create `.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-04-SUMMARY.md` when done
</output>