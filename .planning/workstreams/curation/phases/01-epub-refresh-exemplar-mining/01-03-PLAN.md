---
phase: 01-epub-refresh-exemplar-mining
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - scripts/build_exemplar_index.py
  - scripts/query_exemplars.py
  - scripts/pipeline.py
  - tests/test_build_exemplar_index.py
  - tests/test_query_exemplars.py
  - tests/test_pipeline.py
autonomous: true
requirements: [CINF-02]

must_haves:
  truths:
    - "data/derived/exemplar_index.json exists, carries schema_version, and every one of the 118 hand-curated chapters in data/manual/chapter_roll_overrides.json appears in its exemplars list with the correct regime tag(s)."
    - "Chapter 97 (the documented mid-chapter regime transition) is tagged with regime_tags containing both 2 and 3, and is_boundary: true."
    - "scripts.query_exemplars.retrieve() never returns an exemplar whose regime_tags does not intersect the requested regime — enforced by a passing automated test, not manual inspection."
  artifacts:
    - path: "scripts/build_exemplar_index.py"
      provides: "build_exemplar_index main()/pure functions producing data/derived/exemplar_index.json"
      exports: ["main", "build_index"]
    - path: "scripts/query_exemplars.py"
      provides: "retrieve() — deterministic same-regime exemplar retrieval"
      exports: ["retrieve"]
    - path: "data/derived/exemplar_index.json"
      provides: "Regime-tagged exemplar corpus + machine-readable characterization statistics (D-05)"
      contains: "schema_version"
  key_links:
    - from: "scripts/build_exemplar_index.py"
      to: "scripts/regime_simulator.regime_for_chapter"
      via: "import regime_for_chapter (never re-derived — no parallel implementation)"
      pattern: "from regime_simulator import regime_for_chapter|from scripts.regime_simulator import regime_for_chapter"
    - from: "scripts/pipeline.py"
      to: "scripts/build_exemplar_index.py"
      via: "new Step wired into build_steps() and TARGET_FINAL_STEPS['data']"
      pattern: "build_exemplar_index"
    - from: "scripts/query_exemplars.retrieve"
      to: "data/derived/exemplar_index.json"
      via: "reads the already-loaded index dict's exemplars[].regime_tags"
      pattern: "regime_tags"

  prohibitions:
    - statement: "MUST NOT read the epub file (data/raw/Brocktons_Celestial_Forge.epub) or any epub prose text from any new script or test in this plan — evidence text comes ONLY from already-committed chapter_roll_overrides.json evidence_quotes."
      category: privacy
      status: unverified
      flagged: true
    - statement: "MUST NOT define a second regime-computation function (e.g. a local regime_for_chapter re-implementation inside build_exemplar_index.py) — always import scripts.regime_simulator.regime_for_chapter."
      category: values
      status: unverified
      flagged: true
    - statement: "MUST NOT use any chapter word-count field other than chapter_facts.json's cp_earning_word_count if any word-count figure is computed for the index or its statistics."
      category: values
      status: unverified
      flagged: true
---

<objective>
Build the regime-tagged exemplar index (`data/derived/exemplar_index.json`) mined from the 118-chapter hand-curated corpus, a deterministic same-regime retrieval function, and wire both into the existing pipeline/manifest machinery — satisfying CINF-02's index-and-retrieval success criterion (ROADMAP Success Criterion 3).

Purpose: Phase 2 (verifier) and Phase 3 (agent curation) both consume this index's schema as a downstream contract; regime tagging must exist before retrieval logic is built (per the phase's own ordering note), sourced only from the pipeline's existing regime computation — never re-derived.
Output: `scripts/build_exemplar_index.py`, `scripts/query_exemplars.py`, a `build_exemplar_index` pipeline Step, and `data/derived/exemplar_index.json`.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-CONTEXT.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-RESEARCH.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-PATTERNS.md
@scripts/derive_outstanding_perks.py
@scripts/pipeline.py
@scripts/regime_simulator.py
@data/manual/regime_transitions.json
@tests/test_pipeline.py
</context>

<interfaces>
<!-- scripts/regime_simulator.py (already read in full) -->
def regime_for_chapter(chapter_num: str) -> int:
    """ch97 -> 2 (primary; transitions flip to 3 mid-chapter). ch<=91 -> 1. ch<=96 -> 2. else -> 3."""
def load_regime_transitions(path: Path | None = None) -> list[dict]:
    """Reads data/manual/regime_transitions.json's "transitions" list."""

<!-- data/manual/regime_transitions.json (already read in full; the ONLY boundary entry today) -->
{"transitions": [{"chapter_num": "97", "after_event": {"kind": "perk_acquired", "perk_name": "Nano-Forge"}, "new_regime": 3, "note": "..."}]}

<!-- data/derived/chapter_facts.json shape (already verified this session — "chapters" is a LIST, not a dict) -->
{"chapters": [{"chapter_num": "97", "point_calculation_regime": 3, "cp_earning_word_count": ..., ...}, ...]}
# Build a lookup: {c["chapter_num"]: c for c in chapter_facts["chapters"]}
# Per D-01, this field is trusted as-is and NEVER recomputed by the exemplar miner.
# Verified live: ch97's point_calculation_regime currently reads 3 (a known,
# out-of-scope bug in build_chapter_facts.py's own duplicate regime_for_chapter —
# see "Explicitly out of scope" note below). The boundary-tagging recipe below does
# NOT depend on this field being correct for ch97; it derives ch97's PRE-transition
# regime from scripts.regime_simulator.regime_for_chapter directly.

<!-- data/manual/chapter_roll_overrides.json shape (already read in full) -->
{"_purpose": ..., "association_review": {...}, "chapter_roll_overrides": {
  "<chapter_num>": {"_fingerprint": "sha256:...", "rolls": [
    {"perks": [...], "outcome": "hit"|"miss"|null, "constellation": ...,
     "word_position": null, "mention_chapter_num": null, "mention_word_position": null,
     "display_position_policy": null, "skipped": false, "source_ordinal": null,
     "evidence_quotes": [{"text": ..., "mention_chapter_num": ..., "mention_word_position": ...}],
     "curator_note": null}
  ]}
}}
# 118 curated chapters currently (verified live count).

<!-- scripts/derive_outstanding_perks.py skeleton (already read in full — model this file after it) -->
ROOT = Path(__file__).resolve().parent.parent
DEFAULT_OUTPUT = ROOT / "data" / "derived" / "outstanding_perks_by_chapter.json"
SCHEMA_VERSION = 1
def parse_args(argv=None) -> argparse.Namespace: ...
def main(argv=None) -> None:
    args = parse_args(argv)
    ...
    payload = {"schema_version": SCHEMA_VERSION, ...}
    args.output.write_text(json.dumps(payload, indent=2, ensure_ascii=False))
    print(f"wrote {args.output.relative_to(ROOT)}")

<!-- scripts/pipeline.py Step/TARGET_FINAL_STEPS shape (already read in full) -->
Step(name=..., inputs=inputs(...), outputs=(...,), cmd=_py(root, "script.py"))
TARGET_FINAL_STEPS = {"data": ("build_visualization_facts",), ...}
# chapter_roll_overrides = manual / "chapter_roll_overrides.json" is already a named
# constant in build_steps() — reuse it, do not redeclare a second path literal.

<!-- tests/test_pipeline.py (already read in full — MUST be updated in the same change) -->
DATA_STEP_NAMES = [ "parse_chapters", ..., "build_visualization_facts" ]  # 13 entries
# test_dry_run_on_real_derived_tree_lists_data_steps asserts the literal string
# "DRY-RUN data: 13 step(s)" appears in stdout — both this list and that literal
# string MUST be updated to include "build_exemplar_index" / 14 in the same task
# that adds the new Step, or this existing test will fail.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the exemplar index (regime tagging + boundary dual-tagging + statistics)</name>
  <reversibility rating="costly">D-03 (carried forward): un-registering a manifest-tracked derived artifact later touches data_release.py's release/staleness machinery and any Phase 2/3 consumers keyed to exemplar_index.json's schema.</reversibility>
  <files>scripts/build_exemplar_index.py, tests/test_build_exemplar_index.py</files>
  <behavior>
    - Every chapter_num present in data/manual/chapter_roll_overrides.json's
      "chapter_roll_overrides" key appears exactly once in the output's "exemplars"
      list.
    - A chapter with NO entry in regime_transitions.json's "transitions" list gets
      regime_tags == [chapter_facts_entry["point_calculation_regime"]] and
      is_boundary == False.
    - Chapter "97" (present in regime_transitions.json) gets regime_tags containing
      BOTH regime_simulator.regime_for_chapter("97") (== 2) and the transition's
      int(new_regime) (== 3), and is_boundary == True.
    - statistics.roll_shape_distribution counts rolls across all curated chapters by
      len(roll["perks"]) (e.g. {"0": N, "1": N, "2": N, ...}).
    - Calling the build a second time with unchanged inputs produces byte-identical
      output (deterministic — no set-ordering nondeterminism in regime_tags; sort
      before emitting).
    - A malformed input (chapter_roll_overrides.json missing its top-level
      "chapter_roll_overrides" key) raises ValueError rather than silently producing
      an empty index.
  </behavior>
  <action>
    Create scripts/build_exemplar_index.py modeled directly on
    scripts/derive_outstanding_perks.py's skeleton: argparse with --overrides
    (default data/manual/chapter_roll_overrides.json), --chapter-facts (default
    data/derived/chapter_facts.json), --transitions (default
    data/manual/regime_transitions.json), --output (default
    data/derived/exemplar_index.json); SCHEMA_VERSION = 1; pure functions + main().
    Import regime_for_chapter from regime_simulator (try/except ModuleNotFoundError
    falling back to scripts.regime_simulator, matching this repo's existing dual
    import-path convention) — never redefine it.

    For each chapter_num in chapter_roll_overrides["chapter_roll_overrides"]: look up
    its chapter_facts entry (build a {chapter_num: entry} dict from
    chapter_facts["chapters"], which is a LIST — do not assume dict access), read
    point_calculation_regime as the chapter's primary regime per D-01. Look up
    regime_transitions["transitions"] for an entry whose "chapter_num" matches; if
    none, tags = [point_calculation_regime], is_boundary = False. If found, per D-02
    (mid-chapter regime transitions are dual-tagged with both regimes plus an
    explicit boundary flag), tags = sorted({regime_for_chapter(chapter_num),
    int(entry["new_regime"])}), is_boundary = True (this recipe reads
    regime_for_chapter directly rather than trusting chapter_facts.json's
    point_calculation_regime for the boundary case — see the "Explicitly out of
    scope" note below for why this matters for ch97 specifically).

    For each chapter's rolls array, emit a roll-level record carrying: roll_index,
    perks, outcome, constellation, display_position_policy, evidence_quote_count,
    evidence_quotes (verbatim from the override — already-committed text only, per
    D-04), cp_ledger_checkpoint (if present on the roll; None otherwise). Do NOT open
    or read the epub file anywhere in this script.

    Build a "statistics" block: chapter_count, regime_distribution (count of chapters
    per regime tag), boundary_chapters (sorted list of chapter_nums with
    is_boundary=True), roll_shape_distribution (perks-per-roll counts across all
    rolls), display_position_policy_distribution, evidence_quote_stats (min/max/mean/
    total across all rolls' evidence_quotes), cp_ledger_checkpoint_usage_count.

    Write tests/test_build_exemplar_index.py per the Behavior block above, following
    tests/test_chapter_roll_overrides.py's import style
    (`from scripts.build_exemplar_index import ...`) and tmp_path-based JSON fixtures
    (small synthetic chapter_roll_overrides.json / chapter_facts.json /
    regime_transitions.json — do not depend on live repo data in the unit tests).

    Explicitly out of scope this plan (documented, not silently skipped): RESEARCH.md
    Pitfall 1 documents that build_chapter_facts.py has its own duplicate, out-of-sync
    regime_for_chapter implementation with zero test coverage, which is why
    chapter_facts.json's point_calculation_regime currently reads 3 for ch97 instead
    of the canonical 2. Per D-01 the miner must trust that field as-is for
    NON-boundary chapters and never recompute it; for the boundary case this task's
    recipe reads regime_simulator.regime_for_chapter directly rather than trusting
    the (buggy) chapter_facts.json field, so the index's ch97 tagging is correct
    regardless of whether that separate bug is ever fixed. Fixing build_chapter_facts.py's
    duplicate function is a candidate follow-up (RESEARCH.md Open Question 1), not a
    requirement of CINF-02, and is deliberately not undertaken in this phase.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_build_exemplar_index.py -q</automated>
  </verify>
  <done>
    scripts/build_exemplar_index.py exists and its tests pass, covering: every
    curated chapter present with correct regime tag(s); ch97 dual-tagged {2, 3} with
    is_boundary True; deterministic re-run; loud failure on malformed input.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic same-regime retrieval function</name>
  <files>scripts/query_exemplars.py, tests/test_query_exemplars.py</files>
  <behavior>
    - retrieve(target_regime, index) returns only exemplars whose regime_tags list
      contains target_regime.
    - A boundary-tagged exemplar (regime_tags == [2, 3]) is returned for BOTH
      target_regime=2 and target_regime=3 queries.
    - Calling retrieve() twice with identical arguments returns identical output (no
      randomness, no unstable dict/set ordering).
    - retrieve(target_regime, index, k=N) returns at most N entries, taken from the
      front of a deterministic sort (chapter_num numeric ascending as the tie-break;
      when target_chapter is supplied, sort by proximity to it — Claude's discretion
      per D-06, but documented and covered by a test).
    - No exemplar whose regime_tags does NOT intersect target_regime is ever returned,
      across a fixture index containing at least one entry per regime and one
      boundary entry.
  </behavior>
  <action>
    Create scripts/query_exemplars.py as a pure module (no argparse, no file I/O in
    the retrieval path — it takes an already-loaded index dict) exporting
    `retrieve(target_regime: int, index: dict, *, k: int | None = None, target_chapter:
    str | None = None) -> list[dict]`. Filter index["exemplars"] to entries where
    target_regime is in entry["regime_tags"]; sort deterministically (by numeric
    chapter proximity to target_chapter if given, else by chapter_num ascending using
    a (major, minor) tuple key — the same chapter-sort-key pattern already used
    elsewhere in scripts/, e.g. multi_grab.py's _chapter_sort_key); truncate to k if
    given. A thin `if __name__ == "__main__":` CLI wrapper may be added for manual
    debugging (loads exemplar_index.json, calls retrieve, prints JSON) but the
    importable retrieve() function must not require CLI invocation — Phase 3 imports
    it directly.

    Write tests/test_query_exemplars.py following
    tests/test_build_visualization_facts.py's direct-function-call style
    (`from scripts.query_exemplars import retrieve`), constructing a small synthetic
    in-memory index dict fixture (at least 3 regimes represented, including one
    boundary entry with regime_tags == [2, 3]) per the Behavior block above.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_query_exemplars.py -q</automated>
  </verify>
  <done>
    scripts/query_exemplars.py exists and its tests pass, including the explicit
    same-regime-only constraint test (D-06's required test) and the boundary-entry
    dual-visibility test.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire build_exemplar_index into the pipeline DAG and manifest, then verify green</name>
  <files>scripts/pipeline.py, tests/test_pipeline.py</files>
  <action>
    In scripts/pipeline.py's build_steps(), add a new Step named
    "build_exemplar_index" with inputs = inputs(chapter_roll_overrides, derived /
    "chapter_facts.json", manual / "regime_transitions.json") — reusing the existing
    chapter_roll_overrides constant already declared in this function, not a second
    path literal — outputs = (derived / "exemplar_index.json",), and cmd = _py(root,
    "build_exemplar_index.py"). Add "build_exemplar_index" to
    TARGET_FINAL_STEPS["data"] alongside "build_visualization_facts" (a Step whose
    output nothing else consumes is otherwise silently dropped from the "data" target's
    closure). Update tests/test_pipeline.py's DATA_STEP_NAMES list to include
    "build_exemplar_index" (14 entries total) and update
    test_dry_run_on_real_derived_tree_lists_data_steps's expected stdout count from
    13 to 14 step(s), in the same change (this existing test will otherwise fail).

    Do NOT add any bespoke manifest-registration code to scripts/data_release.py —
    _top_level_json_files() already auto-discovers any *.json file in data/derived/
    carrying a schema_version key (verified this session by direct code read); adding
    a second registration path would itself be a parallel-implementation anti-pattern.

    Run `.venv/bin/python scripts/pipeline.py --target data` (incremental is fine here
    — nothing else changed since Plan 01-02's green run) to produce
    data/derived/exemplar_index.json, then `.venv/bin/python scripts/data_release.py
    manifest` to register it, then `.venv/bin/python scripts/data_release.py
    check-derived` and a full `.venv/bin/python -m pytest` to confirm the new Step and
    test-list update haven't regressed anything.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pipeline.py -q && .venv/bin/python scripts/data_release.py check-derived && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>
    data/derived/exemplar_index.json exists with schema_version present, is
    manifest-registered (visible in data/derived/data_package.json's files list),
    tests/test_pipeline.py reflects the 14-step DAG, and the full pytest suite plus
    check-derived remain green.
  </done>
</task>

</tasks>

<assumption_delta_decision>
Primary noun: "regime tag" (per-chapter). Decision: add-alongside. Rationale:
chapter_facts.json's point_calculation_regime stays a single scalar per D-01 (never
recomputed, never pluralized, and left as-is even for the known-buggy ch97 value) —
the new plural regime_tags + is_boundary representation is added ALONGSIDE it, scoped
entirely to the new exemplar_index.json artifact, and explicitly accepted as new debt
local to that one artifact rather than universalized to chapter_facts.json or any
other existing consumer.
</assumption_delta_decision>

## Flagged Assumptions (unresolved edge-probe items — not silently dropped)

- **CINF-02 (unclassified edge-probe category):** the automated edge-probe classified
  CINF-02 as "unclassified — review manually" and, per D-06, this stays explicitly
  **unresolved** rather than being auto-backstopped. The natural acceptance surface is
  ROADMAP.md's Phase 1 Success Criteria 3 ("An exemplar index built from the
  hand-curated chapters is tagged by CP regime, and a retrieval query for a target
  chapter returns only same-regime exemplars") and 4 ("The index documents the
  corpus's observed evidence-quote patterns, roll-shape distribution, and perk-link
  conventions") — Task 1-3 of this plan (criterion 3) and Plan 01-04 (criterion 4) are
  this phase's concrete proof, but the edge-probe's own classification gap is recorded
  here rather than silently dropped.

## Artifacts this plan produces

- `scripts/build_exemplar_index.py` — new file
  - pure functions (tagging + statistics aggregation), `SCHEMA_VERSION = 1`
  - `main(argv=None)` — CLI entry point with `--overrides`/`--chapter-facts`/`--transitions`/`--output` flags
- `scripts/query_exemplars.py` — new file
  - `retrieve(target_regime: int, index: dict, *, k: int | None = None, target_chapter: str | None = None) -> list[dict]`
- `scripts/pipeline.py` — modified: new `Step(name="build_exemplar_index", ...)` + `TARGET_FINAL_STEPS["data"]` addition
- `tests/test_build_exemplar_index.py` — new file
- `tests/test_query_exemplars.py` — new file
- `tests/test_pipeline.py` — modified: `DATA_STEP_NAMES` (14 entries), dry-run count string updated to "14 step(s)"
- `data/derived/exemplar_index.json` — new derived artifact, `schema_version`-keyed, manifest-registered

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hand-curated manual JSON -> exemplar index | build_exemplar_index.py reads chapter_roll_overrides.json/chapter_facts.json/regime_transitions.json only; never opens the epub |
| New pipeline Step -> existing DAG | Must not silently drop from the "data" target's closure (TARGET_FINAL_STEPS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | Epub prose leaking into a committed artifact via build_exemplar_index.py or its tests | high | mitigate | Script never opens data/raw/*.epub; evidence text sourced only from already-committed chapter_roll_overrides.json evidence_quotes (must_haves.prohibitions) |
| T-01-03-02 | Tampering | A second, drifting regime-computation implementation (repeat of the existing build_chapter_facts.py bug) | medium | mitigate | Import regime_for_chapter from scripts.regime_simulator; no local re-definition (must_haves.prohibitions, key_links) |
| T-01-03-03 | Tampering | New Step silently dropped from the pipeline DAG closure | medium | mitigate | Explicitly added to TARGET_FINAL_STEPS["data"] and verified via tests/test_pipeline.py's updated DATA_STEP_NAMES |
| T-01-03-SC | Tampering | Supply chain (package installs) | n/a | mitigate | No new pip/npm/cargo packages introduced by this plan; stdlib only |
</threat_model>

<verification>
Automated:
- `.venv/bin/python -m pytest tests/test_build_exemplar_index.py tests/test_query_exemplars.py -q` passes.
- `.venv/bin/python -m pytest tests/test_pipeline.py -q` passes (14-step DAG).
- `.venv/bin/python scripts/data_release.py check-derived` and full `pytest` remain green after wiring.
</verification>

<success_criteria>
- data/derived/exemplar_index.json exists, schema_version-keyed, manifest-registered.
- Every hand-curated chapter is regime-tagged; ch97 is dual-tagged with is_boundary True.
- query_exemplars.retrieve() is deterministic and never crosses regime boundaries (tested).
</success_criteria>

<output>
Create `.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-03-SUMMARY.md` when done
</output>
