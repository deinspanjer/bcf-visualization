---
phase: 01-epub-refresh-exemplar-mining
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_epub_freshness.py
  - tests/test_source_epub_hydration.py
  - data/manual/chapter_roll_overrides.json
autonomous: false
requirements: [EPUB-01]

must_haves:
  truths:
    - "Post-hydrate chapter count and nav entries in data/raw/Brocktons_Celestial_Forge.source.json match the private-source clone's current release metadata (data/private-source/Brocktons_Celestial_Forge.metadata.json, tag source-v20260726.1: 198 chapters, last chapter 121.1), and after the full pipeline force-regen, predicted rolls / chapters.json extend past the pre-refresh baseline (195 chapters, last chapter 120.2)."
    - statement: "The epub at data/raw/Brocktons_Celestial_Forge.epub is byte-identical (sha256) to data/private-source/Brocktons_Celestial_Forge.epub — no partial/corrupt hydrate."
      verification: backstop
    - "Running scripts/pipeline.py --target data --force either completes cleanly, or halts ONLY at the documented, pre-existing ch 95.5 multi_grab ValueError — any other failure is a genuine regression requiring investigation, not this phase's silent problem to route around."
  artifacts:
    - path: "scripts/verify_epub_freshness.py"
      provides: "verify_epub_freshness() — mechanical D-09 comparison of hydrated source.json vs private-source metadata.json"
      exports: ["verify_epub_freshness", "main"]
    - path: "tests/test_source_epub_hydration.py"
      provides: "Regression tests for verify_epub_freshness() freshness/staleness detection"
    - path: "data/derived/chapters.json"
      provides: "Regenerated chapter list (198 chapters, last 121.1) after full pipeline force-regen"
  key_links:
    - from: "scripts/verify_epub_freshness.py"
      to: "data/raw/Brocktons_Celestial_Forge.source.json + data/private-source/Brocktons_Celestial_Forge.metadata.json"
      via: "reads both JSON files, compares chapter_count/last_chapter_num/epub_sha256"
      pattern: "chapter_count|last_chapter_num|epub_sha256"
    - from: "scripts/pipeline.py --target data --force"
      to: "scripts/predict_rolls.py -> scripts/multi_grab.merge_paid_units"
      via: "subprocess step invocation; merge_paid_units raises ValueError on ch 95.5 perk-name mismatch"
      pattern: "merge_paid_units|Minor Blessing Zeus"

  prohibitions:
    - statement: "MUST NOT silently edit data/manual/chapter_roll_overrides.json (or any other hand-curated manual file) to resolve a validation failure without an explicit, Dre-approved checkpoint decision naming the exact field/value to change."
      category: values
      status: unverified
      flagged: true
    - statement: "MUST NOT fetch or hydrate the epub via any path other than the existing scripts/sync_private_source_repo.py -> scripts/hydrate_source_epub.py flow (no direct FicHub download, no new script), even as a fallback when staleness is found."
      category: scope
      status: unverified
      flagged: true
    - statement: "MUST NOT make any LLM/Anthropic API call anywhere in this phase's scripts or analysis — this phase is pure deterministic Python."
      category: scope
      status: unverified
      flagged: true
---

<objective>
Verify — without re-running sync/hydrate, per D-10 — that the already-hydrated epub (`data/raw/Brocktons_Celestial_Forge.epub`, `data/raw/Brocktons_Celestial_Forge.source.json`) reflects the private-source clone's newest release (tag `source-v20260726.1`, 198 chapters through 121.1), then force-regenerate the full derived-data pipeline against it. This plan also carries the one known, pre-existing blocker to that regen — a hand-curated data gap in chapter 95.5's multi-grab override — through a human-approved diagnosis-and-fix cycle rather than an autonomous edit.

Purpose: EPUB-01 requires mechanical proof the pipeline reflects the newest release; the pipeline cannot regenerate at all until the ch 95.5 override is resolved (verified directly: `scripts/predict_rolls.py` currently crashes with `ValueError: multi_grab override for ch 95.5 roll #0 references 'Minor Blessing Zeus – Lightning'...`), so this plan follows D-07's order of operations (verify -> regenerate -> diagnose residual failures, with sync/hydrate already done per D-10) and is the tracer that proves the real end-to-end path — verification, sanctioned refresh flow, and the one required human decision — before any later plan can build on a stable derived-data tree.
Output: `scripts/verify_epub_freshness.py` (reusable freshness check), a resolved `chapter_roll_overrides.json` (approved edit only), and a freshly force-regenerated `data/derived/*.json` tree with 198 chapters through 121.1.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/ROADMAP.md
@.planning/workstreams/curation/REQUIREMENTS.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-CONTEXT.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-RESEARCH.md
@scripts/hydrate_source_epub.py
@scripts/data_paths.py
@tests/test_source_epub_hydration.py
</context>

<interfaces>
<!-- scripts/data_paths.py exports (already read; used by every script in this plan) -->
ROOT = Path(__file__).resolve().parent.parent
DATA = Path(os.environ.get("BCF_DATA_DIR", ROOT / "data")).resolve()
RAW = DATA / "raw"
DERIVED = DATA / "derived"
MANUAL = DATA / "manual"

<!-- Confirmed live state this session (2026-07-26) -->
data/raw/Brocktons_Celestial_Forge.source.json:
  chapter_count: 198, last_chapter_num: "121.1", source_kind: "private-source",
  epub_sha256: "d5f73c536f10a97535d3240e075692aa8b72696f235d4737a1062b261dd0ffc2"
data/private-source/Brocktons_Celestial_Forge.metadata.json:
  chapters.count: 198, chapters.last_chapter_friendly_number: "121.1",
  sha256: "d5f73c536f10a97535d3240e075692aa8b72696f235d4737a1062b261dd0ffc2",
  version_tag: "source-v20260726.1"
  -> these already MATCH: D-10's claim that groundwork is done is confirmed live.

data/derived/chapters.json (STALE, pre-regen baseline): 195 chapters, last "120.2"

data/manual/chapter_roll_overrides.json["chapter_roll_overrides"]["95.5"]:
  keys: ["rolls", "_fingerprint"]; rolls[0] = {
    "perks": ["Minor Blessing Zeus – Lightning", "Unnatural Skill: Curses"],
    "mention_chapter_num": null,   # <-- defaults to mechanical chapter "95.5"
    "evidence_quotes": [{"mention_chapter_num": "96", ...}, ...]
  }
data/derived/obtained_perks.json (STALE, pre-regen): both perk names appear with
  "chapter_num": "95" (mechanically paid in chapter 95, NOT 95.5, NOT 96) — this
  is the verified root cause, refining RESEARCH.md Pitfall 4's "ch96" hypothesis.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Verify epub freshness mechanically, then attempt the full pipeline force-regen</name>
  <precondition>data/raw/Brocktons_Celestial_Forge.source.json and data/private-source/Brocktons_Celestial_Forge.metadata.json both exist on disk (Dre's private-source sync + hydrate at tag source-v20260726.1, already completed per D-10)</precondition>
  <files>scripts/verify_epub_freshness.py, tests/test_source_epub_hydration.py</files>
  <behavior>
    - verify_epub_freshness(data_dir) returns {"fresh": True, ...} when source.json's
      chapter_count/last_chapter_num/epub_sha256 all agree with private-source
      metadata.json's chapters.count/chapters.last_chapter_friendly_number/sha256.
    - Returns {"fresh": False, ...} with per-field mismatch detail when chapter_count
      disagrees.
    - Returns {"fresh": False, ...} when last_chapter_num disagrees (count matches).
    - main() exits 1 and prints guidance to run sync_private_source_repo.py then
      hydrate_source_epub.py (the sanctioned flow, D-10 fallback) when fresh is False;
      exits 0 and prints a one-line summary when fresh is True.
  </behavior>
  <action>
    Create scripts/verify_epub_freshness.py following the small-utility CLI shape used
    elsewhere in scripts/ (argparse with --data-dir defaulting to data_paths.DATA, a pure
    verify_epub_freshness(*, data_dir=DATA) -> dict function, then a main()). Read
    data/raw/Brocktons_Celestial_Forge.source.json and
    data/private-source/Brocktons_Celestial_Forge.metadata.json; compare
    source["chapter_count"] to metadata["chapters"]["count"],
    source["last_chapter_num"] to metadata["chapters"]["last_chapter_friendly_number"],
    and source["epub_sha256"] to metadata["sha256"]. Do NOT re-implement epub/nav
    parsing (that already exists in hydrate_source_epub.parse_epub_nav) — this script
    only compares two already-written JSON files. Extend
    tests/test_source_epub_hydration.py with tmp_path-based fixture tests per the
    Behavior block above (mirror this file's existing fixture style — small JSON
    dicts written under tmp_path/data/raw and tmp_path/data/private-source).

    Once tests pass, run `.venv/bin/python scripts/verify_epub_freshness.py` against the
    real repo data (expected: fresh=True, matches the Interfaces block above). Then
    attempt `.venv/bin/python scripts/pipeline.py --target data --force` (force is
    required — RESEARCH.md documents that pipeline.py's own incremental staleness
    detection under-declares chapter_roll_overrides.json as an input to predict_rolls
    and derive_roll_outcomes, so --dry-run/incremental cannot be trusted for this
    one-time refresh). This run is EXPECTED to halt during the predict_rolls step with
    a ValueError from scripts/multi_grab.merge_paid_units referencing chapter 95.5 and
    the perk name "Minor Blessing Zeus – Lightning" — this is a pre-existing
    hand-curated-data gap already documented in
    .planning/workstreams/mobile-ux/phases/01-mobile-state-gesture-plumbing/deferred-items.md,
    not a new regression, and not this task's job to fix (hand-curated overrides are
    authoritative; only Task 2/3 of this plan, gated by Dre's decision, may touch that
    file). If the run instead completes cleanly, or fails with any DIFFERENT error,
    stop and report that discrepancy rather than proceeding to Task 2 as if the known
    blocker were confirmed — that would be new information requiring investigation.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_source_epub_hydration.py -q</automated>
  </verify>
  <done>
    verify_epub_freshness.py exists, its tests pass, running it against the live repo
    reports fresh=True. Running `.venv/bin/python scripts/pipeline.py --target data --force`
    halts with exactly the documented ch 95.5 merge_paid_units ValueError (confirmed by
    the captured output containing "Minor Blessing Zeus" and "ch 95.5"), and this is
    reported to Task 2 rather than silently retried or worked around.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide how to resolve the ch 95.5 multi_grab hand-curated data gap</name>
  <decision>How to resolve the ch 95.5 multi_grab hand-curated data gap blocking the pipeline regen</decision>
  <context>
    Task 1's pipeline attempt halted (as expected) at:
    `ValueError: multi_grab override for ch 95.5 roll #0 references 'Minor Blessing
    Zeus – Lightning' but no obtained perk in the mechanical or mention chapter has
    that name (mechanical paid: ['arena', 'central control', 'gym', 'your robots'];
    mention_chapter_num: 95.5)`.

    Direct verification this session (against the pre-refresh derived tree, which this
    field is unaffected by epub freshness — it is sourced from
    data/raw/Brocktons_Celestial_Forge_Reference.xlsx, last touched 2026-05-09, not the
    epub sync): data/derived/obtained_perks.json lists BOTH "Minor Blessing Zeus –
    Lightning" and "Unnatural Skill: Curses" with "chapter_num": "95" — i.e. they were
    mechanically paid for in chapter 95, not 95.5 and not 96. A companion warning from
    the same pipeline run — "multi_grab: ch 95 override drops paid perk(s) ['Minor
    Blessing Zeus – Lightning', 'Unnatural Skill: Curses'] (not listed in any roll)" —
    confirms chapter 95's own override does not claim these two perks, leaving them
    available. The override roll object in question (under
    chapter_roll_overrides["95.5"]["rolls"][0]) has "mention_chapter_num": null, which
    defaults to the mechanical chapter "95.5" — a chapter that mechanically has neither
    perk. This refines RESEARCH.md Pitfall 4's original hypothesis (which guessed
    mention_chapter_num should be "96", matching the roll's evidence_quotes'
    mention_chapter_num) — the live obtained_perks.json data points to "95", not "96".
    Per D-08 and the project's curation-authority rule, this file is never edited
    without Dre's explicit selection of the exact change.
  </context>
  <options>
    <option id="option-a">
      <name>Set mention_chapter_num to "95" (recommended)</name>
      <pros>Matches obtained_perks.json's live mechanical attribution for both perk names exactly; matches the accompanying "ch 95 override drops" warning (these perks are mechanically available in ch 95 and unclaimed by ch 95's own override); very likely resolves the merge_paid_units lookup on the first retry.</pros>
      <cons>Assumes obtained_perks.json's chapter_num field is authoritative for "where mechanically paid" (verified this session, high confidence, but not re-confirmed against a freshly-regenerated obtained_perks.json — it is unaffected by the epub refresh since it is sourced from the separate Reference.xlsx).</cons>
    </option>
    <option id="option-b">
      <name>Set mention_chapter_num to "96" (RESEARCH.md's original, now-unverified hypothesis)</name>
      <pros>Matches the roll's own evidence_quotes[].mention_chapter_num ("96"), i.e. matches where the perks are narratively described in the story text.</pros>
      <cons>NOT recommended: obtained_perks.json shows no chapter-96 attribution for either perk name, so this is very likely to still fail with the same ValueError (merge_paid_units looks up perks by their mechanical chapter, and per live data that is "95", not "96").</cons>
    </option>
    <option id="option-c">
      <name>Defer — leave chapter_roll_overrides.json unchanged this phase</name>
      <pros>No edit to trust-critical hand-curated data without further investigation (e.g. a fresh obtained_perks.json re-check, or a structural move of the roll object into chapter "95"'s own override array instead of a field edit).</pros>
      <cons>The pipeline cannot complete its force-regen without this resolved; EPUB-02 (full pipeline green) and this plan's Task 3 cannot proceed. Blocks the phase's hard gate for Phases 2-4.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, option-c, or describe an alternative fix (e.g. "move the roll object to chapter 95's override array instead")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the approved fix and complete the full pipeline force-regen</name>
  <files>data/manual/chapter_roll_overrides.json</files>
  <action>
    Apply EXACTLY the field/value (or structural change) Dre approved at Task 2 — no
    deviation, no additional "improvements" to nearby data. If Dre selected "defer"
    (option-c), stop here without editing any file and report the blocker back rather
    than attempting a workaround; Task 3 and Plan 01-02 cannot proceed until this is
    resolved. Otherwise, after applying the approved edit, re-run
    `.venv/bin/python scripts/pipeline.py --target data --force` to confirm the
    predict_rolls step now completes past the previous ValueError. If it fails with a
    DIFFERENT error afterward, stop and report — do not attempt a second unapproved
    edit. On success, confirm data/derived/chapters.json now reports 198 chapters with
    last chapter_num "121.1" (matching the private-source metadata compared in Task 1),
    exceeding the pre-refresh baseline of 195 chapters / chapter 120.2.
  </action>
  <verify>
    <automated>test -f data/derived/chapters.json && python3 -c "import json,sys; d=json.load(open('data/derived/chapters.json')); chs=d['chapters']; sys.exit(0 if (len(chs)>195 and chs[-1]['chapter_num']=='121.1') else 1)"</automated>
  </verify>
  <done>
    The approved edit is committed to chapter_roll_overrides.json (or Task 3 halted
    cleanly on a "defer" decision), the full pipeline force-regen completes without
    exception, and data/derived/chapters.json reflects 198 chapters through 121.1.
  </done>
</task>

</tasks>

## Artifacts this plan produces

- `scripts/verify_epub_freshness.py` — new file
  - `verify_epub_freshness(*, data_dir: Path = DATA) -> dict` — pure comparison function
  - `main()` — CLI entry point, `--data-dir` flag, exits 1 with guidance on staleness
- `tests/test_source_epub_hydration.py` — extended with new test functions covering
  fresh/stale detection for `verify_epub_freshness`
- `data/manual/chapter_roll_overrides.json` — one field edit (approved at Task 2),
  scoped to `chapter_roll_overrides["95.5"]["rolls"][0]` (exact field per Dre's choice)
- `data/derived/*.json` (13 files) — fully force-regenerated by
  `scripts/pipeline.py --target data --force` (chapters.json, chapter_sections.json,
  predicted_rolls.json, roll_text_evidence.json, roll_outcomes.json, timeline.json,
  perk_directory.json, outstanding_perks_by_chapter.json, roll_facts.json,
  roll_validation.json, chapter_facts.json, constellation_lifecycle.json,
  constellation_wireframes.json, visualization_facts.json, data_package.json)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private-source clone -> raw epub | Developer-controlled, gitignored clone (already synced by Dre outside this workflow); trusted input |
| Human decision -> hand-curated manual JSON | The only sanctioned path for chapter_roll_overrides.json edits in this plan is an explicit, Dre-approved checkpoint selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-01 | Tampering | Task 3's edit to data/manual/chapter_roll_overrides.json | high | mitigate | Executor applies ONLY the exact field/value approved at the Task 2 checkpoint; no autonomous edit is permitted (see must_haves.prohibitions) |
| T-01-01-02 | Repudiation | No audit trail for why the override field changed | low | mitigate | Commit message for Task 3's edit cites the checkpoint decision and the verified obtained_perks.json evidence, so git history is the audit trail |
| T-01-01-03 | Tampering | Malformed/oversized epub ingested from the private-source clone | low | accept | Source is a long-trusted, developer-selected, gitignored input (not a multi-tenant upload path); xml.etree does not resolve external entities by default; no change needed this plan |
| T-01-01-SC | Tampering | Supply chain (package installs) | n/a | mitigate | No new pip/npm/cargo packages introduced by this plan; stdlib only |
</threat_model>

<verification>
Automated:
- `.venv/bin/python -m pytest tests/test_source_epub_hydration.py -q` passes.
- `.venv/bin/python scripts/verify_epub_freshness.py` exits 0 against live repo data.
- `.venv/bin/python scripts/pipeline.py --target data --force` completes without exception after Task 3's approved fix.

Manual: Task 2's checkpoint decision is recorded by Dre before Task 3 runs.
</verification>

<success_criteria>
- verify_epub_freshness.py exists, is tested, and reports the hydrated epub as fresh against the private-source clone (EPUB-01 mechanical proof, D-09).
- The ch 95.5 hand-curated data gap is resolved via an explicit, Dre-approved decision — never an autonomous edit.
- The full derived-data pipeline force-regen completes, producing data/derived/chapters.json with 198 chapters through 121.1.
</success_criteria>

<output>
Create `.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/01-01-SUMMARY.md` when done
</output>
