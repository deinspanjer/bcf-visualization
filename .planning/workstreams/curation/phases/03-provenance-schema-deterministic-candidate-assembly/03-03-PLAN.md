---
phase: 03-provenance-schema-deterministic-candidate-assembly
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - scripts/measure_candidate_accuracy.py
  - .planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.json
  - .planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.md
  - tests/test_measure_candidate_accuracy.py
autonomous: true
requirements: [ACUR-01]

must_haves:
  truths:
    - "Stage 1's accuracy against the hand-curated corpus is reported per evidence class (direct/general_only/forward_ref/no_evidence), never collapsed into one headline number (D-08)."
    - "The 11 originally-flagged stub chapters (35.1, 55.1, 97, 100, 103, 104, 106, 109, 112, 114, 116.2) are re-derived from the data at runtime — a chapter whose curated rolls all carry zero evidence_quotes — not hardcoded; the report records whether chapter 104 (now genuinely curated) is included or excluded by that live predicate (D-09)."
    - "The accuracy measurement is a git-committed file (JSON + markdown) under this phase's directory, not console-output-only — Phase 4 reads it to size its own inference spend (D-10)."
    - "The candidate-to-curated-roll matching function never joins on roll_number or source_ordinal identity across the predictor/curator numbering sequences — it matches within a chapter by mechanical position and/or perk-name overlap, deterministic and documented in the report's _method field."
  artifacts:
    - scripts/measure_candidate_accuracy.py
    - .planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.json
    - .planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.md
  key_links:
    - "data/derived/candidate_rolls.json (Plan 03-02) -> measure_candidate_accuracy.py's candidate side"
    - "multi_grab.load_overrides() (Plan 03-01's validated loader) -> measure_candidate_accuracy.py's curated side"
    - "candidate-accuracy-report.json -> Phase 4 planning input (sizes inference spend)"
  prohibitions:
    - "Never edit data/manual/chapter_roll_overrides.json's hand-curated roll content to make the measured accuracy look better — the corpus is the fixture, the measurement is what's under test (curation-authority rule)."
    - "The matching methodology never joins candidates to curated rolls via roll_number or source_ordinal equality across the two numbering sequences (project convention: predictor and curator roll_number sequences diverge and must never be joined)."
    - "No LLM or external API call anywhere in this plan's code."
    - "The report is not a single global pass/fail percentage — per-evidence-class breakdown is mandatory, not optional detail."
    - "{ statement: \"Candidate emission order within a chapter follows narrative position, and the accuracy measurement is deterministic across repeated runs against unchanged inputs (no set/dict iteration nondeterminism).\", verification: \"backstop\" }"
---

<objective>
Measure Stage 1's candidate-assembly accuracy against the 118-chapter hand-curated corpus, broken out per evidence class, per ACUR-01 and ROADMAP success criterion 3 — the load-bearing deliverable of this phase.

CONTEXT.md is explicit: "The measurement is the point. Phase 3's recorded accuracy is the evidence that sizes Phase 4's inference spend." A single headline number would be dominated by the 68% `forward_ref` share and hide that Stage 1 works well on `direct` evidence, which is what D-08 exists to prevent.

Purpose: give Phase 4 a real, measured baseline instead of a guess for how much inference is worth buying.
Output: `scripts/measure_candidate_accuracy.py`, a committed JSON report + markdown summary under this phase's directory, `tests/test_measure_candidate_accuracy.py`.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/03-CONTEXT.md
@.planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/corpus-analysis-report.md
@scripts/mechanical_verifier.py
@scripts/multi_grab.py

Depends on Plan 03-01 (`scripts/chapter_roll_overrides_io.py`, the validated `curated_by`-stamped corpus) and Plan 03-02 (`scripts/build_candidate_rolls.py` / `data/derived/candidate_rolls.json`) — read their SUMMARYs for the exact function signatures/artifact shapes actually landed, since this plan calls both directly.
@.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/03-01-SUMMARY.md
@.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stub-chapter derivation + deterministic candidate-to-curated matching + per-class totals</name>
  <files>scripts/measure_candidate_accuracy.py</files>
  <read_first>
    scripts/mechanical_verifier.py lines 487-519 (`main()`'s report-assembly pattern — per-chapter results dict, a `totals` dict accumulated across chapters; this plan's script extends that shape to be per-evidence-class rather than flat pass/fail/no_evidence)
    scripts/multi_grab.py lines 111-151 (`load_overrides()` — the normalized read of the curated corpus this script consumes: `rolls[]` entries already have `mention_chapter_num`/`display_position_policy` defaulted, `evidence_quotes` always a list)
    03-CONTEXT.md D-08, D-09 (per-class reporting requirement; stub-chapter exclusion re-derived from data, not the hardcoded list — note ch 104 is now genuinely curated and the predicate must reflect that live, not by special-casing "104")
    Project memory: predictor vs curator `roll_number` sequences diverge and are never joined directly — the matching function below must not rely on that identity.
  </read_first>
  <behavior>
    - `derive_stub_chapters(overrides_doc)` returns the set of chapter_nums where every roll in that chapter's `rolls[]` has an empty `evidence_quotes` list; run against the real corpus it must NOT include `"104"` (now genuinely curated with 3 real quotes) and MUST include the other 10 originally-flagged chapters (`35.1, 55.1, 97, 100, 103, 106, 109, 112, 114, 116.2`).
    - `match_candidates_to_curated(chapter_num, curated_rolls, candidates)` pairs each curated roll to at most one candidate and vice versa, using chapter-local position proximity (curated `word_position` vs candidate `word_position` when the curated value is non-null; ordinal chapter-local sequence position as the documented fallback when it is null) plus perk-name-set overlap as a tiebreaker — never `roll_number`/`source_ordinal` equality.
    - A curated roll with no matching candidate is counted `no_curated_counterpart_for_candidate`-style as unmatched on the CANDIDATE side and `missed` on the CURATED side (both directions reported, per D-08's "how many curated rolls Stage 1 proposed a candidate for" AND "how many candidates had no curated counterpart").
    - A matched pair where the candidate's `_derivation.unfilled_fields` is non-empty counts as `partial`, not `matched` outright.
  </behavior>
  <action>
    Create `scripts/measure_candidate_accuracy.py` (module docstring: inputs `data/derived/candidate_rolls.json` + the corpus via `chapter_roll_overrides_io`/`multi_grab.load_overrides()`; output the phase-directory JSON+markdown report; explicitly NOT DAG-wired, mirrors `mechanical_verifier.py`'s standalone QA-CLI posture per D-04's discretion note leaning standalone).

    `derive_stub_chapters(overrides_doc: dict) -> set[str]`: iterate `overrides_doc["chapter_roll_overrides"]` (via `multi_grab.load_overrides()`'s normalized shape, which already guarantees `evidence_quotes` is always a list), a chapter is a stub iff every roll's `evidence_quotes == []`.

    `match_candidates_to_curated(...)`: implement and document the position/perk-overlap matching described above in the module docstring's `_method` field, in enough prose detail that a future reader can re-derive it without reading the code. Sort both curated rolls and candidates by chapter-local position before pairing (deterministic).

    `compute_accuracy(overrides_doc, candidates_doc) -> dict`: exclude stub chapters (via `derive_stub_chapters`) from the denominator; for chapters in the candidate/curated intersection, run the matcher; accumulate per-`evidence_kind` totals: `{"direct": {"curated_rolls": N, "matched": N, "partial": N, "missed": N, "unmatched_candidates": N}, "general_only": {...}, "forward_ref": {...}, "no_evidence": {...}}` (evidence_kind taken from the CANDIDATE side's `_derivation.evidence_kind`, since that's what roll_text_evidence.json assigns per predicted roll regardless of hit/miss). Iterate `sorted(...)` everywhere chapters/candidates are walked — no raw dict/set iteration order in the output (the ordering backstop must-have).
  </action>
  <verify>
    <automated>cd /Users/dre/src/bcf-visualization && PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_measure_candidate_accuracy.py -q</automated>
  </verify>
  <acceptance_criteria>
    `derive_stub_chapters()` run against the live corpus (via `multi_grab.load_overrides()`) returns a 10-chapter set matching the originally-flagged list minus `"104"`.
    `compute_accuracy()` returns a dict keyed by all 4 evidence classes, each with the 5 named counters.
    The module docstring's `_method` field documents the matching rule in prose.
  </acceptance_criteria>
  <done>Stub-chapter derivation and candidate-to-curated matching are pure, documented, deterministic functions producing per-evidence-class totals.</done>
</task>

<task type="auto">
  <name>Task 2: Run against the real corpus, write the committed JSON + markdown report</name>
  <files>.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.json, .planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.md</files>
  <read_first>
    .planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/corpus-analysis-report.md (D-04's sourcing-constraint precedent this task must match: "every number below is read directly from [the JSON]'s statistics block — nothing here is recomputed independently"; mirror this discipline for the markdown summary)
    scripts/mechanical_verifier.py lines 507-519 (stdout summary print pattern to mirror for this script's own summary + the values that seed the markdown)
  </read_first>
  <action>
    `main()` in `scripts/measure_candidate_accuracy.py`: load `data/derived/candidate_rolls.json` (from Plan 03-02) and the corpus via `multi_grab.load_overrides()` (validated per Plan 03-01), run `compute_accuracy`, write the JSON report to `.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.json` (a bare `json.dumps(..., indent=2, ensure_ascii=False)` write, not `write_validated_json` — this is a phase-scoped measurement artifact outside `data/derived/`'s schema-registry convention, same posture as `mechanical_verification_report.json`) with top-level `_method` (the matching-rule prose from Task 1), `_stub_chapters_excluded` (the derived list), `by_evidence_class` (Task 1's per-class totals), `_generated_from` (input file paths + counts, for reproducibility). Print the same summary to stdout.

    Run it: `.venv/bin/python scripts/measure_candidate_accuracy.py`.

    Author `.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.md`, sourced ONLY from the just-written JSON's numbers (no epub prose, no invented figures — cite the JSON path as the source, mirroring `corpus-analysis-report.md`'s opening "Sourcing constraint" paragraph). Cover: overview (total candidates, total curated rolls, stub chapters excluded and why), per-evidence-class table (the 4 classes x 5 counters), and one paragraph interpreting the `direct` class result against D-07's expectation ("Stage 1 should be expected to perform on the direct class") — state plainly whether it does or doesn't, with the actual measured numbers, not a qualitative guess.
  </action>
  <verify>
    <automated>cd /Users/dre/src/bcf-visualization && .venv/bin/python scripts/measure_candidate_accuracy.py && test -f .planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.json && test -f .planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.md && .venv/bin/python -c "
import json
d = json.load(open('.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.json'))
assert set(d['by_evidence_class']) == {'direct','general_only','forward_ref','no_evidence'}
print('OK: report has all 4 evidence classes')
"</automated>
  </verify>
  <acceptance_criteria>
    Both files exist and are git-trackable (not under `data/derived/`, so not caught by the `data/derived/*.json` gitignore rule).
    The JSON report's `by_evidence_class` has all 4 classes, each with the 5 counters from Task 1.
    The markdown report's numbers match the JSON's numbers exactly (spot-checkable by grep).
  </acceptance_criteria>
  <done>Committed JSON + markdown accuracy report exist under the phase directory with real, measured per-evidence-class numbers from the full corpus.</done>
</task>

<task type="auto">
  <name>Task 3: Synthetic-fixture tests for stub derivation and matching</name>
  <files>tests/test_measure_candidate_accuracy.py</files>
  <read_first>
    tests/test_build_exemplar_index.py (pure-function-with-synthetic-fixtures convention to mirror — no epub/real-corpus dependency in these tests, except one explicit real-corpus assertion)
  </read_first>
  <action>
    Add `tests/test_measure_candidate_accuracy.py`: `test_stub_chapter_with_all_empty_evidence_quotes_is_derived` (synthetic overrides doc, one chapter with all-empty `evidence_quotes`, one with a real quote — assert only the first is in the derived set), `test_matching_never_relies_on_roll_number_equality` (construct a curated roll and a candidate with DIFFERENT `roll_number`/`source_ordinal` values but the same chapter-local position — assert they still match; construct a pair with the SAME `roll_number` but wildly different positions/chapters — assert they do NOT match), `test_partial_match_counted_separately_from_full_match` (a candidate with non-empty `_derivation.unfilled_fields` matched to a curated roll — assert it lands in `partial`, not `matched`), `test_curated_roll_with_no_candidate_counted_as_missed` and `test_candidate_with_no_curated_roll_counted_as_unmatched`, `test_determinism_rerun_produces_identical_totals` (run `compute_accuracy` twice against the same synthetic input, assert equal dicts). One real-corpus assertion: `test_live_stub_chapters_exclude_104_include_the_rest` — call `derive_stub_chapters` against the real (Plan-03-01-stamped) corpus via `multi_grab.load_overrides()`, assert `"104" not in stubs` and the other 10 originally-flagged chapter numbers are all present.
  </action>
  <verify>
    <automated>cd /Users/dre/src/bcf-visualization && PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_measure_candidate_accuracy.py -q && PYTHONPATH=scripts .venv/bin/python -m pytest -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    All 7 named tests exist and pass.
    Full `pytest -q` shows exactly the 5 known-accepted pre-existing failures and no others.
  </acceptance_criteria>
  <done>Matching and stub-derivation logic is proven on synthetic fixtures plus one live-corpus assertion; full suite shows no new failures beyond the known-accepted baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate_rolls.json + hand-curated corpus -> committed report | two derived/manual inputs cross into a git-committed artifact Phase 4 trusts to size real inference spend |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | `measure_candidate_accuracy.py`'s matching function | high | mitigate | Matching rule is deterministic and documented in `_method`; a loose matcher that over-counts `matched` would cause Phase 4 to under-provision inference — mitigated by `test_matching_never_relies_on_roll_number_equality` and `test_partial_match_counted_separately_from_full_match` proving the matcher is conservative, not generous, before it ever touches the real corpus |
| T-03-09 | Repudiation | `measure_candidate_accuracy.py`'s output | medium | mitigate | Sorted iteration everywhere (chapter_num, then position) rather than dict/set order; `test_determinism_rerun_produces_identical_totals` proves re-running twice against unchanged inputs yields identical totals, so the committed numbers are reproducible/auditable |
| T-03-10 | Tampering | `data/manual/chapter_roll_overrides.json` (hand-curated data) | critical | accept | Editing the corpus to raise the measured accuracy is explicitly forbidden by the project's curation-authority rule; not code-enforceable within this phase's scope (enforced by review discipline, not a runtime check) |

</threat_model>

<verification>
`.venv/bin/python scripts/measure_candidate_accuracy.py` completes and writes both report files; `PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_measure_candidate_accuracy.py -q` green; full suite shows no new failures beyond the 5 known-accepted baseline.
</verification>

<success_criteria>
- Per-evidence-class accuracy is measured and recorded, not a single headline number.
- Stub-chapter exclusion is derived from data; chapter 104 correctly included (not stubbed) post-2026-08-01 curation.
- Report is git-committed under this phase's directory, readable by Phase 4.
- Matching methodology never joins predictor/curator roll numbering.
</success_criteria>

<output>
Create `.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/03-03-SUMMARY.md` when done
</output>
