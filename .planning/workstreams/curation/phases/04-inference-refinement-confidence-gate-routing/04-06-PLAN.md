---
phase: 04-inference-refinement-confidence-gate-routing
plan: 06
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - scripts/measure_stage2_accuracy.py
  - tests/test_measure_stage2_accuracy.py
  - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/stage2-accuracy-report.md
autonomous: false
requirements: [ACUR-01, ACUR-02]

must_haves:
  truths:
    - "Stage 2's improvement is reported **per evidence class** (`direct`, `general_only`, `forward_ref`, `no_evidence`) against Phase 3's recorded Stage 1 baseline in `candidate-accuracy-report.json`, using the same three-tier curated-position ladder (`word_position`, then the minimum in-chapter `quote_position`, then `ordinal`) so the comparison is like-for-like. A standalone Stage 2 number does not satisfy success criterion 1."
    - "The comparison is computed on the held-out side only. No parameter is changed in this plan, and no tuning decision is revisited after reading held-out numbers."
    - "The regime-boundary sensitivity check is a measurement, not an assertion and not an input: `is_boundary` chapters are reported as routing low more often (or not), with both counts and both denominators shown. Nothing in the code forces a boundary chapter low."
    - "D-18's richness measures are reported alongside the confidence split: fields pre-filled per chapter, and chapters requiring no manual quote-hunting. A run's worth is how much hand-work it removed, not what fraction it auto-wrote."
    - "Every reported percentage is accompanied by its explicit integer numerator and denominator, so no rounding or tie-breaking contract is load-bearing anywhere in the report. [ACUR-02 precision edge]"
    - "Re-running the measurement against unchanged inputs reproduces the report byte-for-byte — sorted iteration throughout, no dict or set ordering leaking into output."
    - "The D-28 tiering recommendation from calibration is either confirmed or overturned by the held-out numbers, and the report states which."
    - "A held-out chapter for which Stage 2 produced no rolls at all is counted explicitly in the per-class denominators as a zero-recovery outcome, never dropped from the denominator. [ACUR-01 empty edge]"
    - "Every held-out chapter is hand-curated, so every one diverts to proposals under CINF-04 and `data/manual/chapter_roll_overrides.json` is byte-unchanged after the held-out run."
    - statement: "If Stage 2 does not measurably move the `forward_ref` class — 68% of predicted rolls and the class the phase exists to reach — the report says so plainly rather than reporting an aggregate that hides it."
      verification: backstop
  artifacts:
    - scripts/measure_stage2_accuracy.py
    - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/stage2-accuracy-report.md
  key_links:
    - "stage2_calibration_split.json holdout side -> run_stage2_batch --split-side holdout"
    - "agent_proposals.json + agent_run_ledger.json -> measure_stage2_accuracy"
    - "candidate-accuracy-report.json by_evidence_class + _position_tier_counts -> the like-for-like Stage 1 baseline"
    - "exemplar_index.json is_boundary -> the D-14 regime-boundary sensitivity measurement"
  prohibitions:
    - "Never tune any parameter after reading held-out numbers; the held-out side is a report surface, not a tuning surface."
    - "Never report a single aggregate figure in place of the per-evidence-class breakdown."
    - "Never force a regime-boundary chapter to low confidence; D-14 is a measurement, not a routing input."
    - "Never lower the confidence bar to raise the high-confidence count."
    - "Never process a chapter with no hand-curated entry in this phase."
    - "Never size a run by a currency ceiling."
    - "Never allow a test in the default pytest suite to perform live inference."
---

<objective>
Break the seal on the held-out side once, measure Stage 2 against Phase 3's recorded Stage 1
baseline per evidence class on the same position ladder, and report the two things the value
function actually cares about: how much hand-work the run removed, and whether regime-boundary
chapters really do route low more often.

Purpose: success criterion 1 is comparative. A standalone Stage 2 number would not answer the
question the phase was built to answer — and Phase 3 just demonstrated how easily a measurement can
mislead when its ruler is wrong.

Output: `scripts/measure_stage2_accuracy.py` and the phase's closing report.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-CONTEXT.md
@.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.md
@.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/stage2-calibration-report.md
@.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-05-SUMMARY.md
@.planning/workstreams/curation/ROADMAP.md
</context>

## Decisions implemented by this plan

| Decision | Where |
|---|---|
| D-14 (regime-boundary chapters must route low more often — verified empirically, never asserted) | T3 |
| D-18 (report the metric that reflects the value function) | T3 |
| D-21 (report on held-out, never on the set that was tuned) | T2 |
| D-22 (per evidence class, against Phase 3's recorded baseline, on the same position ladder) | T2 |
| D-28 (confirm or overturn the calibration tiering recommendation on held-out) | T3 |
| D-29 (the checkpoint approves run size, not dollars) | T1 |

## Reversibility

| Rating | Decision |
|---|---|
| `costly` | Reading the held-out side — once read, it is no longer held out, and a second unbiased report would need a fresh population that does not exist. |
| `reversible` | The report's presentation and metric layout. |

No `one-way` decision exists in this plan. The `checkpoint:decision` in Task 1 exists because the run
consumes Dre's subscription usage window (D-29) and because reading held-out is a once-only act, not
because a door is irreversible.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the held-out run size, and confirm the seal is ready to break</name>
  <decision>The held-out run's chapter count, concurrency, and model tier — and confirmation that tuning is finished, because after this run the held-out side is no longer held out.</decision>
  <context>
    D-21's whole value comes from the held-out side never having informed a parameter. Once it is
    run and read, any later retune would be tuning against the set being reported on, and the
    resulting number could not be trusted. This checkpoint is where Dre confirms the calibration
    verdicts are final.

    What the executor must present before asking:
    - the three calibration verdicts from `stage2-calibration-report.md` — retrieval threshold,
      `get_prose_span` keep-or-drop, and the provisional model-tier recommendation — each with its
      supporting numbers;
    - the held-out chapter count and its `is_boundary` count from
      `data/derived/stage2_calibration_split.json`;
    - the measured per-chapter wall clock and aggregated `usage` from the calibration runs, and the
      projected total for the held-out run at the proposed concurrency;
    - a plain statement that this run consumes usage window and that an interrupted run resumes free
      from the ledger.

    Per D-29 this approves **run size**, not dollars — the struck estimator and `--ceiling-usd` cap
    measured nothing once inference stopped being metered.
  </context>
  <options>
    <option id="full-holdout-recommended-tier">
      <name>Full held-out side on the calibration-recommended tier</name>
      <pros>Cleanest confirmation of the recommendation; the largest denominator for the per-class comparison and the D-14 boundary check.</pros>
      <cons>Cannot distinguish a tier problem from a pipeline problem if the numbers come back weak.</cons>
    </option>
    <option id="full-holdout-both-tiers">
      <name>Full held-out side on both tiers</name>
      <pros>Confirms or overturns the tiering recommendation with a real held-out comparison rather than a calibration-only one.</pros>
      <cons>Roughly double the usage window and wall clock.</cons>
    </option>
    <option id="sample-first">
      <name>A sub-sample of held-out first, then the remainder</name>
      <pros>Catches a systematic problem before consuming the whole window; the remainder still gives a full-population report.</pros>
      <cons>Two approval cycles; the sub-sample's numbers are already read, so it cannot be treated as unread later.</cons>
    </option>
  </options>
  <resume-signal>Select: full-holdout-recommended-tier, full-holdout-both-tiers, or sample-first — and state the approved chapter count, concurrency, and tier(s).</resume-signal>
  <read_first>
    - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/stage2-calibration-report.md — the three verdicts and their numbers
    - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-05-SUMMARY.md — the measured wall clock and aggregated `usage`
    - data/derived/stage2_calibration_split.json — the held-out chapter list and its `is_boundary` count
    - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-CONTEXT.md (D-21, D-29)
  </read_first>
  <action>
    Present the four bullets named in the context block as concrete numbers read from the calibration
    report, the split file and 04-05's SUMMARY — never as estimates invented at checkpoint time.
    State explicitly that approving this run ends tuning. Record the selected option, the approved
    chapter count, concurrency and tier(s) in the plan SUMMARY, and pass them verbatim to
    `run_stage2_batch.py` in Task 2. Build no token or cost estimator to support this presentation.
  </action>
  <verify>
    <human-check>Dre selects an option, states the chapter count, concurrency and tier(s), and confirms tuning is final; all of it is recorded in the SUMMARY and appears verbatim in the Task 2 command line.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the selected option, the approved `--max-chapters`, `--concurrency` and tier(s), and Dre's explicit confirmation that tuning is final.
    - The presentation cites the three calibration verdicts by their numbers and the held-out chapter and `is_boundary` counts from the split file.
    - No token estimator, cost estimator or currency-ceiling flag is created by this task.
  </acceptance_criteria>
  <done>Run size and tier are approved, and the end of tuning is on the record before the held-out side is read.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The held-out run and the like-for-like per-evidence-class comparison</name>
  <reversibility rating="costly">Reading held-out ends its status as held-out; a second unbiased report would need a population that does not exist.</reversibility>
  <precondition>An interactive `claude` CLI subscription auth session is active, `data/derived/stage2_calibration_split.json` exists with a non-empty held-out side, and `.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/stage2-calibration-report.md` records final (not provisional) threshold and tool-surface verdicts.</precondition>
  <files>scripts/measure_stage2_accuracy.py, tests/test_measure_stage2_accuracy.py</files>
  <read_first>
    - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-CONTEXT.md (D-21, D-22)
    - .planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.md — §1a's three-tier position ladder and §2's per-class table, the exact shapes this comparison must mirror
    - scripts/measure_candidate_accuracy.py — the matching algorithm, the `_position_tier_counts` and `by_evidence_class` output blocks, and the sorted-iteration determinism discipline
    - data/derived/candidate_rolls.json — Stage 1's `_derivation` and `evidence_kind` per candidate
    - data/derived/agent_proposals.json — Stage 2's output for the held-out chapters
    - data/derived/agent_run_ledger.json — per-chapter `usage`, `confidence` counts and `read_set`
    - data/manual/chapter_roll_overrides.json — the held-out ground truth
  </read_first>
  <behavior>
    - `measure_stage2_accuracy` re-uses `measure_candidate_accuracy`'s three-tier curated-position ladder verbatim, so Stage 1 and Stage 2 are scored on the same ruler.
    - Every held-out curated roll lands in exactly one evidence class, and the four class denominators sum to the held-out curated-roll total.
    - A held-out chapter for which Stage 2 produced zero rolls contributes to the denominators as a zero-recovery outcome, never a dropped row.
    - Running the measurement twice against unchanged inputs produces byte-identical output.
    - Every reported percentage is emitted together with its integer numerator and denominator.
  </behavior>
  <action>
Run the held-out side at the approved size and tier, then measure it.

Invoke `run_stage2_batch.py --split-side holdout` with the approved `--max-chapters`,
`--concurrency` and `--model`. Never pass `--allow-uncurated`: every held-out chapter is
hand-curated, which is what makes this comparison possible at all, and every one will divert to
proposals under CINF-04 rather than touching the corpus.

Write `scripts/measure_stage2_accuracy.py` with the repo's parse_args / pure-function / main split
and a pure `measure_stage2(overrides_doc, candidates_doc, proposals_doc, ledger_doc, split_doc) ->
dict` core that tests import. Mirror `measure_candidate_accuracy.py` closely enough that the two
reports are readable side by side:

- Resolve each curated roll's position through the **same** three-tier ladder Phase 3 used —
  `word_position` when non-null, else the **minimum** in-chapter `mention_word_position` across the
  roll's evidence quotes (minimum rather than list order, because the connection quote comes first in
  narrative position while later per-perk quotes can trail the roll by thousands of words, and a
  quote naming a different chapter is a different coordinate space and is excluded), else the roll's
  0-based ordinal rank. Report the tier counts, exactly as Phase 3 did, so a reader can see how much
  of the comparison rests on real narrative position.
- Bucket by evidence class (`direct`, `general_only`, `forward_ref`, `no_evidence`) and report, per
  class, Stage 2's matched / partial / missed counts beside Phase 3's recorded figures from
  `candidate-accuracy-report.json`. Deliberately do not collapse to a single headline number: a
  global figure would be dominated by `forward_ref`'s 68% share and would hide where Stage 2
  actually earned its keep. `forward_ref` is the class this phase exists to reach; if Stage 2 does
  not move it, say so plainly.
- Emit every percentage with its integer numerator and denominator so no rounding contract is
  load-bearing.
- Sort every iteration so the output is byte-reproducible, matching `measure_candidate_accuracy.py`'s
  stated sourcing discipline.

Change no parameter in this plan. The held-out side is a report surface, not a tuning surface.

Write `tests/test_measure_stage2_accuracy.py` covering the five behaviors with synthetic documents —
no live inference, no real corpus.
  </action>
  <verify>
    <automated>PYTHONPATH=scripts .venv/bin/python scripts/run_stage2_batch.py --split-side holdout --model "$APPROVED_MODEL" --max-chapters "$APPROVED_MAX" --concurrency "$APPROVED_CONCURRENCY"</automated>
    <automated>PYTHONPATH=scripts .venv/bin/python scripts/measure_stage2_accuracy.py</automated>
    <automated>PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_measure_stage2_accuracy.py -q</automated>
    <automated>git diff --exit-code -- data/manual/chapter_roll_overrides.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/measure_stage2_accuracy.py` exits 0 and writes a JSON report containing a `by_evidence_class` block with all four classes, each carrying Stage 2's counts beside Phase 3's recorded Stage 1 figures, and a `_position_tier_counts` block in the same shape Phase 3 emitted.
    - A test asserts the four class denominators sum to the held-out curated-roll total, and that a chapter with zero Stage 2 rolls appears as a zero-recovery row rather than being dropped.
    - A test asserts the curated-position ladder resolves in the documented precedence, including that a quote whose `mention_chapter_num` differs from the roll's chapter is excluded from the minimum.
    - Running the measurement twice against unchanged inputs produces byte-identical JSON.
    - Every percentage in the JSON has a sibling integer numerator and denominator.
    - `git diff --exit-code -- data/manual/chapter_roll_overrides.json` exits 0 — every held-out chapter is hand-curated and diverted to proposals.
  </acceptance_criteria>
  <done>Stage 2 is measured against Phase 3's baseline on the same ruler, per evidence class, on the held-out side, reproducibly.</done>
</task>

<task type="auto">
  <name>Task 3: The regime-boundary check, the richness metrics, and the phase report</name>
  <files>scripts/measure_stage2_accuracy.py, .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/stage2-accuracy-report.md</files>
  <read_first>
    - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-CONTEXT.md (D-14, D-16, D-18, D-28, and the value-function section in `<domain>`)
    - .planning/workstreams/curation/ROADMAP.md — Phase 4's five success criteria, which this report must answer one by one
    - .planning/workstreams/curation/phases/01-epub-refresh-exemplar-mining/corpus-analysis-report.md — the report voice and structure this one follows
    - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/stage2-calibration-report.md — the provisional tiering recommendation to confirm or overturn
    - data/derived/exemplar_index.json — the `is_boundary` flag per chapter
    - data/derived/agent_run_ledger.json — per-chapter `confidence` counts, `usage`, `read_set`
    - data/derived/agent_proposals.json — the `_stage2.per_roll` blocks carrying `unfilled_fields`
  </read_first>
  <action>
Add the two remaining measurements to `scripts/measure_stage2_accuracy.py` and write the phase's
closing report.

**Regime-boundary sensitivity (D-14).** Partition the held-out chapters by the `is_boundary` flag
from the Phase 1 exemplar index and report the low-confidence share on each side, with both
numerators and both denominators. This is a *measurement*: nothing in the routing code forces a
boundary chapter low, and nothing may be added that does. Report whichever way it comes out. If
boundary chapters do not route low more often, that is a finding about the gate's sensitivity, not a
reason to add a boundary term.

**Richness measures (D-18).** Report, per chapter and in aggregate: fields pre-filled, fields
recorded as evidence-not-found, and the count of chapters requiring no manual quote-hunting — that
is, chapters where every roll arrived with at least one located, position-derived evidence quote.
Report these **alongside** the high/low confidence split, not instead of it. The reason is the value
function: the expensive human act is locating and attaching a quote, not deciding, so a run that
auto-writes little but hands back densely pre-filled chapters is a success, and one that auto-writes
a proud fraction and leaves the rest blank is a failure however good its precision looks.

**Tiering confirmation (D-28).** State whether the held-out numbers confirm or overturn the
calibration report's provisional recommendation, and say which tier the Phase 5 bulk pass should
use. Step down only on evidence — never on price, because there is no price now, and never on
convenience.

Then write `stage2-accuracy-report.md` in the phase directory, following the voice and structure of
Phase 1's `corpus-analysis-report.md` and Phase 3's `candidate-accuracy-report.md`, with an explicit
sourcing-constraint paragraph naming the JSON blocks every number is read from. Answer the ROADMAP's
five Phase 4 success criteria one at a time, in order, each with the number that settles it. Where a
criterion is only partly met, say so plainly — a large proposals volume is a legitimate outcome and a
low high-confidence yield is a finding for Dre's review queue, never a reason to relax verification.

Close with a short section for Phase 5 recording: the recommended model tier, the recommended run
size and concurrency from the measured wall clock and aggregated `usage`, the `get_prose_span`
verdict, and a restatement of the carried obligation that Phase 5 must add a real
`checkpoint:decision` gating the first use of `--allow-uncurated` — the flag alone is necessary but
not sufficient.
  </action>
  <verify>
    <automated>PYTHONPATH=scripts .venv/bin/python scripts/measure_stage2_accuracy.py --with-boundary-check --with-richness</automated>
    <automated>PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_measure_stage2_accuracy.py -q</automated>
    <automated>PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_stage2_*.py -q</automated>
    <automated>.venv/bin/python scripts/verify.py</automated>
  </verify>
  <acceptance_criteria>
    - The JSON report contains a boundary block with low-confidence numerators and denominators for both the `is_boundary` and non-boundary partitions of the held-out side.
    - A test asserts the boundary partition is derived from `exemplar_index.json`'s flag and that no routing code path consults it — `grep -rn 'is_boundary' scripts/stage2_confidence.py scripts/stage2_routing.py scripts/stage2_response.py` returns nothing.
    - The JSON report contains per-chapter and aggregate `fields_prefilled`, `fields_not_found`, and `chapters_requiring_no_manual_quote_hunting`, each with integer counts.
    - `stage2-accuracy-report.md` exists, names the JSON blocks each figure is read from, and answers all five ROADMAP Phase 4 success criteria in order with a supporting number each.
    - The report states whether the held-out numbers confirm or overturn the calibration tiering recommendation, and names the tier Phase 5 should use.
    - The report's Phase 5 section records the recommended run size and concurrency from measured wall clock and aggregated `usage`, the `get_prose_span` verdict, and the carried `--allow-uncurated` checkpoint obligation.
    - `.venv/bin/python scripts/verify.py` shows no NEW failures beyond the documented baseline of 5.
  </acceptance_criteria>
  <done>The regime-boundary question is answered with numbers rather than an assertion, the value-function metrics are reported alongside the confidence split, and the phase closes with a report that answers each ROADMAP success criterion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| held-out side → report | A once-only read; contamination here cannot be undone. |
| measurement code → success claim | Where a wrong ruler produces a confident wrong answer, as Phase 3 demonstrated. |
| held-out run → subscription usage window | A bounded, human-approved consumption. |

## STRIDE Threat Register (ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Tampering (measurement integrity) | post-hoc retuning after reading held-out | high | mitigate | Tuning is declared final at the Task 1 checkpoint and recorded in the SUMMARY; this plan's `files_modified` contains no tunable module (T1, T2). |
| T-04-23 | Tampering | a different position ladder for Stage 2 than for Stage 1 | high | mitigate | The three-tier ladder is reused from `measure_candidate_accuracy.py` and the tier counts are reported so a reader can audit how much rests on real narrative position (T2). |
| T-04-24 | Tampering | D-14 becoming a routing input rather than a measurement | medium | mitigate | A grep criterion asserts `is_boundary` appears in no routing or grading module (T3). |
| T-04-20 | Elevation of privilege | processing chapters with no ground truth | high | mitigate | The held-out list contains only hand-curated chapters; `--allow-uncurated` is never passed; the corpus diff is asserted empty (T2). |
| T-04-09 | Denial of service (self-inflicted) | held-out run size | medium | mitigate | Human `checkpoint:decision` approves chapter count, concurrency and tier; the ledger makes an interrupted run free to resume (T1). |
| T-04-08 | Information disclosure | prose in the committed report | high | mitigate | The report carries counts, rates and verdicts only; no prose beyond the established evidence-quote convention reaches a committed artifact (T3). |
</threat_model>

<verification>
- `PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_stage2_*.py tests/test_measure_stage2_accuracy.py -q` — zero failures.
- `.venv/bin/python scripts/verify.py` — no NEW failures beyond the documented baseline of 5 (4 in `tests/test_forge_curator.py`, 1 in `tests/test_roll_ordinal_contract.py::test_chapter_55_1_source_roll_six_borrows_first_56_prediction`). Judge by the failure set, never by exit 0.
- `git diff --exit-code -- data/manual/chapter_roll_overrides.json` — the hand-curated corpus is byte-unchanged across the entire phase.
- Re-running `scripts/measure_stage2_accuracy.py` produces byte-identical JSON.
</verification>

<success_criteria>
- Stage 2's improvement over Stage 1 is reported per evidence class on the same three-tier position ladder, on the held-out side.
- The regime-boundary sensitivity check is reported with counts and denominators on both partitions, and no routing code consults `is_boundary`.
- D-18's richness measures are reported alongside the confidence split.
- The calibration tiering recommendation is confirmed or overturned on held-out.
- The report answers all five ROADMAP Phase 4 success criteria, each with a supporting number.
- No parameter was changed after the held-out side was read.
</success_criteria>

## Artifacts this phase produces

**New files**
`scripts/measure_stage2_accuracy.py`, `tests/test_measure_stage2_accuracy.py`,
`.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/stage2-accuracy-report.md`,
`stage2-accuracy-report.json` (paired measurement output)

**New symbols**
`measure_stage2`, `resolve_curated_position`, `boundary_sensitivity`, `richness_metrics`

**New CLI flags**
`scripts/measure_stage2_accuracy.py --with-boundary-check`, `--with-richness`, `--output`

<output>
Create `.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-06-SUMMARY.md` when done.
</output>
