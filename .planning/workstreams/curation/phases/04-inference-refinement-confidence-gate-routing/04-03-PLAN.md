---
phase: 04
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - scripts/stage2_dry_run.py
  - scripts/stage2_client.py
  - scripts/run_stage2_batch.py
  - tests/test_stage2_dry_run.py
  - tests/test_run_stage2_batch.py
autonomous: true
requirements: [CINF-04, ACUR-01]

must_haves:
  truths:
    - "Before any multi-chapter run, a dry-run token/cost estimate (request count, cached-prefix size, per-request input tokens, projected cost) is printed and the run refuses to proceed without an explicit confirmation flag or ceiling (D-23)."
    - "A multi-chapter run submits requests via the Anthropic Message Batches API, keys results by custom_id, and never assumes positional correspondence between the request list and the results iterator (D-06)."
    - "A multi-chapter run refuses, by default, to target any chapter that is not already present with curated_by: human in chapter_roll_overrides.json — targeting an uncurated chapter requires an explicit --allow-uncurated flag, which is the code-level realization of D-24's 'checkpoint before the first uncurated run' since Phase 4 itself never runs against uncurated chapters (Phase 5's ACUR-05 batch is the first consumer of that flag)."
  artifacts:
    - path: "scripts/stage2_dry_run.py"
      provides: "estimate_stage2_cost() and its CLI — the D-23 spend gate"
    - path: "scripts/run_stage2_batch.py"
      provides: "the Batches-API multi-chapter runner reusing every Plan 04-01/04-02 module unmodified"
  key_links:
    - from: "scripts/run_stage2_batch.py"
      to: "scripts/stage2_dry_run.py:estimate_stage2_cost"
      via: "the batch runner refuses to submit without a prior estimate having been printed and an explicit --yes/--ceiling-usd confirmation"
      pattern: "estimate_stage2_cost"
    - from: "scripts/run_stage2_batch.py"
      to: "scripts/stage2_ledger.py:needs_reprocessing"
      via: "each chapter in the batch is filtered through the ledger before being included in the submitted request list, so a batch run is itself idempotent across restarts"
      pattern: "needs_reprocessing"
  prohibitions:
    - "Never submit a Batches API request without first printing a dry-run cost estimate in the same invocation."
    - "Never target an uncurated chapter (no curated_by: human entry) from run_stage2_batch.py without the caller explicitly passing --allow-uncurated."
    - "Never assume Batches API results arrive in the same order as the submitted request list — always key by custom_id."
    - "Never call the live Anthropic API (including count_tokens or batches.create) from a test collected by the default pytest run."
---

<objective>
Add spend discipline (D-23) and scale (D-06/D-07/D-08) on top of Plan 04-01's single-chapter
pipeline: a dry-run cost estimator that must run before any spend, and a Message Batches API
runner that processes N chapters through the exact same retrieval/prompt/verify/gate/routing/
ledger modules Plan 04-01 and 04-02 already built and proved — no second implementation of any
of those, only a new submission/collection layer plus the hard uncurated-chapter guard that
stands in for D-24's checkpoint at the code level.

Purpose: This is the mechanism Plans 04-04 (calibration) and 04-05 (held-out) actually invoke to
spend real money on more than one chapter; it is also the mechanism Phase 5's `--allow-uncurated`
consumer will reuse for the 80-chapter bulk run.
Output: `scripts/stage2_dry_run.py` and `scripts/run_stage2_batch.py`, both covered by
mocked-client tests proving the spend gate and the uncurated guard fire correctly with zero
actual spend.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-CONTEXT.md
@scripts/stage2_client.py
@scripts/stage2_ledger.py
@scripts/stage2_retrieval.py
@scripts/stage2_prompt.py
@scripts/stage2_verify.py
@scripts/stage2_confidence.py
@scripts/stage2_routing.py
@scripts/chapter_roll_overrides_io.py

<interfaces>
From the bundled `claude-api` skill (Python, Message Batches API — authoritative for this task, do not guess the shape):
- `client.messages.batches.create(requests=[Request(custom_id="chapter-92", params=MessageCreateParamsNonStreaming(model=..., max_tokens=..., system=..., messages=[...], output_config={...})), ...])` — from `anthropic.types.message_create_params.MessageCreateParamsNonStreaming` and `anthropic.types.messages.batch_create_params.Request`.
- Poll: `client.messages.batches.retrieve(batch_id).processing_status` until `"ended"`.
- Collect: `for result in client.messages.batches.results(batch_id):` — `result.custom_id`, `result.result.type` (`succeeded|errored|canceled|expired`), `result.result.message.content` on success. **Results arrive in arbitrary order — always index the collected dict by `result.custom_id`, never by position in the request list.**
- Token counting for the dry-run estimator: `client.messages.count_tokens(model=..., messages=[...], system=...) -> resp.input_tokens`. Never use `tiktoken` (wrong tokenizer for Claude).
- Prompt caching in a batch: `messages.batches.create` requests generally support the same `cache_control` mechanics as synchronous requests — the shared system prefix `scripts.stage2_prompt.build_stage2_system_blocks(...)`'s `cache_control` marker on the last block carries through unchanged into each `Request`'s `params.system`. Group requests by `target_regime` (same regime -> same exemplar set -> identical system-prefix bytes -> cache hits across the group) before submitting, sorted so same-regime chapters are adjacent in the request list — this is a request-ORDERING detail only, it does not change `custom_id` keying.
- Pricing for the estimator: `claude-opus-5` is $5.00/1M input, $25.00/1M output (Batches API halves both). `claude-sonnet-5` is $3.00/1M input ($2.00 intro through 2026-08-31), $15.00/1M output ($10.00 intro), also halved under Batches. Hardcode these as named constants with a comment citing the cached-2026-06-24 skill pricing table — do not invent a live pricing lookup.

From Plan 04-01/04-02 (reuse unmodified — do not re-derive):
- `scripts/stage2_client.py:call_stage2_sync` — the single-request shape this task's batch submission mirrors; `client` is already an injectable parameter.
- `scripts/stage2_ledger.py:needs_reprocessing`/`load_agent_run_ledger_doc` — filter the target chapter list through this before building the request list.
- `scripts/chapter_roll_overrides_io.py:load_chapter_roll_overrides_doc` — the source of truth for "is this chapter already curated_by: human" (curated) used by both the CINF-04 never-overwrite guard (Plan 04-01, per-write) and this plan's new uncurated-chapter CLI guard (per-run, before submission).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dry-run cost estimator (D-23)</name>
  <read_first>scripts/stage2_prompt.py (build_stage2_system_blocks/build_stage2_user_message), scripts/stage2_client.py (existing constants), the bundled claude-api skill's Python token-counting section (`client.messages.count_tokens`) and cached model-pricing table (2026-06-24), .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-CONTEXT.md D-23.</read_first>
  <files>scripts/stage2_dry_run.py, tests/test_stage2_dry_run.py</files>
  <behavior>
    - `estimate_stage2_cost(chapter_nums, model, *, client=None) -> dict` returns `{"request_count": int, "cached_prefix_tokens": int, "per_request_input_tokens": dict[str,int], "output_tokens_estimate": int, "projected_cost_usd": float}`, computed via `client.messages.count_tokens` calls against the actual system/user blocks each chapter would submit (never a hand-derived token-per-word heuristic).
    - CLI (`--chapters`, `--model`) prints the estimate and exits 0; it never submits a batch itself — that is `run_stage2_batch.py`'s job (Task 2), which imports and calls this function before submitting.
    - Test: with a mocked `count_tokens` returning a fixed value, assert `projected_cost_usd` scales linearly with `request_count` and correctly differs between `claude-opus-5` and `claude-sonnet-5` pricing constants.
  </behavior>
  <action>
    Implement `scripts/stage2_dry_run.py` per the `<behavior>` block, reusing `scripts.stage2_prompt.build_stage2_system_blocks`/`build_stage2_user_message` to build the exact blocks each chapter's real request would send (so the estimate reflects real prompt content, not a guess), and `scripts.stage2_client`'s pricing constants (add `PRICING = {"claude-opus-5": {"input": 5.00, "output": 25.00}, "claude-sonnet-5": {"input": 3.00, "output": 15.00}}` per-million-token USD, batch-halved in the cost formula: `cost = 0.5 * (input_tokens/1e6 * PRICING[model]["input"] + output_tokens_estimate/1e6 * PRICING[model]["output"])`). Set `output_tokens_estimate` to a fixed per-request constant (e.g. 2000 tokens, documented as a rough ceiling for a chapter's worth of graded rolls — do not attempt to predict actual output length).
  </action>
  <verify>
    <automated>PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_stage2_dry_run.py -q</automated>
  </verify>
  <acceptance_criteria>Test passes; `grep -rn "anthropic.Anthropic()" tests/test_stage2_dry_run.py` returns no matches.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Batches-API multi-chapter runner + uncurated-chapter hard guard</name>
  <read_first>scripts/run_stage2_chapter.py (the per-chapter chain to extract a shared post-API-call helper from), scripts/stage2_ledger.py (needs_reprocessing), scripts/stage2_dry_run.py (Task 1's estimator, this task's own plan), scripts/chapter_roll_overrides_io.py (load_chapter_roll_overrides_doc), the bundled claude-api skill's Python Message Batches API section (batches.create/retrieve/results, custom_id keying, arbitrary result order), .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-CONTEXT.md D-06/D-07/D-23/D-24.</read_first>
  <files>scripts/run_stage2_batch.py, tests/test_run_stage2_batch.py</files>
  <behavior>
    - `run_stage2_batch(chapter_nums, model, *, allow_uncurated=False, ceiling_usd=None, client=None)`: (1) if `not allow_uncurated`, load `chapter_roll_overrides_io.load_chapter_roll_overrides_doc()` and raise `ValueError` listing every chapter in `chapter_nums` that is absent or not `curated_by == "human"`; (2) filter `chapter_nums` through `stage2_ledger.needs_reprocessing` per chapter, dropping already-processed ones; (3) call `stage2_dry_run.estimate_stage2_cost` on the filtered list and print it; (4) if `ceiling_usd is not None` and the estimate's `projected_cost_usd` exceeds it, raise `ValueError` without submitting; (5) build one `Request` per remaining chapter with `custom_id=f"chapter-{chapter_num}"`, submit via `client.messages.batches.create`, poll `processing_status` until `"ended"`, then iterate `client.messages.batches.results(batch_id)` keyed by `result.custom_id`; (6) for each succeeded result, run it through the SAME `stage2_verify` -> `stage2_confidence` -> `stage2_routing` -> `stage2_ledger.record_run` chain `run_stage2_chapter.py` already uses per-chapter (extract that chain into a shared helper both CLIs call, rather than duplicating it — see action).
    - Test 1 (uncurated guard): a target list containing one chapter absent from a synthetic overrides doc -> `run_stage2_batch(..., allow_uncurated=False)` raises before any client call.
    - Test 2 (custom_id keying, never positional): a mocked `batches.results` iterator that yields results in a DIFFERENT order than the submitted request list -> assert every chapter's output is routed to the correct chapter (mismatched-order regression guard).
    - Test 3 (ledger pre-filter): a chapter already `needs_reprocessing == False` -> excluded from the submitted request list entirely (assert the mocked `batches.create` call's `requests` argument omits it).
  </behavior>
  <action>
    First, refactor `scripts/run_stage2_chapter.py`'s per-chapter "verify -> grade -> route -> ledger-record" tail into a shared function `process_stage2_response(chapter_num, raw_rolls, stage1_candidates, ...) -> str` (returns the routing decision), importable from both `run_stage2_chapter.py` and the new `run_stage2_batch.py` — this is the no-second-implementation guard for the post-API-call half of the pipeline, mirroring how the pre-API-call half (retrieval/prompt) is already shared. Then implement `run_stage2_batch.py` per the `<behavior>` block above, with a CLI (`--chapters` comma-separated, `--model`, `--allow-uncurated` flag default `False`, `--ceiling-usd` float, `--yes` flag required to actually submit after the estimate prints — omitting `--yes` prints the estimate and exits 0 without spending, matching D-23's "start with a small pilot... verify... then scale" posture).
  </action>
  <verify>
    <automated>PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_run_stage2_batch.py -q</automated>
  </verify>
  <acceptance_criteria>All three tests pass. `grep -rn "anthropic.Anthropic()" tests/test_run_stage2_batch.py` returns no matches.</acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase (Plan 04-03 portion) Produces

- `scripts/stage2_dry_run.py` — `estimate_stage2_cost()`, CLI flags `--chapters`, `--model`
- `scripts/stage2_client.py` — adds `PRICING` constant (modification, not new file)
- `scripts/run_stage2_batch.py` — `run_stage2_batch()`, CLI flags `--chapters`, `--model`, `--allow-uncurated`, `--ceiling-usd`, `--yes`
- `scripts/run_stage2_chapter.py` — extracts `process_stage2_response()` as a shared helper (modification, not new file)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags -> real spend | `--yes`/`--allow-uncurated`/`--ceiling-usd` are the only gates between an operator invocation and a real, multi-chapter API bill. |
| Batches API results -> per-chapter routing | Untrusted, arbitrarily-ordered results must be correctly attributed back to their originating chapter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Denial of Service (budget) | run_stage2_batch.run_stage2_batch | high | mitigate | Estimate-then-`--yes`-then-`--ceiling-usd` three-gate sequence; omitting `--yes` is a no-op preview by construction, matching D-23. |
| T-04-12 | Tampering | run_stage2_batch (uncurated guard) | critical | mitigate | Default-deny on any chapter lacking a `curated_by: human` entry; explicit `--allow-uncurated` required, and this plan's scope never sets it (Phase 5 is the only future consumer). |
| T-04-13 | Repudiation | run_stage2_batch (custom_id keying) | high | mitigate | Results are indexed exclusively by `result.custom_id`; test 2 asserts correct attribution under out-of-order results. |

</threat_model>

<verification>
- `PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_stage2_dry_run.py tests/test_run_stage2_batch.py -q` passes.
- `PYTHONPATH=scripts .venv/bin/python -m pytest -q` shows exactly the 5 known-accepted baseline failures.
</verification>

<success_criteria>
- `stage2_dry_run.py` prints a cost estimate for a chapter list using real `count_tokens` calls against real prompt content (proven with a mocked client in tests; the CLI itself would call the real endpoint when actually invoked).
- `run_stage2_batch.py` refuses an uncurated target chapter without `--allow-uncurated`, refuses to submit without `--yes`, and correctly attributes out-of-order Batches API results back to their chapters.
</success_criteria>

<output>
Create `.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-03-SUMMARY.md` when done
</output>
