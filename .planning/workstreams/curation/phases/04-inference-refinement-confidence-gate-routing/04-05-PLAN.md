---
phase: 04
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - scripts/measure_stage2_accuracy.py
  - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/held-out-accuracy-report.md
  - .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/held-out-accuracy-report.json
  - tests/test_measure_stage2_accuracy.py
autonomous: false
requirements: [ACUR-01, ACUR-02]

must_haves:
  truths:
    - "Stage 2's per-evidence-class accuracy on the held-out split is reported against Phase 3's recorded Stage 1 baseline (candidate-accuracy-report.json) using the same three-tier position ladder, so the comparison is like-for-like (D-22, ROADMAP success criterion 1)."
    - "The held-out run uses whichever model calibration-report.json's _model_tiering_decision recorded, never a re-litigated or independently-guessed choice."
    - "The held-out run spends real API budget only after a printed dry-run cost estimate and an explicit checkpoint approval, informed by the calibration numbers already on record."
    - "The report also states D-18's richness metrics — fields pre-filled per chapter, chapters requiring no further manual quote-hunting — alongside the high/low confidence split, because the value function measures effort saved, not autonomous-write share."
  artifacts:
    - path: ".planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/held-out-accuracy-report.md"
      provides: "the phase-closing measurement: Stage 2 vs Stage 1 per evidence class, regime-boundary check, D-18 richness metrics"
  key_links:
    - from: "held-out-accuracy-report.json"
      to: ".planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.json"
      via: "every number in the held-out report's per-evidence-class comparison table is read directly from Phase 3's recorded JSON, never independently recomputed"
      pattern: "candidate-accuracy-report.json"
  prohibitions:
    - "Never write the held-out report using a different position-tier methodology than Phase 3's three-tier ladder (word_position -> quote_position -> ordinal) — recompute the SAME ladder against Stage 2's own output, do not invent a fourth tier."
    - "Never let the held-out run proceed without Task 1's checkpoint being explicitly approved."
    - "Never silently pass the regime-boundary backstop check — if the held-out data cannot demonstrate boundary-flagged chapters routing to low confidence more often than non-boundary chapters, report that finding plainly rather than asserting it anyway."
  flagged_assumptions:
    - "ACUR-03's routing-correctness acceptance surface (introduced in Plan 04-01) is closed out here by the held-out report's confirmation that every routed corpus write, if any occurred, passed the never-overwrite guard — ROADMAP success criterion 3 is the final arbiter, not a separate spec item."
---

<objective>
Run Stage 2 against the untouched held-out split using the model calibration selected (Plan
04-04), then produce the phase-closing measurement: per-evidence-class accuracy against Phase
3's recorded Stage 1 baseline (D-22), a regime-boundary sensitivity check (D-14, a `backstop`
must-have — demonstrated empirically or reported as not demonstrated, never silently asserted),
and D-18's richness metrics (fields pre-filled per chapter, chapters needing no manual
quote-hunting) — because the value function measures effort saved, not autonomous-write share.

Purpose: This is the number Phase 5 reads to size its own bulk uncurated run, and the evidence
that Stage 2 earned its inference budget on the class Stage 1 could not move — `forward_ref`,
68% of the corpus.
Output: `held-out-accuracy-report.md`/`.json`, gated by one final `checkpoint:human-verify`
(spend approval, informed by the calibration numbers) before the held-out spend.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-CONTEXT.md
@.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/calibration-report.md
@.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.md
@.planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.json
@scripts/measure_candidate_accuracy.py
@scripts/measure_stage2_accuracy.py
@scripts/run_stage2_batch.py

<interfaces>
From Plan 04-04 (already built):
- `scripts/measure_stage2_accuracy.py:compute_calibration_report` — this plan adds a sibling entry point in the same module (do not duplicate the position-tier / comparison logic between the two functions — share a common `_position_tier_for_roll()`/`_match_stage2_to_curated()` helper both call).
- `data/derived/calibration_split.json`'s `held_out` list — the target chapter set for this plan's run.
- `.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/calibration-report.json`'s `_model_tiering_decision` field — the model this plan's Task 2 must use, read programmatically, never re-typed by hand.

From `scripts/measure_candidate_accuracy.py` (Phase 3's own measurement — mirror its methodology exactly, cite it, do not reinvent):
- The three-tier curated-side position ladder (`word_position` -> `quote_position` [minimum `mention_word_position` across same-chapter evidence_quotes] -> `ordinal` fallback) and its `_position_tier_counts` reporting shape — read `candidate-accuracy-report.md` Section 1a for the full precedence rule and reuse it verbatim against Stage 2's held-out output instead of Stage 1's candidates.
- The per-evidence-class table shape (`direct/general_only/forward_ref/no_evidence` x `Curated rolls/Matched/Partial/Missed/Unmatched candidates/Proposed/Missed rate`) — Section 2 of `candidate-accuracy-report.md` is the exact table shape `held-out-accuracy-report.md` must produce for Stage 2, so the two reports read side by side without translation.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Spend approval — held-out run, informed by calibration</name>
  <read_first>.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/calibration-report.md (Plan 04-04's output), data/derived/calibration_split.json.</read_first>
  <what-built>
    `calibration-report.md` (Plan 04-04) with per-model confidence-gate agreement numbers and a
    recorded `_model_tiering_decision`. `scripts/stage2_dry_run.py` (Plan 04-03) can now estimate
    the held-out run's cost using that decided model against `calibration_split.json`'s
    `held_out` chapter list.
  </what-built>
  <how-to-verify>
    1. Read `.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/calibration-report.md` — confirm the calibration numbers and the `_model_tiering_decision` are acceptable to proceed on.
    2. Run `PYTHONPATH=scripts .venv/bin/python scripts/stage2_dry_run.py --chapters "$(python3 -c "import json;print(','.join(json.load(open('data/derived/calibration_split.json'))['held_out']))")" --model "$(python3 -c "import json;print(json.load(open('.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/calibration-report.json'))['_model_tiering_decision'])")"` and review the printed cost estimate.
    3. Confirm this is acceptable to spend. There is no ground truth past this line for a real uncurated run (D-24) — but the held-out set IS still curated, so this run remains self-checking; the decision here is purely about spend size, not about crossing the uncurated boundary (Phase 4 never crosses it — see Plan 04-03's `--allow-uncurated` guard).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the held-out run at the estimated cost, or state a reduced chapter subset / abort.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute held-out run</name>
  <read_first>.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/calibration-report.json (_model_tiering_decision), data/derived/calibration_split.json (held_out), scripts/run_stage2_batch.py.</read_first>
  <files>scripts/measure_stage2_accuracy.py</files>
  <action>
    Read the calibration-selected model from `calibration-report.json["_model_tiering_decision"]` and the held-out chapter list from `data/derived/calibration_split.json["held_out"]`. Call `run_stage2_batch.run_stage2_batch(held_out_chapters, selected_model, allow_uncurated=False, ceiling_usd=<Task 1's approved ceiling>, client=None)`. As with calibration, every held-out chapter is `curated_by: "human"`, so every output routes to the proposals sidecar — this is expected. `stage2_ledger` naturally makes this resumable if interrupted (Plan 04-02).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('data/derived/agent_proposals.json')); split=json.load(open('data/derived/calibration_split.json'))['held_out']; present=[c for c in split if c in d.get('agent_proposals',{})]; assert len(present) == len(split), (len(present), len(split))"</automated>
  </verify>
  <acceptance_criteria>Every chapter in `calibration_split.json`'s `held_out` list has a corresponding entry in `agent_proposals.json` after this task.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-evidence-class report vs Stage 1, regime-boundary check, D-18 richness metrics</name>
  <read_first>scripts/measure_candidate_accuracy.py (three-tier position ladder Section 1a, per-evidence-class table shape Section 2, _method algorithm), scripts/measure_stage2_accuracy.py (Plan 04-04's compute_calibration_report, to share helpers with), scripts/build_exemplar_index.py (_tag_chapter, for is_boundary), .planning/workstreams/curation/phases/03-provenance-schema-deterministic-candidate-assembly/candidate-accuracy-report.md and .json, .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-CONTEXT.md D-14/D-18/D-22.</read_first>
  <files>scripts/measure_stage2_accuracy.py, .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/held-out-accuracy-report.md, .planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/held-out-accuracy-report.json, tests/test_measure_stage2_accuracy.py</files>
  <behavior>
    - `compute_held_out_report(proposals_doc, overrides_doc, held_out_chapter_nums, stage1_baseline_doc, exemplar_index_doc) -> dict`: for each held-out chapter's curated rolls, resolve the curated-side position via the same three-tier ladder Phase 3 used (word_position -> quote_position -> ordinal), match against Stage 2's proposed rolls for that chapter by position proximity (never by `roll_number`), and bucket into the same `direct/general_only/forward_ref/no_evidence` classes Phase 3's report already assigns per curated roll — read the evidence class from Stage 1's own `candidate_rolls.json`/`roll_text_evidence.json` per roll (never re-derive evidence_kind, it already exists upstream).
    - `_position_tier_for_roll` / `_match_stage2_to_curated`: extracted as shared helpers importable from `compute_calibration_report` (Plan 04-04) too — no duplicated matching logic between the two report functions.
    - Regime-boundary check: partition held-out chapters into boundary (`is_boundary` from `build_exemplar_index._tag_chapter`, reused) vs non-boundary, compute each group's low-confidence rate, and record whether boundary chapters route to low confidence at a measurably higher rate — report the actual numbers either way; if the direction is NOT demonstrated, say so plainly rather than asserting D-14.
    - D-18 richness metrics: per chapter, count of roll fields pre-filled (non-null/non-empty across `perks`, `outcome`, `constellation`, `evidence_quotes`) versus total fields, and a count of chapters where every roll has zero `unfilled_fields` (i.e. requires no further manual quote-hunting at all).
    - Test: a small synthetic `proposals_doc`/`overrides_doc`/`stage1_baseline_doc` fixture (3-4 chapters, at least one boundary chapter, at least one of each evidence class) proving the per-class bucketing and the richness-metric counts are correct against hand-computed expected values.
  </behavior>
  <action>
    Implement `compute_held_out_report` per the `<behavior>` block in `scripts/measure_stage2_accuracy.py` (alongside Plan 04-04's `compute_calibration_report`, sharing the extracted helpers). Write `held-out-accuracy-report.json` (bare `json.dumps`, unregistered — same QA-instrument posture as Phase 3's report) containing the per-evidence-class table (Stage 2 vs Stage 1, side by side, reading Stage 1's numbers directly from `candidate-accuracy-report.json` per the interfaces note — never recomputed), the regime-boundary numbers, and the D-18 richness metrics. Author `held-out-accuracy-report.md` as a hand-written prose summary sourced only from the JSON's own numbers, mirroring `candidate-accuracy-report.md`'s structure and its "Sourcing constraint" discipline (every number traceable to the JSON, nothing independently re-derived in the prose).
  </action>
  <verify>
    <automated>PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_measure_stage2_accuracy.py -q</automated>
  </verify>
  <acceptance_criteria>
    Test passes. `held-out-accuracy-report.json` exists and contains a `direct/general_only/forward_ref/no_evidence` breakdown for Stage 2 alongside Phase 3's Stage 1 numbers, a `_regime_boundary_check` object with real computed rates for both groups (not a boolean assertion with no numbers behind it), and a `_richness_metrics` object with per-chapter pre-fill counts and a `chapters_requiring_no_manual_quote_hunting` count.
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase (Plan 04-05 portion) Produces

- `scripts/measure_stage2_accuracy.py` — adds `compute_held_out_report()`, `_position_tier_for_roll()`, `_match_stage2_to_curated()` (module started in Plan 04-04)
- `.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/held-out-accuracy-report.md` / `.json` — the phase-closing committed report

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint:human-verify approval -> real spend | The only gate between reviewing calibration numbers and spending on the held-out run. |
| held-out-accuracy-report.json -> Phase 5 sizing decision | A wrong or inflated number here mis-sizes Phase 5's bulk uncurated spend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Denial of Service (budget) | Task 1/2 (held-out spend) | high | mitigate | Blocking `checkpoint:human-verify` requires reviewing both the calibration report and a fresh, real cost estimate for the held-out set before Task 2 may run. |
| T-04-17 | Repudiation | held-out-accuracy-report.json | medium | mitigate | Every reported number is either read directly from `candidate-accuracy-report.json` (Stage 1 side) or computed by a tested pure function (Stage 2 side) — no hand-typed numbers in the prose report, matching Phase 3's own sourcing-constraint discipline. |
| T-04-18 | Tampering (silent gap) | `_regime_boundary_check` | medium | mitigate | The backstop must-have is reported with real numbers whichever way they point; a non-demonstration is written down explicitly rather than the D-14 claim being asserted regardless. |

</threat_model>

<verification>
- `PYTHONPATH=scripts .venv/bin/python -m pytest tests/test_measure_stage2_accuracy.py -q` passes.
- `PYTHONPATH=scripts .venv/bin/python -m pytest -q` shows exactly the 5 known-accepted baseline failures.
- `held-out-accuracy-report.json` exists, is valid JSON, and contains `_regime_boundary_check` and `_richness_metrics` keys.
</verification>

<success_criteria>
- Stage 2's held-out accuracy is reported per evidence class against Phase 3's recorded baseline using the identical three-tier ladder.
- The regime-boundary sensitivity claim is backed by real, reported numbers — never a silent pass.
- D-18's richness metrics are reported alongside the confidence split, closing the value-function reporting requirement.
- This is the last plan in Phase 4; ROADMAP success criteria 1-5 are all addressed across Plans 04-01 through 04-05.
</success_criteria>

<output>
Create `.planning/workstreams/curation/phases/04-inference-refinement-confidence-gate-routing/04-05-SUMMARY.md` when done
</output>
