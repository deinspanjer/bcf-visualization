---
phase: 04-mobile-cutover-accessibility
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - tests/test_lighthouse_accessibility.py
  - web/mobile.css
  - index.html
autonomous: false
requirements: [MOBX-03]
user_setup: []

must_haves:
  truths:
    - "Lighthouse Accessibility scores 90 or higher on the MOBILE preset, and the score is produced by a committed, re-runnable pytest module rather than a one-off manual invocation — a gate that cannot be re-run silently rots, and this is the last phase that will look at it (D-40)."
    - "The form factor is ASSERTED from the report, not assumed. MOBX-03 says mobile preset; a run that silently used the desktop preset would produce a different, easier score against a layout this milestone did not build."
    - "The Lighthouse tool lives entirely outside `web/`. It is invoked ephemerally and is never added to any package.json, lockfile or bundler config in this repo — `web/` stays dependency-free and build-step-free per the project's own constraint."
    - "The tool version is PINNED. An unpinned invocation resolves to whatever was published most recently, so the gate would measure a different tool on every run and the legitimacy review below would apply to a version nobody inspected."
    - "A sub-90 score is worked as a task list, not escalated as a checkpoint. Accessibility findings are concrete and mostly mechanical — a missing accessible name is fixed and re-run, never turned into a blocking question for Dre. Only a finding that would force a genuine design change escalates (D-41)."
    - "Any remediation stays inside this milestone's write surface: `web/mobile.css` and `index.html` only. A finding whose only fix is an edit to the frozen desktop stylesheet escalates to Dre rather than being fixed."
    - "The Lighthouse result is NOT evidence for MOBX-03's 44x44 clause. The audit's tap-target rule measures 24x24 — a lower, different success criterion — so the two checks are independent and both are required (RESEARCH Pitfall 1)."
    - "The Lighthouse run happens against a real local http URL from the existing staged-site fixture, not a file URL and not a bespoke server — the app fetches its data bundle over http and would render an error state otherwise."
    - "D-03 is re-verified against the real audit, closing the ROADMAP's own Phase 4 note: page zoom stays ENABLED and the viewport meta never disables user scaling. The Phase 1 interview weighed this against the accessibility score and chose zoom; the audit is the first chance to confirm that choice did not cost the score, and it must not be reversed to gain points."
    - "The npm package this gate executes has been reviewed by a human before its first invocation. The automated legitimacy seam returned a SUS verdict, and an agent does not clear a SUS verdict on its own."

  prohibitions:
    - "MUST NOT add lighthouse — or any other tool — as a dependency of `web/`, or create a package.json, lockfile or bundler config anywhere in this repo to make the run easier. The zero-build-step constraint on the shipped app is the reason this project has no supply-chain surface at runtime, and a test-time convenience is not worth opening one."
    - "MUST NOT report the Lighthouse score as evidence that tap targets are 44x44. The two live under one requirement's text and measure different thresholds; conflating them publishes an accessibility conformance claim the code has not earned."
    - "MUST NOT lower the 90 threshold, switch to the desktop preset, or narrow the audited page set to make the number pass. The gate exists to be inconvenient in exactly the case where the app is not accessible."
    - "MUST NOT disable user scaling in the viewport meta to influence the score. D-03 chose page zoom over the audit's convenience at the Phase 1 interview, and zoom is how readers with low vision use this app at all — trading it for points would make the score better and the app worse for the exact people the score is meant to represent."
    - "MUST NOT self-approve the package legitimacy checkpoint. The automated seam returned SUS; an agent clearing its own supply-chain review is the failure mode the checkpoint exists to prevent, and it is not auto-approvable under any mode."
    - "MUST NOT fix an accessibility finding by editing the frozen desktop stylesheet. That file's one sanctioned edit this milestone was made in plan 04-01 and is gated by a committed test; a second edit escalates to Dre instead."

  flagged_assumptions:
    - id: FA-MOBX-07
      origin: planner (RESEARCH Package Legitimacy Audit marked lighthouse [ASSUMED]; the automated seam run at planning time returned SUS)
      requirement: MOBX-03
      status: unresolved
      note: "`gsd-tools query package-legitimacy check --ecosystem npm lighthouse` returned verdict SUS with the single reason `too-new`. The surrounding signals contradict a genuinely new package: 4,123,823 weekly downloads, source repo github.com/GoogleChrome/lighthouse, not deprecated, no postinstall script, and RESEARCH independently read a Google LLC copyright header out of the fetched tarball. The `too-new` signal appears to be keyed on the most recent RELEASE date (2026-07-20), not on package age. That reading is the planner's inference and must be confirmed by a human before the tool is executed — see Task 1."
      criteria:
        - "A human confirms on npmjs.com that the package is the long-established Google-published Lighthouse and not a recent lookalike."
        - "The pinned version is the one whose source RESEARCH actually inspected, so the review and the executed code refer to the same artifact."
        - "The decision and its reasoning are recorded in the summary and carried to the Phase D+E gate agenda."

  artifacts:
    - "tests/test_lighthouse_accessibility.py — the committed, re-runnable mobile-preset accessibility gate"
    - "The recorded Lighthouse score, form factor, tool version and the list of any non-perfect audits, in the plan summary"
    - "Any remediation commits, each naming the audit id that prompted them"
    - "The package legitimacy ruling for the audit tool"

  key_links:
    - "the existing staged-site fixture -> Lighthouse's target URL; a file URL would break the data-bundle fetch and score a broken page"
    - "the pinned tool version -> the legitimacy review; an unpinned invocation would execute code nobody reviewed"
    - "this gate -> MOBX-03's score clause ONLY; its 44x44 clause is closed independently in plan 04-04"
    - "a sub-90 finding -> a remediation commit in web/mobile.css or index.html; a finding that would need a frozen-file edit -> Dre, not a commit"
---

<objective>
Turn Lighthouse Accessibility into a committed gate the project can re-run forever: a pytest module that serves the real app, runs the mobile-preset accessibility audit at a pinned tool version, asserts the score, and prints the failing audits as a work list.

Purpose: MOBX-03's score clause, and D-40's insistence that the gate be repeatable rather than a screenshot of a number someone once saw.

Output: `tests/test_lighthouse_accessibility.py`, a recorded score at or above 90, and any remediation commits.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-RESEARCH.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-PATTERNS.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-VALIDATION.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-02-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-03-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-04-SUMMARY.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...` — this worktree has NO `.venv` of its own.
- Commit signing is disabled repo-wide; `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`. Never `git add .`.
- `web/style.css` and `web/mobile-gestures.js` are frozen and gated by `tests/test_freeze_proof.py`. This plan's remediation surface is `web/mobile.css` and `index.html` ONLY.
- Environment confirmed available during research: Node with npx, and system Google Chrome. If Chrome is absent on some other machine, Playwright's already-downloaded Chromium can be pointed at via the tool's Chrome-path environment variable — note it, do not build a fallback.
</session_constraints>

## Artifacts this phase produces

Phase-wide symbol inventory, creations AND deletions. This plan owns the rows marked **[05]**. The complete table, including plan 01's seven deletions, is in `04-01-PLAN.md`.

| Symbol / artifact | Kind | Plan |
|---|---|---|
| **DELETED** the banner render function, call site, storage constant, state field, migration entry, CSS block and stale media wrapper | removals | 01 |
| `tests/test_freeze_proof.py` | new test file | 01 |
| the `aria-live="polite"` region, `mobileRollAnnouncement()`, `announceMobileRoll()`, the mobile keydown branch, `.mobile-live-region` | live region + keyboard | 02 |
| the landing-page meta row, dialog, `<noscript>` guard, inline script; `tests/test_landing_page.py` | landing page | 03 |
| `.mobile-cinema-scrub-fab::before` + `position: relative`; the reduced-motion timer branch | hit area + reduced motion | 04 |
| `tests/test_lighthouse_accessibility.py` | new test file (new shape: shells to a CLI and parses JSON) | **[05]** |
| the recorded score, form factor, pinned tool version and non-perfect audit list | phase record | **[05]** |
| any accessibility remediation commits in `web/mobile.css` / `index.html` | fixes | **[05]** |
| the package legitimacy ruling for the audit tool | gate record | **[05]** |
| `COVERAGE.md`, the §6 evidence table, the Phase D+E gate rulings | phase artifacts | planner / 06 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy review of the Lighthouse CLI before its first execution</name>

  <read_first>
    - `04-RESEARCH.md` § "Package Legitimacy Audit" — the audit table, its `[ASSUMED]` tagging on age and downloads, and the note that the formal seam check was left to the planner.
    - `04-RESEARCH.md` § "Environment Availability" — the exact version research fetched and executed, and the source files it read out of the tarball.
  </read_first>

  <what-built>
    Nothing yet. This checkpoint gates the first execution of an npm package. Plan 04-05's Task 2 will invoke the Lighthouse CLI via npx to produce MOBX-03's accessibility score. `npx` downloads and executes that package, so it is code execution from a public registry even though nothing is installed into this repo.
  </what-built>

  <how-to-verify>
1. **Read the automated seam result, recorded here verbatim from the planning-time run of `gsd-tools query package-legitimacy check --ecosystem npm lighthouse`:**

   - verdict: **SUS**
   - reasons: `too-new`
   - weekly downloads: 4,123,823
   - source repo: `github.com/GoogleChrome/lighthouse`
   - deprecated: no
   - postinstall script: none
   - most recent publish: 2026-07-20

2. **The planner's reading, which you are being asked to confirm or reject:** the `too-new` signal appears to key on the most recent RELEASE date rather than the package's age. Every other signal points at a long-established, heavily-used, Google-published tool, and `04-RESEARCH.md` independently read a `Copyright 2016 Google LLC` header out of the fetched tarball this cycle.

3. **Check it yourself in under a minute:** open `https://www.npmjs.com/package/lighthouse` and confirm the publisher, the repository link, the download volume, and that the package's first release long predates this milestone.

4. **Confirm the version pin.** Task 2 will invoke version `13.4.1` explicitly — the version `04-RESEARCH.md` actually fetched, ran and read source from. Confirm you want the gate pinned to that version rather than floating to latest.

5. **Rule.** Approve to allow the invocation, or reject — in which case MOBX-03's Lighthouse clause cannot be closed automatically and routes to you as a scope decision, not to an agent as a workaround.
  </how-to-verify>

  <acceptance_criteria>
    - Dre has explicitly ruled on the SUS verdict; the ruling and its reasoning are recorded in the summary.
    - The version pin is confirmed and matches the version research inspected.
    - This checkpoint is NOT auto-approvable under any configuration, including autonomous mode and auto-advance. An agent may not infer, assume, or self-issue approval of a supply-chain review — that is the specific failure this gate exists to prevent.
    - If rejected, no `npx` invocation is written or run, and the MOBX-03 score clause is carried to the Phase D+E gate as an open scope item rather than silently dropped.
  </acceptance_criteria>

  <resume-signal>Type "approved" to allow the pinned Lighthouse invocation, or "rejected" with your reasoning.</resume-signal>

  <done>Dre has ruled on the audit tool's legitimacy and on the version pin, with the ruling recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The committed mobile-preset Lighthouse Accessibility gate, and any remediation it surfaces</name>

  <precondition>Task 1's legitimacy checkpoint is approved. Also: `node`, `npx` and a Chrome binary are available on this machine — research confirmed all three here. If any is missing, halt and report rather than skipping the test or lowering the gate.</precondition>

  <read_first>
    - `04-PATTERNS.md` § "tests/test_lighthouse_accessibility.py — no analog" — confirmation this is a new test shape in the suite and the instruction to reuse the existing staged-site fixture rather than hand-rolling a server.
    - `tests/helpers/web_runtime_site.py` lines 40-60 and 565-700 — `StagedWebRuntimeSite.url_for`, the server, and `staged_web_runtime_site`'s signature and yielded object. Note this fixture also now stages the landing page (plan 04-03).
    - `04-RESEARCH.md` § "Standard Stack → Supporting" and § "Common Pitfalls → Pitfall 1" — the tool's real tap-target threshold, and why the score is not evidence for the 44 px clause.
    - `04-CONTEXT.md` D-40 and D-41 — the repeatability requirement and the explicit instruction that a sub-90 score is a work list rather than a checkpoint.
    - `tests/test_freeze_proof.py` (plan 04-01's output) — the browser-free, subprocess-driven test shape to mirror; this module is closer to that than to the Playwright tests.
  </read_first>

  <files>tests/test_lighthouse_accessibility.py, web/mobile.css, index.html</files>

  <behavior>
    - Test 1: the accessibility category score for the app, run at the mobile form factor, is 0.90 or greater.
    - Test 2: the report's own recorded form factor is mobile — proving the preset rather than assuming it.
    - Test 3: the report's recorded tool version matches the pinned version — proving the reviewed artifact is the executed one.
    - Test 4: no package manifest, lockfile or bundler config exists anywhere under `web/` after the run, so the ephemeral invocation left no footprint.
    - Failure mode: when the score is below the threshold, the assertion message lists every audit in the accessibility category whose score is not perfect, with its id and title — so the failure IS the work list rather than a bare number.
    - Failure mode: when the tool or a browser is unavailable, the test FAILS with the underlying error, never skips. A silently skipped accessibility gate reads as a pass.
  </behavior>

  <action>
Write the gate, run it, then work whatever it finds.

**1. The runner.** Create `tests/test_lighthouse_accessibility.py` as a browser-free pytest module in the shape of `tests/test_freeze_proof.py` rather than the Playwright suite. Inside the existing staged-site context manager, resolve the app's real http URL from the fixture's own url helper — Lighthouse must fetch the data bundle over http, so a file URL would score a broken page. Invoke the CLI through `npx` with the yes flag and an explicit version pin of `13.4.1`, restricting the run to the accessibility category, emitting JSON to a temp path, and passing headless Chrome flags. Do NOT pass a desktop preset; the tool's default form factor is mobile, which is what MOBX-03 asks for, and Test 2 proves it rather than trusting it.

Pin the version and the threshold as module-level constants with a comment explaining that the pin ties the executed code to the artifact reviewed at Task 1's checkpoint.

Run the subprocess with an explicit generous timeout and `check=False`, then assert on the return code with the captured stderr in the assertion message. Parse the JSON report and read the accessibility category's score, the recorded form factor, and the recorded tool version.

**2. Make failure actionable.** Build the assertion message for the score test by walking the accessibility category's audit references, looking up each audit in the report, and listing every one whose score is neither perfect nor inapplicable, as id plus title. That list is the work list D-41 describes — the point is that a red run tells you what to fix rather than that you fell short.

**3. Never skip.** Do not add a skip marker, an availability guard that skips, or a try/except that swallows a missing tool. If Node, npx or Chrome is unavailable, the test must fail with the real error. Note in a comment that another machine lacking system Chrome can point the tool at Playwright's already-downloaded Chromium through the tool's Chrome-path environment variable; do not build that fallback here.

**4. Work the findings.** Run it. If the score is at or above the threshold, record the number, the form factor, the version and any non-perfect audits in the summary and stop. If it is below, treat each finding as a task: fix, re-run, repeat. Fixes land in `web/mobile.css` or `index.html` only — those are this milestone's write surface. Commit each remediation naming the audit id that prompted it, so the record shows what was fixed and why.

Escalate to Dre — do not fix, and do not lower anything — in exactly two cases: a finding whose only remedy is an edit to the frozen desktop stylesheet, and a finding that would force a genuine design change to an approved visual. A missing accessible name is never an escalation; it is a fix.

**5. Re-verify D-03 against the real audit.** The ROADMAP's own Phase 4 note says the Phase 1 interview's user-scalable decision is re-checked here against a real Lighthouse run. Add an assertion that the app's viewport meta still permits page zoom, and record in the summary whether the audit raised any viewport-related finding. If it did, the resolution is to record the tension for the gate — never to disable zoom for points. See `must_haves.prohibitions`.

**6. State the independence.** Record in the summary that this gate closes MOBX-03's score clause and closes nothing else — in particular, that the tool's own tap-target audit measures a lower threshold than MOBX-03's 44 px bar, which plan 04-04 closed by a separate check.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_lighthouse_accessibility.py -x -q</automated>
    <automated>test "$(grep -c 'pytest.skip\|@pytest.mark.skip\|pytest.importorskip' tests/test_lighthouse_accessibility.py)" = "0"</automated>
    <automated>test "$(grep -c '13.4.1' tests/test_lighthouse_accessibility.py)" -ge "1"</automated>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_freeze_proof.py tests/test_desktop_smoke.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_lighthouse_accessibility.py -x -q` exits 0 and collects at least 4 items.
    - The recorded accessibility score is at or above 0.90 and is written into the summary as a number, alongside the form factor and the tool version read out of the report.
    - The report's recorded form factor is asserted to be mobile — the assertion exists in the file, not merely in the summary.
    - The report's recorded tool version is asserted equal to the pinned constant.
    - `grep -c 'pytest.skip\|@pytest.mark.skip\|pytest.importorskip' tests/test_lighthouse_accessibility.py` returns 0.
    - No `package.json`, lockfile or bundler config exists anywhere under `web/` or at the repo root after the run.
    - Any remediation commit names the audit id that prompted it, and touches only `web/mobile.css` or `index.html`.
    - `pytest tests/test_freeze_proof.py tests/test_desktop_smoke.py -x -q` still exits 0 after any remediation.
    - The app's viewport meta is asserted to permit page zoom (D-03 re-verified), and the summary records whether the audit raised any viewport finding and how it was resolved.
    - The summary states that this gate closes only MOBX-03's score clause and is not evidence for its 44 px clause.
  </acceptance_criteria>

  <reversibility rating="reversible">A new standalone test module with no production dependents; any remediation is scoped to the two mobile-write-surface files.</reversibility>

  <done>A committed pytest module runs the pinned Lighthouse CLI against the real staged app at the mobile form factor, asserts the accessibility score at or above 90 along with the form factor and version, fails loudly rather than skipping when the tool is unavailable, and prints a work list on failure — with the actual score recorded and any findings fixed inside the milestone's write surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public npm registry → local code execution | `npx` downloads and executes a third-party package on the developer's machine at test time. Nothing is installed into the repo, but the code still runs. |
| an audit score → an accessibility conformance claim | A number produced by a tool with its own thresholds is being used as evidence for a requirement written with different ones. |
| audit findings → the frozen desktop surface | A remediation reflex could reach into the frozen stylesheet to fix a finding. |
| test tooling → the shipped app's dependency surface | The app's zero-dependency, zero-build-step property is a security property, not just a preference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | supply chain — the Lighthouse CLI fetched and executed by npx | high | mitigate | The automated legitimacy seam returned SUS (`too-new`); Task 1 is a blocking human review that is never auto-approvable, and the invocation is pinned to the exact version research fetched and read source from. Nothing is installed into the repo, so the shipped app's dependency surface is unchanged. |
| T-04-20 | Tampering | a package manifest, lockfile or bundler config appearing in the repo to make the run more convenient | high | mitigate | Prohibited outright; asserted by this plan's own test and re-asserted by `tests/test_freeze_proof.py`'s dependency scan on every run. |
| T-04-21 | Repudiation | a silently skipped accessibility gate read as a pass | high | mitigate | The module contains no skip marker of any kind, asserted by grep; an unavailable tool fails with the real error. |
| T-04-22 | Repudiation | a passing score reported as evidence for the 44 px tap-target clause | high | mitigate | Explicit prohibition; the summary must state the independence; the 44 px clause is closed by a separate check in plan 04-04. |
| T-04-23 | Repudiation | reaching the threshold by changing the preset, the threshold, or the audited page instead of the app | high | mitigate | The form factor and the tool version are asserted from the report itself, and the threshold is a module constant whose value is an acceptance criterion. Changing any of them is visible in the diff. |
| T-04-24 | Tampering | an accessibility fix landing in the frozen desktop stylesheet | high | mitigate | Remediation surface is limited to two files by the plan and by `files_modified`; a finding needing a frozen-file edit escalates to Dre. `tests/test_freeze_proof.py` catches it either way. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_lighthouse_accessibility.py -x -q` — green, score >= 0.90 recorded.
2. The report's form factor and tool version are asserted in the module, not just noted.
3. No skip marker exists in the module.
4. No package manifest, lockfile or bundler config exists under `web/` or the repo root.
5. `pytest tests/test_freeze_proof.py tests/test_desktop_smoke.py -x -q` still green after any remediation.
</verification>

<success_criteria>
- The audit tool's legitimacy is ruled on by a human before it is ever executed, and the invocation is pinned to the reviewed version.
- Lighthouse Accessibility scores 90 or higher at the mobile form factor, proven by a committed re-runnable test.
- Failures produce a work list, not a bare number.
- The gate never skips.
- The repo gains no dependency surface, and the frozen files are untouched.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/04-mobile-cutover-accessibility/04-05-SUMMARY.md` when done.
</output>
