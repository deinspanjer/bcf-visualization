---
phase: 4
slug: mobile-cutover-accessibility
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-02
---

# Phase 4 — UI Design Contract: Mobile Cutover & Accessibility

> **This phase's net-new visual surface is deliberately small.** Per `04-RESEARCH.md`, three of the
> hardest-looking items are already shipped or structurally satisfied: the 36×36 compact icon buttons already
> clear 44×44 (Phase 2's `content-box` + padding technique), `prefers-reduced-motion` is already enforced
> globally by frozen `web/style.css:62`, and MOBX-05 is already implemented (D-51). This document does **not**
> re-specify any of those. It locks the contract for the genuinely new surfaces: the `.mobile-cinema-scrub-fab`
> hit-area overlay, the repo-root landing page additions (MOBX-02), and the visually-hidden live region
> (MOBX-03). **All design tokens (spacing scale, typography roles, color split, copywriting conventions) are
> inherited verbatim from `../02-portrait-layout/02-UI-SPEC.md` and `../03-landscape-layout/03-UI-SPEC.md` — this
> document adds no new font size, weight, or hex value.** Generated by gsd-ui-researcher, verified by
> gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | **none** — no shadcn, no component framework, no bundler (unchanged from Phases 2/3) |
| Preset | not applicable |
| Component library | none — hand-authored DOM via `web/app.js`'s existing `el()` helper; the landing page (`index.html`) is hand-authored static HTML with no build step |
| Icon library | none — this phase introduces no new icon. The `?` icon reused on the landing page is the same glyph shape as `web/app.js`'s existing `mobileHelpIcon()` inline SVG (top-cluster help button), redrawn as inline SVG directly in `index.html` since the two files share no JS module boundary |
| Font | Reuse the three frozen `web/style.css:25-27` font-family variables (`--serif`/`--sans`/`--mono`) for `web/`. `index.html` keeps its own existing local font stack (`"Avenir Next", "Segoe UI", system-ui, sans-serif` for UI chrome; `Georgia, "Times New Roman", serif` for the letter body; `ui-monospace, SFMono-Regular, Menlo, Consolas, monospace` for chips/labels) — these are already byte-identical to `web/style.css`'s values, just declared a second time because `index.html` is a standalone file outside the `web/` module graph. No new font is introduced. |

No shadcn gate applies — same `Tool: none` by-design contract as Phases 2/3.

---

## Deletions (MOBX-01 / D-37 / D-38)

This phase's most consequential visual change is a removal, not an addition. Recorded here as contract, not just as a code diff:

| What disappears | Where | Why it is safe to delete |
|---|---|---|
| `.portrait-banner` and its 3 child-selector rules (`strong`, `button`, `button:hover`, `svg`) | `web/style.css:326-358` | Superseded by the real portrait UI shipped in Phase 2 — the banner was the pre-Phase-2 "rotate for a better experience" fallback |
| The stale `@media (max-width: 900px), …` wrapper around `.portrait-banner.is-visible` | `web/style.css:360-362` | This is the OLD breakpoint query, deliberately left diverged since Phase 3 moved the real layout query to the height-based clause (`app.js:73` comment). Deleting it removes the second, stale breakpoint definition entirely — after this phase there is exactly ONE breakpoint pair in the repo (D-38), restoring D-06's single-source intent. |
| `renderPortraitBanner()` and its call site | `web/app.js:1228-1233`, `:1098` | The one sanctioned edit to a §0.2 read-only function this milestone (D-37) |
| `LS_PORTRAIT_DISMISSED`, `app.portraitDismissed`, its migration-purge entry | `web/app.js:62`, `:145`, `:428` | Dead once nothing reads it. Per RESEARCH Pitfall 5: **do NOT bump `STORAGE_VERSION`** to purge the orphaned `bcf:portrait-dismissed` localStorage key — the orphaned key has zero functional impact and a version bump would force 34 test-fixture edits across 4 files for no behavioral gain. |

No replacement visual element mounts in the deleted banner's place. Its absence — proven by `document.querySelector('.portrait-banner') === null` — is the acceptance bar, not a new empty-state design.

---

## Spacing Scale

**Inherited verbatim from `../02-portrait-layout/02-UI-SPEC.md`** (8-point scale: xs 4 / sm 8 / md 16 / lg 24 / xl 32 / 2xl 48 / 3xl 64) and its locked exceptions table, plus Phase 3's landscape exceptions and safe-area contract. Nothing in this phase forks that scale. This phase's only new spacing values:

### New locked value: cinema-scrub FAB hit-area overlay (D-39)

| Element | Value | Rationale |
|---|---|---|
| `.mobile-cinema-scrub-fab` | Painted size stays **40×40 CSS px** (unchanged — Phase 3's locked `.fab-play.sm` visual) | D-39 forbids visually enlarging it |
| `.mobile-cinema-scrub-fab::before` (new, this phase) | `content: ""`, `position: absolute`, `inset: -4px` (→ a 48×48 hit-testable box), no `background`/`box-shadow`/`border-radius` | Padding-based expansion would grow the glow/circle (Pitfall 3, RESEARCH) since `box-shadow`/`background` paint through the padding box by default. A transparent `::before` is the ONLY correct technique here — it does not touch the FAB's own box model, so the painted 40×40 circle and its `box-shadow: 0 0 22px …` glow stay pixel-identical. |
| `.mobile-cinema-scrub-fab` (existing rule, gains one new declaration) | add `position: relative;` | Required so the `::before`'s `position: absolute` resolves against the button itself, not some further ancestor — the button currently has no `position` declared |

**Verification note carried from RESEARCH/VALIDATION:** this hit-area expansion is real and improves on-device tap reliability, but `getBoundingClientRect()` on `.mobile-cinema-scrub-fab` will still report **40×40** — a `::before`'s absolute-position overflow does not change its host's own layout box. Do not write a `getBoundingClientRect() >= 44` assertion for this element (it will always fail); verify via `page.mouse.click()` at an offset 2–3px outside the visible circle and confirm it still registers, or via manual on-device tap-at-edge testing. This is unlike `.mobile-icon-btn.compact`, whose padding-based technique DOES grow its own box and IS correctly verified by `getBoundingClientRect()`.

### New locked value: landing page additions (MOBX-02, index.html)

`index.html`'s existing local spacing is pixel-precise HUD chrome (`clamp()`-based, not an 8pt grid), matching the same "lock what's approved, extend with the same idiom" precedent Phase 2 set for `web/mobile.css`. New elements follow the *existing* local idiom, not a fresh 8pt re-grid:

| Element | Value | Source / rationale |
|---|---|---|
| `.landing-meta-row` (new wrapper: title chip + author credit + `?` button) | `display: flex; align-items: center; gap: 10px; flex-wrap: wrap; margin-bottom: 18px;` | `10px`/`18px` match the existing local scale already in use for `.actions` (`gap: 14px 18px`) and `.subject` (`margin-bottom: 18px` at the `@media (max-width: 700px)` breakpoint) — reuses adjacent values rather than inventing a third |
| Placement | Inserted as the **first child of `.message-panel`**, immediately before `.constellation` (i.e., before the `<p class="subject">` line) | Functions as a masthead/byline above the Survey letter, never inside `.letter` (which stays verbatim per MOBX-02's explicit constraint) |
| `.landing-title-chip` | `padding: 5px 11px; border-radius: 999px;` (identical padding/radius token to `web/mobile.css`'s `.chip` — Phase 2's locked "Chip padding: 5px 11px" value) | Reuses the exact chip padding token already locked in `02-UI-SPEC.md`'s Spacing Scale, applied to the landing page's own CSS since the two files don't share a stylesheet |
| `.landing-help-btn` (the `?` button) | `width: 44px; height: 44px;` (native 44×44 — built at-size from the start, no hit-area trick needed since this is brand-new markup, not a locked-visual retrofit) | MOBX-03's 44×44 floor. Unlike the FAB, there is no pre-approved smaller visual reference constraining this control's size — build it correctly the first time. |
| `<dialog id="landing-help-dialog">` padding | `padding: 24px 20px;` | Matches the landing page's existing `.message-panel` mobile padding (`24px 18px` at `@media (max-width: 700px)`) within 2px, close enough to read as the same design language without inventing a fourth value |

---

## Typography

**Inherited verbatim** from `../02-portrait-layout/02-UI-SPEC.md`'s 4-role table (Micro-label / Body / Heading / Display) and Phase 3's landscape mappings. No new size or weight. New elements map onto existing roles:

| Element | Role | Size | Weight | Line Height | Notes |
|---|---|---|---|---|---|
| `.landing-title-chip` text (`"Brockton's Celestial Forge"`) | Micro-label (`--mono` equivalent — landing page's local monospace stack) | 10px | 400 | 1.3 | Same size/weight/line-height as `web/mobile.css`'s `.mobile-chip` (Phase 2 Micro-label row) |
| `.landing-author-credit` text (`"by LordRoustabout"`) | Body (landing page's local sans stack) | 12px | 400 | 1.4 | Matches Phase 2's Body role band (11–13px) |
| `.landing-help-btn` glyph (`"?"`) | Micro-label-adjacent glyph, not running text | 18px | 400 | n/a (single glyph, centered) | Matches `.mobile-icon-btn svg { width:18px; height:18px }`'s visual weight in the in-app equivalent |
| Landing help dialog title (`"Brockton's Celestial Forge"`) | Heading (landing page's local serif stack) | 16px | 400 | 1.2 | Same size as the in-app Help overlay's `.mobile-credit-block .title` (Phase 2 Heading row) |
| Landing help dialog byline (`"by LordRoustabout · Worm × Jumpchain"`) | Body | 12px | 400 | 1.4 | — |
| Landing help dialog source links (SV/FF/AO3) | Micro-label | 10px | 400 | 1.3 | Same size as the in-app `.mobile-source-row` links |
| Landing help dialog pointer line (see Copywriting below) | Body | 12px | 400 | 1.4 | — |
| Landing help dialog CTA (`"Got it — read on"`) | Body, bold | 12px | **700** | 1.4 | Reuses Phase 2's locked Bold-700 CTA convention (`.got-it` is the only precedent for a bold CTA in this project's mobile UI) |

**Weights: exactly 2 (400/700), unchanged.** No third weight is introduced anywhere in this phase.

---

## Color

**Inherited verbatim** from `../02-portrait-layout/02-UI-SPEC.md`. All `web/` values reused from `web/style.css:9-27`'s frozen `:root`. `index.html`'s own local `:root` block (confirmed by direct read) already declares the **identical hex values** as the frozen `web/style.css` tokens it duplicates (`--void: #03080d`, `--cyan: #5cf4ff`, `--green: #7bffbd`, `--edge`/`--edge-soft`/`--glow`/`--panel` — same rgba stops). This is not a second palette; it is the same palette declared twice because the two files share no stylesheet. New elements reuse these existing local variables, never a new hex value:

| Element | Color | Source |
|---|---|---|
| `.landing-title-chip` background/border | `var(--panel)` / `var(--edge)` | Same secondary-surface pairing as `web/mobile.css`'s `.mobile-chip` |
| `.landing-title-chip` text | `var(--cyan)` | Accent — new reserved-for usage, see below |
| `.landing-author-credit` text | `var(--muted)` | Landing page's existing secondary-text token (already used for `.secondary-link`) |
| `.landing-help-btn` background/border | `var(--panel)` / `var(--edge)` | Same pairing as every in-app `.mobile-icon-btn` |
| `.landing-help-btn` glyph + `:active`/focus state | `var(--cyan)` | Accent |
| `<dialog>` backdrop | `::backdrop { background: rgba(3, 8, 13, 0.72); }` (derived from `--void` at higher opacity — same family as the app's existing `.mobile-surface-backdrop` treatment, no new hex) | Native `<dialog>`'s `::backdrop` pseudo-element |
| `<dialog>` panel background/border | `var(--panel)` / `var(--edge)` (matches `.message-panel`'s own background family, at the panel's own opacity) | — |

**Accent (`--cyan`) reserved-for list — appended, not forked:** landing-page title chip text · landing-page `?` button glyph and active/focus state. This is the identical semantic Phase 2 already locked ("small HUD interactive/informational elements"), extended to the landing page's two new controls.

No destructive actions exist on the landing page (same as every prior mobile-ux phase — no delete/discard UI). `--rose`/`--green` outcome semantics are not touched by this phase's landing-page work.

---

## Copywriting Contract

### Landing page (MOBX-02) — new this phase

**Decision: duplicate a condensed subset of the in-app Help content, do not reuse the in-app markup.** Justification (resolves RESEARCH's Open Question 1 and the plan §3.4 either/or): `index.html` is a standalone static file with its own `<style>` block and, as of this phase, its own small inline `<script>` — it shares no JS module, no bundler, and no build step with `web/app.js`. "Reusing" the in-app markup would require either (a) loading `web/app.js` on the landing page just to call `renderMobileHelpOverlay()`, which drags the entire app bootstrap (data fetch, `app` state object, render pipeline) onto a page whose whole point is to be a fast, dependency-free entry point, or (b) extracting `renderMobileHelpOverlay()`'s body into a third shared file, which is unwarranted architecture for ~6 lines of duplicated copy. Duplication is the smaller, more honest cost, matching the "add rules only" spirit of this milestone's freeze discipline (the alternative — coupling the landing page to the app bundle — is the kind of new dependency this codebase's zero-build-step rule exists to prevent). The full 7-row gesture how-to is NOT duplicated (it is meaningless before entering the app, per RESEARCH); the dialog instead points the reader to the in-app Help.

| Element | Copy |
|---|---|
| `.landing-title-chip` | `Brockton's Celestial Forge` |
| `.landing-author-credit` | `by LordRoustabout` |
| `.landing-help-btn` | `aria-label="Help"`, visible glyph `?`, `title="About & help"` |
| Help dialog label | `About & help` |
| Help dialog title | `Brockton's Celestial Forge` |
| Help dialog byline | `by `**`LordRoustabout`**` · Worm × Jumpchain` (bold author name, same convention as the in-app credit block) |
| Help dialog source links | `SV ↗` / `FF ↗` / `AO3 ↗` — identical labels, hrefs literally copied from `web/app.js`'s `STORY_LINKS` constant (lines 116-119). **Flag for the executor:** these are two independent hardcoded copies of the same 3 URLs by construction (no shared module); if a story link ever changes, both `web/app.js:116-119` and `index.html`'s dialog must be updated together — same class of duplication risk already accepted for the identical author name/title strings appearing in both files today. |
| Help dialog pointer line | `Full gestures, settings, and playback help are available once you open the visualization.` |
| Help dialog CTA | `Got it — read on` (reuses Phase 2's exact locked CTA string for cross-surface consistency — same button copy in three places now: in-app Help overlay, and this landing dialog) |
| Existing letter, subject, actions row | **Unchanged, verbatim.** No edit to `.letter`, `.subject`, or the existing `.actions` nav (`Open visualization` / `View source`). |

### Live region (MOBX-03 / D-42..D-45) — new this phase

Announcement fires **only** when `setWordPos()` is called from one of these four sites (all pre-existing call sites, per `04-RESEARCH.md`'s exact line citations — no fifth trigger is added):

| Trigger | Code site | Fires? |
|---|---|---|
| Double-tap (snap to last roll) | `onDoubleTap` (`app.js:4419-4421`) | Yes |
| Swipe-step (±1 roll) | `onSwipeStep` (`app.js:4428-4430`) | Yes |
| Rail-scrub **release** (`onScrubEnd`) | `app.js:4467` | Yes |
| Continuous rail-scrub **drag** (every pointermove before release) | `attachRailScrub`'s per-move callback (`~app.js:4463`) | **No** — announcing on every drag-move frame would be exactly the "continuous interruption" D-42 forbids; only the settled release position is announced |
| Mobile keyboard step (ArrowRight/ArrowLeft/Home, D-46/D-47) | new branch, `app.js:4564`'s handler | Yes |
| Plain single tap (portrait tap-to-pause / landscape chrome-reveal) | `onTap` (`app.js:4399`) | **No** — resolves an ambiguity in D-42's literal phrasing: a plain tap never calls `setWordPos()` in the current codebase (it only toggles play state or reveals hidden chrome), so there is no position change to announce. Flagged explicitly rather than silently decided: if Dre's gate review wants tap to also re-announce the current roll, that is a one-line addition at the `onTap` callback, not a re-plan. |
| Free playback (rAF tick, `updatePlaybackFrame`/`updateMobileFieldLogFrame`) | — | **No** — silent during playback per D-42/D-45 |
| Surface open/close (Settings/About/Help) | — | **No** — `role="dialog"` focus-trap is already announced natively (D-44) |
| Play/pause toggle | — | **No** — the FAB's own `aria-label` flip already covers this (D-44) |

**If `lastRollAtWord(app.wordPos)` returns `null`** at the target position (e.g., scrubbed before the first roll fires), **no announcement fires** — silence, not a placeholder string. This matches the project's established "absent element, not placeholder copy" convention (Phase 2/3's amber-chip and field-log-live-card omission rows).

**Announcement phrasing** (proposed by RESEARCH, grounded in the real 670-roll dataset; **not yet Dre-approved copy** — confirm or adjust at this phase's own gate review, per RESEARCH Assumption A3):

| Outcome shape | Phrasing | Fields used |
|---|---|---|
| Hit, single paid perk (209/260 hits) | `"Roll {n} of {total}. {perkName}, {cp} CP."` | `perkDisplayLabel(paidRollPerks(roll)[0])`, `rollTotalCost(roll)` |
| Hit, free-only (0 live examples today; code path kept for robustness) | `"Roll {n} of {total}. {perkName} (free)."` | `perkDisplayLabel(roll.free_perks[0])` |
| Hit, multi-grab (51/670 rolls, `paidCount` > 1) | `"Roll {n} of {total}. {firstPerkName} and {paidCount - 1} more, {cp} CP."` | `rollMarkerModel(roll).paidCount`, `perkDisplayLabel(paidRollPerks(roll)[0])`, `rollTotalCost(roll)` |
| Miss (410/670 rolls — **the common case**) | `"Roll {n} of {total}. Miss."` — deliberately terse per D-45; never append a cost estimate | `rollMarkerModel(roll).isMissLike` |
| `source_kind === "trigger"` (1 roll, `word_position: null`) | No special case — unreachable as a scrub/tap target since `word_position` is null; falls through to ordinary hit phrasing if ever reached | — |
| `evidence_kind === "untracked_acquisition"` (0 rolls in current dataset) | **Unresolved — no live example.** `rollMarkerModel(roll).isUntracked` is already computed; do not build a dead-code phrasing branch for a zero-instance case. Flag as an explicit assumption row (see UI Considerations) rather than guessing wording. | — |

`{n}`/`{total}` = `app.data.story.rolls.indexOf(roll) + 1` / `app.data.story.rolls.length` — the identical expression the field-log header/cinema-scrub count already use. **Never** `roll.roll_label` (null on 669/670 rolls) or any `*_ordinal` field (divergent numbering systems per project convention).

**Live region update mechanism (technical note, not visual, but load-bearing for correctness):** screen readers only announce on a DOM text *mutation* — an identical string written twice in a row (e.g., scrubbing away from and back onto the same roll) produces no second announcement unless the node is actually changed. Clear `textContent` to `""` and then set the new message (two synchronous writes, same tick) so a repeat-landing still re-announces.

#### Locked hiding technique — `.mobile-live-region`

**Added after UI-checker review flagged its absence, and it is the single most failure-prone value in this phase.** The live region has no visual appearance, but *how* it is hidden decides whether the feature works at all: `display: none` and `visibility: hidden` both remove the node from the **accessibility tree**, so the element would exist in the DOM, satisfy every presence assertion, and announce nothing. That is a silent failure that passes CI — precisely the class of defect this milestone has been bitten by three times on hardware.

Use the standard accessible-hiding ("visually hidden" / "sr-only") pattern, as a locked value, not executor inference:

| Property | Value | Why |
|---|---|---|
| `position` | `absolute` | Removes it from layout so it can never affect the sky/dock geometry |
| `width` / `height` | `1px` / `1px` | Non-zero: a 0×0 box is dropped from the accessibility tree by some engines |
| `overflow` | `hidden` | Clips the content that overflows the 1px box |
| `clip-path` | `inset(50%)` | The modern clipping primitive |
| `white-space` | `nowrap` | Stops long announcements wrapping and reflowing the 1px box |
| `margin` | `-1px` | Removes the 1px from flow entirely |
| `border` / `padding` | `0` | Prevents inherited box additions re-introducing a visible sliver |

**Prohibited on this element:** `display: none`, `visibility: hidden`, `hidden` attribute, `aria-hidden="true"`, `content-visibility: hidden`, or zero width/height — every one of them silences the announcement while leaving the node present.

🧪 **backstop** — DOM presence and computed style are assertable in Playwright, but *whether a screen reader actually speaks it* is not: no headless browser runs VoiceOver. Confirmed at the device pass (04-VALIDATION.md Manual-Only item 3), where Dre enables VoiceOver, performs one gesture, and reports whether the announcement is spoken and whether it stays silent through playback.

### Keyboard equivalents (D-46/D-47/D-48) — behavioral, no new visual copy

No new visible copy. `?` toggles the existing Help overlay (identical `openMobileSurface("help")` call the on-screen `?` button already makes) — closes on a second press, matching every other surface's toggle convention.

---

## UI Considerations

> State coverage for this phase's genuinely new surfaces: the landing page's meta-row + dialog, and the live
> region. Empty/error COPY is defined above in Copywriting Contract; this section covers state coverage and
> references those rows. Everything NOT listed here (loading/error pre-branch, Settings/About/Help content
> states, mini-rail/cinema-scrub zero-one-many, safe-area insets) is unchanged from Phases 2/3 and is not
> re-probed — this phase touches none of those surfaces' logic.

Applicable state considerations resolved: 6 covered, 5 backstop, 1 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|---|---|---|---|
| No-JS | Landing page `?` button + dialog | 🧪 backstop | The `?` button's `onclick` attribute calls `document.getElementById('landing-help-dialog').showModal()` inline (no external script file, keeping the page's zero-build-step nature). With JS disabled, the attribute never fires and the button is inert — but the page's core function (the `Open visualization` link, `<a href>`, always works) is unaffected. Add `<noscript><style>.landing-help-btn{display:none}</style></noscript>` immediately after the button so no-JS visitors never see a dead affordance. Verification: a Playwright context with JS disabled loads the page and confirms the `?` button is absent from the accessibility tree (not merely present-but-broken). |
| Dialog unsupported (pre-`<dialog>`-era browser, no polyfill) | Landing page dialog | 🧪 backstop | Calling `.showModal()` on a browser without `HTMLDialogElement` support throws. Guard the inline handler: `onclick="this.nextElementSibling.showModal && this.nextElementSibling.showModal()"` (or equivalent feature-check) so the failure mode is a silent no-op, never a console error surfaced to the user. This is supplementary content — the visualization link remains fully usable without it. No polyfill is added (RESEARCH Assumption A1: `<dialog>`'s native behavior is treated as sufficient without a live-browser check this session). |
| Long story title | `.landing-title-chip` (`"Brockton's Celestial Forge"`, fixed static text — never data-driven, so "long" only matters at the smallest viewport) | ✅ covered | At 27 characters and 10px mono with the existing `flex-wrap: wrap` on `.landing-meta-row`, the chip either fits on one line or the row wraps onto a second line — it never truncates or overlaps the adjacent author-credit/`?` button, because `flex-wrap` (not `nowrap` + ellipsis) is the chosen strategy here. This differs deliberately from the in-app dock title's ellipsis convention: the landing page has vertical room to spare (it is not a fixed-height HUD dock), so wrapping reads better than truncating a title that names the actual work. |
| Live region: no active roll at target position | Live region | ✅ covered | Silence (no announcement fires) rather than a placeholder string — see Copywriting Contract |
| Live region: repeat-landing on the same roll | Live region | ✅ covered | `textContent` cleared then re-set in the same tick, forcing a re-announcement even without a text diff — see Copywriting Contract's "update mechanism" note |
| Live region: plain-tap ambiguity in D-42's trigger list | Live region trigger set | ✅ covered | Resolved explicitly: plain tap never calls `setWordPos()` in the current codebase, so it is not a trigger — see the trigger table above. Flagged for gate-review confirmation, not silently assumed away. |
| `evidence_kind === "untracked_acquisition"` phrasing | Live region announcement | ⚠️ **unresolved** | Zero live examples in the current 670-roll dataset (`Counter` returned zero, per RESEARCH). `rollMarkerModel(roll).isUntracked` exists and could route to distinct phrasing, but no wording is proposed here — writing one now would be guessing at a case nobody has seen. **Explicit assumption for the planner:** if this shape never appears in the dataset before ship, this row can stay dead code with a `// TODO: no live example — see 04-UI-SPEC.md` comment rather than invented copy; if it does appear, route it through the same terse-outcome-first phrasing style as the Miss row (e.g. a proposed `"Roll {n} of {total}. Untracked acquisition."`) and confirm at the gate. |
| `.mobile-cinema-scrub-fab::before` z-index vs. adjacent pill content | Cinema-scrub pill (`.mobile-cinema-scrub`) | 🧪 backstop | The FAB is the pill's first flex child with `gap: 8px` to the progress track. A `-4px` inset `::before` extends 4px into that gap on the track side — at 8px gap this leaves 4px of dead zone before the track's own hit area begins, which should not create a double-hit-target collision, but has not been measured on a real pill layout. Verify no dead-zone / double-registration at the FAB-to-track boundary during the device pass (same pass already scheduled for the D-49 desktop-mode measurement). |
| Landing dialog at very narrow viewport (320px) | `<dialog id="landing-help-dialog">` | 🧪 backstop | Native `<dialog>`'s default sizing (`max-width: min(...)`, browser UA stylesheet) has not been measured at 320px in this codebase. Add an explicit `max-width: min(420px, calc(100vw - 32px))` to the dialog's own rule (not relying on UA defaults) so it cannot overflow the viewport horizontally; verify with a Playwright viewport-320 screenshot. |
| Focus return after dialog close | `<dialog id="landing-help-dialog">` | ✅ covered | Native `<dialog>`'s default behavior does not automatically restore focus to the invoking button on `close()` in all browsers — add an explicit `dialog.addEventListener('close', () => helpBtn.focus())` in the same small inline `<script>` so keyboard/screen-reader users land back on the `?` button, not on `<body>`. This is the one piece of manual JS `<dialog>` doesn't give for free (RESEARCH Assumption A1's stated "few more lines" fallback). |

---

## Registry Safety

Not applicable — no component registry (no shadcn, no npm packages in `web/` or at the repo root). All markup hand-authored; the landing page's inline `<script>` is a dozen lines of vanilla DOM API calls (`showModal`/`close`/`addEventListener`), not a library. Unchanged from Phases 2/3.

| Registry | Blocks Used | Safety Gate |
|---|---|---|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: pending
- [x] Dimension 2 Visuals: pending
- [x] Dimension 3 Color: pending
- [x] Dimension 4 Typography: pending
- [x] Dimension 5 Spacing: pending
- [x] Dimension 6 Registry Safety: pending

**Approval:** approved 2026-08-02 (gsd-ui-checker, 6/6 dimensions). One BLOCKER was raised and fixed before approval: the live region had no declared hiding technique, which would have shipped an element that passes every DOM assertion while announcing nothing. Re-verified after the fix. — awaiting gsd-ui-checker review.
