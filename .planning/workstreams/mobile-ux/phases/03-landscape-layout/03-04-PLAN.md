---
phase: 03-landscape-layout
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - .planning/workstreams/mobile-ux/phases/03-landscape-layout/COVERAGE.md
  - .planning/workstreams/mobile-ux/phases/03-landscape-layout/03-04-SUMMARY.md
autonomous: false
requirements: [MOBL-01, MOBL-02, MOBL-03, MOBL-04]
user_setup: []

must_haves:
  truths:
    - "The full web suite (test_desktop_smoke + test_mobile_plumbing + test_mobile_portrait + test_mobile_landscape) passes in ONE invocation, with the landscape file adding tests on top of the 33-test Phase 2 baseline and zero pre-existing test lost."
    - "Freeze proof against the pre-Phase-3 base commit: web/style.css, web/mobile-gestures.js and tests/test_mobile_plumbing.py are byte-identical across the WHOLE phase, not merely since the last commit."
    - "No dependency crept into web/: a filesystem scan finds no package.json, lockfile or bundler config anywhere under web/, and web/index.html's script and stylesheet tag set is unchanged since the pre-Phase-3 base commit."
    - "COVERAGE.md exists in the phase directory carrying the exact API-coverage declaration sentence for this phase, with no fabricated capability matrix."
    - "An INTEGRATION_PLAN.md §6 acceptance-checklist evidence table maps every applicable Layout / Gestures / Scrubber / Persistence / Help bullet to the automated test that proves it, or marks it hardware-only or out-of-scope — never silently omitted."
    - "D-23 is satisfied on BOTH prongs: the Playwright viewport-swap rotation test is committed and green, AND Dre has completed a real-device pass. Neither alone closes MOBL-03 — Phase 2 demonstrated a fully green suite still missing four device-only defects."
    - "The §5 Phase C gate review is ruled on by Dre, not by any agent, and covers the landscape safe-area contract in BOTH notch rotations, the auto-hide feel, the iOS edge-swipe-back route in landscape, and every flagged assumption carried by plans 01-03."
    - statement: "On real iOS Safari in landscape, the right rail clears the notch in BOTH rotation directions and the sky's focal label never slides under it — safe-area insets read 0px in every emulator and in Chrome/Android, so this contract can only be confirmed on a notched iPhone."
      verification: backstop
    - statement: "On real iOS Safari in landscape, the OS edge-swipe-back gesture dismisses an open flyout rather than leaving the app (WebKit bug 248303 is a historical regression Phase 2 verified only in portrait)."
      verification: backstop
    - statement: "The 4000ms auto-hide window and the reveal-vs-pause double-tap read as intentional cinematic behavior rather than as controls disappearing on the reader — a timing judgment a test can assert but not evaluate."
      verification: backstop
    - statement: "At word position 0 with no roll yet fired, the empty field log reads as an empty log rather than a broken panel. If it reads as broken at the gate, routing a minimal placeholder string is a same-phase fix, not a re-plan (UI-SPEC UI Considerations)."
      verification: backstop

  prohibitions:
    - "MUST NOT declare the phase done on a green suite alone. This project has twice shipped a fully passing suite that hid a defect a reader would have hit on the first day; the hardware pass is the evidence, not a formality."
    - "MUST NOT auto-approve, self-approve, or infer approval of the Phase C gate under any mode, including autonomous or auto-advance. The gate exists so a human rules on judgments no agent can make from emulation."
    - "MUST NOT fabricate an external-API capability matrix in COVERAGE.md. This phase integrates no external API; the honest declaration is the deliverable and an invented matrix would be a false record."

  flagged_assumptions:
    - id: FA-GATE-01
      origin: planner (carried from plans 01-03)
      requirement: MOBL-01, MOBL-02, MOBL-03, MOBL-04
      status: unresolved
      note: "Every flagged assumption authored in plans 01-03 (FA-MOBL-01 through FA-MOBL-04, all originating from edge-probe rows the deterministic probe returned as unclassified) is carried onto this gate's agenda for Dre's ruling. They were never probe-resolved and must not be closed by an agent."
      criteria:
        - "FA-MOBL-01 (layout/field log): zero-roll, word-position-0, single-roll, smallest-viewport, and long-quote cases all reviewed on device."
        - "FA-MOBL-02 (auto-hide): the 4000ms boundary, the reset behavior, the paused case, and the reveal-with-tap-to-pause-off no-trap guarantee all reviewed on device."
        - "FA-MOBL-03 (rotation): boundary matrix, surface-open rotation, mid-drag rotation, armed-timer rotation, and the round trip all reviewed on device."
        - "FA-MOBL-04 (surface stack): backdrop tap over the rail, opener toggle, focus wrap, and the iOS edge-swipe-back route all reviewed on device."

  artifacts:
    - ".planning/workstreams/mobile-ux/phases/03-landscape-layout/COVERAGE.md"
    - "The §6 acceptance-checklist evidence table (in 03-04-SUMMARY.md)"
    - "The freeze-proof diff record against the pre-Phase-3 base commit (in 03-04-SUMMARY.md)"
    - "The §5 Phase C gate agenda and Dre's rulings (in 03-04-SUMMARY.md)"

  key_links:
    - "pre-Phase-3 base commit -> the freeze-proof diff target (a bare working-tree diff proves nothing once work is committed)"
    - "D-23 Playwright rotation test (plan 03) + D-23 real-device pass (this plan) -> MOBL-03 closure; neither alone suffices"
    - "Phase C gate approval -> Phase 4 (Mobile Cutover) unblocking, which deletes the portrait banner globally"
---

<objective>
Close Phase 3: prove the whole landscape surface in one suite run, prove the frozen files were never touched across the entire phase, record the phase's API-coverage declaration and §6 acceptance evidence, and take the `INTEGRATION_PLAN.md` §5 Phase C gate to Dre with a real-device pass.

Purpose: MOBL-01..04 closure. Phase 2 established that a green suite is necessary but not sufficient — its iOS pass found four defects emulation missed, one of which was introduced by the fix for another. This plan is the phase's evidence step and its human gate.

Output: `COVERAGE.md`, the freeze-proof and §6 evidence records, and Dre's gate rulings.
</objective>

<execution_context>
@/Users/dre/src/bcf-visualization/.claude/gsd-core/workflows/execute-plan.md
@/Users/dre/src/bcf-visualization/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-CONTEXT.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-VALIDATION.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-UI-SPEC.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-01-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-02-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-03-SUMMARY.md
@.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-05-SUMMARY.md
</context>

<session_constraints>
- Worktree `/Users/dre/src/bcf-visualization/.claude/worktrees/bcf-mobile-ux-phase-2-d08bbc`, branch `claude/bcf-mobile-ux-phase-2-d08bbc`. Do NOT merge to main.
- Run every pytest through `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest ...`.
- Commit signing is disabled repo-wide; `--no-verify` remains forbidden.
- Do NOT touch `data/`, `scripts/` pipeline files, or `.planning/workstreams/curation/`. Never `git add .`.
</session_constraints>

## Artifacts this phase produces

Full phase-wide symbol inventory (this plan creates the rows marked **[04]**).

| Symbol / artifact | Kind | Plan |
|---|---|---|
| `renderMobileLandscape()`, `renderMobileSkyRegion(frame)`, `renderMobileLandscapeSidebar(frame)` | render functions (`web/app.js`) | 01 |
| `renderMobileFieldLog()`, `renderMobileFieldLogChildren(rows)`, `mobileFieldLogRows()` | render + model adapter | 01 |
| `mobileFieldLogQuoteText(roll)`, `mobileTruncate(s, n)` | helpers | 01 |
| `renderMobileCinemaScrub()`, `renderMobileCinemaScrubTrackChildren()` | render functions | 01 |
| `updateMobileLandscapeFrame()`, `updateMobileFieldLogFrame()` | incremental tier | 01 |
| `mobileScrubWidthDefaultForLayout()`, `app.mobileRailWidthLayout` | Pitfall-6 width guard | 01 |
| landscape arms in `render()` / `cachePlaybackDomRefs()` / `updatePlaybackFrame()`; `mobileChChipText()` branch | dispatch + copy | 01 |
| landscape CSS block in `web/mobile.css`; D-32 scroll-lock un-nesting | styles | 01 |
| `attachMobileGestures()` (D-34), `mobileScrubSurfaceEl()`, `rebuildMobileCinemaScrubTrack()` | gesture lifecycle | 02 |
| `resetMobileChromeHideTimer()`, `revealMobileChrome()`, `app.mobileChromeHideTimer` | auto-hide timer | 02 |
| `render()` mobile-block guard widened to every non-desktop layout; `togglePlayback()` wiring | dispatch edits | 02 |
| landscape-arrival handling in `onLayoutMaybeChanged()`; landscape flyout CSS | dispatch + styles | 03 |
| `tests/test_mobile_landscape.py` (new in 01, extended in 02 and 03) | tests | 01–03 |
| `tests/test_mobile_portrait.py::test_landscape_no_longer_falls_back_to_desktop_shell` | rewritten regression guard | 01 |
| `tests/helpers/web_runtime_site.py` — one `evidence_quotes` entry on a `dense-rolls` roll | fixture | 01 |
| `COVERAGE.md` | phase artifact | **[04]** |
| §6 acceptance-evidence table, freeze-proof record, Phase C gate rulings | phase closure records | **[04]** |

<tasks>

<task type="auto">
  <name>Task 1: Full-suite sweep, whole-phase freeze proof, COVERAGE.md, and the §6 acceptance-evidence table</name>

  <precondition>The pre-Phase-3 base commit is identifiable — the last commit before plan 03-01's first task commit (at planning time this is `344b37d docs(03): UI design contract — safe-area and a11y gaps closed`; resolve it from `git log --oneline` rather than assuming, and record the resolved SHA in the summary). Halt if the phase's task commits cannot be distinguished from the planning commits.</precondition>

  <read_first>
    - `.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-05-SUMMARY.md` — the whole sweep/freeze-proof/COVERAGE precedent, especially the `key-decisions` entry explaining why the freeze diff targets the phase's BASE commit rather than the working tree, and the §6 evidence-table format to mirror.
    - `.planning/workstreams/mobile-ux/phases/02-portrait-layout/COVERAGE.md` — the exact file shape to mirror.
    - `design/mobile-ux/INTEGRATION_PLAN.md` §6 — the acceptance checklist whose Layout / Gestures / Scrubber / Persistence / Help groups this table covers, and §0.5 (the desktop smoke checklist).
    - `03-01-SUMMARY.md`, `03-02-SUMMARY.md`, `03-03-SUMMARY.md` — every test name this phase added, every decision recorded, and every deviation, so the evidence table cites real test names rather than planned ones.
    - `03-VALIDATION.md` — the per-task verification map and the Wave 0 requirements this sweep closes out.
  </read_first>

  <files>.planning/workstreams/mobile-ux/phases/03-landscape-layout/COVERAGE.md</files>

  <action>
Produce the phase's evidence, not new functionality.

**1. Full-suite sweep.** Run `tests/test_desktop_smoke.py`, `tests/test_mobile_plumbing.py`, `tests/test_mobile_portrait.py` and `tests/test_mobile_landscape.py` in ONE invocation and record the exact pass count. Compare against the 33-test Phase 2 baseline and confirm no pre-existing test was deleted rather than merely renamed — the one sanctioned rename this phase makes is the landscape-fallback guard, recorded in `03-01-SUMMARY.md`.

**2. Whole-phase freeze proof.** Resolve the pre-Phase-3 base commit (the last commit before plan 03-01's first task commit) from `git log --oneline` and record its SHA. Diff that commit against HEAD for `web/style.css`, `web/mobile-gestures.js` and `tests/test_mobile_plumbing.py` and require an empty diff for all three. A bare working-tree diff is trivially clean once everything is committed and proves nothing about the whole phase — the base-commit target is the point. Also record a `--numstat` diff of `tests/test_desktop_smoke.py` against the same base; any change there must be individually justified in the summary, as Phase 2 did for its single superseded assertion.

**3. Dependency scan.** Walk `web/` for `package.json`, `package-lock.json`, `yarn.lock` and any bundler config and require zero matches; diff `web/index.html` against the base commit and require an empty diff, proving no script or stylesheet tag was added. Record both results.

**4. COVERAGE.md.** Write `.planning/workstreams/mobile-ux/phases/03-landscape-layout/COVERAGE.md` containing exactly this declaration sentence and nothing that reads as a capability matrix: `No external API integration: landscape UI layout touches only local DOM/localStorage and static outbound links.` Mirror the Phase 2 file's minimal shape. Do not invent an API table — this phase integrates no external API/SDK/service and the honest declaration is the deliverable.

**5. §6 acceptance-evidence table.** Assemble it in `03-04-SUMMARY.md`, one row per applicable `INTEGRATION_PLAN.md` §6 bullet across Layout / Gestures / Scrubber / Persistence / Help, each citing the automated test that proves it by file and test name, or marked hardware-only (routed to Task 2) or out-of-scope-for-this-phase with the owning phase named. Every landscape bullet Phase 2 marked out-of-scope must now carry a real citation. Never silently omit a bullet.

**6. Gate agenda.** Assemble the Phase C gate agenda for Task 2 in the same summary, itemising: the four carried flagged assumptions (FA-MOBL-01..04) with their concrete criteria; the safe-area contract in both notch rotations; the auto-hide feel; the iOS edge-swipe-back route in landscape; the word-position-0 empty-log reading; the 40x40 cinema-scrub FAB sub-44px exception (flagged, remediated in Phase 4 if Lighthouse flags it); the `dense-rolls` fixture addition; and every deviation recorded in plans 01-03. Do not pre-answer any of them.
  </action>

  <verify>
    <automated>/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py</automated>
    <automated>BASE_SHA=$(git log --format=%H -1 344b37d) && git diff --exit-code "$BASE_SHA" HEAD -- web/style.css web/mobile-gestures.js tests/test_mobile_plumbing.py web/index.html</automated>
    <automated>test ! -e web/package.json && test ! -e web/package-lock.json && test ! -e web/yarn.lock && test ! -e web/vite.config.js && grep -F -c 'No external API integration: landscape UI layout touches only local DOM/localStorage and static outbound links.' .planning/workstreams/mobile-ux/phases/03-landscape-layout/COVERAGE.md</automated>
  </verify>

  <acceptance_criteria>
    - The four-file pytest invocation exits 0 in a single run, and the recorded pass count is greater than the 33-test Phase 2 baseline by at least the number of new landscape tests named in `03-01`/`03-02`/`03-03` summaries.
    - `git diff --exit-code <BASE_SHA> HEAD -- web/style.css web/mobile-gestures.js tests/test_mobile_plumbing.py` exits 0, where `<BASE_SHA>` is the resolved pre-Phase-3 base commit recorded in the summary.
    - A filesystem walk of `web/` reports zero matches for `package.json`, `package-lock.json`, `yarn.lock` and any bundler config file, and `git diff --exit-code <BASE_SHA> HEAD -- web/index.html` exits 0.
    - `COVERAGE.md` exists at the phase directory and `grep -F -c 'No external API integration: landscape UI layout touches only local DOM/localStorage and static outbound links.' <path>` returns 1.
    - The §6 evidence table in `03-04-SUMMARY.md` has a row for every applicable checklist bullet, and every row is either a `tests/...::test_name` citation, an explicit hardware-only marker, or an explicit out-of-scope marker naming the owning phase — no blank cells.
    - The gate agenda in `03-04-SUMMARY.md` lists FA-MOBL-01 through FA-MOBL-04 by id with their criteria, and no agenda item carries a pre-supplied answer.
  </acceptance_criteria>

  <reversibility rating="reversible">Documentation and evidence only; no source change.</reversibility>

  <done>The whole suite is green in one run, the three frozen files are proven byte-identical across the entire phase, `web/` is proven still dependency-free, `COVERAGE.md` carries the exact declaration, and the §6 evidence table plus the gate agenda are assembled and unanswered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: INTEGRATION_PLAN.md §5 Phase C gate review with Dre, including the D-23 real-device iOS pass</name>

  <precondition>A real iOS device running Safari is available to Dre, reachable over `ios-webkit-debug-proxy` plus the target-based WebKit inspector protocol (Safari's own Inspector window must be closed — the session is single-client). Phase 2 completed this exact workflow on iOS 18.5. If no iOS hardware is available, halt and report rather than substituting Android or emulation: D-23 makes the device pass a hard, non-optional gate and Phase 2 proved Android cannot evidence the toolbar/`svh`, safe-area or Vibration-API behaviors.</precondition>

  <read_first>
    - `03-04-SUMMARY.md` (Task 1's output) — the §6 evidence table, the freeze-proof record and the gate agenda.
    - `design/mobile-ux/INTEGRATION_PLAN.md` §5 Phase C — the gate's own steps and exit criteria.
    - `.planning/workstreams/mobile-ux/phases/02-portrait-layout/02-05-SUMMARY.md` — the iOS pass workflow, the four device-found defects, and the process lesson that fixing defect #2 introduced defect #3 which only a SECOND device pass caught.
    - `03-UI-SPEC.md` § "Safe-area insets (landscape puts the notch on a SHORT edge)" — the both-rotation-directions contract that only hardware can confirm.
    - `03-RESEARCH.md` Pitfall 5 — why the iOS edge-swipe-back route must be re-exercised in landscape rather than inherited from portrait's pass.
  </read_first>

  <what-built>
    The complete landscape surface: a shared sky region at ~75% width beside a 224px right rail carrying a live field log (top 2/3) over a Settings/About control dock (bottom 1/3); an auto-hiding cinema-scrub pill with a 4000ms idle window, reveal-vs-pause tap semantics and a monotonic zoom-aware drag; rotation hand-off preserving word position, play state, speed, zoom and every preference toggle; and Settings/About/Help reusing the Phase 2 surface stack with backdrop dismissal, focus trap and back-gesture handling. The D-12 interim landscape fallback is retired. The full web suite is green in one run and the three frozen files are proven byte-identical across the whole phase.
  </what-built>

  <how-to-verify>
1. **Desktop smoke (required, non-negotiable).** Run `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py` and confirm it passes. A phase that fails the §0.5 smoke test is not done. Then open the app in a desktop browser at ≥1100px and walk the §0.5 checklist by eye.

2. **Serve the app locally and open it on a real iPhone over Safari** (Phase 2's workflow: `ios-webkit-debug-proxy` plus the target-based WebKit inspector protocol; close Safari's own Inspector window first).

3. **Landscape layout (MOBL-01).** Hold the phone sideways. Confirm: the sky occupies roughly three quarters of the width; the right rail shows a field log whose top card is the roll you are on and whose entries below are the recent rolls; the Settings/About dock sits under it. **Rotate to the OTHER landscape direction and check both again** — the notch moves to the opposite short edge and the safe-area contract has never been exercised on hardware. Confirm the rail is not clipped on either side and the sky's focal label never slides under the notch.

4. **Auto-hide feel (MOBL-02).** Start playback, put the phone down, and watch. Confirm the cinema-scrub pill fades out after about four seconds while the top chips and the field log stay visible. Confirm it does NOT fade while paused. Then, with the chrome hidden, tap the sky once — it must reappear WITHOUT pausing. Tap again — it should pause (or do nothing, if you have tap-to-pause off in Settings). Turn tap-to-pause OFF and repeat the hidden-chrome tap: the controls must still come back. Judge whether the timing reads as cinematic or as controls vanishing on you.

5. **Rotation hand-off (MOBL-03 — this phase's hard gate).** With playback running, rotate the phone. Confirm you keep your place, the playback keeps running, and the speed, timeline zoom and toggles are all as you left them. Repeat while a Settings flyout is open — the flyout must still be open after the rotation, not dismissed. Repeat while your finger is mid-drag on the scrub — the rotation must happen immediately (an ignored rotation reads as a freeze), the drag must abort cleanly, and the next drag must behave normally.

6. **Flyouts (MOBL-04).** Open Settings from the landscape dock. Confirm: tapping the backdrop OVER THE RIGHT RAIL closes it (not just tapping over the sky); pressing the Settings button again closes it; **the iOS edge-swipe-back gesture closes it rather than leaving the app** — this specific route was only ever verified in portrait, and WebKit has a documented history of not firing the event it relies on. Confirm the panel never paints under the dock buttons.

7. **Empty states.** Scrub to the very start of the story. Confirm the field log's empty form reads as an empty log, not a broken panel. If it reads as broken, say so — routing a minimal placeholder string is a same-phase fix.

8. **Rule on the gate agenda** in `03-04-SUMMARY.md`: the four carried flagged assumptions (FA-MOBL-01..04), the 40x40 cinema-scrub play button's sub-44px exception, the `dense-rolls` fixture addition, and every deviation plans 01-03 recorded.

9. If anything is wrong, report it here. Fixes route through `/gsd-plan-phase 3 --gaps` rather than in-place plan edits. **Expect to need a second device pass after any fix** — in Phase 2 the fix for one device defect introduced another that only a second pass caught.
  </how-to-verify>

  <acceptance_criteria>
    - `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py` exits 0 and the result is recorded in the summary.
    - A real iOS Safari device pass has been run in BOTH landscape rotation directions, with the device model and iOS/Safari version recorded in the summary.
    - Every numbered step above has an explicit outcome recorded — pass, fail with detail, or explicitly deferred with an owner. No step is left blank.
    - Every gate-agenda item, including FA-MOBL-01 through FA-MOBL-04, carries an explicit ruling from Dre.
    - The gate's disposition is recorded verbatim as Dre stated it. This checkpoint is NOT auto-approvable under any configuration, including `workflow.auto_advance` and autonomous mode; an agent may not infer, assume or self-issue approval.
  </acceptance_criteria>

  <resume-signal>Type "approved" to close the Phase C gate, or describe the issues found (each will be routed to `/gsd-plan-phase 3 --gaps`).</resume-signal>

  <done>Dre has walked the §5 Phase C gate and the §6 acceptance checklist on real iOS Safari hardware in both landscape rotation directions, ruled on every agenda item including the four carried flagged assumptions, and either approved the gate or enumerated the defects to route to gap closure — with the desktop smoke test recorded as passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent claims → phase record | The summary is the durable evidence a later reader and Phase 4 rely on; an unverified claim here becomes an unexamined assumption there. |
| emulation results → shipping decision | Playwright/Chromium cannot reproduce the iOS dynamic toolbar, `svh` settle, safe-area insets, real touch latency, or the OS back gesture. |
| repository → frozen desktop surface | The whole phase's changes must be provably confined to `web/app.js`, `web/mobile.css` and tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Repudiation | claiming MOBL-03 is satisfied without hardware evidence | medium | mitigate | D-23 requires both prongs. Task 2 is a blocking human gate that cannot be auto-approved; the device model and OS version are recorded, and every checklist step carries an explicit outcome. |
| T-03-11 | Tampering | a desktop or gesture-contract regression hidden behind a passing suite | medium | mitigate | The freeze proof diffs `web/style.css`, `web/mobile-gestures.js` and `tests/test_mobile_plumbing.py` against the phase's BASE commit, not the working tree, so a mid-phase edit that was later reverted-and-re-added is still caught; `tests/test_desktop_smoke.py` changes are individually justified. |
| T-03-12 | Tampering | a build-step or dependency creeping into the zero-dependency `web/` surface | medium | mitigate | A filesystem walk asserts no `package.json`/lockfile/bundler config exists under `web/`, and `web/index.html`'s tag set is diffed against the base commit. |
| T-03-13 | Repudiation | a fabricated external-API capability matrix in `COVERAGE.md` | low | mitigate | The declaration sentence is fixed and asserted by an exact-match grep; anything resembling an invented capability table fails review. |
| T-03-SC | Tampering | package-manager supply chain | high | accept | No package-manager install task exists anywhere in this phase; the dependency scan in Task 1 is the standing proof. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package is introduced, so no legitimacy checkpoint applies. |
</threat_model>

<verification>
1. `/Users/dre/src/bcf-visualization/.venv/bin/python -m pytest tests/test_desktop_smoke.py tests/test_mobile_plumbing.py tests/test_mobile_portrait.py tests/test_mobile_landscape.py` — one invocation, all green.
2. `git diff --exit-code <BASE_SHA> HEAD -- web/style.css web/mobile-gestures.js tests/test_mobile_plumbing.py` — empty.
3. `git diff --exit-code <BASE_SHA> HEAD -- web/index.html` — empty; and a `web/` walk finds no dependency or bundler file.
4. `COVERAGE.md` present with the exact declaration sentence.
5. Task 2's blocking gate is resolved by Dre, with device model and OS version recorded and every agenda item ruled on.
</verification>

<success_criteria>
- The full web suite passes in one run with no pre-existing test lost.
- The three frozen files are byte-identical across the whole phase, measured against the pre-Phase-3 base commit.
- `web/` is proven still framework-free and build-step-free.
- `COVERAGE.md` carries the exact declaration.
- The §6 acceptance-evidence table has no blank or omitted row.
- Dre has ruled on the §5 Phase C gate on real iOS hardware in both landscape rotations, closing D-23's second prong.
</success_criteria>

<output>
Create `.planning/workstreams/mobile-ux/phases/03-landscape-layout/03-04-SUMMARY.md` when done.
</output>
